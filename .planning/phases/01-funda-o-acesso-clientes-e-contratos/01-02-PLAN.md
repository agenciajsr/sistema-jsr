---
phase: 01
plan: 2
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/index.ts
  - drizzle.config.ts
  - .env.example
  - .env.local
autonomous: false
requirements: [ACES-01, ACES-02, ACES-03, CLI-01, CLI-02, CLI-03, CLI-04]

user_setup:
  - service: supabase
    why: "Postgres (dados) + Auth (login multi-usuário) para todo o sistema — nenhuma outra parte da fase funciona sem um projeto Supabase real"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> chave publishable (ou anon, conforme o que o dashboard exibir — ver Pitfall 4 em 01-RESEARCH.md)"
      - name: SUPABASE_SECRET_KEY
        source: "Supabase Dashboard -> Settings -> API -> chave secret (ou service_role) — NUNCA prefixar com NEXT_PUBLIC_"
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string modo 'Transaction' (porta 6543)"
      - name: DIRECT_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string modo 'Direct connection' (porta 5432)"
    dashboard_config:
      - task: "Criar o projeto Supabase (conta + novo projeto)"
        location: "supabase.com -> New Project"

must_haves:
  truths:
    - "Schema Drizzle define profiles, clientes e contratos com os campos exatos de D-05 e D-08"
    - "Tabelas existem no Postgres real do projeto Supabase após a migração"
    - "Conexão de runtime usa o pooler (6543, prepare:false) e migrações usam a conexão direta (5432)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Tabelas profiles/clientes/contratos + enums role/nicho/cliente_status"
    - path: "drizzle.config.ts"
      provides: "Configuração drizzle-kit apontando para DIRECT_URL"
    - path: "src/lib/db/index.ts"
      provides: "Cliente Drizzle de runtime com prepare:false contra DATABASE_URL"
  key_links:
    - from: "src/lib/db/index.ts"
      to: "DATABASE_URL (pooled, 6543)"
      via: "postgres(process.env.DATABASE_URL!, { prepare: false })"
      pattern: "prepare:\\s*false"
    - from: "drizzle.config.ts"
      to: "DIRECT_URL (direto, 5432)"
      via: "dbCredentials.url"
      pattern: "DIRECT_URL"
---

<objective>
Definir o schema Drizzle completo desta fase (profiles, clientes, contratos), configurar as duas connection strings do Supabase Postgres (pooled para runtime, direta para migrações — Pitfall 2 de 01-RESEARCH.md), e aplicar a primeira migração contra um projeto Supabase real.

Purpose: Este schema é a fundação de dados referenciada por todas as fases futuras (Fase 2 vincula contas de anúncio ao cliente, Fase 4 calcula MRR a partir de `contratos`, etc.) — errar aqui (ex: usar `float` para dinheiro, ou misturar pooled/direct) propaga problemas por todo o roadmap.
Output: Tabelas `profiles`, `clientes`, `contratos` criadas no Postgres do Supabase; clientes Drizzle configurados corretamente para runtime e migração.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md
</context>

<interfaces>
<!-- Código de referência extraído de 01-RESEARCH.md — Pattern 3 e Pitfall 2/4. Usar exatamente esta estrutura. -->

Schema Drizzle esperado (adaptar apenas se necessário, manter nomes de tabelas/colunas):
```typescript
// src/lib/db/schema.ts
import { pgTable, pgEnum, uuid, text, timestamp, date, numeric, index } from 'drizzle-orm/pg-core'
import { relations } from 'drizzle-orm'

export const roleEnum = pgEnum('role', ['admin', 'membro'])
export const nichoEnum = pgEnum('nicho', ['ecommerce', 'negocio_local', 'infoproduto'])
export const clienteStatusEnum = pgEnum('cliente_status', ['ativo', 'pausado', 'encerrado'])

// Estende auth.users (gerenciado pelo Supabase) — NÃO redefinir auth.users aqui.
export const profiles = pgTable('profiles', {
  id: uuid('id').primaryKey(), // == auth.users.id, setado explicitamente no insert
  nome: text('nome').notNull(),
  role: roleEnum('role').notNull().default('membro'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
})

export const clientes = pgTable('clientes', {
  id: uuid('id').primaryKey().defaultRandom(),
  nome: text('nome').notNull(),
  nicho: nichoEnum('nicho').notNull(),
  status: clienteStatusEnum('status').notNull().default('ativo'),
  contatoNome: text('contato_nome'),
  contatoTelefone: text('contato_telefone'),
  contatoEmail: text('contato_email'),
  notas: text('notas'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
})

export const contratos = pgTable('contratos', {
  id: uuid('id').primaryKey().defaultRandom(),
  clienteId: uuid('cliente_id').notNull().references(() => clientes.id, { onDelete: 'cascade' }),
  dataInicio: date('data_inicio').notNull(),
  dataVencimento: date('data_vencimento').notNull(),
  valorMensal: numeric('valor_mensal', { precision: 10, scale: 2 }).notNull(), // NUNCA float/real para dinheiro
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => ({
  clienteIdx: index('contratos_cliente_id_idx').on(table.clienteId, table.dataInicio),
}))

export const clientesRelations = relations(clientes, ({ many }) => ({
  contratos: many(contratos),
}))
export const contratosRelations = relations(contratos, ({ one }) => ({
  cliente: one(clientes, { fields: [contratos.clienteId], references: [clientes.id] }),
}))
```

Cliente Drizzle de runtime (pooled):
```typescript
// src/lib/db/index.ts
import { drizzle } from 'drizzle-orm/postgres-js'
import postgres from 'postgres'
import * as schema from './schema'

const client = postgres(process.env.DATABASE_URL!, { prepare: false }) // pooled, porta 6543
export const db = drizzle({ client, schema })
```

Config de migração (direto):
```typescript
// drizzle.config.ts
import { defineConfig } from 'drizzle-kit'

export default defineConfig({
  schema: './src/lib/db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DIRECT_URL!, // conexão direta, porta 5432 — NUNCA a pooled
  },
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Definir schema Drizzle (profiles, clientes, contratos) e clientes de conexão</name>
  <files>src/lib/db/schema.ts, src/lib/db/index.ts, drizzle.config.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-04 a D-08 — campos exatos de cliente e contrato)
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (Pattern 3, Pitfall 2, Pitfall 3 — pooled vs direct, prepare:false)
  </read_first>
  <action>
    Criar os 3 arquivos exatamente como no bloco `<interfaces>` acima:
    - `src/lib/db/schema.ts` com os 3 enums (`role`, `nicho`, `cliente_status`) e as 3 tabelas (`profiles`, `clientes`, `contratos`) com as relations.
    - `src/lib/db/index.ts` usando `postgres(process.env.DATABASE_URL!, { prepare: false })` (pooled, porta 6543).
    - `drizzle.config.ts` usando `process.env.DIRECT_URL!` (direto, porta 5432).

    Importante: `valorMensal` DEVE ser `numeric('valor_mensal', { precision: 10, scale: 2 })` — nunca `real`/`float`/`double precision` (causa erro de arredondamento no MRR da Fase 4).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/db/schema.ts contém `pgEnum('role'`, `pgEnum('nicho'`, `pgEnum('cliente_status'`
    - src/lib/db/schema.ts contém `numeric('valor_mensal'`
    - src/lib/db/index.ts contém `prepare: false`
    - drizzle.config.ts contém `DIRECT_URL`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Schema Drizzle completo criado com os 3 modelos e 3 enums, clientes de conexão pooled/direct corretamente separados.</done>
</task>

<task type="auto">
  <name>Task 2: Criar .env.example com todas as variáveis necessárias</name>
  <files>.env.example</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (Pitfall 2 e Pitfall 4)
  </read_first>
  <action>
    Criar `.env.example` na raiz com exatamente estas chaves (sem valores reais, apenas placeholders):
    ```
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
    SUPABASE_SECRET_KEY=
    DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-region.pooler.supabase.com:6543/postgres?pgbouncer=true
    DIRECT_URL=postgresql://postgres.[project-ref]:[password]@aws-region.pooler.supabase.com:5432/postgres
    SEED_ADMIN_EMAIL=
    SEED_ADMIN_PASSWORD=
    ```
    Garantir que `.env.local` está no `.gitignore` (o `create-next-app` do Task 1 do plano 01-01 já adiciona isso por padrão — confirmar).
  </action>
  <verify>
    <automated>test -f .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contém `DATABASE_URL=`
    - .env.example contém `DIRECT_URL=`
    - .env.example contém `SUPABASE_SECRET_KEY=`
    - .env.example contém `SEED_ADMIN_EMAIL=`
    - .gitignore contém `.env.local` (ou `.env*.local`)
  </acceptance_criteria>
  <done>.env.example documenta todas as variáveis necessárias para rodar o projeto; .env.local segue ignorado pelo git.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Schema Drizzle completo e configuração de conexão pooled/direct prontos para aplicar contra um Postgres real.</what-built>
  <how-to-verify>
    1. Se ainda não existir, criar um projeto em https://supabase.com (New Project) — plano gratuito é suficiente para ~10 clientes.
    2. Em **Settings -> API**, copiar:
       - "Project URL" -> `NEXT_PUBLIC_SUPABASE_URL`
       - a chave **publishable** (ou `anon`, dependendo do que o dashboard exibir) -> `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
       - a chave **secret** (ou `service_role`) -> `SUPABASE_SECRET_KEY` (NUNCA prefixar com `NEXT_PUBLIC_`)
    3. Em **Settings -> Database -> Connection string**, copiar:
       - modo **Transaction** (porta 6543) -> `DATABASE_URL` (adicionar `?pgbouncer=true` no final se não vier incluso)
       - modo **Direct connection** (porta 5432) -> `DIRECT_URL`
    4. Copiar `.env.example` para `.env.local` e preencher todos os valores acima, além de `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` (email e senha temporária que serão usados para o primeiro Admin no plano 01-03).
  </how-to-verify>
  <resume-signal>Digite "aplicado" quando `.env.local` estiver preenchido com todos os valores reais do projeto Supabase, ou descreva o erro encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Aplicar migração e verificar tabelas criadas</name>
  <files>drizzle/ (arquivos gerados)</files>
  <read_first>
    - src/lib/db/schema.ts (schema criado no Task 1 desta mesma tarefa)
  </read_first>
  <action>
    Com `.env.local` preenchido (Task anterior), rodar:
    ```
    npx drizzle-kit push
    ```
    Isso aplica o schema diretamente no Postgres do projeto Supabase (usando `DIRECT_URL`), criando as tabelas `profiles`, `clientes`, `contratos` e os 3 enums.

    Se o comando falhar por erro de conexão, confirmar que `DIRECT_URL` está correta (porta 5432, não 6543) e que a senha do banco não contém caracteres especiais não escapados na URL.
  </action>
  <verify>
    <automated>npx drizzle-kit push --force</automated>
  </verify>
  <acceptance_criteria>
    - Comando `npx drizzle-kit push --force` executa sem erro de conexão e sem diffs pendentes (a segunda execução deve ser um no-op idempotente, provando que a primeira aplicação teve efeito real no Postgres)
    - Tabelas `profiles`, `clientes`, `contratos` existem no Postgres do projeto Supabase (confirmar via Supabase Dashboard -> Table Editor, ou `npx drizzle-kit studio`)
  </acceptance_criteria>
  <done>Migração aplicada com sucesso; as 3 tabelas e 3 enums existem no Postgres real do projeto Supabase.</done>
</task>

</tasks>

<verification>
`npx drizzle-kit push --force` executado com sucesso (idempotente na segunda chamada) contra o projeto Supabase real; tabelas visíveis no Table Editor do Supabase Dashboard ou via `npx drizzle-kit studio`.
</verification>

<success_criteria>
Schema Drizzle completo (profiles/clientes/contratos) aplicado em um Postgres Supabase real, com `.env.local` configurado corretamente separando conexão pooled (runtime) de conexão direta (migrações).
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-02-SUMMARY.md`
</output>
