---
phase: 01
plan: 4
type: tdd
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/lib/validations/cliente.ts
  - src/lib/validations/contrato.ts
  - tests/validations/cliente.test.ts
  - tests/validations/contrato.test.ts
autonomous: true
requirements: [CLI-01, CLI-02]

must_haves:
  truths:
    - "Formulário de cliente rejeita nome vazio e nicho fora do enum antes de chegar ao banco"
    - "Formulário de contrato rejeita valor mensal zero/negativo e data de vencimento anterior/igual à data de início"
  artifacts:
    - path: "src/lib/validations/cliente.ts"
      provides: "clienteSchema (Zod) — fonte única de validação client+server (CLI-01)"
    - path: "src/lib/validations/contrato.ts"
      provides: "contratoSchema (Zod) — fonte única de validação client+server (CLI-02)"
  key_links:
    - from: "src/lib/validations/cliente.ts"
      to: "src/lib/db/schema.ts (nichoEnum, clienteStatusEnum)"
      via: "valores do z.enum devem ser idênticos aos valores do pgEnum"
      pattern: "ecommerce.*negocio_local.*infoproduto"
---

<objective>
Criar os schemas Zod de validação de Cliente e Contrato — única fonte de validação reutilizada tanto pelo formulário (React Hook Form + resolver) quanto pelas Server Actions (Plan 01-07), seguindo o padrão RED→GREEN por schema.

Purpose: CLI-01 e CLI-02 exigem que dados inválidos (nome vazio, nicho inexistente, valor negativo, datas invertidas) nunca cheguem ao banco. Regras de validação com I/O bem definido (`expect(schema.safeParse(input).success).toBe(...)`) — candidato natural a TDD.
Output: `clienteSchema` e `contratoSchema` com testes Vitest cobrindo casos válidos e inválidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md
</context>

<interfaces>
<!-- Valores de enum devem bater exatamente com src/lib/db/schema.ts (Plan 01-02) -->
De `src/lib/db/schema.ts` (criado no Plan 01-02):
```typescript
export const nichoEnum = pgEnum('nicho', ['ecommerce', 'negocio_local', 'infoproduto'])
export const clienteStatusEnum = pgEnum('cliente_status', ['ativo', 'pausado', 'encerrado'])
```
Os valores do `z.enum` abaixo DEVEM usar exatamente estas strings.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: clienteSchema (Zod) — CLI-01</name>
  <files>src/lib/validations/cliente.ts, tests/validations/cliente.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-05 — campos do cliente; "Claude's Discretion" — contato/notas são opcionais)
    - tests/validations/cliente.test.ts (stub criado no Plan 01-01 — substituir o `describe.todo` pelos testes reais)
    - src/lib/db/schema.ts (nichoEnum, clienteStatusEnum — valores exatos do enum)
  </read_first>
  <behavior>
    - Válido mínimo: `{ nome: "Loja X", nicho: "ecommerce" }` (status default "ativo", contato/notas ausentes) -> `safeParse(...).success === true`
    - Válido completo: `{ nome: "Loja X", nicho: "negocio_local", status: "pausado", contatoNome: "João", contatoTelefone: "11999999999", contatoEmail: "joao@x.com", notas: "Cliente antigo" }` -> `success === true`
    - Inválido: `nome: ""` -> `success === false`, erro no campo `nome`
    - Inválido: `nicho: "outro"` (fora do enum) -> `success === false`
    - Inválido: `contatoEmail: "nao-e-email"` (quando fornecido) -> `success === false`
    - Válido: `contatoEmail` ausente (undefined) -> `success === true` (campo opcional, per "Claude's Discretion" de 01-CONTEXT.md)
  </behavior>
  <action>
    RED: Escrever em `tests/validations/cliente.test.ts` os 6 casos do bloco `<behavior>` usando `describe('clienteSchema', () => { it(...) })` e `clienteSchema.safeParse(...)`. Rodar `npx vitest run tests/validations/cliente.test.ts` — deve falhar (módulo não existe ainda).

    GREEN: Criar `src/lib/validations/cliente.ts`:
    ```typescript
    import { z } from 'zod'

    export const clienteSchema = z.object({
      nome: z.string().min(1, 'Nome é obrigatório'),
      nicho: z.enum(['ecommerce', 'negocio_local', 'infoproduto'], {
        message: 'Selecione um nicho válido',
      }),
      status: z.enum(['ativo', 'pausado', 'encerrado']).default('ativo'),
      contatoNome: z.string().optional(),
      contatoTelefone: z.string().optional(),
      contatoEmail: z.string().email('Email inválido').optional().or(z.literal('')),
      notas: z.string().optional(),
    })

    export type ClienteInput = z.infer<typeof clienteSchema>
    ```
    Rodar `npx vitest run tests/validations/cliente.test.ts` novamente — deve passar.
  </action>
  <verify>
    <automated>npx vitest run tests/validations/cliente.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/cliente.ts exporta `clienteSchema` e o tipo `ClienteInput`
    - clienteSchema usa `z.enum(['ecommerce', 'negocio_local', 'infoproduto']` (valores idênticos ao pgEnum)
    - `npx vitest run tests/validations/cliente.test.ts` passa com os 6 casos do `<behavior>` cobertos
  </acceptance_criteria>
  <done>clienteSchema criado e testado; nome/nicho obrigatórios com valores corretos, contato/notas opcionais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: contratoSchema (Zod) — CLI-02</name>
  <files>src/lib/validations/contrato.ts, tests/validations/contrato.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-07, D-08 — contrato é sempre mensalidade recorrente, campos exatos)
    - tests/validations/contrato.test.ts (stub criado no Plan 01-01 — substituir o `describe.todo` pelos testes reais)
  </read_first>
  <behavior>
    - Válido: `{ dataInicio: "2026-01-01", dataVencimento: "2026-12-31", valorMensal: 1500 }` -> `success === true`
    - Inválido: `valorMensal: 0` -> `success === false`
    - Inválido: `valorMensal: -100` -> `success === false`
    - Inválido: `dataVencimento: "2026-01-01"` igual a `dataInicio: "2026-01-01"` -> `success === false` (vencimento deve ser estritamente posterior ao início)
    - Inválido: `dataVencimento` anterior a `dataInicio` -> `success === false`
    - Inválido: `dataInicio: "data-invalida"` (formato incorreto) -> `success === false`
  </behavior>
  <action>
    RED: Escrever os 6 casos do bloco `<behavior>` em `tests/validations/contrato.test.ts`. Rodar `npx vitest run tests/validations/contrato.test.ts` — deve falhar.

    GREEN: Criar `src/lib/validations/contrato.ts`:
    ```typescript
    import { z } from 'zod'

    const dataRegex = /^\d{4}-\d{2}-\d{2}$/

    export const contratoSchema = z
      .object({
        dataInicio: z.string().regex(dataRegex, 'Data de início inválida'),
        dataVencimento: z.string().regex(dataRegex, 'Data de vencimento inválida'),
        valorMensal: z.coerce.number().positive('Valor mensal deve ser maior que zero'),
      })
      .refine((data) => data.dataVencimento > data.dataInicio, {
        message: 'Data de vencimento deve ser posterior à data de início',
        path: ['dataVencimento'],
      })

    export type ContratoInput = z.infer<typeof contratoSchema>
    ```
    Rodar `npx vitest run tests/validations/contrato.test.ts` novamente — deve passar.
  </action>
  <verify>
    <automated>npx vitest run tests/validations/contrato.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/validations/contrato.ts exporta `contratoSchema` e o tipo `ContratoInput`
    - contratoSchema usa `.positive(` para `valorMensal`
    - contratoSchema usa `.refine(` para validar `dataVencimento > dataInicio`
    - `npx vitest run tests/validations/contrato.test.ts` passa com os 6 casos do `<behavior>` cobertos
  </acceptance_criteria>
  <done>contratoSchema criado e testado; valor mensal sempre positivo, vencimento sempre posterior ao início (D-07/D-08).</done>
</task>

</tasks>

<verification>
`npx vitest run tests/validations/` retorna exit code 0 com todos os casos de `<behavior>` cobertos e passando (não apenas `describe.todo`).
</verification>

<success_criteria>
clienteSchema e contratoSchema prontos para reuso idêntico no formulário (React Hook Form resolver) e nas Server Actions do Plan 01-07 — nenhuma duplicação de regra de validação entre client e server.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-04-SUMMARY.md`
</output>
