---
phase: 01
plan: 3
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - proxy.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/proxy.ts
  - src/app/(auth)/login/page.tsx
  - src/actions/auth.ts
  - src/app/(app)/layout.tsx
  - src/lib/auth/session.ts
  - scripts/seed-admin.ts
  - package.json
autonomous: true
requirements: [ACES-01, ACES-02, ACES-03]

must_haves:
  truths:
    - "Usuário faz login com email e senha via formulário e é redirecionado para a área autenticada"
    - "Acessar uma rota protegida sem sessão válida redireciona para /login (mesmo em navegação direta/refresh, não só client-side)"
    - "Sessão persiste entre acessos (cookie gerenciado pelo Supabase Auth via proxy.ts)"
    - "Existe um caminho para criar o primeiro usuário Admin (script seed-admin.ts) já que não há auto-cadastro (D-02)"
  artifacts:
    - path: "proxy.ts"
      provides: "Refresh de sessão + proteção de rotas em toda navegação (Next.js 16 — NÃO middleware.ts)"
    - path: "src/actions/auth.ts"
      provides: "Server Actions signIn/signOut"
    - path: "src/lib/auth/session.ts"
      provides: "getCurrentUser()/requireAdmin() para checagem de papel (D-01/D-03) usados pelas fases seguintes"
    - path: "scripts/seed-admin.ts"
      provides: "Bootstrap do primeiro Admin via Supabase Admin API"
  key_links:
    - from: "proxy.ts"
      to: "supabase.auth.getUser()"
      via: "revalidação de sessão a cada requisição"
      pattern: "supabase\\.auth\\.getUser\\(\\)"
    - from: "src/actions/auth.ts"
      to: "supabase.auth.signInWithPassword"
      via: "Server Action signIn"
      pattern: "signInWithPassword"
---

<objective>
Implementar a autenticação Supabase Auth SSR completa: trio de clientes (browser/server/proxy), `proxy.ts` (renomeação obrigatória de `middleware.ts` no Next.js 16 — Pitfall 1 de 01-RESEARCH.md), página de login, Server Actions de login/logout, layout protegido, helper de papel (Admin/Membro) e script de bootstrap do primeiro Admin.

Purpose: Cobre ACES-01 (login), ACES-02 (multi-usuário — via seed-admin + base para Plan 01-06), ACES-03 (sessão persistente). É o único ponto de integração de autenticação que todas as fases futuras vão reutilizar.
Output: Login funcional, rotas protegidas, helper de papel reutilizável, primeiro Admin bootstrapável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md
</context>

<interfaces>
<!-- Código de referência de 01-RESEARCH.md — Pattern 1, Pattern 2, Pitfall 1, Pitfall 6. Usar exatamente esta estrutura. -->

Cliente server (Server Components/Actions) — `cookies()` é assíncrono no Next.js 16:
```typescript
// src/lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll() },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options))
          } catch {
            // chamado de um Server Component — seguro ignorar, proxy.ts atualiza a sessão
          }
        },
      },
    }
  )
}
```

`proxy.ts` — OBRIGATORIAMENTE este nome (não `middleware.ts`) e função `proxy` (não `middleware`) no Next.js 16:
```typescript
// proxy.ts (raiz do projeto)
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function proxy(request: NextRequest) {
  let response = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          response = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) => response.cookies.set(name, value, options))
        },
      },
    }
  )

  // IMPORTANTE: usar getUser(), não getSession() — getUser() revalida contra o servidor Supabase
  const { data: { user } } = await supabase.auth.getUser()

  const isAuthRoute = request.nextUrl.pathname.startsWith('/login')
  if (!user && !isAuthRoute) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  return response
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)'],
}
```
Runtime é fixo em `nodejs` em `proxy.ts` — não configurar `runtime: 'edge'`.

Bootstrap do primeiro Admin (script one-off, não faz parte do app rodando):
```typescript
// scripts/seed-admin.ts — rodar uma vez: npx tsx scripts/seed-admin.ts
import { createClient } from '@supabase/supabase-js'
import { db } from '../src/lib/db'
import { profiles } from '../src/lib/db/schema'

const supabaseAdmin = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SECRET_KEY!, // apenas servidor, nunca expor
  { auth: { autoRefreshToken: false, persistSession: false } }
)

async function main() {
  const { data, error } = await supabaseAdmin.auth.admin.createUser({
    email: process.env.SEED_ADMIN_EMAIL!,
    password: process.env.SEED_ADMIN_PASSWORD!,
    email_confirm: true,
  })
  if (error || !data.user) throw error ?? new Error('createUser não retornou usuário')

  await db.insert(profiles).values({ id: data.user.id, nome: 'Admin', role: 'admin' })
  console.log(`Admin criado: ${data.user.email} (${data.user.id})`)
}

main()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trio de clientes Supabase SSR + proxy.ts</name>
  <files>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/proxy.ts, proxy.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 1 — leitura obrigatória sobre a renomeação middleware.ts -> proxy.ts)
  </read_first>
  <action>
    Criar 4 arquivos:

    1. `src/lib/supabase/client.ts` — cliente para Client Components usando `createBrowserClient` de `@supabase/ssr`, lendo `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.

    2. `src/lib/supabase/server.ts` — exatamente o código do bloco `<interfaces>` acima (cliente para Server Components/Actions, `cookies()` assíncrono).

    3. `src/lib/supabase/proxy.ts` — função `updateSession(request: NextRequest)` que encapsula a lógica de refresh de sessão + redirect (a mesma lógica do bloco `proxy.ts` do `<interfaces>`, extraída para ser chamada pelo arquivo raiz).

    4. `proxy.ts` na RAIZ do projeto (não em `src/`, não `middleware.ts`) — exporta `export async function proxy(request: NextRequest)` chamando `updateSession` de `src/lib/supabase/proxy.ts`, e exporta `export const config = { matcher: [...] }` exatamente como no bloco `<interfaces>`.

    CRÍTICO (Pitfall 1 de 01-RESEARCH.md): o arquivo DEVE se chamar `proxy.ts`, não `middleware.ts` — no Next.js 16.2, um arquivo `middleware.ts` é silenciosamente ignorado no build, sem erro nem aviso, deixando rotas protegidas acessíveis sem sessão válida.
  </action>
  <verify>
    <automated>test -f proxy.ts && ! test -f middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - arquivo `proxy.ts` existe na raiz do projeto
    - `proxy.ts` contém `export async function proxy`
    - `proxy.ts` NÃO contém `export function middleware` nem `export async function middleware`
    - arquivo `middleware.ts` NÃO existe em nenhum lugar do projeto
    - src/lib/supabase/server.ts contém `await cookies()`
    - a lógica de checagem de sessão usa `supabase.auth.getUser()`, não `supabase.auth.getSession()`
  </acceptance_criteria>
  <done>Trio de clientes Supabase SSR criado; proxy.ts (não middleware.ts) protege rotas e revalida sessão via getUser() a cada requisição.</done>
</task>

<task type="auto">
  <name>Task 2: Página de login + Server Actions de autenticação + layout protegido</name>
  <files>src/app/(auth)/login/page.tsx, src/actions/auth.ts, src/app/(app)/layout.tsx</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md (seção "Copywriting Contract" — rótulos "Email"/"Senha", botão "Entrar", mensagem de erro de login)
    - src/lib/supabase/server.ts (criado no Task 1 desta mesma tarefa)
  </read_first>
  <action>
    1. `src/actions/auth.ts` — Server Actions:
       - `signIn(formData: FormData)`: lê `email`/`senha`, chama `supabase.auth.signInWithPassword({ email, password })`; em caso de erro retorna `{ error: 'Não foi possível entrar. Verifique seu email e senha e tente novamente.' }` (copy exata do UI-SPEC); em caso de sucesso, `redirect('/clientes')`.
       - `signOut()`: chama `supabase.auth.signOut()`, `redirect('/login')`.

    2. `src/app/(auth)/login/page.tsx` — formulário Client Component com React Hook Form: campos rotulados exatamente "Email" e "Senha" (per UI-SPEC), botão "Entrar", chama a Server Action `signIn` via `<form action={signIn}>` ou `useFormState`. Exibe a mensagem de erro retornada pela action.

    3. `src/app/(app)/layout.tsx` — Server Component que busca o usuário autenticado via `src/lib/supabase/server.ts` (`supabase.auth.getUser()`); se não houver usuário, redireciona para `/login` (defesa em profundidade, complementar ao `proxy.ts`); renderiza um shell simples (nav com nome do usuário + botão de logout chamando `signOut`).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/auth.ts exporta `signIn` e `signOut`
    - src/actions/auth.ts contém `signInWithPassword`
    - login/page.tsx contém o texto `Email`, `Senha` e `Entrar`
    - login/page.tsx ou src/actions/auth.ts contém a string `Não foi possível entrar. Verifique seu email e senha e tente novamente.`
    - src/app/(app)/layout.tsx contém `getUser`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Login funcional com copy exata do UI-SPEC; layout da área autenticada verifica sessão e oferece logout.</done>
</task>

<task type="auto">
  <name>Task 3: Helper de papel (Admin/Membro) + script de bootstrap do primeiro Admin</name>
  <files>src/lib/auth/session.ts, scripts/seed-admin.ts, package.json</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-01, D-02, D-03 — papéis e regra de bootstrap)
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (Pitfall 5, Pitfall 6 — autorização em código de aplicação, não RLS; bootstrap do primeiro Admin)
    - src/lib/db/schema.ts (tabela `profiles` criada no plano 01-02)
  </read_first>
  <action>
    1. `src/lib/auth/session.ts`:
       ```typescript
       import { createClient } from '@/lib/supabase/server'
       import { db } from '@/lib/db'
       import { profiles } from '@/lib/db/schema'
       import { eq } from 'drizzle-orm'

       export async function getCurrentUser() {
         const supabase = await createClient()
         const { data: { user } } = await supabase.auth.getUser()
         if (!user) return null
         const profile = await db.query.profiles.findFirst({ where: eq(profiles.id, user.id) })
         if (!profile) return null
         return { id: user.id, email: user.email, nome: profile.nome, role: profile.role }
       }

       export async function requireAdmin() {
         const current = await getCurrentUser()
         if (!current || current.role !== 'admin') {
           return { error: 'Apenas administradores podem executar esta ação.' as const }
         }
         return { user: current }
       }
       ```
       Nota de arquitetura (Pitfall 5 de 01-RESEARCH.md): a autorização Admin/Membro é feita em código de aplicação (esta função), não via Postgres RLS — a conexão Drizzle usa o role `postgres` que ignora RLS por padrão. Isso é uma decisão deliberada desta fase, não um descuido.

    2. `scripts/seed-admin.ts` — exatamente o código do bloco `<interfaces>` acima.

    3. Adicionar em `package.json` `scripts`: `"seed:admin": "tsx scripts/seed-admin.ts"`.

    4. Rodar `npm run seed:admin` (com `.env.local` já preenchido pelo plano 01-02) para criar o primeiro usuário Admin real, usando os valores de `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` de `.env.local`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/auth/session.ts exporta `getCurrentUser` e `requireAdmin`
    - scripts/seed-admin.ts contém `auth.admin.createUser` e `role: 'admin'`
    - package.json contém `"seed:admin": "tsx scripts/seed-admin.ts"`
    - `npm run seed:admin` executado com sucesso cria um usuário em Supabase Auth e uma linha em `profiles` com `role = 'admin'` (confirmar via Supabase Dashboard -> Authentication -> Users, e Table Editor -> profiles)
  </acceptance_criteria>
  <done>Helper de papel reutilizável criado; primeiro usuário Admin bootstrapado com sucesso no projeto Supabase real.</done>
</task>

</tasks>

<verification>
`npm run build` sem erros. Login manual com o Admin criado pelo script funciona (verificação completa de ACES-01/02/03 fica para o checkpoint final do plano 01-09, mas o build e a criação do usuário devem ser confirmados aqui).
</verification>

<success_criteria>
Autenticação Supabase Auth SSR completa: proxy.ts protege rotas, login funcional com copy do UI-SPEC, helper de papel Admin/Membro pronto para uso pelas Server Actions das fases seguintes, primeiro Admin bootstrapado.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-03-SUMMARY.md`
</output>
