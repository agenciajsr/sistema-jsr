---
phase: 01
plan: 5
type: tdd
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/lib/contratos/current.ts
  - src/lib/contratos/renovacao.ts
  - tests/db/current-contrato.test.ts
  - tests/actions/contratos.test.ts
autonomous: true
requirements: [CLI-02, CLI-03, CLI-04]

must_haves:
  truths:
    - "Ao ver um cliente com várias renovações, a equipe sempre identifica corretamente qual é o contrato vigente (o de data_inicio mais recente)"
    - "Editar/renovar um contrato nunca apaga ou sobrescreve o registro anterior — sempre cria um novo registro (D-06)"
  artifacts:
    - path: "src/lib/contratos/current.ts"
      provides: "selecionarContratoAtual — deriva o contrato vigente sem flag armazenada (evita drift, per 01-RESEARCH.md Anti-Patterns)"
    - path: "src/lib/contratos/renovacao.ts"
      provides: "construirRegistroRenovacao — garante que toda renovação é um INSERT, nunca um UPDATE do registro anterior"
  key_links:
    - from: "src/lib/contratos/current.ts"
      to: "contratos.dataInicio"
      via: "ordenação decrescente por dataInicio, primeiro item = atual"
      pattern: "sort|localeCompare"
    - from: "src/lib/contratos/renovacao.ts"
      to: "contratos (tabela)"
      via: "objeto retornado não contém 'id' — força db.insert, nunca db.update"
      pattern: "NovoContratoRecord"
---

<objective>
Implementar, com TDD, a lógica de negócio pura (sem I/O de banco) que sustenta o modelo de histórico de contratos (D-06): derivação do "contrato atual" a partir de uma lista de contratos, e construção do registro de uma renovação garantindo que ela é sempre um novo registro, nunca uma edição do anterior.

Purpose: CLI-03 (editar) e CLI-04 (lista com status resumido) dependem de uma regra sem ambiguidade sobre "qual é o contrato vigente" — 01-RESEARCH.md descarta explicitamente uma flag `is_current` (pode dessincronizar) em favor de derivação pela data de início mais recente. Ambas as funções têm I/O bem definido (`expect(fn(input)).toBe(output)`) e nenhuma dependência de banco real — TDD puro, rápido, sem mocks de conexão.
Output: `selecionarContratoAtual` e `construirRegistroRenovacao`, testados via Vitest, prontos para uso pelas Server Actions do Plan 01-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md
</context>

<interfaces>
<!-- Estas duas funções são PURAS (sem acesso a banco) por design — testáveis sem mock de conexão Postgres. -->
<!-- O Plan 01-07 (Server Actions) vai importar e usar ambas junto com src/lib/db (Drizzle) e src/lib/validations/contrato.ts (Plan 01-04). -->

Forma esperada de `src/lib/contratos/current.ts`:
```typescript
export type ContratoRow = {
  id: string
  clienteId: string
  dataInicio: string
  dataVencimento: string
  valorMensal: string
}

export function selecionarContratoAtual(contratos: ContratoRow[]): ContratoRow | null {
  // implementação: ordenar por dataInicio decrescente, retornar o primeiro; null se vazio
}
```

Forma esperada de `src/lib/contratos/renovacao.ts` (NÃO importa de src/lib/validations/contrato.ts — recebe os campos já validados como tipo local, para manter este arquivo livre de dependências cruzadas):
```typescript
export type DadosRenovacao = {
  dataInicio: string
  dataVencimento: string
  valorMensal: number
}

export type NovoContratoRecord = {
  clienteId: string
  dataInicio: string
  dataVencimento: string
  valorMensal: string
}

export function construirRegistroRenovacao(clienteId: string, dados: DadosRenovacao): NovoContratoRecord {
  // implementação: mapeia para um registro NOVO (sem 'id', sem referência ao contrato anterior)
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: selecionarContratoAtual — derivação do contrato vigente (CLI-04)</name>
  <files>src/lib/contratos/current.ts, tests/db/current-contrato.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (seção "Anti-Patterns to Avoid" — por que não usar flag `is_current`; bloco "Current-contract derivation")
    - tests/db/current-contrato.test.ts (stub criado no Plan 01-01 — substituir o `describe.todo` pelos testes reais)
  </read_first>
  <behavior>
    - Dado 3 contratos com `dataInicio` "2025-01-01", "2026-01-01", "2025-06-01" (fora de ordem), retorna o de `dataInicio` "2026-01-01" (o mais recente)
    - Dado 1 único contrato, retorna esse mesmo contrato
    - Dado array vazio `[]`, retorna `null`
    - Dado 2 contratos com a MESMA `dataInicio` (edge case), retorna um dos dois sem lançar erro
  </behavior>
  <action>
    RED: Escrever os 4 casos do bloco `<behavior>` em `tests/db/current-contrato.test.ts` usando `describe('selecionarContratoAtual', ...)`. Rodar `npx vitest run tests/db/current-contrato.test.ts` — deve falhar.

    GREEN: Criar `src/lib/contratos/current.ts`:
    ```typescript
    export type ContratoRow = {
      id: string
      clienteId: string
      dataInicio: string
      dataVencimento: string
      valorMensal: string
    }

    export function selecionarContratoAtual(contratos: ContratoRow[]): ContratoRow | null {
      if (contratos.length === 0) return null
      return [...contratos].sort((a, b) => b.dataInicio.localeCompare(a.dataInicio))[0]
    }
    ```
    Rodar `npx vitest run tests/db/current-contrato.test.ts` novamente — deve passar.
  </action>
  <verify>
    <automated>npx vitest run tests/db/current-contrato.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/contratos/current.ts exporta `selecionarContratoAtual` e o tipo `ContratoRow`
    - a função usa comparação por string de data (`localeCompare` ou equivalente) sobre `dataInicio`, não um índice/flag armazenado
    - `npx vitest run tests/db/current-contrato.test.ts` passa com os 4 casos do `<behavior>` cobertos
  </acceptance_criteria>
  <done>Derivação do contrato vigente implementada e testada, sem depender de flag `is_current` (evita o anti-padrão de drift documentado em 01-RESEARCH.md).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: construirRegistroRenovacao — histórico nunca editado in-place (CLI-02, CLI-03)</name>
  <files>src/lib/contratos/renovacao.ts, tests/actions/contratos.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-06 — cada renovação cria um novo registro, mantendo os anteriores)
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (seção "Anti-Patterns to Avoid" — "Editing a contrato row in place for a renewal")
    - tests/actions/contratos.test.ts (stub criado no Plan 01-01 — substituir o `describe.todo` pelos testes reais)
  </read_first>
  <behavior>
    - Dado `clienteId: "abc-123"` e `dados: { dataInicio: "2026-01-01", dataVencimento: "2026-12-31", valorMensal: 1500 }`, retorna um objeto com `clienteId: "abc-123"`, `dataInicio: "2026-01-01"`, `dataVencimento: "2026-12-31"`, `valorMensal: "1500"` (string, compatível com `numeric` do Postgres)
    - O objeto retornado NUNCA contém a chave `id` (garante que o chamador vai usar `db.insert`, nunca `db.update` de um registro existente)
    - Chamar a função duas vezes com o mesmo `clienteId` mas dados diferentes (simulando duas renovações) produz dois objetos independentes, sem nenhuma referência cruzada entre eles (cada um é um registro novo e completo)
  </behavior>
  <action>
    RED: Escrever os 3 casos do bloco `<behavior>` em `tests/actions/contratos.test.ts` usando `describe('construirRegistroRenovacao', ...)`. Verificar explicitamente `expect(resultado).not.toHaveProperty('id')`. Rodar `npx vitest run tests/actions/contratos.test.ts` — deve falhar.

    GREEN: Criar `src/lib/contratos/renovacao.ts`:
    ```typescript
    export type DadosRenovacao = {
      dataInicio: string
      dataVencimento: string
      valorMensal: number
    }

    export type NovoContratoRecord = {
      clienteId: string
      dataInicio: string
      dataVencimento: string
      valorMensal: string
    }

    export function construirRegistroRenovacao(clienteId: string, dados: DadosRenovacao): NovoContratoRecord {
      return {
        clienteId,
        dataInicio: dados.dataInicio,
        dataVencimento: dados.dataVencimento,
        valorMensal: String(dados.valorMensal),
      }
    }
    ```
    Rodar `npx vitest run tests/actions/contratos.test.ts` novamente — deve passar.
  </action>
  <verify>
    <automated>npx vitest run tests/actions/contratos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/contratos/renovacao.ts exporta `construirRegistroRenovacao`, `DadosRenovacao` e `NovoContratoRecord`
    - src/lib/contratos/renovacao.ts NÃO importa de `@/lib/validations/contrato` (mantém a função livre de dependência cruzada com o Plan 01-04)
    - o tipo `NovoContratoRecord` não inclui campo `id`
    - `npx vitest run tests/actions/contratos.test.ts` passa com os 3 casos do `<behavior>` cobertos
  </acceptance_criteria>
  <done>Função pura que constrói o registro de renovação implementada e testada; o design (sem campo `id`) torna estruturalmente impossível o chamador fazer um UPDATE em vez de INSERT, cumprindo D-06.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/db/current-contrato.test.ts tests/actions/contratos.test.ts` retorna exit code 0 com todos os casos de `<behavior>` cobertos e passando.
</verification>

<success_criteria>
Lógica de histórico de contrato (derivação do vigente + construção de renovação como novo registro) implementada como funções puras, testadas sem necessidade de conexão real com o Postgres, prontas para uso pelo Plan 01-07.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-05-SUMMARY.md`
</output>
