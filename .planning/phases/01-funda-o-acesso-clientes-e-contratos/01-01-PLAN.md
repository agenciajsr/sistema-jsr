---
phase: 01
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - components.json
  - vitest.config.ts
  - tests/validations/cliente.test.ts
  - tests/validations/contrato.test.ts
  - tests/db/current-contrato.test.ts
  - tests/actions/contratos.test.ts
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04]

must_haves:
  truths:
    - "Projeto Next.js 16 App Router roda localmente e builda sem erros"
    - "Componentes shadcn/ui necessários para formulários e cards (D-09) estão disponíveis em src/components/ui/"
    - "Suite de testes Vitest executa e retorna código de saída 0"
  artifacts:
    - path: "package.json"
      provides: "Scripts dev/build/test e dependências do stack fixado em CLAUDE.md"
    - path: "components.json"
      provides: "Preset shadcn/ui: style new-york, baseColor neutral, cssVariables true (01-UI-SPEC.md)"
    - path: "vitest.config.ts"
      provides: "Configuração do test runner com alias @ -> ./src"
  key_links:
    - from: "package.json scripts.test"
      to: "vitest run"
      via: "npm run test"
      pattern: "\"test\":\\s*\"vitest run\""
---

<objective>
Inicializar o projeto Next.js 16 (App Router + TypeScript + Tailwind), instalar todas as dependências do stack fixado em CLAUDE.md, inicializar o shadcn/ui com o preset aprovado em 01-UI-SPEC.md, e configurar o Vitest com stubs de teste para as fases seguintes (Wave 0 Gaps de 01-VALIDATION.md).

Purpose: Este é o projeto greenfield da Agência JSR — não existe nenhum código ainda (apenas `.planning/` e `CLAUDE.md`). Todas as fases seguintes (auth, clientes, contratos) dependem desta fundação.
Output: Projeto Next.js funcional, componentes shadcn/ui instalados, Vitest configurado com stubs de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar projeto Next.js 16 e instalar dependências do stack</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx</files>
  <read_first>
    - CLAUDE.md (tabela "Core Technologies" e "Supporting Libraries" — versões exatas fixadas)
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-RESEARCH.md (seção "Standard Stack" e "Installation")
  </read_first>
  <action>
    A pasta do projeto está vazia exceto por `.planning/`, `.git/`, `.claude/` e `CLAUDE.md` — projeto 100% greenfield.

    1. Rodar (na raiz do repositório, sem sobrescrever `.planning/`/`.git/`/`.claude/`/`CLAUDE.md`):
       ```
       npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --eslint --turbopack --use-npm
       ```
       Se o CLI perguntar sobre sobrescrever arquivos existentes, manter `.planning/`, `.git/`, `.claude/`, `CLAUDE.md` intactos e prosseguir apenas com a criação dos arquivos do Next.js.

    2. Instalar dependências de runtime (versões exatas por CLAUDE.md/01-RESEARCH.md):
       ```
       npm install @supabase/supabase-js@2.110.2 @supabase/ssr@0.12.0 drizzle-orm@0.45.2 postgres@3.4.9 zod@4.4.3 react-hook-form@7.81.0 @hookform/resolvers@^5.4.0 date-fns@^4.4.0
       ```

    3. Instalar dependências de desenvolvimento:
       ```
       npm install -D drizzle-kit@0.31.10 vitest tsx
       ```
       (`tsx` é necessário para rodar `scripts/seed-admin.ts` no Plan 01-03, e para o script `test`/`seed:admin` do package.json.)

    4. Confirmar em `package.json` que `next` está na linha 16.x e que todas as libs acima aparecem em `dependencies`/`devDependencies`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém `"next"` com versão iniciando em `16.`
    - package.json dependencies contém `@supabase/ssr`, `drizzle-orm`, `postgres`, `zod`, `react-hook-form`, `@hookform/resolvers`, `date-fns`
    - package.json devDependencies contém `drizzle-kit`, `vitest`, `tsx`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Projeto Next.js 16 App Router criado, builda sem erros, todas as dependências do stack instaladas nas versões fixadas.</done>
</task>

<task type="auto">
  <name>Task 2: Inicializar shadcn/ui com preset aprovado e instalar componentes necessários</name>
  <files>components.json, src/components/ui/*, src/app/globals.css</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md (seções "Design System" e "Registry Safety" — preset e lista exata de componentes)
  </read_first>
  <action>
    O preset aprovado pelo usuário em 01-UI-SPEC.md é: estilo **New York**, cor base **Neutral**, `cssVariables: true`, ícones `lucide-react`.

    1. Rodar:
       ```
       npx shadcn@latest init
       ```
       Se o CLI expuser flags não-interativas (`--base-color neutral`, `--style new-york`, `-y`), usá-las. Se for interativo, responder exatamente:
       - TypeScript: sim
       - Style: New York
       - Base color: Neutral
       - CSS variables: sim (yes)

    2. Instalar os componentes exatos listados em 01-UI-SPEC.md § Registry Safety (registry oficial shadcn, nenhum registry de terceiros necessário nesta fase):
       ```
       npx shadcn@latest add button input label textarea select form card badge alert-dialog dropdown-menu sonner
       ```

    3. Confirmar que `components.json` tem `"style": "new-york"`, `"baseColor": "neutral"` (ou `"neutral"` dentro de `tailwind.baseColor`), `"cssVariables": true`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components.json contém `"style": "new-york"`
    - components.json contém `"cssVariables": true`
    - src/components/ui/ contém os arquivos: button.tsx, input.tsx, label.tsx, textarea.tsx, select.tsx, form.tsx, card.tsx, badge.tsx, alert-dialog.tsx, dropdown-menu.tsx, sonner.tsx
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>shadcn/ui inicializado com o preset New York/Neutral/cssVariables aprovado, todos os 11 componentes necessários instalados em src/components/ui/.</done>
</task>

<task type="auto">
  <name>Task 3: Configurar Vitest e criar stubs de teste (Wave 0 Gaps)</name>
  <files>vitest.config.ts, package.json, tests/validations/cliente.test.ts, tests/validations/contrato.test.ts, tests/db/current-contrato.test.ts, tests/actions/contratos.test.ts</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-VALIDATION.md (seção "Wave 0 Requirements")
  </read_first>
  <action>
    Criar `vitest.config.ts` na raiz:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import path from 'path'

    export default defineConfig({
      test: {
        environment: 'node',
        globals: false,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
    })
    ```

    Adicionar em `package.json` `scripts`:
    ```json
    "test": "vitest run"
    ```

    Criar os 4 arquivos de stub abaixo — cada um usando `describe.todo` para marcar o que será implementado nos planos 01-04 e 01-05 (Vitest trata `describe.todo` como passante, não como falha):

    `tests/validations/cliente.test.ts`:
    ```typescript
    import { describe } from 'vitest'

    // CLI-01: schema de validação do cliente — implementado em 01-04-PLAN.md
    describe.todo('clienteSchema')
    ```

    `tests/validations/contrato.test.ts`:
    ```typescript
    import { describe } from 'vitest'

    // CLI-02: schema de validação do contrato — implementado em 01-04-PLAN.md
    describe.todo('contratoSchema')
    ```

    `tests/db/current-contrato.test.ts`:
    ```typescript
    import { describe } from 'vitest'

    // CLI-04: derivação do contrato atual — implementado em 01-05-PLAN.md
    describe.todo('selecionarContratoAtual')
    ```

    `tests/actions/contratos.test.ts`:
    ```typescript
    import { describe } from 'vitest'

    // CLI-03: renovação de contrato sempre cria novo registro (D-06) — implementado em 01-05-PLAN.md
    describe.todo('construirRegistroRenovacao')
    ```
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts existe e contém `vitest/config`
    - package.json contém `"test": "vitest run"`
    - `npx vitest run` finaliza com exit code 0
    - os 4 arquivos de stub existem nos caminhos exatos listados acima
  </acceptance_criteria>
  <done>Vitest configurado e rodando (exit 0), stubs de teste criados para as 4 lacunas identificadas em 01-VALIDATION.md.</done>
</task>

</tasks>

<verification>
`npm run build` e `npx vitest run` ambos retornam exit code 0. `components.json` reflete o preset aprovado. Nenhum arquivo de `.planning/`, `.git/` ou `CLAUDE.md` foi sobrescrito.
</verification>

<success_criteria>
Projeto Next.js 16 rodando, shadcn/ui inicializado com preset correto e componentes instalados, Vitest configurado com stubs para as 4 lacunas de teste da fase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-01-SUMMARY.md`
</output>
