---
phase: 01
plan: 8
type: execute
wave: 4
depends_on: ["01-07"]
files_modified:
  - src/components/cliente-form.tsx
  - src/components/contrato-form.tsx
  - src/components/cliente-card.tsx
  - src/app/(app)/clientes/page.tsx
  - src/app/(app)/clientes/novo/page.tsx
  - src/app/(app)/clientes/[id]/page.tsx
  - src/app/(app)/clientes/[id]/editar/page.tsx
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04]

must_haves:
  truths:
    - "Usuário cadastra um cliente com nome, nicho e primeiro contrato através de um formulário"
    - "Usuário vê a lista de todos os clientes em cards, cada um mostrando status, nicho, valor mensal e vigência do contrato (D-10)"
    - "Usuário edita cliente e registra renovação de contrato (aparecendo no histórico, não substituindo o anterior)"
    - "Apenas Admin vê os botões de exclusão de cliente/contrato (D-03), com confirmação via AlertDialog (copy exata do UI-SPEC)"
  artifacts:
    - path: "src/app/(app)/clientes/page.tsx"
      provides: "Lista de clientes em cards (CLI-04)"
    - path: "src/app/(app)/clientes/novo/page.tsx"
      provides: "Formulário de cadastro (cliente + primeiro contrato) (CLI-01, CLI-02)"
    - path: "src/app/(app)/clientes/[id]/page.tsx"
      provides: "Detalhe do cliente: contrato atual + histórico + registrar renovação + exclusão (CLI-02, CLI-03, CLI-04)"
    - path: "src/app/(app)/clientes/[id]/editar/page.tsx"
      provides: "Edição de dados do cliente (CLI-03)"
  key_links:
    - from: "src/app/(app)/clientes/novo/page.tsx"
      to: "src/actions/clientes.ts createClienteComContrato"
      via: "submit do formulário"
      pattern: "createClienteComContrato"
    - from: "src/app/(app)/clientes/page.tsx"
      to: "src/lib/contratos/current.ts selecionarContratoAtual"
      via: "cada card exibe o contrato vigente derivado, não uma flag"
      pattern: "selecionarContratoAtual"
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "src/actions/contratos.ts deleteContrato / src/actions/clientes.ts deleteCliente"
      via: "AlertDialog de confirmação, botão visível apenas se role === 'admin'"
      pattern: "role === 'admin'"
---

<objective>
Construir a interface do usuário completa desta fase: formulário de cadastro de cliente (com primeiro contrato embutido), lista de clientes em cards, e página de detalhe com edição, histórico de contratos e exclusão restrita a Admin — seguindo exatamente o contrato visual de 01-UI-SPEC.md.

Purpose: É a camada visível ao usuário que fecha CLI-01 a CLI-04 — sem ela, os Server Actions do Plan 01-07 não têm como ser acionados pela equipe da JSR.
Output: Fluxo completo de cliente/contrato navegável no navegador, com cards, formulários e diálogos de confirmação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md
@.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md
</context>

<interfaces>
De `src/actions/clientes.ts` (Plan 01-07):
```typescript
export async function createClienteComContrato(clienteInput: ClienteInput, contratoInput: ContratoInput): Promise<{ data: { clienteId: string } } | { error: string }>
export async function updateCliente(id: string, input: ClienteInput): Promise<{ data: { id: string } } | { error: string }>
export async function deleteCliente(id: string): Promise<{ data: { id: string } } | { error: string }>
```

De `src/actions/contratos.ts` (Plan 01-07):
```typescript
export async function registrarContrato(clienteId: string, input: ContratoInput): Promise<{ data: { clienteId: string } } | { error: string }>
export async function deleteContrato(id: string): Promise<{ data: { id: string } } | { error: string }>
export async function getContratosDoCliente(clienteId: string): Promise<{ contratoAtual: ContratoRow | null; historico: ContratoRow[] }>
```

De `src/lib/validations/cliente.ts` e `src/lib/validations/contrato.ts` (Plan 01-04) — usar como `zodResolver(clienteSchema)` / `zodResolver(contratoSchema)` no React Hook Form.

De `src/lib/auth/session.ts` (Plan 01-03): `getCurrentUser()` retorna `{ role: 'admin' | 'membro', ... } | null` — usar para gating dos botões de exclusão.

Cores de badge de status exatas (01-UI-SPEC.md § "Status Badge Colors"):
| Status | Cor |
|---|---|
| ativo | `#16A34A` |
| pausado | `#D97706` |
| encerrado | `#71717A` |

Nota de escopo (01-UI-SPEC.md): a vigência do contrato no card é texto neutro (ex: "Faltam 20 dias" / "Vencimento: 15/08/2026"), SEM cor de alerta — lógica de alerta visual é escopo da Fase 3/4, não implementar aqui.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Formulário de cadastro de cliente + primeiro contrato</name>
  <files>src/components/cliente-form.tsx, src/app/(app)/clientes/novo/page.tsx</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md (seção "Copywriting Contract" — CTA "Cadastrar Cliente", CTA secundário "Salvar Cliente", mensagem de erro de formulário)
    - src/lib/validations/cliente.ts e src/lib/validations/contrato.ts (Plan 01-04)
    - src/actions/clientes.ts (Plan 01-07)
  </read_first>
  <action>
    1. `src/components/cliente-form.tsx` (Client Component): formulário combinado com React Hook Form usando dois resolvers (ou um schema único combinando `clienteSchema` + `contratoSchema` via `z.object({ cliente: clienteSchema, contrato: contratoSchema })`). Campos: nome, nicho (Select: e-commerce/negócio local/infoproduto), status (Select, default "ativo"), contato responsável (nome + telefone/email, opcionais), notas (Textarea, opcional), e os campos do primeiro contrato: data de início, data de vencimento, valor mensal. Botão "Salvar Cliente" (per UI-SPEC).

    2. `src/app/(app)/clientes/novo/page.tsx`: título de página "Cadastrar Cliente" (tipografia Display, 28px/600 per UI-SPEC), renderiza `ClienteForm`, ao submeter chama `createClienteComContrato`; em sucesso, `redirect('/clientes')` com toast de sucesso (sonner); em erro, toast com "Não foi possível salvar. Verifique os dados e tente novamente." (copy exata do UI-SPEC).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/clientes/novo/page.tsx contém o texto `Cadastrar Cliente`
    - src/components/cliente-form.tsx contém o texto `Salvar Cliente`
    - src/components/cliente-form.tsx usa `zodResolver` importado de `@hookform/resolvers/zod`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Formulário combinado de cliente + primeiro contrato funcional, com copy exata do UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Lista de clientes em cards (CLI-04)</name>
  <files>src/components/cliente-card.tsx, src/app/(app)/clientes/page.tsx</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-09, D-10 — layout em cards, conteúdo exato do card)
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md (seções "Color" § Status Badge Colors, "Spacing Scale", "Copywriting Contract" § empty state)
    - src/lib/contratos/current.ts (Plan 01-05)
  </read_first>
  <action>
    1. `src/components/cliente-card.tsx`: recebe um `cliente` + seu `contratoAtual` (derivado via `selecionarContratoAtual`). Renderiza (per D-10):
       - Badge de status com cor exata: ativo `#16A34A`, pausado `#D97706`, encerrado `#71717A` (usar `style={{ backgroundColor: ... }}` ou classes Tailwind arbitrárias `bg-[#16A34A]` etc.).
       - Nicho (e-commerce / negócio local / infoproduto).
       - Valor mensal do contrato atual (MRR), formatado em R$ (ex: `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`).
       - Vigência do contrato: "Faltam N dias" ou data de vencimento, em texto neutro (cor padrão/secundária, SEM cor de alerta — escopo de Fase 3/4, não implementar aqui). Usar `date-fns` (`differenceInDays`) para calcular N dias.
       - Link para `/clientes/[id]`.

    2. `src/app/(app)/clientes/page.tsx` (Server Component): busca todos os clientes + seus contratos (via `getContratosDoCliente` por cliente, aceitável em ~10 clientes per 01-RESEARCH.md Open Question 2), grid responsivo (gap `xl` = 32px per UI-SPEC), renderiza `ClienteCard` por cliente. Se lista vazia, exibe empty state exato do UI-SPEC: título "Nenhum cliente cadastrado ainda", corpo "Cadastre seu primeiro cliente para começar a acompanhar contratos e status.", botão "Cadastrar Cliente" linkando para `/clientes/novo`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/cliente-card.tsx contém `#16A34A`, `#D97706` e `#71717A`
    - src/app/(app)/clientes/page.tsx contém o texto `Nenhum cliente cadastrado ainda`
    - src/app/(app)/clientes/page.tsx contém `selecionarContratoAtual` ou usa `getContratosDoCliente` (que já aplica a derivação)
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Lista de clientes em cards funcional, mostrando status/nicho/MRR/vigência per D-10, com empty state per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Detalhe do cliente — edição, histórico de contratos, renovação e exclusão (admin-only)</name>
  <files>src/app/(app)/clientes/[id]/page.tsx, src/app/(app)/clientes/[id]/editar/page.tsx, src/components/contrato-form.tsx</files>
  <read_first>
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-CONTEXT.md (D-03, D-06 — exclusão admin-only, distinção contrato atual vs histórico)
    - .planning/phases/01-funda-o-acesso-clientes-e-contratos/01-UI-SPEC.md (seção "Confirmação de exclusão" — copy exata dos dois AlertDialogs; "Copy adicional" — badge "Contrato Atual", seção "Histórico de Contratos")
    - src/actions/contratos.ts, src/actions/clientes.ts (Plan 01-07)
    - src/lib/auth/session.ts (Plan 01-03)
  </read_first>
  <action>
    1. `src/components/contrato-form.tsx` (Client Component): formulário de renovação com React Hook Form + `zodResolver(contratoSchema)` (dataInicio, dataVencimento, valorMensal), botão "Registrar Contrato" (per UI-SPEC), chama `registrarContrato(clienteId, input)`.

    2. `src/app/(app)/clientes/[id]/page.tsx` (Server Component):
       - Busca o cliente e `getContratosDoCliente(id)`.
       - Exibe dados do cliente, badge "Contrato Atual" sobre o contrato com `contratoAtual.id`, seção "Histórico de Contratos" listando os demais registros de `historico` (ordenados por `dataInicio` desc).
       - Botão "Registrar Contrato" abre `ContratoForm` (Dialog ou seção inline).
       - Botões "Editar" (link para `/clientes/[id]/editar`) sempre visíveis; botões de exclusão de cliente e de cada contrato do histórico SOMENTE renderizados se `getCurrentUser().role === 'admin'` (D-03).
       - AlertDialog de exclusão de cliente com copy EXATA do UI-SPEC: título "Excluir cliente", corpo "Esta ação não pode ser desfeita. O cliente e todo o histórico de contratos vinculados serão removidos permanentemente. Deseja continuar?", botões "Cancelar" / "Excluir cliente" (variante destructive `#DC2626`), chama `deleteCliente(id)`.
       - AlertDialog de exclusão de contrato com copy EXATA do UI-SPEC: título "Excluir contrato", corpo "Esta ação não pode ser desfeita. Este registro de contrato será removido permanentemente do histórico. Deseja continuar?", botões "Cancelar" / "Excluir contrato", chama `deleteContrato(id)`.
       - Toasts (sonner) de sucesso/erro para cada ação.

    3. `src/app/(app)/clientes/[id]/editar/page.tsx`: reutiliza a parte "cliente" de `ClienteForm` (ou um componente derivado) pré-preenchida com os dados atuais, chama `updateCliente(id, input)` ao submeter.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/clientes/[id]/page.tsx contém os textos `Contrato Atual` e `Histórico de Contratos`
    - src/app/(app)/clientes/[id]/page.tsx contém o texto exato `Esta ação não pode ser desfeita. O cliente e todo o histórico de contratos vinculados serão removidos permanentemente. Deseja continuar?`
    - src/app/(app)/clientes/[id]/page.tsx contém uma checagem `role === 'admin'` (ou equivalente `!== 'admin'`) controlando a renderização dos botões de exclusão
    - src/components/contrato-form.tsx contém o texto `Registrar Contrato`
    - `npm run build` finaliza com exit code 0
  </acceptance_criteria>
  <done>Página de detalhe completa: edição, histórico de contratos claramente distinto do atual (D-06), renovação, exclusão restrita a Admin com confirmação de copy exata.</done>
</task>

</tasks>

<verification>
`npm run build` sem erros. Fluxo completo navegável: /clientes (lista) -> /clientes/novo (cadastro) -> /clientes/[id] (detalhe, histórico, renovação, exclusão) -> /clientes/[id]/editar.
</verification>

<success_criteria>
Interface completa de clientes e contratos implementada per D-09/D-10 e 01-UI-SPEC.md, consumindo os Server Actions do Plan 01-07 — CLI-01 a CLI-04 utilizáveis de ponta a ponta pelo navegador.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-acesso-clientes-e-contratos/01-08-SUMMARY.md`
</output>
