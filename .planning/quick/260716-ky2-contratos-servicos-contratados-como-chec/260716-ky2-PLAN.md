---
phase: quick-260716-ky2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0031_contratos_servicos.sql
  - scripts/aplicar-migration-0031.ts
  - src/lib/db/schema.ts
  - src/lib/contratos/servicos-contratados.ts
  - src/lib/contratos/servicos-contratados.test.ts
  - src/lib/validations/contrato.ts
  - src/actions/crm.ts
  - src/actions/contratos.ts
  - src/components/crm/converter-cliente-dialog.tsx
  - src/components/contratos/editar-contrato-dialog.tsx
  - src/lib/contratos/variaveis.ts
  - src/lib/contratos/template-trafego.ts
  - src/app/(app)/contratos/tabela-contratos.tsx
autonomous: true
requirements: [QUICK-KY2]
must_haves:
  truths:
    - "Ao converter um Ganho em cliente (e ao editar um contrato), a equipe marca 1+ serviços via checklist, cada um com valor próprio, em pt-BR"
    - "Quando 'Tráfego Pago' está marcado, aparecem checkboxes de plataforma (Meta Ads / Google Ads) e pelo menos 1 é obrigatória"
    - "O valor mensal do contrato é a SOMA dos valores dos serviços (calculada, não digitada)"
    - "O texto/PDF do contrato descreve o objeto conforme os serviços marcados, citando as plataformas do tráfego pago"
    - "A tabela /contratos exibe os serviços contratados de cada contrato; contratos antigos (sem estrutura) continuam funcionando"
  artifacts:
    - path: "drizzle/0031_contratos_servicos.sql"
      provides: "Coluna aditiva contratos.servicos (jsonb nullable)"
    - path: "src/lib/contratos/servicos-contratados.ts"
      provides: "Schema Zod, soma total, rótulos e texto do objeto por serviço (módulo puro)"
      exports: ["servicosContratadosSchema", "somaServicos", "PLATAFORMAS_TRAFEGO"]
    - path: "src/components/crm/converter-cliente-dialog.tsx"
      provides: "Checklist de serviços com valor individual + plataformas condicionais"
  key_links:
    - from: "src/components/crm/converter-cliente-dialog.tsx"
      to: "src/actions/crm.ts"
      via: "payload servicos: [{servico, valor, plataformas?}]"
      pattern: "servicos"
    - from: "src/lib/contratos/template-trafego.ts"
      to: "src/lib/contratos/variaveis.ts"
      via: "vars.servicos alimenta a cláusula do objeto"
      pattern: "servicos"
---

<objective>
Estruturar os serviços contratados no contrato: seleção por checklist (1+ serviços com valor individual; plataformas Meta/Google quando Tráfego Pago), valor total = soma, texto/PDF do contrato montado conforme os serviços marcados, exibição na listagem — dado preparado para MRR por serviço no futuro.

Purpose: hoje o contrato guarda um único `servico` (text) e um `valorMensal` digitado — não representa clientes com múltiplos serviços nem alimenta MRR por serviço.
Output: coluna jsonb `servicos` retrocompatível, módulo puro testado, dialogs atualizados, cláusula do objeto dinâmica, badges na tabela.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/db/schema.ts (tabela contratos, linhas ~67-95)
@src/lib/crm/servicos.ts (SERVICOS_JSR — lista fechada dos 4 serviços)
@src/lib/contratos/variaveis.ts
@src/lib/contratos/template-trafego.ts
@src/lib/validations/contrato.ts
@src/actions/contratos.ts
@src/actions/crm.ts (converterOportunidadeEmCliente)
@src/components/crm/converter-cliente-dialog.tsx
@src/components/contratos/editar-contrato-dialog.tsx
@src/app/(app)/contratos/tabela-contratos.tsx
@scripts/aplicar-migration-0030.ts (padrão do script de aplicação)

<interfaces>
Contrato de dados NOVO (decisão travada: checklist + plataformas, NÃO pacotes):

```typescript
// src/lib/contratos/servicos-contratados.ts (módulo PURO, zero import de db/react)
export const PLATAFORMAS_TRAFEGO = { meta_ads: 'Meta Ads', google_ads: 'Google Ads' } as const
export type PlataformaTrafego = keyof typeof PLATAFORMAS_TRAFEGO

export type ServicoContratado = {
  servico: ServicoJsr            // chave de SERVICOS_JSR
  valor: number                  // > 0, mensal, em reais
  plataformas?: PlataformaTrafego[] // OBRIGATÓRIO (1+) se servico === 'trafego_pago'; ausente nos demais
}

export const servicosContratadosSchema: z.ZodType<ServicoContratado[]> // min 1, sem serviço duplicado
export function somaServicos(itens: ServicoContratado[]): number
export function descricaoObjetoServicos(itens: ServicoContratado[]): string[] // parágrafos pt-BR p/ cláusula do objeto
export function rotuloPlataformas(p?: PlataformaTrafego[]): string // 'Meta Ads e Google Ads' | 'Meta Ads' | ...
```

Já existente e reutilizável:
- `SERVICOS_JSR` / `SERVICOS_KEYS` / `rotuloServico` em src/lib/crm/servicos.ts
- `montarVariaveisContrato` (variaveis.ts) → `montarSecoesContrato` (template-trafego.ts) → PDF (pdf.tsx) e preview leem as MESMAS seções — mexer só no template propaga para os dois.
- Padrão migration: SQL à mão 100% aditiva com `--> statement-breakpoint`, script `scripts/aplicar-migration-00NN.ts` lendo DIRECT_URL em `sql.begin()`. NUNCA `drizzle-kit migrate` (controle vazio → replay destrutivo).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0031 + módulo puro servicos-contratados.ts (TDD)</name>
  <files>drizzle/0031_contratos_servicos.sql, scripts/aplicar-migration-0031.ts, src/lib/db/schema.ts, src/lib/contratos/servicos-contratados.ts, src/lib/contratos/servicos-contratados.test.ts</files>
  <behavior>
    - servicosContratadosSchema: aceita [{servico:'trafego_pago', valor:1500, plataformas:['meta_ads']}]; rejeita array vazio, valor <= 0, serviço duplicado, trafego_pago sem plataformas (ou com []), plataformas em serviço que não é trafego_pago
    - somaServicos: [1500, 800] → 2300; casas decimais (1500.5 + 799.5 → 2300) sem erro de float grosseiro (arredondar a 2 casas)
    - descricaoObjetoServicos: 1 parágrafo por serviço em pt-BR; para trafego_pago o texto cita as plataformas via rotuloPlataformas ('Meta Ads e Google Ads' quando ambas)
    - rotuloPlataformas: ['meta_ads'] → 'Meta Ads'; ambas → 'Meta Ads e Google Ads'; undefined → ''
  </behavior>
  <action>
    1. Escrever os testes acima em servicos-contratados.test.ts (Vitest) — rodar e ver FALHAR.
    2. Implementar src/lib/contratos/servicos-contratados.ts (módulo puro, comentários em pt-BR) até os testes passarem. Textos de descricaoObjetoServicos por serviço (base, ajustar redação jurídica simples):
       - trafego_pago: "Gestão de tráfego pago (criação, gerenciamento e otimização de campanhas de anúncios) nas plataformas {plataformas};"
       - landing_page: "Criação e/ou manutenção de landing page e site do(a) CONTRATANTE;"
       - crm_automacao: "Implantação e configuração de CRM e automações de atendimento;"
       - estrategia: "Consultoria de estratégia e estruturação de marketing digital;"
    3. Criar drizzle/0031_contratos_servicos.sql — 100% aditiva:
       `ALTER TABLE "contratos" ADD COLUMN IF NOT EXISTS "servicos" jsonb;`
       Comentário no topo explicando: nullable, contratos antigos usam fallback servico+valorMensal (retrocompatível — restrição crítica do projeto).
    4. Adicionar `servicos: jsonb('servicos')` na tabela contratos em src/lib/db/schema.ts com comentário: `// [{servico, valor, plataformas?}] — ver src/lib/contratos/servicos-contratados.ts; null = contrato legado (usa servico/valorMensal)`.
    5. Criar scripts/aplicar-migration-0031.ts copiando o padrão do aplicar-migration-0030.ts (DIRECT_URL, sql.begin, split por '--> statement-breakpoint').
    6. Aplicar a migration: antes, conferir estado real do banco (query information_schema.columns de contratos — confirmar que 'servicos' não existe e que as colunas da 0029/0030 existem, provando que estão aplicadas). Rodar `npx tsx --env-file=.env.local scripts/aplicar-migration-0031.ts`. Se DIRECT_URL não estiver disponível, deixar o script pronto e sinalizar como pendente no SUMMARY (nunca drizzle-kit migrate).
  </action>
  <verify>
    <automated>npx vitest run src/lib/contratos/servicos-contratados.test.ts</automated>
  </verify>
  <done>Testes passando (RED→GREEN commitados), coluna servicos no schema, migration 0031 criada e aplicada (ou script pronto + pendência registrada)</done>
</task>

<task type="auto">
  <name>Task 2: Checklist de serviços nos dialogs (conversão Ganho→Cliente e edição) + actions</name>
  <files>src/components/crm/converter-cliente-dialog.tsx, src/components/contratos/editar-contrato-dialog.tsx, src/actions/crm.ts, src/actions/contratos.ts, src/lib/validations/contrato.ts</files>
  <action>
    Decisão travada do usuário: checklist de serviços com valor individual (NÃO pacotes); plataformas só para tráfego pago; tudo em pt-BR.

    1. converter-cliente-dialog.tsx: substituir o Select único de serviço por um CHECKLIST dos 4 serviços (SERVICOS_JSR, rótulos pt-BR com acento na UI: "Tráfego Pago", "Landing Page e Site", "CRM e Automação", "Estratégia e Estruturação"). Cada serviço marcado revela um input de valor mensal (R$). Quando "Tráfego Pago" marcado, revelar checkboxes "Meta Ads" e "Google Ads" (mínimo 1 — validar com mensagem pt-BR). Remover o input de mensalidade digitada: exibir o TOTAL calculado (somaServicos, formatado BRL) como resumo somente leitura. Validar com servicosContratadosSchema antes de enviar. Cores/estados novos devem ter variante dark: (restrição do projeto).
    2. src/actions/crm.ts (converterOportunidadeEmCliente): aceitar `servicos: ServicoContratado[]` no payload, validar com servicosContratadosSchema no servidor, gravar no contrato: `servicos` (jsonb), `valorMensal` = somaServicos (NUNCA confiar em total vindo do cliente), `servico` = servicos[0].servico (compat legado — colunas/telas que leem servico continuam funcionando).
    3. src/lib/validations/contrato.ts: estender contratoEdicaoSchema com `servicos: servicosContratadosSchema.nullable().optional()` — null/ausente = contrato legado.
    4. editar-contrato-dialog.tsx: adicionar o mesmo checklist (componente pode ser extraído para src/components/contratos/servicos-checklist.tsx e reutilizado nos dois dialogs, se ficar mais limpo). Contrato legado abre com checklist pré-populado a partir de `servico` + `valorMensal` quando servicos é null (melhor esforço: 1 item, sem plataformas pré-marcadas se for trafego_pago — usuário marca). Ao salvar com serviços: atualizarDadosContrato grava servicos + recalcula valorMensal = soma + sincroniza servico = servicos[0]. Se o usuário não tocar nos serviços de um contrato legado, comportamento atual permanece (retrocompatibilidade — nada obrigatório novo).
    5. src/actions/contratos.ts (atualizarDadosContrato): aceitar/gravar o campo novo com a mesma regra do item 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Conversão e edição salvam servicos estruturados com total = soma; trafego_pago exige 1+ plataforma; contratos legados seguem editáveis sem obrigar serviços</done>
</task>

<task type="auto">
  <name>Task 3: Texto/PDF do contrato conforme serviços + exibição na listagem</name>
  <files>src/lib/contratos/variaveis.ts, src/lib/contratos/template-trafego.ts, src/lib/contratos/variaveis.test.ts, src/app/(app)/contratos/tabela-contratos.tsx, src/actions/contratos.ts</files>
  <action>
    1. variaveis.ts: `montarVariaveisContrato` passa a receber também `servicos` (jsonb cru do contrato) e expõe em VariaveisContrato: `servicos: ServicoContratado[] | null` (parse com servicosContratadosSchema; inválido/null → null = legado) e `linhasValorPorServico: string[]` (ex.: "Tráfego Pago (Meta Ads e Google Ads): R$ 1.500,00") para uso opcional na cláusula de valor. Atualizar os call sites (preview/[id]/page.tsx e onde o PDF monta variáveis) para repassar o campo.
    2. template-trafego.ts (montarSecoesContrato):
       - Com `vars.servicos` presente: a CLÁUSULA 1ª (objeto) insere, após o 1.1, os parágrafos de descricaoObjetoServicos numerados (1.1.1, 1.1.2, ...), mantendo os itens específicos de tráfego (criação de conta, campanhas etc.) SOMENTE quando trafego_pago estiver entre os serviços; o parágrafo-objeto final cita os serviços contratados e as plataformas. Na CLÁUSULA 2ª, após o 2.1, adicionar parágrafo com a composição do valor por serviço (linhasValorPorServico) quando houver 2+ serviços.
       - Com `vars.servicos === null` (legado): texto EXATAMENTE como hoje — snapshot atual preservado, sem regressão nos contratos já enviados.
       - Título: manter TITULO_CONTRATO para legado/só-tráfego; com serviços múltiplos usar "CONTRATO DE PRESTAÇÃO DE SERVIÇOS DE MARKETING DIGITAL" (título e menção na abertura).
    3. Testes em variaveis.test.ts (ou arquivo novo): contrato com servicos → variáveis contêm a lista parseada e linhas formatadas; contrato legado (servicos null) → comportamento idêntico ao atual; secoes de contrato multi-serviço contêm as plataformas no texto.
    4. tabela-contratos.tsx + listarTodosContratos/getContratosDoCliente (actions/contratos.ts): incluir `servicos` no select; na coluna de serviço, exibir badges compactas dos serviços contratados (rótulo pt-BR; tráfego pago com sufixo das plataformas em title/tooltip); fallback para rotuloServico(servico) quando servicos é null. Badges com variante dark:.
    5. Conferir que o PDF (pdf.tsx) e o preview imprimível renderizam as novas seções sem mudança neles (ambos consomem montarSecoesContrato) — só ajustar se algum tipo mudar.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/contratos</automated>
  </verify>
  <done>Contrato novo multi-serviço gera texto/PDF com objeto e valores por serviço (plataformas citadas); contrato legado gera texto idêntico ao atual; /contratos mostra os serviços de cada contrato</done>
</task>

</tasks>

<verification>
- `npx vitest run` — suíte completa verde (incluindo testes novos do módulo puro e das variáveis)
- `npx tsc --noEmit` sem erros
- Read-only no banco: coluna `servicos` existe em contratos (information_schema) após aplicação da 0031
- Fluxo manual: converter um Ganho marcando Tráfego Pago (Meta Ads) + CRM com valores → contrato criado com valorMensal = soma; abrir preview → cláusula do objeto cita ambos os serviços e a plataforma
</verification>

<success_criteria>
- Checklist de serviços (1+) com valor individual nos dois pontos de criação/edição de contrato; plataformas obrigatórias no tráfego pago
- valorMensal sempre = soma dos serviços (calculado no servidor)
- Texto/PDF dinâmico por serviços marcados; contratos legados sem nenhuma mudança de texto
- Serviços visíveis em /contratos; dado jsonb pronto para MRR por serviço (estrutura {servico, valor, plataformas?})
- Migration 0031 aditiva aplicada via script manual (nunca drizzle-kit migrate)
</success_criteria>

<output>
After completion, create `.planning/quick/260716-ky2-contratos-servicos-contratados-como-chec/260716-ky2-SUMMARY.md`
</output>
