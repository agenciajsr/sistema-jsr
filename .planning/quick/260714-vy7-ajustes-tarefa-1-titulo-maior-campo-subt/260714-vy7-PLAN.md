---
phase: quick-260714-vy7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0018_*.sql
  - src/lib/validations/tarefa.ts
  - src/actions/tarefas.ts
  - src/lib/tarefas/dados.ts
  - src/lib/tarefas/recorrencia.ts
  - src/lib/tarefas/quadro.ts
  - src/lib/tarefas/quadro.test.ts
  - src/app/(app)/tarefas/tarefas-quadro.tsx
  - src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx
  - src/app/(app)/tarefas/nova/nova-tarefa-form.tsx
autonomous: true
requirements: [FIX-1-subtitulo, FIX-2-recorrencia-so-no-dia, FIX-3-datas-sem-duplicacao]

must_haves:
  truths:
    - "A tarefa tem um campo subtítulo separado da descrição (no detalhe e na criação)"
    - "O título é maior e claro no detalhe e no formulário de criação"
    - "Abrir /tarefas por padrão NÃO mostra ocorrências recorrentes futuras"
    - "Uma tarefa recorrente aparece só no seu dia (ou ao navegar até ele)"
    - "Os campos de data mostram um único calendário (input nativo), sem ícone duplicado"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Coluna subtitulo (text, nullable) na tabela tarefas"
      contains: "subtitulo"
    - path: "src/lib/validations/tarefa.ts"
      provides: "subtitulo opcional em tarefaSchema e atualizarTarefaSchema"
      contains: "subtitulo"
    - path: "src/actions/tarefas.ts"
      provides: "criarTarefa e atualizarTarefa gravam subtitulo"
      contains: "subtitulo"
    - path: "src/lib/tarefas/dados.ts"
      provides: "subtitulo no select camposCard e no tipo TarefaCard"
      contains: "subtitulo"
    - path: "src/lib/tarefas/quadro.ts"
      provides: "intervaloPadrao termina em HOJE (sem futuro por padrão)"
      contains: "intervaloPadrao"
    - path: "src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx"
      provides: "Título text-[36px] + Input de subtítulo + datas sem CalendarDays redundante"
    - path: "src/app/(app)/tarefas/nova/nova-tarefa-form.tsx"
      provides: "Título maior + campo subtítulo (estado próprio) + datas sem CalendarDays"
  key_links:
    - from: "tarefa-detalhe.tsx (Input subtítulo)"
      to: "atualizarTarefa({ subtitulo })"
      via: "salvarCampo no onBlur"
      pattern: "salvarCampo\\(\\{ ?subtitulo"
    - from: "nova-tarefa-form.tsx (estado subtitulo)"
      to: "criarTarefa({ subtitulo })"
      via: "envio no salvar()"
      pattern: "subtitulo"
    - from: "quadro.ts intervaloPadrao"
      to: "getTarefasDoPeriodo → janelaMaterializacao"
      via: "fim = hoje ⇒ ate = hoje (materialização não passa de hoje por padrão)"
      pattern: "fim: hoje"
---

<objective>
Aplicar 3 correções de feedback na tela de Tarefas do Sistema JSR:

- FIX 1 — Título MAIOR + campo SUBTÍTULO de verdade (coluna nova, separada da descrição), no detalhe e na criação.
- FIX 2 — Recorrência aparece SÓ no dia (bug: hoje materializa até 60 dias no futuro ao abrir /tarefas).
- FIX 3 — Campos de data sem duplicação visual: remover o ícone CalendarDays redundante e deixar só o `<input type="date">` nativo.

Purpose: Corrigir problemas reais que o usuário reportou usando o sistema — subtítulo confuso/ausente, quadro poluído por ocorrências futuras, e calendário duplicado.
Output: Migration ADITIVA (gerada, NÃO aplicada) + schema/validation/actions/dados atualizados + engine de janela corrigida + duas telas ajustadas. UI 100% em português, fidelidade ao mockup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Ferramenta interna, solo dev + Claude. Stack: Next.js 16 (App Router) + Drizzle + Supabase Postgres. Datas SEMPRE como string 'YYYY-MM-DD'. Queries do módulo tarefas são SEQUENCIAIS (nunca paralelizar com Promise — pool max=3, ver src/lib/db/index.ts).

Regras do módulo já estabelecidas (não quebrar):
- Recorrência = molde (eh_molde) + ocorrências materializadas preguiçosamente ao abrir /tarefas. Idempotência garantida pelo índice único (tarefa_mae_id, data) + engine pura que filtra o que já existe.
- `ocorreEm` / `datasDaRegra` são a REGRA de calendário — NÃO mudam nesta tarefa. Só muda a JANELA padrão (o intervalo default do quadro).
- Migração: gerar com drizzle-kit, revisar (só ADD COLUMN), e NÃO aplicar (o orquestrador aplica). `drizzle-kit generate` só lê o schema/meta — não conecta no banco.

<interfaces>
Contratos-chave que o executor usa diretamente (extraídos do código atual):

Tabela `tarefas` (src/lib/db/schema.ts) — HOJE tem titulo/descricao/notas/data/dataInicio… e NÃO tem subtitulo. Coluna nova a adicionar logo após `titulo`:
```ts
titulo: text('titulo').notNull(),
subtitulo: text('subtitulo'),   // NOVA — nullable
notas: text('notas'),
```

Zod (src/lib/validations/tarefa.ts): `tarefaSchema` (criação) e `atualizarTarefaSchema` (edição parcial). Padrão: campos de texto opcionais = `z.string().optional()`.

Actions (src/actions/tarefas.ts):
- `criarTarefa(input)` → insere valores; hoje grava `descricao: v.descricao || null`.
- `atualizarTarefa(id, campos)` → monta `set` condicional: `if (v.campo !== undefined) set.campo = ...`.

dados.ts:
- `TarefaCard` (tipo) — precisa de `subtitulo: string | null`.
- `camposCard` (select) — precisa de `subtitulo: tarefas.subtitulo`. `paraCard` faz `...row`, então o campo flui automaticamente. `TarefaDetalhe = TarefaCard & {...}`, logo o detalhe recebe subtitulo de graça.

Engine da janela (src/lib/tarefas/recorrencia.ts):
```ts
const JANELA_PASSADO_DIAS = 30
const JANELA_FUTURO_MAX_DIAS = 60
export function janelaMaterializacao(hoje, diaSelecionado?): { de, ate }
//  de = hoje-30 ; ate = min(max(hoje, diaSelecionado), hoje+60)
```
⚠️ `janelaMaterializacao` JÁ está correta: quando `diaSelecionado === hoje`, ela devolve `ate = hoje` (sem futuro). A CAUSA do bug NÃO está aqui — está no intervalo padrão do quadro que hoje já inclui 6 dias de futuro.

Intervalo padrão (src/lib/tarefas/quadro.ts):
```ts
export function intervaloPadrao(hoje): { inicio, fim } {
  return { inicio: hoje, fim: somaDias(hoje, 6) }   // ← FUTURO por padrão = a causa
}
```
`dados.ts` chama `janelaMaterializacao(hoje, fim)` — com `fim = hoje+6` a série futura inteira materializa. Corrigir o default para terminar em HOJE resolve, sem tocar em `ocorreEm`/`datasDaRegra`.
</interfaces>

@src/lib/db/schema.ts
@src/lib/tarefas/recorrencia.ts
@src/lib/tarefas/quadro.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Camada de dados — coluna subtitulo (schema + migration + validation + actions + dados)</name>
  <files>src/lib/db/schema.ts, drizzle/0018_*.sql, src/lib/validations/tarefa.ts, src/actions/tarefas.ts, src/lib/tarefas/dados.ts</files>
  <action>
Adicionar o campo `subtitulo` (texto, nullable) ponta a ponta. FIX-1 (camada de dados).

1) schema.ts — na tabela `tarefas`, adicionar `subtitulo: text('subtitulo')` logo após a linha `titulo: text('titulo').notNull(),`. Nullable (sem `.notNull()`).

2) Migration ADITIVA — rodar `npx drizzle-kit generate` (só lê schema/meta, não conecta no banco). Isso cria `drizzle/0018_*.sql`. REVISAR o arquivo: deve conter APENAS `ALTER TABLE "tarefas" ADD COLUMN "subtitulo" text;` (e a atualização do snapshot em drizzle/meta). Se o generate produzir qualquer outra alteração inesperada, PARAR e reportar. ⚠️ NÃO aplicar a migration (nada de `drizzle-kit migrate`/`push`/`supabase db push`) — o orquestrador aplica.

3) validations/tarefa.ts — adicionar `subtitulo: z.string().optional(),` em DOIS lugares: `tarefaSchema` (junto de `descricao`) e `atualizarTarefaSchema` (junto de `descricao`).

4) actions/tarefas.ts:
   - `criarTarefa`: no objeto `.values({...})` do insert, adicionar `subtitulo: v.subtitulo || null,` (logo após `titulo: v.titulo,`).
   - `atualizarTarefa`: no bloco de montagem do `set`, adicionar `if (v.subtitulo !== undefined) set.subtitulo = v.subtitulo || null` (junto do `if (v.descricao !== undefined)`). NÃO precisa adicionar ao array `rastreados` de atividade (fora de escopo).

5) dados.ts:
   - No tipo `TarefaCard`, adicionar `subtitulo: string | null` (logo após `titulo: string`).
   - No objeto `camposCard`, adicionar `subtitulo: tarefas.subtitulo,` (logo após `titulo: tarefas.titulo,`). `paraCard` já faz `...row`, então o campo propaga para card e detalhe sem mais mudanças.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    Além disso: confirmar que existe UM arquivo `drizzle/0018_*.sql` e que seu conteúdo é somente `ADD COLUMN "subtitulo"` (revisão manual do diff da migration).
  </verify>
  <done>schema/validation/actions/dados aceitam e persistem `subtitulo`; migration 0018 gerada com só ADD COLUMN e NÃO aplicada; `npx tsc --noEmit` sem erros novos.</done>
</task>

<task type="auto">
  <name>Task 2: Recorrência só no dia — intervalo padrão termina em HOJE</name>
  <files>src/lib/tarefas/recorrencia.ts, src/lib/tarefas/quadro.ts, src/lib/tarefas/quadro.test.ts, src/app/(app)/tarefas/tarefas-quadro.tsx</files>
  <action>
FIX-2. Causa real: o intervalo PADRÃO do quadro (`intervaloPadrao`) já inclui 6 dias de futuro (`fim: somaDias(hoje, 6)`), então `getTarefasDoPeriodo` chama `janelaMaterializacao(hoje, hoje+6)` e materializa a série futura inteira. A regra de calendário (`ocorreEm`/`datasDaRegra`) e a própria `janelaMaterializacao` estão corretas — NÃO mexer nelas. A correção é o DEFAULT: mostrar por padrão de HOJE-30 (atrasadas) até HOJE, nunca futuro. Futuro só ao navegar explicitamente (de/ate na URL).

1) recorrencia.ts — trocar `const JANELA_PASSADO_DIAS = 30` por `export const JANELA_PASSADO_DIAS = 30` (para reusar o mesmo teto de atrasadas no intervalo padrão). NÃO alterar `JANELA_FUTURO_MAX_DIAS`, `janelaMaterializacao`, `ocorreEm` nem `datasDaRegra`.

2) quadro.ts — em `intervaloPadrao`, importar `JANELA_PASSADO_DIAS` do `'./recorrencia'` (já importa `somaDias` de lá) e trocar o retorno para:
   `return { inicio: somaDias(hoje, -JANELA_PASSADO_DIAS), fim: hoje }`
   Assim o default termina em HOJE (sem futuro) e ainda mostra atrasadas até hoje. Atualizar o comentário/JSDoc da função para refletir "de hoje-30 (atrasadas) até HOJE — nunca futuro por padrão".

3) tarefas-quadro.tsx — os 3 links "Nova Tarefa" usam `data=${dados.inicio}` como data padrão da nova tarefa. Como `inicio` agora é HOJE-30, trocar os 3 para `data=${dados.hoje}` (o tipo `TarefasDoPeriodo` já expõe `hoje`), senão a nova tarefa nasceria datada 30 dias atrás. Locais: o botão "Nova Tarefa" do cabeçalho, o botão do estado vazio, e o "Adicionar tarefa" de cada coluna (este mantém o `status=${s}`, só troca a data). NÃO mexer nos `<Input type="date">` da toolbar (linhas que usam `dados.inicio`/`dados.fim` como valor do range — devem seguir mostrando o intervalo real).

4) quadro.test.ts — atualizar o bloco `describe('intervaloPadrao', ...)` para a nova expectativa (janela de 30 dias terminando em hoje):
   - `intervaloPadrao('2026-07-14')` ⇒ `{ inicio: '2026-06-14', fim: '2026-07-14' }`
   - `intervaloPadrao('2026-07-28')` ⇒ `{ inicio: '2026-06-28', fim: '2026-07-28' }`
   Ajustar os textos/descrições dos `it(...)` se mencionarem "7 dias". Os testes de `janelaMaterializacao` (recorrencia.test.ts) NÃO mudam — o comportamento dela é o mesmo.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Por padrão /tarefas cobre [hoje-30, hoje] e NUNCA materializa/mostra futuro; navegar para um dia futuro (de/ate) ainda materializa até lá (teto hoje+60 preservado); Nova Tarefa nasce datada em HOJE; `npm test` e `npx tsc --noEmit` passam.</done>
</task>

<task type="auto">
  <name>Task 3: Detalhe — título maior + Input de subtítulo + datas sem CalendarDays</name>
  <files>src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx</files>
  <action>
FIX-1 (UI detalhe) + FIX-3 (datas). Depende da Task 1 (campo subtitulo em TarefaDetalhe/actions).

1) Estado do subtítulo — junto dos outros `useState` (perto de `const [titulo, setTitulo] = useState(tarefa.titulo)`), adicionar:
   `const [subtitulo, setSubtitulo] = useState(tarefa.subtitulo ?? '')`

2) Título MAIOR — no `<Input>` do título (hoje `className="... text-[32px] leading-tight font-bold tracking-tight ..."`), trocar `text-[32px]` por `text-[36px]`. Manter `font-bold tracking-tight` e o resto (border-0, px-0, shadow-none, focus-visible:ring-0).

3) Input de SUBTÍTULO — logo ABAIXO do Input de título, dentro do mesmo `<div className="space-y-1">`, adicionar um `<Input>` editável:
   - `value={subtitulo}` / `onChange={(e) => setSubtitulo(e.target.value)}`
   - `onBlur`: se `subtitulo` (aparado) mudou em relação a `tarefa.subtitulo ?? ''`, chamar `salvarCampo({ subtitulo: subtitulo.trim() })`.
   - `placeholder="Adicionar subtítulo..."`, `aria-label="Subtítulo da tarefa"`.
   - Estilo de subtítulo (menor e discreto, sem caixa): `className="h-auto border-0 px-0 text-base text-muted-foreground shadow-none focus-visible:ring-0"`.
   ⚠️ A DESCRIÇÃO continua APENAS na seção "Descrição" (aba Detalhes) — NÃO duplicar a descrição embaixo do título. O subtítulo é campo próprio, independente de `descricao`.

4) Datas sem ícone duplicado (FIX-3) — nas DUAS células "Data de início" e "Prazo", remover o elemento `<CalendarDays className="size-4 shrink-0 text-muted-foreground" />` que precede o `<Input type="date">`. Deixar só o `<Input type="date" ... className={DATA_CELULA}>` (o input nativo já traz o próprio ícone/calendário ao clicar). O wrapper `<div className="flex items-center gap-2">` pode ficar com o Input como único filho. NÃO tocar nas células "Tempo estimado" (ícone Clock) nem "Projeto / Cliente" (ícone Building2) — esses não são inputs de data.
   ⚠️ MANTER o import de `CalendarDays` — ele ainda é usado no cabeçalho do card de checklist (`renderGrupo`).

Preservar todo o resto (grade de status/responsável/prioridade/etiquetas, abas, checklists, comentários, anexos, atividade, recorrência).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Título em text-[36px]; Input de subtítulo salva via salvarCampo({ subtitulo }) no onBlur; descrição segue só na seção Descrição; células de data mostram um único calendário (nativo) sem CalendarDays; import de CalendarDays mantido (usado no checklist); `npx tsc --noEmit` limpo.</done>
</task>

<task type="auto">
  <name>Task 4: Criar — título maior + campo subtítulo + datas sem CalendarDays</name>
  <files>src/app/(app)/tarefas/nova/nova-tarefa-form.tsx</files>
  <action>
FIX-1 (UI criar) + FIX-3 (datas). Depende da Task 1.

1) Estado do subtítulo — junto dos outros `useState`, adicionar `const [subtitulo, setSubtitulo] = useState('')`. Manter `descricao` como está (é a Descrição completa da aba Detalhes).

2) Título MAIOR — no `<Input>` do título (hoje `className="... text-[26px] leading-tight font-semibold ..."`), trocar por `text-[36px] leading-tight font-bold tracking-tight` (alinhar ao detalhe). Manter border-0, px-0, shadow-none, focus-visible:ring-0, autoFocus.

3) Campo SUBTÍTULO no lugar da "Descrição breve..." — o `<Textarea>` sob o título que hoje liga a `descricao` (placeholder "Descrição breve...") passa a ser o SUBTÍTULO. Trocar por um `<Input>`:
   - `value={subtitulo}` / `onChange={(e) => setSubtitulo(e.target.value)}`
   - `placeholder="Adicionar subtítulo..."`, `aria-label="Subtítulo da tarefa"`.
   - `className="h-auto border-0 px-0 text-base text-muted-foreground shadow-none focus-visible:ring-0"`.
   A Descrição COMPLETA na aba "Detalhes" (o `<Textarea>` com placeholder "Detalhes, contexto, links...") continua ligada a `descricao` — NÃO mexer nela.

4) Enviar subtítulo — na chamada `criarTarefa({...})` dentro de `salvar()`, adicionar `subtitulo: subtitulo.trim(),` (perto de `descricao: descricao.trim(),`).

5) Datas sem ícone duplicado (FIX-3) — nas células "Data de início" e "Prazo", remover os dois `<CalendarDays className="size-4 shrink-0 text-muted-foreground" />` que precedem os `<Input type="date">`. Deixar só os inputs nativos (`className={DATA_CELULA}`). NÃO tocar em "Tempo estimado" (Clock) nem "Projeto / Cliente" (Building2).
   ⚠️ Após remover os dois usos, `CalendarDays` fica SEM uso no arquivo → REMOVER `CalendarDays,` do bloco de import de `lucide-react` (senão o lint/tsc acusa import não usado).

Se o `<Textarea>` sob o título for substituído por `<Input>` e o import de `Textarea` deixar de ser usado, verificar: `Textarea` AINDA é usado na aba Descrição — então MANTER o import de Textarea.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Título text-[36px]; campo subtítulo (estado próprio) sob o título gravando em `subtitulo` via criarTarefa; Descrição completa segue em `descricao`; datas com um único calendário nativo; import CalendarDays removido; build e lint sem erros NOVOS (conhecidos fora de escopo: ui/sidebar.tsx, hooks/use-mobile.ts, sync-meta-ads.ts, deferred-items.md).</done>
</task>

</tasks>

<verification>
Rodar no fim, tudo limpo (sem erros NOVOS):
- `npx tsc --noEmit`
- `npm test` (vitest — recorrencia.test.ts e quadro.test.ts passando, incluindo o intervaloPadrao atualizado)
- `npm run build`
- `npm run lint`

Verificação conceitual de FIX-2 (raciocínio, não teste automatizado): criar "toda segunda" numa terça → abrir /tarefas na terça NÃO mostra nenhuma ocorrência dessa série; ela só aparece na segunda seguinte (ou ao navegar até lá pelos campos de data).

Erros conhecidos e ACEITÁVEIS (fora de escopo): ui/sidebar.tsx, hooks/use-mobile.ts, sync-meta-ads.ts, e itens em deferred-items.md.
</verification>

<success_criteria>
- Coluna `subtitulo` (nullable) no schema; migration 0018 gerada com só ADD COLUMN e NÃO aplicada.
- subtitulo aceito em validation, gravado em criarTarefa/atualizarTarefa, selecionado em dados.ts e presente em TarefaCard/TarefaDetalhe.
- Detalhe: título text-[36px] + Input de subtítulo (onBlur salva) sem duplicar a descrição; datas com um único calendário.
- Criar: título maior + campo subtítulo próprio (Descrição completa intacta) + subtitulo enviado; datas com um único calendário; import CalendarDays removido.
- Recorrência: default do quadro = [hoje-30, hoje], sem futuro; futuro só sob navegação explícita; Nova Tarefa datada em HOJE; testes passando.
- tsc + test + build + lint sem erros novos.
</success_criteria>

<output>
Após completar, criar `.planning/quick/260714-vy7-ajustes-tarefa-1-titulo-maior-campo-subt/260714-vy7-SUMMARY.md`.
</output>
