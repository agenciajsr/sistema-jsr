---
phase: quick-260717-i26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/financeiro/a-receber.ts
  - src/lib/financeiro/a-receber.test.ts
  - src/actions/financeiro.ts
  - src/app/(app)/financeiro/page.tsx
  - src/app/(app)/financeiro/contas-table.tsx
  - src/app/(app)/financeiro/previsao-por-mes.tsx
  - src/lib/tarefas/ficha-cliente.ts
  - src/app/(app)/clientes/[id]/page.tsx
  - src/components/ficha/tarefas-cliente.tsx
autonomous: true
requirements: [QUICK-I26]
must_haves:
  truths:
    - "Aba 'A Receber' do /financeiro mostra por padrão só pendentes dos próximos 30 dias + todas as vencidas"
    - "Um toggle 'Mostrar todas' revela as ~35 pendentes completas (nada foi apagado)"
    - "Card 'Previsão por mês' lista meses futuros com soma (ex.: ago/2026 — R$ 9.000)"
    - "Ficha do cliente tem seção Tarefas com as tarefas do cliente: abertas primeiro, concluídas como histórico"
  artifacts:
    - path: "src/lib/financeiro/a-receber.ts"
      provides: "filtro 30 dias puro + label de mês pt-BR, testado com vitest"
    - path: "src/app/(app)/financeiro/previsao-por-mes.tsx"
      provides: "card Previsão por mês"
    - path: "src/components/ficha/tarefas-cliente.tsx"
      provides: "seção Tarefas na ficha do cliente"
  key_links:
    - from: "src/app/(app)/financeiro/page.tsx"
      to: "getPrevisaoReceitaPorMes (actions/financeiro.ts)"
      via: "chamada SEQUENCIAL após getVisaoCobrancas (nunca dentro dos Promise.all existentes)"
      pattern: "getPrevisaoReceitaPorMes"
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "getTarefasDoClienteFicha"
      via: "query agregada sequencial na carga da ficha"
      pattern: "getTarefasDoClienteFicha"
---

<objective>
Duas melhorias de exibição (zero migration, zero mudança de dado):
1. /financeiro, aba "A Receber": por padrão mostrar só pendentes dos próximos 30 dias + vencidas, com toggle "Mostrar todas"; novo card "Previsão por mês" com soma agregada dos meses futuros (GROUP BY mês no banco) — preserva a visão de MRR futuro (ex.: 26 semanais do Yury até jan/2027 viram somas mensais, não 26 linhas).
2. Ficha do cliente: nova seção "Tarefas" listando tarefas da tabela `tarefas` com `clienteId` do cliente — abertas primeiro + histórico de concluídas, via query agregada (não N+1).

Output: módulo puro testado, action agregada nova, card de previsão, toggle na tabela, aba Tarefas na ficha.
</objective>

<context>
@./CLAUDE.md
@src/app/(app)/financeiro/page.tsx
@src/app/(app)/financeiro/contas-table.tsx
@src/actions/financeiro.ts (getContasAReceber linha ~409)
@src/app/(app)/clientes/[id]/page.tsx (Tabs linhas ~310-315)
@src/lib/db/schema.ts (tarefas linha ~275, relation cliente ~376)

Restrições locked (STATE.md / decisões do usuário):
- Queries SEQUENCIAIS e agregadas — pool max=5, NUNCA adicionar itens aos Promise.all existentes; novas queries entram em sequência após `getVisaoCobrancas` (padrão já usado em quick-260716-sr5).
- Nenhum dado apagado, nenhum schema alterado — mudança É SÓ DE EXIBIÇÃO.
- UI 100% em pt-BR, dark mode com variantes `dark:` quando usar cor fora dos tokens.
- Matemática/agrupamento testável = módulo puro (padrão de `@/lib/financeiro/calculos`, zero import de db/auth/react).
- Não usar 'use server' em módulos de leitura comuns; a ficha do cliente é Server Component e chama funções de dados direto.

<interfaces>
Linha de getContasAReceber (já existente, NÃO mudar a query — o filtro é de exibição):
```ts
{ id, descricao, valor: string, data: 'YYYY-MM-DD', status: 'pago'|'pendente'|'vencido',
  clienteNome, centroCusto, formaPagamento, responsavelNome, comprovanteUrl }
```
Colunas relevantes de `tarefas`: titulo, subtitulo, status ('a_fazer'|'em_andamento'|'concluida'|'nao_realizada'), prioridade, data (date), clienteId, responsavelId, ehMolde (moldes NUNCA aparecem em listas), concluidaEm, codigo.
Helper de data já existente: `hojeBrasilia()` em `@/lib/date-br` retorna 'YYYY-MM-DD'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo puro de A Receber (filtro 30 dias + label de mês) + action agregada de previsão</name>
  <files>src/lib/financeiro/a-receber.ts, src/lib/financeiro/a-receber.test.ts, src/actions/financeiro.ts, src/app/(app)/financeiro/page.tsx</files>
  <behavior>
    Testes vitest (padrão dos módulos puros existentes) para `src/lib/financeiro/a-receber.ts`:
    - `filtrarAReceber(contas, hoje, mostrarTodas)`: com mostrarTodas=false mantém (a) toda conta status='vencido', (b) toda pendente com data <= hoje, (c) pendente com data até hoje+30 dias; corta pendente com data > hoje+30. Com mostrarTodas=true devolve tudo. Ordenação por data preservada.
    - Casos de borda: data exatamente hoje+30 (entra), virada de ano (hoje='2026-12-15' inclui '2027-01-10'), lista vazia.
    - `labelMesPtBr('2026-08')` → 'ago/2026' (usar array fixo de abreviações pt-BR, não depender de Intl locale no runtime — determinístico em teste).
    Toda a matemática de data em strings 'YYYY-MM-DD' (padrão do projeto — evita ambiguidade de timezone).
  </behavior>
  <action>
    1. Criar `src/lib/financeiro/a-receber.ts` (módulo PURO: zero import de db/auth/react) com `filtrarAReceber`, `labelMesPtBr` e o tipo `ContaAReceber` mínimo ({ data: string; status: string }) genérico via generic `T extends`.
    2. Testes primeiro (RED), depois implementação (GREEN).
    3. Em `src/actions/financeiro.ts`, adicionar `getPrevisaoReceitaPorMes()`: query AGREGADA única com GROUP BY no banco —
       `select to_char(data, 'YYYY-MM') as mes, sum(valor) as total from transacoes where tipo='receita' and status in ('pendente','vencido') and data > hoje group by 1 order by 1` (via Drizzle `sql` template; `hoje` = hojeBrasilia(); retornar `{ mes: string; total: number }[]`). Guard de sessão igual às demais (getCurrentUser → []). NÃO alterar getContasAReceber.
    4. Em `page.tsx` do financeiro: chamar `const previsaoMeses = await getPrevisaoReceitaPorMes()` SEQUENCIALMENTE logo após `const visaoCobrancas = await getVisaoCobrancas()` (comentário citando a regra do pool). Acrescentar ao array retornado por carregarDados e à desestruturação. NÃO mexer nos dois Promise.all existentes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/financeiro/a-receber.test.ts</automated>
  </verify>
  <done>Módulo puro com testes passando; getPrevisaoReceitaPorMes agregada no banco; page.tsx carrega a previsão sequencialmente sem tocar nos lotes existentes.</done>
</task>

<task type="auto">
  <name>Task 2: UI da aba A Receber — filtro padrão 30 dias com toggle + card Previsão por mês</name>
  <files>src/app/(app)/financeiro/contas-table.tsx, src/app/(app)/financeiro/previsao-por-mes.tsx, src/app/(app)/financeiro/page.tsx</files>
  <action>
    1. `contas-table.tsx` (client): quando `tipo === 'receita'`, adicionar estado `mostrarTodas` (useState false) e aplicar `filtrarAReceber(contas, hoje, mostrarTodas)` — `hoje` chega por prop nova opcional `hoje?: string` (passada da page via hojeBrasilia(); evita divergência server/client). Acima da tabela, linha com contagem ("Mostrando X de Y — próximos 30 dias + vencidas") e botão/Switch "Mostrar todas" / "Mostrar próximos 30 dias" (pt-BR). Total do rodapé usa a lista FILTRADA exibida. Comportamento da aba de despesas ('pagar') inalterado (sem toggle, lista completa como hoje).
    2. Criar `previsao-por-mes.tsx` (pode ser server component simples): Card "Previsão por mês" recebendo `{ mes, total }[]`; cada linha `labelMesPtBr(mes)` + valor em BRL (Intl 'pt-BR'), com destaque tabular-nums; estado vazio "Nenhuma receita futura prevista." Usar tokens do tema (bg-muted, text-muted-foreground) — dark mode automático; se usar cor literal, incluir variante `dark:`.
    3. Na aba "receber" da page: renderizar o card de Previsão por mês acima (ou ao lado, grid responsivo) do Card "Contas a Receber"; passar `hoje` para ContasTable. A contagem do TabsTrigger "A Receber (n)" passa a refletir a lista filtrada padrão (calcular na page com filtrarAReceber) — o KPI "A Receber" (resumo.aReceber) fica INTOCADO.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Aba A Receber abre mostrando só 30 dias + vencidas com card de previsão mensal somada; "Mostrar todas" revela as ~35; nenhum dado alterado no banco; textos pt-BR e dark mode ok.</done>
</task>

<task type="auto">
  <name>Task 3: Ficha do cliente — seção "Tarefas" (abertas primeiro + histórico), query agregada</name>
  <files>src/lib/tarefas/ficha-cliente.ts, src/components/ficha/tarefas-cliente.tsx, src/app/(app)/clientes/[id]/page.tsx</files>
  <action>
    1. Criar `src/lib/tarefas/ficha-cliente.ts` (módulo server comum, SEM 'use server' — a ficha é Server Component e chama direto, padrão de `@/lib/cobrancas/dados`): `getTarefasDoClienteFicha(clienteId)` com DUAS queries sequenciais agregadas e limitadas:
       - Abertas: `status in ('a_fazer','em_andamento')` and `ehMolde = false` and `clienteId = X`, order by data asc, limit 50; join leftJoin profiles para responsavelNome.
       - Histórico: `status in ('concluida','nao_realizada')` mesmas condições, order by concluidaEm desc nulls last (fallback data desc), limit 20.
       Selecionar só as colunas exibidas: id, codigo, titulo, subtitulo, status, prioridade, data, concluidaEm, responsavelNome. Retornar `{ abertas, historico }`. NUNCA listar moldes (ehMolde=true).
    2. Criar `src/components/ficha/tarefas-cliente.tsx`: componente de exibição (server component simples, sem interação) com dois blocos — "Abertas" (badge de prioridade e status, data de vencimento dd/MM, destaque `text-destructive` se data < hoje) e "Histórico" (concluídas com data de conclusão; nao_realizada com badge cinza "Não realizada"). Estado vazio: "Nenhuma tarefa vinculada a este cliente." Link "Ver em Tarefas" para `/tarefas`. Tudo pt-BR, tokens do tema (variantes dark: para cores pastel, conforme memória de dark mode).
    3. Em `clientes/[id]/page.tsx`: adicionar `const tarefasFicha = await getTarefasDoClienteFicha(id)` SEQUENCIALMENTE junto às demais cargas da ficha (após a última query existente, nunca em Promise.all). Nova aba `<TabsTrigger value="tarefas">🗒️ Tarefas</TabsTrigger>` (entre Checklist e Acompanhamento) + `<TabsContent value="tarefas">` renderizando TarefasCliente. Contagem de abertas no rótulo da aba se > 0 (ex.: "Tarefas (2)").
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Ficha do cliente exibe aba Tarefas com abertas primeiro e histórico de concluídas (a 1 tarefa real do banco aparece); 2 queries agregadas sequenciais, sem N+1, moldes excluídos.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — suíte completa verde (nenhuma regressão nos ~420+ testes).
- `npm run build` — build Next sem erro de tipos.
- Nenhuma migration gerada; `src/lib/db/schema.ts` intocado; getContasAReceber intocada.
- Nenhum Promise.all existente ganhou item novo (grep nos diffs de page.tsx).
</verification>

<success_criteria>
- A receber padrão = próximos 30 dias + vencidas, com toggle para todas; previsão mensal agregada visível e batendo com as mesmas transações pendentes.
- Seção Tarefas na ficha do cliente funcionando com dado real, abertas primeiro + histórico.
- Textos pt-BR, dark mode ok, queries sequenciais/agregadas.
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260717-i26-financeiro-a-receber-com-filtro-30-dias-/260717-i26-SUMMARY.md`
</output>
