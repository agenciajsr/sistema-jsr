---
phase: quick-260717-i26
plan: 01
subsystem: financeiro, clientes
tags: [a-receber, previsao-mensal, tarefas-ficha, filtro-exibicao]
requires: []
provides:
  - "filtrarAReceber/labelMesPtBr (módulo puro testado)"
  - "getPrevisaoReceitaPorMes (agregada GROUP BY mês)"
  - "getTarefasDoClienteFicha + aba Tarefas na ficha"
affects: [/financeiro, /clientes/[id]]
tech-stack:
  added: []
  patterns: ["módulo puro sob TDD", "queries sequenciais agregadas pós-lotes"]
key-files:
  created:
    - src/lib/financeiro/a-receber.ts
    - src/lib/financeiro/a-receber.test.ts
    - src/app/(app)/financeiro/previsao-por-mes.tsx
    - src/lib/tarefas/ficha-cliente.ts
    - src/components/ficha/tarefas-cliente.tsx
  modified:
    - src/actions/financeiro.ts
    - src/app/(app)/financeiro/page.tsx
    - src/app/(app)/financeiro/contas-table.tsx
    - src/app/(app)/clientes/[id]/page.tsx
decisions:
  - "Filtro 30 dias é SÓ de exibição (getContasAReceber intocada); toggle revela tudo"
  - "labelMesPtBr usa array fixo de abreviações pt-BR (determinístico, sem Intl locale)"
  - "Histórico de tarefas ordena por concluidaEm desc nulls last com fallback data desc"
metrics:
  duration: "~15min"
  completed: "2026-07-17"
---

# Quick 260717-i26: Financeiro A Receber com filtro 30 dias + Tarefas na ficha — Summary

Aba "A Receber" passa a abrir só com pendentes dos próximos 30 dias + vencidas (toggle "Mostrar todas" revela tudo), com card "Previsão por mês" somando os meses futuros no banco; ficha do cliente ganha aba Tarefas (abertas primeiro + histórico) com 2 queries agregadas sequenciais.

## Tarefas

| Task | Descrição | Commits |
| ---- | --------- | ------- |
| 1 | Módulo puro filtrarAReceber/labelMesPtBr (TDD, 12 testes) + getPrevisaoReceitaPorMes agregada + carga sequencial na page | 15129c0 (RED), 13729f0 (GREEN) |
| 2 | UI: toggle 30 dias/todas na ContasTable (só receitas), card Previsão por mês, badge da aba pela visão padrão | 477aeb6 |
| 3 | Aba Tarefas na ficha do cliente: getTarefasDoClienteFicha (abertas limit 50 + histórico limit 20, moldes excluídos) + componente TarefasCliente | 2054329 |

## Detalhes

- **Filtro de exibição, não de dado**: getContasAReceber intocada; `filtrarAReceber` mantém toda vencida + pendente até hoje+30 (inclusive, com virada de ano correta em strings YYYY-MM-DD). Total do rodapé usa a lista exibida; KPI "A Receber" (resumo.aReceber) intocado.
- **Previsão por mês**: `getPrevisaoReceitaPorMes` faz UMA query com `to_char(data,'YYYY-MM')` + `sum(valor)` para receitas pendentes/vencidas com data > hoje; chamada SEQUENCIAL após `getVisaoCobrancas` (nenhum Promise.all existente foi alterado).
- **Ficha do cliente**: `getTarefasDoClienteFicha` (módulo server comum, sem 'use server') roda 2 queries sequenciais com leftJoin em profiles; `ehMolde=true` nunca listado. Aba "🗒️ Tarefas" entre Checklist e Acompanhamento, com contagem de abertas no rótulo.
- **pt-BR + dark mode**: textos todos em português; badges pastel com variantes `dark:`; demais cores via tokens do tema.

## Deviations from Plan

None - plano executado como escrito. (Única escolha de layout: card de previsão ao lado da tabela em grid 1/3 + 2/3 no desktop, empilhado no mobile — dentro do "acima ou ao lado" previsto.)

## Verificação

- `npx vitest run` — 2897 testes passando (12 novos), 0 falhas.
- `npx tsc --noEmit` — sem erros; `npm run build` — build Next ok.
- Nenhuma migration; schema.ts e getContasAReceber intocados; nenhum Promise.all ganhou item.

## Self-Check: PASSED

- FOUND: src/lib/financeiro/a-receber.ts, a-receber.test.ts, previsao-por-mes.tsx, ficha-cliente.ts, tarefas-cliente.tsx
- FOUND: commits 15129c0, 13729f0, 477aeb6, 2054329
