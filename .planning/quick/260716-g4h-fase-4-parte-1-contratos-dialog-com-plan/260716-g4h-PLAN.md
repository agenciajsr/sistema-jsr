---
phase: quick-260716-g4h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0029_contratos_fluxo.sql
  - src/lib/contratos/fluxo.ts
  - src/lib/contratos/fluxo.test.ts
  - src/lib/validations/documentos.ts
  - src/lib/validations/documentos.test.ts
  - src/actions/crm.ts
  - src/components/crm/converter-cliente-dialog.tsx
  - src/app/contrato/[token]/page.tsx
  - src/app/contrato/[token]/formulario-contratante.tsx
  - src/actions/contrato-publico.ts
  - src/app/(app)/contratos/page.tsx
  - src/components/contratos/copiar-link-botao.tsx
autonomous: true
requirements: [FUNIL-F4P1]
must_haves:
  truths:
    - "Ao converter um ganho em cliente, o usuário escolhe duração (3/6 meses), serviço e mensalidade, e um contrato é criado com token único e status aguardando_dados"
    - "O dialog de sucesso e a página /contratos exibem o link /contrato/[token] com botão copiar"
    - "O cliente abre /contrato/[token] no celular SEM login, vê dados pré-preenchidos, escolhe PJ ou PF, preenche e envia; status vira dados_recebidos"
    - "Token inválido mostra página de erro amigável; reenvio sobrescreve os dados (idempotente)"
    - "/contratos lista cliente (link), serviço, duração, mensalidade, status do fluxo (badge), datas e botão copiar link"
  artifacts:
    - path: "drizzle/0029_contratos_fluxo.sql"
      provides: "Migration aditiva (colunas novas em contratos) — GERADA, NÃO aplicada"
    - path: "src/lib/validations/documentos.ts"
      provides: "Validação CPF/CNPJ com dígito verificador (módulo puro)"
    - path: "src/app/contrato/[token]/page.tsx"
      provides: "Página pública mobile-first de coleta de dados do contratante"
    - path: "src/actions/contrato-publico.ts"
      provides: "Submit público validando token, salva jsonb e muda status"
  key_links:
    - from: "src/actions/crm.ts (converterOportunidadeEmCliente)"
      to: "tabela contratos"
      via: "insert com token crypto.randomUUID/randomBytes e statusFluxo aguardando_dados"
      pattern: "statusFluxo.*aguardando_dados"
    - from: "src/app/contrato/[token]/formulario-contratante.tsx"
      to: "src/actions/contrato-publico.ts"
      via: "server action pública validando token antes de gravar"
      pattern: "salvarDadosContratante"
---

<objective>
Fase 4 Parte 1 do funil — fundação de Contratos: a conversão Ganho→Cliente passa a coletar duração/serviço/mensalidade e gera um registro de contrato com token único e link público de coleta de dados do contratante (PJ/PF); a página /contratos é reformada para acompanhar o fluxo.

Purpose: eliminar a coleta manual de dados contratuais via WhatsApp; preparar o terreno para a Parte 2 (PDF + Autentique + assinatura).
Output: schema/migration aditiva, dialog de conversão estendido, página pública /contrato/[token], /contratos reformada, módulos puros testados (CPF/CNPJ + status do fluxo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/FUNIL-AGENCIA-PLANEJAMENTO.md
@.planning/quick/260716-ezd-fase-3-do-funil-ganho-no-crm-converte-le/260716-ezd-SUMMARY.md
@src/lib/db/schema.ts
@src/actions/crm.ts
@src/components/crm/converter-cliente-dialog.tsx
@src/app/(app)/contratos/page.tsx
@src/actions/contratos.ts

<interfaces>
Estado atual relevante (verificado no código em 16/jul/2026):

Tabela `contratos` (src/lib/db/schema.ts:67-76) — HOJE:
```ts
export const contratos = pgTable('contratos', {
  id: uuid('id').primaryKey().defaultRandom(),
  clienteId: uuid('cliente_id').notNull().references(() => clientes.id, { onDelete: 'cascade' }),
  dataInicio: date('data_inicio').notNull(),
  dataVencimento: date('data_vencimento').notNull(),
  valorMensal: numeric('valor_mensal', { precision: 10, scale: 2 }).notNull(), // dinheiro NUNCA float
  createdAt: ...
})
```
→ **Mensalidade = valorMensal (coluna EXISTENTE).** O MRR do /financeiro já deriva de contratos.valorMensal — criar o contrato na conversão automaticamente alimenta o MRR. NÃO criar coluna duplicada de mensalidade.

Auth: NÃO existe src/middleware.ts. A proteção é o layout `src/app/(app)/layout.tsx` (getCurrentUser + redirect /login). Rota pública = criar FORA do grupo (app): `src/app/contrato/[token]/` no root de app/. Nenhuma mudança de middleware necessária.

Conversão (Fase 3): `converterOportunidadeEmCliente(oportunidadeId)` em src/actions/crm.ts:657 — idempotente em 3 níveis, updates sequenciais (nunca Promise.all), degradação graciosa com console.warn quando coluna não existe. Dialog: src/components/crm/converter-cliente-dialog.tsx (130 linhas, useTransition, toasts com "Abrir ficha").

/contratos atual: src/app/(app)/contratos/page.tsx (113 linhas) — Server Component com StatCards + Table lendo `listarTodosContratos` de src/actions/contratos.ts. Reformar em cima, mantendo maxDuration e o padrão formatarData sem Date.

Serviços da JSR (memória crm-modelo-lead-negocio): tráfego pago, landing pages, CRM, estruturação/estratégia — confirmar a lista exata usada no CRM (buscar constante de serviços em src/lib/crm/) e reutilizar.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fundação — schema aditivo, migration 0029, módulos puros (CPF/CNPJ + fluxo) e conversão estendida</name>
  <files>src/lib/db/schema.ts, drizzle/0029_contratos_fluxo.sql, src/lib/validations/documentos.ts, src/lib/validations/documentos.test.ts, src/lib/contratos/fluxo.ts, src/lib/contratos/fluxo.test.ts, src/actions/crm.ts, src/components/crm/converter-cliente-dialog.tsx</files>
  <behavior>
    - documentos.test.ts: validarCpf/validarCnpj com dígito verificador — casos válidos, dígito errado, tamanho errado, todos-iguais (111.111.111-11 inválido), com/sem máscara; formatarCpf/formatarCnpj
    - fluxo.test.ts: rótulo/cor de badge por status (aguardando_dados, dados_recebidos, aguardando_assinatura, assinado); montarDadosContrato({duracaoMeses, mensalidade, hoje}) → dataInicio=hoje, dataVencimento=hoje+duracaoMeses (date-fns addMonths, strings YYYY-MM-DD sem fuso); gerarToken retorna string ≥32 chars imprevisível (crypto)
  </behavior>
  <action>
    1. Schema (ADITIVO, nada removido/alterado nas colunas existentes) — novas colunas em `contratos`:
       - `token` text UNIQUE (nullable — contratos antigos não têm)
       - `statusFluxo` text (nullable; valores: 'aguardando_dados' | 'dados_recebidos' | 'aguardando_assinatura' | 'assinado' — usar text + união TS, NÃO pgEnum, para migration mais simples e evolução na Parte 2)
       - `duracaoMeses` integer (nullable)
       - `servico` text (nullable)
       - `dadosContratante` jsonb (nullable)
       - `dadosRecebidosEm` timestamp with time zone (nullable)
       Gerar migration com drizzle-kit generate e CONFERIR o SQL: deve conter APENAS ALTER TABLE contratos ADD COLUMN + unique de token (se vier contaminada pelo snapshot, editar à mão como no 260716-ezd). Renomear para `0029_contratos_fluxo.sql`, ajustar _journal.json. **NÃO APLICAR** (regra do projeto — dono aplica na mão).
    2. Módulos puros (TDD, RED→GREEN): `src/lib/validations/documentos.ts` (validarCpf/validarCnpj com dígito verificador, aceitando com/sem máscara; helpers de formatação) e `src/lib/contratos/fluxo.ts` (STATUS_FLUXO com rótulo pt-BR + classes de badge incl. variantes dark:, montarDadosContrato, gerarToken usando crypto.randomBytes(32).toString('base64url') — módulo puro pode importar node:crypto, mas zero import de db/auth/react).
    3. `converterOportunidadeEmCliente` em src/actions/crm.ts: aceitar novo parâmetro `{ duracaoMeses: 3|6, servico: string, mensalidade: number }` (Zod na action). Após criar/vincular o cliente, INSERT em contratos (sequencial, nunca Promise.all): clienteId, dataInicio/dataVencimento de montarDadosContrato, valorMensal=mensalidade, token=gerarToken(), statusFluxo='aguardando_dados', duracaoMeses, servico. Degradação graciosa: se o insert falhar por coluna inexistente (migration 0029 não aplicada), console.warn e a conversão SEGUE sem contrato (padrão da Fase 3). Retornar `{ clienteId, contratoToken? }`. Idempotência: se o cliente já existia E já tem contrato com token e statusFluxo, retornar o token existente em vez de criar outro contrato.
    4. Dialog `converter-cliente-dialog.tsx`: adicionar campos duração (3/6 meses — Select ou botões), serviço (Select com os serviços da JSR reutilizando a constante existente do CRM) e mensalidade (R$, input numérico) — React Hook Form + Zod, tudo obrigatório. Após sucesso, trocar o conteúdo para estado de SUCESSO no próprio dialog: link completo `${window.location.origin}/contrato/${token}` com botão Copiar (navigator.clipboard + toast "Link copiado"); manter ação "Abrir ficha". Se a action retornar sem token (degradação), mostrar o sucesso sem o bloco do link.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validations/documentos.test.ts src/lib/contratos/fluxo.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Migration 0029 gerada (só ADD COLUMN, não aplicada); testes dos módulos puros verdes; conversão cria contrato aguardando_dados com token; dialog coleta duração/serviço/mensalidade e mostra link copiável no sucesso.</done>
</task>

<task type="auto">
  <name>Task 2: Página pública /contrato/[token] com formulário PJ/PF e submit público idempotente</name>
  <files>src/app/contrato/[token]/page.tsx, src/app/contrato/[token]/formulario-contratante.tsx, src/actions/contrato-publico.ts</files>
  <action>
    1. `src/app/contrato/[token]/page.tsx` (Server Component, FORA do grupo (app) — sem auth; conferir que o root layout src/app/layout.tsx não força login — não força, é o (app)/layout.tsx que faz). Busca o contrato por token + join do cliente (nome/email/telefone) em query única sequencial. Token não encontrado ou statusFluxo nulo → tela de erro amigável em português ("Link inválido ou expirado — fale com a equipe JSR"). `export const maxDuration = 60`.
    2. Layout público limpo, MOBILE-FIRST (cliente abre no WhatsApp): logo JSR (public/logo-jsr.png), título "Dados para o contrato", card único, sem sidebar/header do app. Suportar dark mode via classes padrão (a página herda globals.css).
    3. `formulario-contratante.tsx` (Client): React Hook Form + zodResolver.
       - Primeiro campo: tipo de contratante — "Pessoa Jurídica" | "Pessoa Física" (radio/botões grandes, thumb-friendly), alternando os blocos de campos.
       - PJ: razaoSocial, cnpj, enderecoSede, telefone, nomeRepresentante, nacionalidade, estadoCivil, profissao, cpf (representante), enderecoRepresentante, email.
       - PF: nomeCompleto, cpf, nacionalidade, estadoCivil, profissao, endereco, telefone, email.
       - Pré-preencher nome/email/telefone com os dados do cliente (props do server). Zod usa validarCpf/validarCnpj de src/lib/validations/documentos.ts (refine com mensagem pt-BR); máscara leve de CPF/CNPJ no onChange é bem-vinda mas opcional.
       - Se o contrato já tem dadosContratante (reenvio), pré-preencher com eles.
    4. `src/actions/contrato-publico.ts` ('use server', PÚBLICA — sem getCurrentUser): `salvarDadosContratante(token, dados)`. Valida token (SELECT por token; inexistente → erro amigável), valida `dados` com o MESMO schema Zod (nunca confiar só no client), UPDATE contratos SET dados_contratante = jsonb, status_fluxo = 'dados_recebidos', dados_recebidos_em = now() WHERE token = $token. Idempotente: reenvio sobrescreve. NÃO regredir status se já estiver em aguardando_assinatura/assinado (Parte 2) — nesses casos ainda aceitar e sobrescrever o jsonb, mas manter o status (documentar no código). Nunca vazar erro técnico: try/catch com mensagem genérica em português. Sucesso → tela/estado "Dados recebidos! A equipe JSR vai preparar seu contrato."
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Rota /contrato/[token] abre sem login com dados pré-preenchidos; PJ/PF alternam campos; CPF/CNPJ validados com dígito verificador; submit grava jsonb + dados_recebidos; token inválido → erro amigável; reenvio sobrescreve.</done>
</task>

<task type="auto">
  <name>Task 3: Reformar a página /contratos com fluxo, badges e copiar link</name>
  <files>src/app/(app)/contratos/page.tsx, src/components/contratos/copiar-link-botao.tsx, src/actions/contratos.ts</files>
  <action>
    1. Ler a página atual antes de mexer (StatCards + tabela via listarTodosContratos). Estender `listarTodosContratos` (ou criar consulta nova em src/actions/contratos.ts) para trazer também token, statusFluxo, duracaoMeses, servico — com degradação graciosa: se as colunas não existirem ainda (migration 0029 pendente), capturar o erro e recair na consulta antiga (campos novos = null), padrão getWorkspaceAtual. Queries SEQUENCIAIS.
    2. Tabela reformada, colunas: Cliente (Link para /clientes/[id]), Serviço, Duração ("3 meses"/"6 meses"; "—" p/ legado), Mensalidade (formatadorMoeda existente), Status do fluxo (Badge usando STATUS_FLUXO de src/lib/contratos/fluxo.ts — Aguardando dados / Dados recebidos / Aguardando assinatura / Assinado; contrato legado sem statusFluxo → badge neutro "Manual"), Início/Fim (formatarData existente), Ações: botão copiar link (só quando token existe).
    3. `copiar-link-botao.tsx` (Client, pequeno): recebe token, monta `${window.location.origin}/contrato/${token}`, navigator.clipboard.writeText + toast "Link copiado" (sonner), ícone Copy do lucide com feedback visual (Check por 2s). Badges com variantes dark: (memória dark-mode).
    4. Manter os StatCards existentes (ajustar se fizer sentido: ex. contagem por status do fluxo), maxDuration e o resto do padrão da página. Tudo em português.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>/contratos mostra cliente linkado, serviço, duração, mensalidade, badge de status do fluxo, datas e copiar link; contratos legados aparecem sem quebrar; build limpo.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — suíte inteira verde (incl. documentos.test.ts e fluxo.test.ts novos)
- `npx tsc --noEmit` e `npm run build` limpos
- Migration `drizzle/0029_contratos_fluxo.sql` contém APENAS ADD COLUMN aditivos (+ unique do token) e NÃO foi aplicada
- Nenhum Promise.all novo em actions; nenhuma rota pública além de /contrato/[token] e da action pública
- git add por arquivo (NUNCA `git add -A`)
</verification>

<success_criteria>
- Conversão Ganho→Cliente coleta duração (3/6), serviço e mensalidade; cria contrato `aguardando_dados` com token e alimenta o MRR via valorMensal existente
- Link /contrato/[token] copiável no dialog de sucesso e em /contratos
- Página pública mobile-first funciona sem login, PJ/PF com validação de CPF/CNPJ por dígito verificador, submit idempotente muda para `dados_recebidos`
- Tudo degrada graciosamente enquanto a migration 0029 não for aplicada (conversão segue sem contrato; /contratos recai na consulta antiga)
- Fora do escopo respeitado: nada de PDF/Autentique/webhook/ativação automática (Parte 2)
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260716-g4h-fase-4-parte-1-contratos-dialog-com-plan/260716-g4h-SUMMARY.md` e registrar o checkpoint humano pendente: aplicar migration 0029 na mão (script com DIRECT_URL em sql.begin, modelo scripts/aplicar-migration-0024.ts) + `git push origin master` + testar o fluxo completo em produção.
</output>
