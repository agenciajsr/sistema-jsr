---
phase: 260715-1rq-reformular-pagina-crm-no-visual-do-mocku
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [CRM-MOCKUP]
files_modified:
  - src/lib/crm/tempo.ts
  - src/lib/crm/tempo.test.ts
  - src/lib/crm/origem.ts
  - src/lib/crm/dados.ts
  - src/components/crm/kpis-crm.tsx
  - src/components/crm/kanban-crm.tsx
  - src/components/crm/card-oportunidade.tsx
  - src/components/crm/barra-origem-leads.tsx
  - src/components/crm/crm-view.tsx
  - src/app/(app)/crm/page.tsx
autonomous: true

must_haves:
  truths:
    - "A pagina /crm mostra 6 KPIs reais no topo (total de oportunidades, valor da origem, taxa de conversao, ganhas, atividades atrasadas, sem contato +7d)"
    - "Cada coluna do kanban mostra o nome da etapa, contagem, VALOR TOTAL PARADO (somaValor) e a PROBABILIDADE da etapa"
    - "Cada card mostra o badge de origem (VIA MANUAL/WHATSAPP/LANDING/META/INDICACAO), o valor, o tempo desde a criacao e o aviso 'Nao contatado' quando aplicavel"
    - "O rodape mostra a barra 'Origem dos leads' com uma pilula por canal e o percentual real sobre o total de oportunidades abertas"
    - "As abas Kanban/Lista/Calendario aparecem; so Kanban renderiza o board, Lista e Calendario mostram placeholder 'Visao em construcao' sem dado falso"
    - "A busca filtra as oportunidades ja carregadas por titulo/contato/empresa no client"
    - "Se o workspace/pipeline nao existem (migration 0019), a pagina segue mostrando o aviso amarelo 'CRM ainda nao ativado' sem quebrar"
  artifacts:
    - path: "src/lib/crm/tempo.ts"
      provides: "helper tempoRelativoCurto(date) em pt-BR (agora/5min/3h/15d/2m/2a)"
      exports: ["tempoRelativoCurto"]
    - path: "src/lib/crm/tempo.test.ts"
      provides: "testes Vitest do helper de tempo com datas fixas"
    - path: "src/lib/crm/origem.ts"
      provides: "mapa origem -> {rotulo, nome, cor} e helpers rotuloOrigem/nomeOrigem/corOrigem"
      exports: ["ORIGEM_META", "rotuloOrigem", "nomeOrigem", "corOrigem"]
    - path: "src/lib/crm/dados.ts"
      provides: "getCrmVisaoGeral() retornando kanban + kpis + origens, tudo com queries sequenciais/agregadas; getKanban REMOVIDA (getCrmVisaoGeral e a unica fonte da pagina)"
      exports: ["getCrmVisaoGeral"]
    - path: "src/components/crm/kpis-crm.tsx"
      provides: "faixa de 6 StatCard com os KPIs do CRM"
    - path: "src/components/crm/barra-origem-leads.tsx"
      provides: "barra de pilulas por origem com percentual"
    - path: "src/components/crm/crm-view.tsx"
      provides: "orquestrador client: header + seletor de pipeline + abas + busca"
    - path: "src/app/(app)/crm/page.tsx"
      provides: "server component que busca getCrmVisaoGeral e renderiza CrmView, mantendo a degradacao graciosa"
  key_links:
    - from: "src/app/(app)/crm/page.tsx"
      to: "getCrmVisaoGeral"
      via: "await no server component"
      pattern: "getCrmVisaoGeral\\("
    - from: "src/lib/crm/dados.ts"
      to: "crmOportunidades (GROUP BY status)"
      via: "query agregada de KPIs"
      pattern: "groupBy"
    - from: "src/components/crm/card-oportunidade.tsx"
      to: "src/lib/crm/origem.ts + src/lib/crm/tempo.ts"
      via: "import dos helpers"
      pattern: "rotuloOrigem|tempoRelativoCurto"
    - from: "src/components/crm/crm-view.tsx"
      to: "src/components/crm/kanban-crm.tsx + barra-origem-leads.tsx"
      via: "render das abas e do rodape"
      pattern: "KanbanCrm|BarraOrigemLeads"
---

<objective>
Reformular a pagina /crm para reproduzir o mockup definitivo do usuario (ver
`design-referencia-crm-kanban`): header com seletor de pipeline + abas de visao,
faixa de 6 KPIs, colunas do kanban com valor parado + probabilidade, cards com
badge de origem e tempo relativo, e barra "Origem dos leads" no rodape — TUDO com
dados reais agregados do banco, nada de mock.

Purpose: dar fidelidade visual premium (padrao Linear/Stripe do projeto) ao CRM
sem inventar funcionalidade — o que ainda nao existe (Lista/Calendario, filtros,
periodo) aparece como placeholder honesto, nao como dado falso.

Output: helpers puros (tempo, origem), a funcao de dados getCrmVisaoGeral (que
SUBSTITUI getKanban como fonte da pagina), os componentes de apresentacao
(KpisCrm, KanbanCrm reformulado, CardOportunidade reformulado, BarraOrigemLeads)
e o orquestrador client CrmView, com a page.tsx buscando os dados no server.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
IMPORTANTE (CLAUDE.md): tudo em portugues; mensagens de UI SEM acentos que
possam corromper (o codigo existente ja evita acentos em strings de UI dinamicas
como toasts — siga o mesmo estilo). NAO adicionar dependencias novas: date-fns
(^4.4.0), vitest (^4.1.10), tabs/dropdown-menu/tooltip/badge/input/select ja
existem em src/components/ui/. NAO criar migration nem alterar schema — as
colunas necessarias ja existem.

DECISAO DE RECONCILIACAO (checker warning): a nova `getCrmVisaoGeral()` SUBSTITUI
`getKanban` como unica fonte de dados da pagina. `getKanban` sera REMOVIDA na
Task 1 (nada mais a usa apos a Task 3 reescrever a page.tsx). Assim os campos
novos e OBRIGATORIOS de `OportunidadeCard` (`createdAt`, `semContato`) nao criam
literal incompleto — a unica funcao que constroi cards passa a ser
`getCrmVisaoGeral`, que os preenche. Unico importador atual de getKanban:
src/app/(app)/crm/page.tsx (reescrito na Task 3). Nenhum outro arquivo importa.

@src/lib/crm/dados.ts
@src/actions/crm.ts
@src/components/stat-card.tsx
@src/components/crm/kanban-crm.tsx
@src/components/crm/card-oportunidade.tsx
@src/components/crm/nova-oportunidade-dialog.tsx
@src/app/(app)/crm/page.tsx

<interfaces>
<!-- Contratos ja existentes no codebase que o executor deve usar direto. -->

De src/lib/crm/dados.ts (tipos atuais — OportunidadeCard sera ESTENDIDO;
getKanban sera REMOVIDA e substituida por getCrmVisaoGeral):
```typescript
export type OportunidadeCard = {
  id: string; titulo: string; valor: number | null; tipoReceita: string | null
  etapaId: string; ordemNaEtapa: number; origem: string | null
  contatoNome: string | null; empresaNome: string | null
  dataPrevistaFechamento: string | null
  // + createdAt: string; semContato: boolean  (adicionados na Task 1)
}
export type EtapaKanban = { id: string; nome: string; ordem: number; cor: string | null; probabilidade: number | null }
export type ColunaKanban = { etapa: EtapaKanban; oportunidades: OportunidadeCard[]; total: number; somaValor: number }
export type Kanban = { configurado: boolean; pipelineNome: string | null; etapas: EtapaKanban[]; colunas: ColunaKanban[] }
// export async function getKanban(): Promise<Kanban>  // <- REMOVER na Task 1
```

De src/components/stat-card.tsx (REUTILIZAR para os 6 KPIs — nao criar card novo):
```typescript
type StatCardColor = 'primary' | 'success' | 'warning' | 'danger'
function StatCard(props: {
  label: string; value: string; icon: React.ComponentType<{ className?: string }>
  color?: StatCardColor; trend?: {...}; helper?: string
}): JSX.Element
```

Colunas relevantes do schema (src/lib/db/schema.ts — NAO alterar):
- crm_oportunidades: status ('aberta'|'ganha'|'perdida'), valor (numeric), origem (text), etapaId, pipelineId, createdAt
- crm_etapas: probabilidade (integer 0-100), cor, ordem
- crm_tarefas: oportunidadeId, concluida (bool), dataVencimento (timestamptz), workspaceId

Origens conhecidas (crm_contatos.origem / crm_oportunidades.origem):
'manual' | 'landing_page' | 'meta_lead_ad' | 'whatsapp' | 'indicacao' | 'outro'

Actions existentes (NAO reescrever) — o card ja usa moverOportunidade,
ganharOportunidade, perderOportunidade; o dialog usa criarOportunidade.

Padrao de queries (topo de dados.ts): pool max=3, max_pipeline=0 —
QUERIES SEQUENCIAIS E AGREGADAS, nunca Promise.all, nunca N+1 por coluna/card.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers puros (tempo, origem) + funcao de dados getCrmVisaoGeral (substitui getKanban)</name>
  <files>src/lib/crm/tempo.ts, src/lib/crm/tempo.test.ts, src/lib/crm/origem.ts, src/lib/crm/dados.ts</files>
  <behavior>
    tempoRelativoCurto(date: Date | string, agora?: Date): string — rotulo CURTO pt-BR
    - diff < 60s -> "agora"
    - diff < 60min -> "5min"
    - diff < 24h -> "3h"
    - diff < 30 dias -> "15d"
    - diff < 12 meses -> "2m" (m = meses)
    - senao -> "2a" (a = anos)
    Pode usar date-fns (differenceInSeconds/Minutes/Hours/Days/Months/Years) OU math simples.
    Testes com datas FIXAS (agora fixo, sem depender de Date.now()):
    - Test 1: 30s atras -> "agora"
    - Test 2: 5 min atras -> "5min"
    - Test 3: 3h atras -> "3h"
    - Test 4: 15 dias atras -> "15d"
    - Test 5: ~65 dias atras -> "2m"
    - Test 6: ~2 anos atras -> "2a"

    origem.ts (sem teste obrigatorio — mapa simples):
    - ORIGEM_META: Record<string, { rotulo: string; nome: string; cor: string }>
      manual -> { rotulo: 'VIA MANUAL', nome: 'Manual', cor: '<classe tailwind>' }
      whatsapp -> { rotulo: 'VIA WHATSAPP', nome: 'WhatsApp', cor: ... }
      landing_page -> { rotulo: 'VIA LANDING', nome: 'Landing page', cor: ... }
      meta_lead_ad -> { rotulo: 'VIA META', nome: 'Meta Ads', cor: ... }
      indicacao -> { rotulo: 'VIA INDICACAO', nome: 'Indicacao', cor: ... }
      outro -> { rotulo: 'VIA OUTRO', nome: 'Outro', cor: ... }
    - rotuloOrigem(origem): string — fallback 'VIA OUTRO' para null/desconhecido
    - nomeOrigem(origem): string — fallback 'Outro'
    - corOrigem(origem): string — fallback da cor de 'outro'
  </behavior>
  <action>
    1) Criar src/lib/crm/tempo.ts com `tempoRelativoCurto(date, agora = new Date())`
       conforme <behavior>. Aceitar Date ou string ISO. Comentario curto em pt-BR.
    2) Criar src/lib/crm/tempo.test.ts (padrao do repo: `import { describe, it, expect } from 'vitest'`),
       usando um `agora` fixo (ex.: new Date('2026-07-15T12:00:00Z')) e datas
       calculadas a partir dele — cobrir os 6 casos do <behavior>.
    3) Criar src/lib/crm/origem.ts conforme <behavior>. Cores como CLASSES tailwind
       (ex.: 'bg-emerald-500', 'bg-sky-500', ...) — serao usadas como bolinha/dot na
       barra de origem e como accent do badge. Manter enxuto.
    4) Editar src/lib/crm/dados.ts:
       - REMOVER a funcao `getKanban` inteira (linhas ~53-140) e o helper
         `KANBAN_VAZIO` se so ela o usava — `getCrmVisaoGeral` passa a ser a unica
         fonte da pagina (a page.tsx migra para ela na Task 3; nenhum outro arquivo
         importa getKanban — grep confirmou). Assim o literal
         `const card: OportunidadeCard = {...}` que faltava os campos novos deixa de
         existir e nao quebra o tsc.
       - Manter e ESTENDER OportunidadeCard com: `createdAt: string` (ISO) e
         `semContato: boolean` (OBRIGATORIOS — a unica fonte, getCrmVisaoGeral, os preenche).
       - Manter os tipos EtapaKanban, ColunaKanban e Kanban (ainda usados por CrmVisaoGeral).
       - Adicionar tipos:
         `export type KpisCrm = { totalOportunidades: number; valorOrigem: number; taxaConversao: number; ganhas: number; atividadesAtrasadas: number; semContato: number }`
         `export type OrigemDistrib = { origem: string; total: number; pct: number }`
         `export type CrmVisaoGeral = Kanban & { kpis: KpisCrm; origens: OrigemDistrib[] }`
       - Criar `export async function getCrmVisaoGeral(): Promise<CrmVisaoGeral>` com
         QUERIES SEQUENCIAIS (nada de Promise.all), reaproveitando a logica que estava
         em getKanban:
         (1) getWorkspaceAtual (null -> retorna VAZIO: configurado false, colunas [],
             kpis zerados, origens [])
         (2) pipeline padrao (idem VAZIO se ausente)
         (3) etapas do pipeline
         (4) oportunidades ABERTAS com leftJoin contato/empresa (adicionar
             crmOportunidades.createdAt ao select)
         (5) KPI por status: `SELECT status, count(*)::int, coalesce(sum(valor),0)`
             GROUP BY status WHERE pipelineId = pipeline.id — deriva totalOportunidades
             (abertas), valorOrigem (sum valor abertas), ganhas (count ganha),
             perdidas (count perdida). taxaConversao = ganhas+perdidas > 0
             ? Math.round(ganhas/(ganhas+perdidas)*100) : 0.
         (6) atividadesAtrasadas: `SELECT count(*)::int FROM crm_tarefas
             WHERE workspaceId = workspace.id AND concluida = false
             AND dataVencimento < now()` (uma query).
         (7) tarefas concluidas por oportunidade: `SELECT DISTINCT oportunidadeId
             FROM crm_tarefas WHERE workspaceId = workspace.id AND concluida = true
             AND oportunidadeId IS NOT NULL` -> montar Set<string> em memoria.
         (8) origens: `SELECT origem, count(*)::int FROM crm_oportunidades
             WHERE pipelineId = pipeline.id AND status = 'aberta' GROUP BY origem`.
             Normalizar origem null -> 'outro'; pct = total>0 ? round(count/total*100).
       - Montar colunas (total + somaValor por etapa) com o mesmo merge em memoria que
         getKanban fazia, agora preenchendo tambem createdAt e semContato em cada card.
       - Heuristica semContato (DOCUMENTAR em comentario no codigo): uma oportunidade
         aberta esta "sem contato" quando `createdAt < now-7d` E nao possui nenhuma
         tarefa CONCLUIDA (id fora do Set do passo 7). Marcar `semContato` em cada
         card; o KPI `semContato` = contagem de cards abertos marcados.
       - Em qualquer catch/tabelas inexistentes: degradar retornando o VAZIO
         (configurado false, colunas [], kpis zerados, origens []).
    Observacao: os aggregates (5),(6),(8) sao GROUP BY / count no banco — NAO iterar
    todas as linhas em memoria para conta-los (segue o comentario do topo de dados.ts).
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx vitest run src/lib/crm/tempo.test.ts</automated>
  </verify>
  <done>tempo.test.ts passa (6 casos); origem.ts exporta ORIGEM_META + helpers; dados.ts NAO exporta mais getKanban, exporta getCrmVisaoGeral, KpisCrm, OrigemDistrib, CrmVisaoGeral e OportunidadeCard tem createdAt + semContato; nenhum literal de card fica incompleto.</done>
</task>

<task type="auto">
  <name>Task 2: Componentes de apresentacao (KpisCrm, KanbanCrm, CardOportunidade, BarraOrigemLeads)</name>
  <files>src/components/crm/kpis-crm.tsx, src/components/crm/kanban-crm.tsx, src/components/crm/card-oportunidade.tsx, src/components/crm/barra-origem-leads.tsx</files>
  <action>
    1) Criar src/components/crm/kpis-crm.tsx (server-friendly, recebe `kpis: KpisCrm`):
       faixa `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-4`
       com 6 <StatCard> (reutilizar src/components/stat-card.tsx), icones lucide:
       - TOTAL DE OPORTUNIDADES -> Target (color primary), value = totalOportunidades
       - VALOR DA ORIGEM -> DollarSign (primary), value = BRL(valorOrigem)
       - TAXA DE CONVERSAO -> TrendingUp (success), value = `${taxaConversao}%`
       - GANHAS -> Trophy (success), value = ganhas
       - ATIVIDADES ATRASADAS -> AlertTriangle (warning quando >0, senao primary), value = atividadesAtrasadas
       - SEM CONTATO (+7D) -> PhoneOff (warning quando >0, senao primary), value = semContato
       Formatar BRL com `new Intl.NumberFormat('pt-BR', { style:'currency', currency:'BRL' })`.
    2) Reformular src/components/crm/kanban-crm.tsx (continua 'use client'): manter o
       board horizontal, mas o HEADER de cada coluna passa a exibir: nome, Badge de
       contagem, VALOR TOTAL PARADO (formatoBRL(coluna.somaValor)) e a PROBABILIDADE
       da etapa (`${coluna.etapa.probabilidade ?? 0}% prob.`) quando houver. Rodape de
       cada coluna: botao "+ Adicionar oportunidade". Aceitar props novas:
       `oportunidadesVisiveis?: Set<string>` (filtro da busca — quando presente, so
       renderiza cards cujo id esta no set; contagem/valor do header seguem os dados
       originais da coluna) e um callback opcional `onAdicionar?(etapaId)` para o
       rodape. Se onAdicionar nao vier, o botao pode ficar inerte (o dialog e ligado
       no CrmView/page — ver Task 3); mantenha o board funcionando sem ele.
    3) Reformular src/components/crm/card-oportunidade.tsx (continua 'use client',
       mantendo mover/ganhar/perder INTACTOS): adicionar no topo do card um Badge de
       origem usando `rotuloOrigem(oportunidade.origem)` (de src/lib/crm/origem.ts),
       o indicador de tempo `tempoRelativoCurto(oportunidade.createdAt)` (de
       src/lib/crm/tempo.ts) proximo ao topo, e um aviso discreto "Nao contatado"
       (texto ambar, icone AlertTriangle size-3) quando `oportunidade.semContato`.
       Manter valor, contato/empresa, o Select de etapa e os botoes Ganhar/Perder.
    4) Criar src/components/crm/barra-origem-leads.tsx (server-friendly, recebe
       `origens: OrigemDistrib[]`): titulo "Origem dos leads" + link visual inerte
       "Ver relatorio completo" (com title="em breve"); uma pilula por origem usando
       `nomeOrigem` + `corOrigem` (bolinha colorida) + `${pct}%`. Se origens vazio,
       mostrar texto discreto "Sem oportunidades abertas".
    NAO introduzir dependencia nova. Manter arquivos enxutos e comentarios em pt-BR.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit</automated>
  </verify>
  <done>Os 4 componentes existem e tipam; KanbanCrm mostra valor + probabilidade no header e botao de adicionar no rodape; CardOportunidade mostra badge de origem + tempo + aviso "Nao contatado"; BarraOrigemLeads renderiza pilulas com % real; nenhuma dependencia nova.</done>
</task>

<task type="auto">
  <name>Task 3: Orquestrador CrmView + page.tsx (server) com degradacao graciosa</name>
  <files>src/components/crm/crm-view.tsx, src/app/(app)/crm/page.tsx</files>
  <action>
    1) Criar src/components/crm/crm-view.tsx ('use client') recebendo o resultado de
       getCrmVisaoGeral (kanban + kpis + origens + etapas). Estrutura fiel ao mockup:
       - Header: titulo "CRM" + subtitulo "Gerencie seu pipeline de vendas e
         oportunidades". Linha de acao: seletor de pipeline exibindo o pipelineNome
         com Badge "Padrao" (usar DropdownMenu de src/components/ui/dropdown-menu, com
         so o pipeline atual por ora — sem trocar de pipeline no v1); a direita um
         botao de periodo "Selecione um periodo" INERTE (title="em breve").
       - Linha secundaria: <NovaOportunidadeDialog etapas={etapas} /> (reaproveitar,
         NAO reescrever), um Input de busca "Buscar oportunidades..." (estado local),
         botao de filtro e engrenagem INERTES (title="em breve"); a direita as abas
         Kanban/Lista/Calendario (usar Tabs de src/components/ui/tabs).
       - Busca: filtro CLIENT-SIDE sobre as oportunidades ja carregadas por
         titulo/contatoNome/empresaNome (case-insensitive, sem acento importa pouco).
         Computar um `Set<string>` de ids visiveis e passar para KanbanCrm como
         `oportunidadesVisiveis` (quando a busca esta vazia, passar undefined =
         mostra tudo).
       - Aba Kanban: <KpisCrm kpis={kpis} />, depois <KanbanCrm .../>, depois
         <BarraOrigemLeads origens={origens} /> no rodape.
       - Abas Lista e Calendario: placeholder discreto centralizado "Visao em
         construcao" (borda tracejada, texto muted) — SEM dado falso.
    2) Reescrever src/app/(app)/crm/page.tsx (server component, manter
       `export const maxDuration = 60`): TROCAR o import `getKanban` por
       `getCrmVisaoGeral` (getKanban nao existe mais apos a Task 1 — NAO deixar import
       orfao) e remover o import de KanbanCrm (agora vive dentro do CrmView). Buscar
       `const dados = await getCrmVisaoGeral()`. Se `!dados.configurado`, renderizar o
       MESMO aviso amarelo atual "CRM ainda nao ativado" (degradacao graciosa — nao
       quebrar). Caso contrario, renderizar <CrmView dados={dados} />.
    Comentarios e UI em portugues; sem dependencia nova.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npm run build</automated>
  </verify>
  <done>`npm run build` conclui limpo e a rota /crm compila; nenhum import orfao de getKanban; a pagina renderiza header + abas + 6 KPIs + kanban (valor/probabilidade nas colunas, cards com origem/tempo/aviso) + barra de origem; busca filtra client-side; sem pipeline/workspace (0019 ausente) mostra o aviso amarelo sem erro.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/crm/tempo.test.ts` passa (6 casos com datas fixas).
- `npm run build` limpo; rota /crm no output de build; NENHUM import orfao de getKanban.
- Revisao visual (nao automatizada, pos-deploy): 6 KPIs, colunas com valor+prob,
  cards com badge de origem/tempo/aviso, barra de origem com %, abas
  Lista/Calendario em placeholder, busca filtrando.
- Sem dependencias novas em package.json (git diff package.json vazio).
</verification>

<success_criteria>
- getCrmVisaoGeral SUBSTITUI getKanban e retorna kpis reais (via GROUP BY/count no
  banco, queries sequenciais) + origens + kanban, degradando para vazio/zerado sem quebrar.
- getKanban removida; nenhum literal de OportunidadeCard fica incompleto; tsc/build limpos.
- Helpers tempoRelativoCurto e origem centralizados e reutilizados pelo card e
  pela barra; tempo testado no Vitest.
- Fidelidade visual do mockup com StatCard reutilizado; Lista/Calendario/filtros/
  periodo como placeholders honestos (nada de dado falso).
- Actions mover/ganhar/perder/criar intactas; migrations/schema intocados.
</success_criteria>

<output>
Ao concluir, criar `.planning/quick/260715-1rq-reformular-pagina-crm-no-visual-do-mocku/260715-1rq-SUMMARY.md`.
</output>
