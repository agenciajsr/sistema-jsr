---
phase: quick-260719-qf5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/relatorios/parse-actions-extended.ts
  - src/lib/relatorios/gerar-relatorio.ts
  - src/lib/relatorios/engine.ts
  - src/lib/relatorios/engine.test.ts
  - src/lib/relatorios/variaveis.ts
  - src/actions/crm-lead.ts
autonomous: true
requirements: [QF5-01, QF5-02]
must_haves:
  truths:
    - "Na UI de relatórios configuráveis, existem categorias Engajamento e Vídeo com engajamento, custo por engajamento, cliques no link, custo por clique no link, curtidas da página, visualizações de vídeo e custo por visualização"
    - "Templates com {{engajamento}}, {{cliques_link}}, {{video_views}} etc. interpolam valores reais em vez de —"
    - "Ao salvar a origem na ficha do lead, os cards das oportunidades desse contato no kanban mostram a nova origem"
  artifacts:
    - path: "src/lib/relatorios/variaveis.ts"
      provides: "Categorias engajamento e video no CATALOGO_VARIAVEIS + LINHAS_METRICAS"
      contains: "custo_por_video_view"
    - path: "src/lib/relatorios/engine.ts"
      provides: "contextoDeMetricas expõe as novas chaves"
      contains: "cliques_link"
    - path: "src/actions/crm-lead.ts"
      provides: "atualizarLead propaga origem para crm_oportunidades"
  key_links:
    - from: "src/lib/relatorios/gerar-relatorio.ts"
      to: "src/lib/relatorios/parse-actions-extended.ts"
      via: "parseActionsRelatorio retorna videoViews/curtidasPagina agregados em MetricasConta"
      pattern: "videoViews"
    - from: "src/lib/relatorios/engine.ts"
      to: "src/lib/relatorios/variaveis.ts"
      via: "chaves do contexto batem 1:1 com chaves do catálogo"
      pattern: "video_views"
    - from: "src/actions/crm-lead.ts"
      to: "crm_oportunidades.origem"
      via: "update where contatoId + workspaceId quando v.origem !== undefined"
      pattern: "crmOportunidades"
---

<objective>
Duas correções pontuais: (1) ampliar o catálogo de variáveis dos relatórios configuráveis com categorias Engajamento e Vídeo (engajamento, custo por engajamento, cliques no link, custo por clique no link, curtidas da página, visualizações de vídeo e custo por visualização), cobrindo todos os objetivos da Meta; (2) propagar a origem salva na ficha do lead (crm_contatos) para todas as oportunidades do contato (crm_oportunidades), que é o que o card do kanban lê.

Purpose: hoje a UI de relatórios não oferece métricas de engajamento/vídeo mesmo com o parse já extraindo parte delas; e a origem editada na ficha não aparece nos cards.
Output: catálogo completo por categoria + origem consistente ficha↔kanban.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/relatorios/variaveis.ts
@src/lib/relatorios/engine.ts
@src/lib/relatorios/parse-actions-extended.ts
@src/lib/relatorios/gerar-relatorio.ts
@src/lib/relatorios/engine.test.ts
@src/actions/crm-lead.ts

Regras do projeto: TUDO em português (labels, comentários). NUNCA rodar drizzle-kit migrate — e não há migration aqui (colunas já existem). Módulos variaveis.ts/engine.ts/parse-actions-extended.ts são PUROS (sem imports de db/auth/react) — manter assim.

<interfaces>
De src/lib/relatorios/parse-actions-extended.ts:
```typescript
export type MetricasRelatorio = { compras; leads; conversas; addToCart; checkout; landingPageView; linkClicks; engajamento; receita: number }
export function parseActionsRelatorio(actions: unknown): Omit<MetricasRelatorio, 'receita'>
```

De src/lib/relatorios/gerar-relatorio.ts — `MetricasConta` é literal em 3 pontos: `agregarContaPeriodo` (acumuladores + objeto final), `consolidarContas`, e `compilarBlocos` em engine.ts; além do helper `metricas()` em engine.test.ts (linha ~19). Todos precisam dos campos novos.

De src/actions/crm-lead.ts (atualizarLead, ~linha 589): update em `crmContatos` com `...(v.origem !== undefined ? { origem: v.origem } : {})`, filtrado por `and(eq(crmContatos.id, id), eq(crmContatos.workspaceId, workspace.id))`, seguido de `revalidatePath('/crm')`. A tabela `crmOportunidades` tem colunas `origem`, `contatoId`, `workspaceId` (conferir se o import de `crmOportunidades` já existe no topo do arquivo; adicionar se faltar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse e agregação — videoViews e curtidasPagina em MetricasConta</name>
  <files>src/lib/relatorios/parse-actions-extended.ts, src/lib/relatorios/gerar-relatorio.ts, src/lib/relatorios/engine.ts, src/lib/relatorios/engine.test.ts</files>
  <action>
    Em parse-actions-extended.ts:
    - Adicionar `const VIDEO_VIEW_TYPES = ['video_view']` e `const CURTIDAS_PAGINA_TYPES = ['like']`.
    - Adicionar `videoViews: number` e `curtidasPagina: number` a `MetricasRelatorio` e retorná-los em `parseActionsRelatorio` via `somarGrupo` (incluir no fallback `{ ...: 0 }` quando actions não é array). Sem mexer no sync — apenas actions já persistidas.

    Em gerar-relatorio.ts:
    - Adicionar `videoViews` e `curtidasPagina` (number) a `MetricasConta`.
    - Em `agregarContaPeriodo`: novos acumuladores inicializados em 0, somados do parse, incluídos no objeto `metricas`.
    - Em `consolidarContas`: inicializar em 0 no literal e somar no loop.

    Em engine.ts `compilarBlocos`: inicializar em 0 no literal e somar no loop.

    Em engine.test.ts: adicionar `videoViews: 0, curtidasPagina: 0` ao helper `metricas()` (linha ~19). Se testes somarem campos, ajustar expectativas conforme necessário.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/relatorios</automated>
  </verify>
  <done>Tipos compilam sem erro e testes existentes passam com os novos campos default 0 em todos os construtores de MetricasConta.</done>
</task>

<task type="auto">
  <name>Task 2: Catálogo e contexto — categorias Engajamento e Vídeo</name>
  <files>src/lib/relatorios/variaveis.ts, src/lib/relatorios/engine.ts</files>
  <action>
    Em variaveis.ts:
    - Ampliar `CategoriaVariavel` com `'engajamento' | 'video'`.
    - Adicionar ao `CATALOGO_VARIAVEIS` (labels em português):
      - engajamento: `engajamento` "Engajamento" (numero), `custo_por_engajamento` "Custo por engajamento" (moeda), `cliques_link` "Cliques no link" (numero), `custo_por_clique_link` "Custo por clique no link" (moeda), `curtidas_pagina` "Curtidas da página" (numero).
      - video: `video_views` "Visualizações de vídeo" (numero), `custo_por_video_view` "Custo por visualização" (moeda).
    - `LABELS_CATEGORIA`: `engajamento: 'Engajamento'`, `video: 'Vídeo'`.
    - `LINHAS_METRICAS`: linhas prontas no mesmo estilo com emoji, ex.: `engajamento: '🤝 *Engajamento:* {{engajamento}}'`, `custo_por_engajamento: '💰 *Custo por engajamento:* {{custo_por_engajamento}}'`, `cliques_link: '🔗 *Cliques no link:* {{cliques_link}}'`, `custo_por_clique_link: '💰 *Custo por clique no link:* {{custo_por_clique_link}}'`, `curtidas_pagina: '👍 *Curtidas da página:* {{curtidas_pagina}}'`, `video_views: '🎬 *Visualizações de vídeo:* {{video_views}}'`, `custo_por_video_view: '💰 *Custo por visualização:* {{custo_por_video_view}}'`.

    Em engine.ts `contextoDeMetricas`, expor as novas chaves:
    - `engajamento: m.engajamento`
    - `custo_por_engajamento: m.engajamento > 0 ? m.spend / m.engajamento : null`
    - `cliques_link: m.linkClicks`
    - `custo_por_clique_link: m.linkClicks > 0 ? m.spend / m.linkClicks : null`
    - `curtidas_pagina: m.curtidasPagina`
    - `video_views: m.videoViews`
    - `custo_por_video_view: m.videoViews > 0 ? m.spend / m.videoViews : null`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/relatorios</automated>
  </verify>
  <done>Cada chave nova do catálogo tem valor correspondente em contextoDeMetricas e linha em LINHAS_METRICAS; UI de relatórios (que itera o catálogo por categoria) passa a oferecer as novas métricas sem mudança adicional.</done>
</task>

<task type="auto">
  <name>Task 3: CRM — propagar origem da ficha para as oportunidades do contato</name>
  <files>src/actions/crm-lead.ts</files>
  <action>
    Em `atualizarLead` (~linha 589), após o update em `crmContatos` retornar `salvo` e antes do `revalidatePath('/crm')`: quando `v.origem !== undefined`, executar também:
    ```ts
    await db
      .update(crmOportunidades)
      .set({ origem: v.origem, updatedAt: new Date() })
      .where(and(eq(crmOportunidades.contatoId, id), eq(crmOportunidades.workspaceId, workspace.id)))
    ```
    (usar `updatedAt` só se a coluna existir no schema de crmOportunidades — conferir; senão, apenas `origem`). Adicionar `crmOportunidades` ao import de schema no topo se ainda não estiver. Comentário em português explicando: o card do kanban lê a origem da oportunidade, então a edição na ficha propaga.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Salvar origem na ficha atualiza crm_contatos.origem E crm_oportunidades.origem de todas as oportunidades do contato no workspace; demais campos da ficha não tocam oportunidades.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sem erros.
- `npx vitest run src/lib/relatorios` verde.
- Grep: toda chave nova de CATALOGO_VARIAVEIS aparece em contextoDeMetricas e em LINHAS_METRICAS.
</verification>

<success_criteria>
- Catálogo de relatórios cobre engajamento e vídeo, organizado por categoria, com formatação pt-BR correta (moeda/número).
- MetricasConta tem videoViews/curtidasPagina com default 0 em agregarContaPeriodo, consolidarContas, compilarBlocos e no helper de teste.
- Origem salva na ficha reflete nos cards do kanban após revalidatePath('/crm').
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260719-qf5-relatorios-catalogo-completo-de-metricas/260719-qf5-SUMMARY.md`
</output>
