---
phase: quick-260715-0zf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0019_*.sql (gerada)
  - src/lib/validations/crm.ts
  - src/lib/crm/workspace.ts
  - src/lib/crm/lead.ts
  - src/lib/crm/lead.test.ts
  - src/lib/crm/ingest.ts
  - src/lib/crm/dados.ts
  - src/actions/crm.ts
  - src/actions/crm-cadastros.ts
  - src/app/api/crm/leads/route.ts
  - src/app/(app)/crm/page.tsx
  - src/app/(app)/funil/page.tsx
  - src/components/crm/* (novos)
  - src/components/app-sidebar.tsx
autonomous: true
requirements: [CRM-SCHEMA, CRM-ACTIONS, CRM-API-LEADS, CRM-KANBAN]
user_setup:
  - service: vercel
    why: "Token de autenticacao da API publica de captacao de leads"
    env_vars:
      - name: CRM_LEADS_TOKEN
        source: "Gerar valor aleatorio (ex.: openssl rand -hex 32) e cadastrar em Vercel -> Settings -> Environment Variables (e no .env.local)"

must_haves:
  truths:
    - "POST /api/crm/leads com x-crm-token valido cria contato + oportunidade na primeira etapa do pipeline padrao e registra o lead no inbox"
    - "POST /api/crm/leads repetido (mesmo lead no mesmo dia) retorna duplicado:true e NAO cria segundo contato/oportunidade"
    - "POST /api/crm/leads sem token valido retorna 401"
    - "/crm mostra o kanban do pipeline padrao com colunas por etapa, contagem e soma de valor por coluna"
    - "Usuario cria oportunidade pelo dialog, move de etapa via select no card, ganha/perde/reabre — cada acao gera linha em crm_atividades"
    - "Ganhar oportunidade com criarCliente=true cria linha em clientes e vincula clienteId na empresa e na oportunidade"
    - "/funil redireciona para /crm e a sidebar tem item CRM"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "10 tabelas novas: workspaces, workspace_membros, crm_empresas, crm_contatos, crm_pipelines, crm_etapas, crm_oportunidades, crm_tarefas, crm_atividades, crm_lead_inbox"
      contains: "crm_oportunidades"
    - path: "drizzle"
      provides: "Migration 0019 gerada (NAO aplicada) com seed idempotente do workspace JSR + pipeline Vendas + 6 etapas + membros"
    - path: "src/actions/crm.ts"
      provides: "Actions de pipelines, etapas e oportunidades (mover/ganhar/perder/reabrir)"
      exports: ["moverOportunidade", "ganharOportunidade", "perderOportunidade", "reabrirOportunidade"]
    - path: "src/actions/crm-cadastros.ts"
      provides: "Actions de empresas, contatos e tarefas comerciais"
    - path: "src/app/api/crm/leads/route.ts"
      provides: "Endpoint publico de captacao de leads com token + dedup"
      exports: ["POST"]
    - path: "src/app/(app)/crm/page.tsx"
      provides: "Kanban funcional basico do pipeline padrao"
    - path: "src/lib/crm/lead.ts"
      provides: "Modulo PURO testado: normalizarTelefone + dedupHash"
      min_lines: 20
  key_links:
    - from: "src/app/api/crm/leads/route.ts"
      to: "src/lib/crm/ingest.ts"
      via: "processarLead compartilhado entre API route e action manual"
      pattern: "processarLead"
    - from: "src/actions/crm.ts"
      to: "src/lib/crm/workspace.ts"
      via: "getWorkspaceAtual() em toda action"
      pattern: "getWorkspaceAtual"
    - from: "src/app/(app)/crm/page.tsx"
      to: "src/lib/crm/dados.ts"
      via: "getKanban() no server component"
      pattern: "getKanban"
    - from: "src/app/(app)/funil/page.tsx"
      to: "/crm"
      via: "redirect do next/navigation"
      pattern: "redirect\\('/crm'\\)"
---

<objective>
Backend completo do CRM comercial (workspaces, empresas, contatos, pipelines, oportunidades, tarefas comerciais, atividades, inbox de leads + API publica de captacao) e substituicao da pagina /funil mock por /crm com kanban real basico.

Proposito: hoje o funil de vendas da agencia e uma tela 100% mock. Este plano cria a fundacao de dados e regras de negocio do CRM (padrao Pipedrive: ganho/perdido como status, nao etapa) e a primeira tela funcional, incluindo captacao automatica de leads de landing pages via API.

Output: migration 0019 GERADA (nao aplicada), 10 tabelas novas, 2 arquivos de Server Actions, endpoint POST /api/crm/leads, pagina /crm com kanban, /funil redirecionando, item CRM na sidebar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/lib/db/schema.ts
@src/actions/tarefas.ts
@src/lib/validations/tarefa.ts
@src/lib/auth/session.ts
@src/app/(app)/funil/page.tsx
@src/app/api/cron/sync-meta/route.ts
@src/components/app-sidebar.tsx

<interfaces>
<!-- Contratos ja existentes que o executor vai consumir — sem exploracao extra. -->

De src/lib/auth/session.ts:
```typescript
export type CurrentUser = { id: string; email: string | undefined; nome: string; role: 'admin' | 'membro' }
export const getCurrentUser: () => Promise<CurrentUser | null> // cache()d por request
export function requireAdmin(): Promise<{ user: CurrentUser } | { error: string }>
```

De src/lib/db/schema.ts (referencias FK):
```typescript
export const profiles   // profiles.id uuid PK
export const clientes   // clientes.id uuid PK; nicho: nichoEnum ('ecommerce'|'negocio_local'|'infoproduto'); status: clienteStatusEnum (inclui 'aguardando_inicio'); contatoNome/contatoTelefone/contatoEmail
```

Padrao de protecao por token (src/app/api/cron/sync-meta/route.ts):
```typescript
const secret = process.env.CRON_SECRET
if (secret) { if (request.headers.get('authorization') !== `Bearer ${secret}`) return 401 }
else console.warn('... nao configurado — rota desprotegida.')
// Para /api/crm/leads: MESMA logica, mas header `x-crm-token` comparado com CRM_LEADS_TOKEN,
// e SEM fallback desprotegido: token ausente/errado => 401 sempre (endpoint publico).
```

Sidebar (src/components/app-sidebar.tsx, array `nav` linha ~49): NAO existe link /funil hoje.
ADICIONAR item novo apos 'Clientes': `{ title: 'CRM', url: '/crm', icon: Target }` (Target do lucide-react; se preferir, KanbanSquare — conferir se existe na versao instalada).
</interfaces>

<conventions>
REGRAS INEGOCIAVEIS DO CODEBASE (ver schema.ts e tarefas.ts):
- TUDO em portugues (tabelas, colunas snake_case no banco, camelCase no Drizzle, comentarios, mensagens, UI).
- uuid('id').primaryKey().defaultRandom(); createdAt/updatedAt timestamp withTimezone defaultNow.
- Dinheiro: numeric, NUNCA float. valor da oportunidade: numeric(12,2).
- text (NAO pgEnum) para status/tipos/papeis/origens que podem ganhar valores novos sem migration.
- autorNome denormalizado em crm_atividades ('Sistema' para automacoes).
- Server Actions: 'use server', comecam com getCurrentUser(), retornam { data } | { error }, revalidatePath('/crm'), mensagens de erro em portugues SEM acentos (estilo: 'Sessao expirada. Faca login novamente.').
- QUERIES SEQUENCIAIS dentro de cada funcao (pool max=3, max_pipeline=0) — NADA de Promise.all interno. Ver comentario em src/lib/db/index.ts e o padrao de getTarefasDoPeriodo.
- Log de atividade NUNCA derruba a mutacao que registra: helper com try/catch que so loga (copiar padrao registrarAtividade de tarefas.ts).
- Migrations: `npx drizzle-kit generate` gera 0019_*.sql. NAO aplicar no banco (nao ha script npm de migrate; usuario aplica — 0015-0018 tambem estao pendentes e rodam em ordem). Seed vai DENTRO da 0019 (append manual de SQL idempotente ao arquivo gerado — seguro porque a 0019 ainda nao foi aplicada em lugar nenhum).
</conventions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema Drizzle (10 tabelas), migration 0019 com seed idempotente, validacoes Zod e helpers puros do CRM</name>
  <files>src/lib/db/schema.ts, drizzle/0019_*.sql (gerada), src/lib/validations/crm.ts, src/lib/crm/workspace.ts, src/lib/crm/lead.ts, src/lib/crm/lead.test.ts</files>
  <behavior>
    Testes do modulo puro src/lib/crm/lead.ts (vitest, zero import de db/auth/react):
    - normalizarTelefone('(31) 99876-5432') => '31998765432' (so digitos)
    - normalizarTelefone('+55 31 9 9876-5432') => '5531998765432'
    - normalizarTelefone('') e undefined => null
    - dedupHash('landing_page', 'Ana@Ex.com', null, '2026-07-15') e dedupHash('landing_page', 'ana@ex.com', null, '2026-07-15') => IGUAIS (email case-insensitive)
    - dedupHash com fontes diferentes ou dias diferentes => hashes DIFERENTES
    - dedupHash sem email usa telefoneNormalizado; determina-se sempre 64 chars hex (sha256 via node:crypto)
  </behavior>
  <action>
    1. **Schema** — acrescentar ao FIM de src/lib/db/schema.ts (secao `// --- CRM comercial ---`), com comentario de cabecalho explicando: prefixo crm_, workspaces preparado para multi-tenant mas v1 tem UMA linha, e "NAO confundir crm_tarefas com o modulo tarefas operacional (intocado)". Tabelas EXATAMENTE como decidido:
       - `workspaces`: id uuid PK defaultRandom, nome text notNull, slug text notNull + uniqueIndex, createdAt.
       - `workspaceMembros` ('workspace_membros'): id, workspaceId FK workspaces (cascade) notNull, profileId FK profiles (cascade) notNull, papel text notNull default 'vendedor' (comentario: 'admin'|'gestor'|'vendedor' — text de proposito), createdAt. uniqueIndex (workspaceId, profileId).
       - `crmEmpresas` ('crm_empresas'): id, workspaceId FK notNull (cascade), nome notNull, cnpj, segmento, site, instagram, telefone, cidade, estado, notas, donoId FK profiles (set null), clienteId FK clientes (set null — preenchido quando vira cliente da agencia), createdAt/updatedAt. index (workspaceId, nome).
       - `crmContatos` ('crm_contatos'): id, workspaceId notNull (cascade), empresaId FK crmEmpresas (set null, nullable), nome notNull, email, telefone, telefoneNormalizado ('telefone_normalizado' — so digitos, p/ dedup), cargo, origem text notNull default 'manual' (comentario: 'manual','landing_page','meta_lead_ad','whatsapp','indicacao','outro'), origemDetalhe jsonb, donoId FK profiles (set null), createdAt/updatedAt. index (workspaceId, email) e (workspaceId, telefoneNormalizado).
       - `crmPipelines` ('crm_pipelines'): id, workspaceId notNull (cascade), nome notNull, ordem integer notNull default 0, padrao boolean notNull default false, createdAt.
       - `crmEtapas` ('crm_etapas'): id, pipelineId FK crmPipelines (cascade) notNull, nome notNull, ordem integer notNull, cor text, probabilidade integer (0-100, nullable), createdAt. Comentario: ganho/perdido NAO sao etapas — sao status da oportunidade (padrao Pipedrive).
       - `crmOportunidades` ('crm_oportunidades'): id, workspaceId notNull (cascade), pipelineId FK ({ onDelete: 'restrict' }) notNull, etapaId FK crmEtapas ({ onDelete: 'restrict' }) notNull, empresaId FK (set null), contatoId FK crmContatos (set null), titulo notNull, valor numeric(12,2), tipoReceita text default 'mensalidade', status text notNull default 'aberta' ('aberta','ganha','perdida'), motivoPerda text, ganhaEm/perdidaEm timestamp withTimezone, donoId FK profiles (set null), origem text, servicosInteresse jsonb, dataPrevistaFechamento date, ordemNaEtapa integer notNull default 0, clienteId FK clientes (set null), createdAt/updatedAt. index (pipelineId, etapaId, status) e (workspaceId, status).
       - `crmTarefas` ('crm_tarefas'): id, workspaceId notNull (cascade), oportunidadeId FK crmOportunidades (cascade, nullable), contatoId FK (set null), tipo text notNull default 'followup' ('ligacao','whatsapp','email','reuniao','followup','outro'), titulo notNull, notas, dataVencimento timestamp withTimezone notNull, concluida boolean notNull default false, concluidaEm timestamp withTimezone, donoId FK profiles (set null), createdAt/updatedAt. index (oportunidadeId, concluida) e (donoId, dataVencimento).
       - `crmAtividades` ('crm_atividades'): id, workspaceId notNull (cascade), oportunidadeId FK (cascade, nullable), contatoId FK (set null), empresaId FK (set null), tipo text notNull (text livre — comentario com os tipos), autorId uuid (nullable, SEM FK — segue acompanhamentos), autorNome text notNull ('Sistema' p/ automacoes), campo/de/para/detalhe text, createdAt. index (oportunidadeId, createdAt).
       - `crmLeadInbox` ('crm_lead_inbox'): id, workspaceId notNull (cascade), fonte text notNull, payload jsonb notNull, status text notNull default 'pendente' ('pendente','processado','descartado','erro'), erroDetalhe text, contatoId FK (set null), oportunidadeId FK (set null), dedupHash text notNull + uniqueIndex, recebidoEm timestamp withTimezone notNull defaultNow, processadoEm timestamp withTimezone, createdAt. Relations Drizzle para as principais (oportunidade->etapa/pipeline/contato/empresa, contato->empresa, etc.).
    2. **Migration**: rodar `npx drizzle-kit generate` (gera drizzle/0019_*.sql). Conferir que so contem as tabelas/indices novos (aditiva). Depois APPENDAR ao final do arquivo gerado o seed SQL idempotente (com `--> statement-breakpoint` entre statements, seguindo o formato dos arquivos drizzle existentes):
       - workspace: `INSERT INTO "workspaces" ("nome","slug") SELECT 'JSR','jsr' WHERE NOT EXISTS (SELECT 1 FROM "workspaces" WHERE "slug"='jsr');`
       - membros: `INSERT INTO "workspace_membros" ("workspace_id","profile_id","papel") SELECT w.id, p.id, CASE WHEN p.role='admin' THEN 'admin' ELSE 'vendedor' END FROM "profiles" p CROSS JOIN "workspaces" w WHERE w.slug='jsr' ON CONFLICT DO NOTHING;`
       - pipeline Vendas (padrao=true) com WHERE NOT EXISTS por (workspace, nome);
       - 6 etapas via INSERT..SELECT com VALUES ('Novo Lead',0,10),('Contato Feito',1,20),('Qualificado',2,40),('Reunião Agendada',3,60),('Proposta Enviada',4,75),('Negociação',5,90) guardado por `NOT EXISTS (SELECT 1 FROM "crm_etapas" WHERE pipeline_id = p.id)`.
       NAO aplicar a migration (0015-0018 tambem pendentes; usuario aplica em ordem).
    3. **Modulo puro** src/lib/crm/lead.ts (TDD — testes primeiro em lead.test.ts): `normalizarTelefone(v?: string | null): string | null` (remove nao-digitos; vazio => null) e `dedupHash(fonte: string, email: string | null | undefined, telefoneNormalizado: string | null | undefined, dia: string): string` (sha256 hex de `${fonte}|${email?.trim().toLowerCase() || telefoneNormalizado || ''}|${dia}` via node:crypto). Zero import de db/react.
    4. **Helper de workspace** src/lib/crm/workspace.ts: `export const getWorkspaceAtual = cache(async () => ...)` — SELECT da linha unica de workspaces (limit 1); retorna a linha ou null (comentario: v1 single-tenant, multi-tenant futuro troca este helper).
    5. **Validacoes** src/lib/validations/crm.ts, seguindo o estilo de tarefa.ts (opcionalUuid, opcionalData, z.input types): empresaSchema, contatoSchema (exigir nome; email valido se presente), pipelineSchema, etapaSchema (probabilidade 0-100), oportunidadeSchema (titulo obrigatorio; valor `z.coerce.number().nonnegative().optional()`; tipoReceita enum mensalidade|projeto; etapaId uuid), atualizarOportunidadeSchema, crmTarefaSchema (titulo, tipo, dataVencimento ISO datetime), e `leadEntradaSchema` para a API: { fonte: z.enum(['landing_page','meta_lead_ad','whatsapp','manual','outro']), nome: min(1), email opcional, telefone opcional, empresa opcional, mensagem opcional, servicosInteresse: z.array(z.string()).optional(), valorEstimado: z.coerce.number().optional(), extra: z.record(z.string(), z.unknown()).optional() } com `.refine` exigindo email OU telefone ('Informe email ou telefone do lead.').
  </action>
  <verify>
    <automated>npm test -- src/lib/crm && npx drizzle-kit generate --dry-run 2>/dev/null || npm test -- src/lib/crm</automated>
  </verify>
  <done>Migration 0019_*.sql existe (aditiva + seed idempotente no fim, NAO aplicada); schema.ts compila; testes de lead.ts passam; validacoes e getWorkspaceAtual criados.</done>
</task>

<task type="auto">
  <name>Task 2: Server Actions do CRM (regras de negocio) + ingest compartilhado + API publica POST /api/crm/leads</name>
  <files>src/actions/crm.ts, src/actions/crm-cadastros.ts, src/lib/crm/ingest.ts, src/app/api/crm/leads/route.ts</files>
  <action>
    Padrao de TODA action: 'use server'; comeca com getCurrentUser() ('Sessao expirada. Faca login novamente.' se null); resolve workspace via getWorkspaceAtual() ('Workspace nao encontrado. Aplique a migration 0019.' se null); valida com Zod; retorno { data } | { error }; try/catch com console.error e mensagem em portugues sem acentos; revalidatePath('/crm') apos mutacao; queries SEQUENCIAIS. Helper interno `registrarAtividadeCrm(...)` copia o padrao de registrarAtividade de tarefas.ts (try/catch que so loga, NUNCA derruba a mutacao) — grava em crm_atividades com autorNome do usuario ou 'Sistema'.

    1. **src/actions/crm-cadastros.ts** — cadastros basicos:
       - criarEmpresa / atualizarEmpresa / deletarEmpresa (deletar exige requireAdmin)
       - criarContato / atualizarContato / deletarContato (deletar exige requireAdmin). Ao criar/atualizar: telefoneNormalizado = normalizarTelefone(telefone). criarContato registra atividade 'contato_criado'.
       - criarTarefaCrm / concluirTarefaCrm(id, concluida) / deletarTarefaCrm — concluir seta concluidaEm=now (ou null ao reabrir) e registra atividade 'tarefa_concluida' (ou 'tarefa_criada' na criacao) quando houver oportunidadeId.
       - getEmpresas / getContatos (listas simples do workspace, orderBy nome).
    2. **src/actions/crm.ts** — pipelines, etapas e oportunidades:
       - Pipelines: criarPipeline, renomearPipeline, reordenarPipelines(ids ordenados => reescreve ordem), definirPipelinePadrao (transacao ou 2 updates sequenciais: desmarca padrao=false no workspace, marca o novo — SEMPRE exatamente 1 padrao), excluirPipeline (requireAdmin; recusar se tiver oportunidades: 'Nao e possivel excluir um pipeline com oportunidades.').
       - Etapas: criarEtapa, atualizarEtapa, reordenarEtapas(pipelineId, ids ordenados), excluirEtapa (requireAdmin; contar oportunidades na etapa ANTES — se > 0 retornar 'Nao e possivel excluir uma etapa com oportunidades. Mova-as antes.'; o FK restrict e a trava final).
       - Oportunidades: criarOportunidade (ordemNaEtapa = count na etapa; registra atividade 'criacao'; aceita contatoNome/empresaNome livres do dialog — se vierem, cria contato/empresa minimos antes, sequencialmente), atualizarOportunidade, deletarOportunidade (requireAdmin).
       - `moverOportunidade(id, etapaId, ordemNaEtapa?)`: le a oportunidade + nomes das etapas de/para (queries sequenciais), atualiza etapaId + ordemNaEtapa (default: count na etapa destino), registra atividade 'mudanca_etapa' com de/para = NOMES das etapas.
       - `ganharOportunidade(id, opts?: { criarCliente?: boolean })`: status='ganha', ganhaEm=now, atividade 'ganho'. Se criarCliente e a oportunidade tem empresaId cuja empresa NAO tem clienteId: inserir em clientes (nome=empresa.nome, status 'aguardando_inicio', nicho 'negocio_local' default, contatoNome/contatoTelefone/contatoEmail do crm_contato da oportunidade se houver) e gravar clienteId na empresa E na oportunidade (updates sequenciais). Retornar { data: { clienteId? } }.
       - `perderOportunidade(id, motivoPerda)`: motivo trim obrigatorio ('Informe o motivo da perda.'); status='perdida', perdidaEm=now, motivoPerda, atividade 'perda' com detalhe=motivo.
       - `reabrirOportunidade(id)`: status='aberta', limpa ganhaEm/perdidaEm/motivoPerda, atividade 'reabertura'.
       - getAtividadesDaOportunidade(id) (orderBy createdAt desc, limit 50).
    3. **src/lib/crm/ingest.ts** — `processarLead(lead: LeadEntrada, workspaceId: string)` compartilhado (NAO e action; server-only). Fluxo com queries sequenciais:
       a. telefoneNormalizado + dedupHash (dia = hoje em Brasilia — reusar hojeBrasilia() de @/lib/date-br);
       b. INSERT em crm_lead_inbox com payload bruto + onConflictDoNothing({ target: dedupHash }) + .returning({ id }); se nao retornou linha => duplicado, retorna { duplicado: true };
       c. dedup de contato: buscar no workspace por email (lower(email) = lower($1)) OU telefoneNormalizado; achou => reusa; senao cria (origem=fonte, origemDetalhe=payload, empresaId de empresa criada/achada por nome se `empresa` veio no lead);
       d. pipeline padrao (padrao=true) e sua PRIMEIRA etapa (menor ordem); cria oportunidade (titulo = nome do lead + empresa se houver, ex.: 'Lead: Ana (Padaria X)', valor=valorEstimado, origem=fonte, servicosInteresse, donoId null, status 'aberta', ordemNaEtapa=count);
       e. atividades 'lead_recebido' (autorNome 'Sistema', detalhe=fonte) e 'criacao';
       f. UPDATE inbox: status='processado', processadoEm=now, contatoId, oportunidadeId. Retorna { contatoId, oportunidadeId }.
       Qualquer erro apos (b): UPDATE inbox status='erro' + erroDetalhe (mensagem do erro) em try/catch proprio, e re-throw.
    4. **src/app/api/crm/leads/route.ts** — `export const runtime = 'nodejs'`, `export const maxDuration = 60`. POST: (a) token: `request.headers.get('x-crm-token')` comparado com `process.env.CRM_LEADS_TOKEN`; env ausente OU header divergente => NextResponse.json({ error: 'Nao autorizado.' }, { status: 401 }) — SEM modo desprotegido (diferente do cron: endpoint publico); (b) body JSON validado com leadEntradaSchema (erro => 400 com primeira mensagem do Zod); (c) getWorkspaceAtual() — null => 503 'Workspace nao configurado.'; (d) processarLead — duplicado => 200 { duplicado: true }; sucesso => 200 { ok: true, contatoId, oportunidadeId }; excecao => 500 { error: 'Erro ao processar o lead.' } (ja registrada no inbox como status erro).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Actions completas com as 4 regras de negocio (mover/ganhar/perder/reabrir) gravando atividades; ingest compartilhado com dedup de inbox (hash) e de contato (email/telefone); rota POST /api/crm/leads protegida por x-crm-token retornando 401/400/200-duplicado/200-ok/500 conforme o caso; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Pagina /crm com kanban basico, /funil vira redirect, item CRM na sidebar</name>
  <files>src/lib/crm/dados.ts, src/app/(app)/crm/page.tsx, src/components/crm/kanban-crm.tsx, src/components/crm/card-oportunidade.tsx, src/components/crm/nova-oportunidade-dialog.tsx, src/app/(app)/funil/page.tsx, src/components/app-sidebar.tsx</files>
  <action>
    1. **src/lib/crm/dados.ts** — `getKanban()`: server-only, queries AGREGADAS SEQUENCIAIS (padrao do repo — nada de N+1 nem Promise.all): (1) workspace via getWorkspaceAtual, (2) pipeline padrao, (3) etapas do pipeline orderBy ordem, (4) TODAS as oportunidades abertas do pipeline em UMA query com leftJoin em crm_contatos e crm_empresas (nomes), orderBy (etapaId, ordemNaEtapa), (5) contatos e (6) etapas para o form do dialog se necessario. Merge em memoria: colunas = etapas com { etapa, oportunidades[], total: count, somaValor: soma de valor (Number(valor ?? 0)) }. Retornar tambem flag `configurado: boolean` (false quando workspace/pipeline nao existem — migration 0019 ainda nao aplicada) para a pagina degradar graciosamente com aviso 'Aplique a migration 0019 para ativar o CRM.' em vez de quebrar.
    2. **src/app/(app)/crm/page.tsx** — server component com `export const maxDuration = 60` (padrao das paginas do grupo app). Header h1 'CRM' + subtitulo 'Pipeline de vendas — oportunidades abertas.' + botao Nova oportunidade (dialog). Renderiza `<KanbanCrm colunas={...} etapas={...} />`. Visual FUNCIONAL simples com Cards shadcn — o visual definitivo vira do mockup do usuario depois (deixar comentario dizendo isso).
    3. **Componentes client** em src/components/crm/:
       - kanban-crm.tsx: grid horizontal com scroll (`flex gap-4 overflow-x-auto` ou grid), uma coluna por etapa: cabecalho com nome, badge de contagem e soma formatada em R$ (Intl.NumberFormat('pt-BR')); lista de cards.
       - card-oportunidade.tsx: titulo, empresa/contato, valor em R$, badge tipoReceita; acoes: Select (shadcn) de etapa que chama `moverOportunidade` (useTransition + toast sonner no erro), botoes/menu Ganhar (chama ganharOportunidade — v1 pode passar criarCliente=false; confirm() simples perguntando se deseja criar cliente e passar true), Perder (prompt/input de motivo obrigatorio antes de chamar perderOportunidade). SEM drag-and-drop no v1 — mover via select e suficiente.
       - nova-oportunidade-dialog.tsx: form minimo (titulo obrigatorio, valor, contato-nome, empresa-nome, etapa via Select) com react-hook-form + zodResolver chamando criarOportunidade; seguir o padrao de visibilidade do repo (useState, como ContratoForm evita Dialog fora do registry — usar o que ja existir em components/ui; se ui/dialog.tsx existir, pode usar).
    4. **src/app/(app)/funil/page.tsx** — substituir TODO o conteudo por: `import { redirect } from 'next/navigation'` e `export default function FunilPage() { redirect('/crm') }` (rota mantida para nao quebrar historico/links). Remover import do funilMock. Se `funilMock`/`FunilEtapaMock` ficarem sem NENHUM uso (conferir com grep), remove-los de src/lib/mock/extra.ts.
    5. **src/components/app-sidebar.tsx** — adicionar `{ title: 'CRM', url: '/crm', icon: Target }` logo apos o item Clientes (import Target de lucide-react; nao existe link /funil hoje — e um item NOVO).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build passa; /crm renderiza kanban do pipeline padrao com totais por coluna (ou aviso gracioso se a 0019 nao foi aplicada); dialog cria oportunidade; card move/ganha/perde; /funil redireciona para /crm; sidebar mostra CRM.</done>
</task>

</tasks>

<verification>
- `npm test` — testes de src/lib/crm/lead.test.ts passam (junto com os 190 existentes).
- `npm run build` — compila sem erros (tsc + Next).
- `ls drizzle/0019_*.sql` — migration existe, contem CREATE TABLE das 10 tabelas + seed idempotente no final, e NAO foi aplicada (nenhum comando de migrate executado).
- Grep de sanidade: `processarLead` usado na route; `getWorkspaceAtual` usado nas actions; `redirect('/crm')` em funil/page.tsx; item CRM em app-sidebar.tsx.
- Apos o usuario aplicar as migrations (0015-0019, em ordem) e configurar CRM_LEADS_TOKEN: `curl -X POST https://<app>/api/crm/leads -H "x-crm-token: <token>" -H "Content-Type: application/json" -d '{"fonte":"landing_page","nome":"Teste","email":"teste@ex.com"}'` retorna 200 ok e o lead aparece em /crm na coluna Novo Lead; repetir o mesmo curl no mesmo dia retorna { duplicado: true }.
</verification>

<success_criteria>
- 10 tabelas novas no schema Drizzle seguindo TODAS as convencoes do repo (portugues, uuid, numeric p/ dinheiro, text em vez de pgEnum p/ valores evolutivos, autorNome denormalizado).
- Migration 0019 gerada e NAO aplicada, com seed idempotente (workspace JSR, membros, pipeline Vendas, 6 etapas) que roda junto do deploy sem passo manual.
- Regras de negocio: mover/ganhar (com conversao opcional em cliente)/perder (motivo obrigatorio)/reabrir, cada uma gravando crm_atividades; dedup de contato por email/telefone; exatamente 1 pipeline padrao; etapa com oportunidades nao pode ser excluida.
- API POST /api/crm/leads: 401 sem token, 400 body invalido, idempotente por dedupHash, inbox com trilha de erro.
- /crm funcional (kanban simples, nova oportunidade, mover via select); /funil redireciona; sidebar com item CRM; modulo tarefas operacional e demais telas INTOCADOS.
</success_criteria>

<output>
Apos completar, criar `.planning/quick/260715-0zf-backend-completo-do-crm-comercial-worksp/260715-0zf-SUMMARY.md` seguindo o template de summary. Registrar: (a) migration 0019 GERADA e NAO aplicada (pendentes 0015-0019, aplicar em ordem); (b) setup do usuario: criar env CRM_LEADS_TOKEN no .env.local e na Vercel.
</output>
