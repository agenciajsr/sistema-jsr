---
phase: quick-260711-tg5
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [tg5-ficha-cliente-real]
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0001_*.sql
  - drizzle/meta/0001_snapshot.json
  - drizzle/meta/_journal.json
  - src/actions/checklist.ts
  - src/actions/acompanhamento.ts
  - src/actions/financeiro.ts
  - src/actions/trafego.ts
  - src/actions/alertas.ts
  - src/components/ficha/checklist-cliente.tsx
  - src/components/ficha/acompanhamento-form.tsx
  - src/components/ficha/cobranca-cliente.tsx
  - src/components/ficha/vincular-conta-ficha.tsx
  - src/app/(app)/clientes/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "A ficha do cliente NÃO importa mais getMockDaFicha nem exibe MockNotice"
    - "KPIs (MRR, próxima cobrança, verba rodando, status da conta) vêm de dados reais do banco"
    - "Cobrança é editável: marcar pago/pendente/vencido, adicionar cobrança e alternar 'usa Asaas' persistem no banco"
    - "Contas de anúncio vinculadas ao cliente aparecem com performance real; contas não-vinculadas podem ser vinculadas na ficha"
    - "Checklist persiste: adicionar item, marcar concluído e excluir salvam no banco"
    - "Acompanhamento persiste: nota adicionada é salva com autor = usuário logado"
    - "Alertas do cliente aparecem na ficha quando existem"
    - "A migration foi GERADA (aditiva) mas NÃO aplicada"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Tabelas checklist_items e acompanhamentos, enum frequencia_checklist, coluna clientes.usaAsaas"
      contains: "checklist_items"
    - path: "drizzle/0001_*.sql"
      provides: "Migration aditiva (CREATE TABLE/TYPE/ADD COLUMN) — não aplicada"
      contains: "CREATE TABLE"
    - path: "src/actions/checklist.ts"
      provides: "getChecklistDoCliente, addChecklistItem, toggleChecklistItem, deleteChecklistItem"
      exports: ["getChecklistDoCliente", "addChecklistItem", "toggleChecklistItem", "deleteChecklistItem"]
    - path: "src/actions/acompanhamento.ts"
      provides: "getAcompanhamentosDoCliente, addAcompanhamento"
      exports: ["getAcompanhamentosDoCliente", "addAcompanhamento"]
    - path: "src/app/(app)/clientes/[id]/page.tsx"
      provides: "Ficha religada a dados reais, sem mock"
      contains: "getChecklistDoCliente"
  key_links:
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "src/actions/checklist.ts + acompanhamento.ts + financeiro.ts + trafego.ts + alertas.ts"
      via: "server actions"
      pattern: "getChecklistDoCliente|getAcompanhamentosDoCliente|getCobrancasDoCliente|getContasDoCliente|getAlertasDoCliente"
    - from: "src/components/ficha/cobranca-cliente.tsx"
      to: "src/actions/financeiro.ts"
      via: "updateTransacaoStatus / setUsaAsaas / createCobranca"
      pattern: "updateTransacaoStatus|setUsaAsaas"
---

<objective>
Religar a Ficha do Cliente (`src/app/(app)/clientes/[id]/page.tsx`) — hoje 100% alimentada por `getMockDaFicha` — a dados REAIS do banco, preservando exatamente o layout premium de abas. Tornam-se funcionais e persistidos: cobrança (editável), vínculo de contas de anúncio, checklist e acompanhamento; e passam a aparecer os alertas do cliente.

Purpose: Eliminar a última grande tela mock do sistema. O usuário reclamou que "cobrança não edita, não vincula conta, checklist/acompanhamento não salvam, não tem alertas do cliente". Agora que as tabelas reais existem, a ficha vira operacional de verdade.

Output: 2 tabelas novas (`checklist_items`, `acompanhamentos`) + coluna `clientes.usaAsaas` + enum de frequência; server actions novas/estendidas; componentes client persistentes; página religada sem mock. Migration GERADA mas NÃO aplicada (orquestrador aplica depois).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contratos-chave já existentes no codebase. O executor DEVE reusá-los, sem reexplorar. -->

Schema atual (src/lib/db/schema.ts) — reusar SEM alterar colunas existentes:
- clientes { id, nome, nicho, status, contatoNome, contatoTelefone, contatoEmail, notas, createdAt, updatedAt } — ADICIONAR só a coluna usaAsaas.
- transacoes { id, tipo ('receita'|'despesa'), categoria ('mensalidade'|...), clienteId (nullable, onDelete set null), descricao, valor numeric, data (date, YYYY-MM-DD), status ('pago'|'pendente'|'vencido' default 'pendente'), diaVencto int nullable, notas, createdAt, updatedAt }
- adAccounts { id, clienteId (nullable), plataforma ('meta'|'google'), metaAccountId, nome, accountStatus int nullable, currency, ativo, ... }
- profiles { id (== auth.users.id), nome, role }

Auth (src/lib/auth/session.ts):
  getCurrentUser(): Promise<{ id: string; email?: string; nome: string; role: 'admin'|'membro' } | null>
  requireAdmin(): Promise<{ user } | { error: string }>

Contratos (src/actions/contratos.ts) — JÁ REAIS, preservar:
  getContratosDoCliente(clienteId): Promise<{ contratoAtual, historico }>
  deleteContrato(id) (admin-only)

Aggregate de tráfego (src/lib/trafego/aggregate.ts) — REUSAR para performance das contas do cliente:
  getResumoCliente(clienteId: string, periodoDias: 7 | 30): Promise<ResumoCliente | null>
  // ResumoCliente.totais.spend, .contasUnificadas, .temDados, .heroi, .ranking, .serieSpendPorDia

Tráfego (src/actions/trafego.ts) — REUSAR:
  getContasNaoVinculadas(): Promise<{ id, nome, metaAccountId, accountStatus }[]>
  vincularContaAoCliente(adAccountId, clienteId | null): revalida /campanhas (AJUSTAR p/ revalidar também a ficha)

Alertas (src/actions/alertas.ts) — REUSAR:
  getAlertas(): Promise<Alerta[]>  // Alerta tem .clienteId — filtrar por ele
  // Alerta { id, tipo, severidade ('critico'|'atencao'|'info'), titulo, detalhe, clienteNome, clienteId, dataRelevante }

Financeiro (src/actions/financeiro.ts) — REUSAR/ESTENDER:
  createTransacao(input), deleteTransacao(id) (admin), listTransacoes(...)
</interfaces>

@src/app/(app)/clientes/[id]/page.tsx
@src/lib/db/schema.ts
@src/actions/financeiro.ts
@src/lib/trafego/aggregate.ts
@src/components/ficha/checklist-cliente.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + migration ADITIVA (gerar, NÃO aplicar)</name>
  <files>src/lib/db/schema.ts, drizzle/0001_*.sql, drizzle/meta/0001_snapshot.json, drizzle/meta/_journal.json</files>
  <action>
Atualizar `src/lib/db/schema.ts` (apenas ADIÇÕES — não remover/alterar nada existente):

1. Novo enum: `export const frequenciaChecklistEnum = pgEnum('frequencia_checklist', ['diaria','semanal','mensal'])`.
2. Coluna nova em `clientes`: adicionar `usaAsaas: boolean('usa_asaas').notNull().default(false)` (a coluna vem com default, então é aditiva segura para linhas existentes).
3. Nova tabela `checklistItems`:
   `export const checklistItems = pgTable('checklist_items', { id: uuid('id').primaryKey().defaultRandom(), clienteId: uuid('cliente_id').notNull().references(() => clientes.id, { onDelete: 'cascade' }), tarefa: text('tarefa').notNull(), frequencia: frequenciaChecklistEnum('frequencia').notNull(), concluido: boolean('concluido').notNull().default(false), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow() })` com índice em (clienteId).
4. Nova tabela `acompanhamentos`:
   `export const acompanhamentos = pgTable('acompanhamentos', { id: uuid('id').primaryKey().defaultRandom(), clienteId: uuid('cliente_id').notNull().references(() => clientes.id, { onDelete: 'cascade' }), autorId: uuid('autor_id'), autorNome: text('autor_nome').notNull(), nota: text('nota').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow() })` com índice em (clienteId, createdAt).
5. Estender `clientesRelations` com `checklistItems: many(checklistItems)` e `acompanhamentos: many(acompanhamentos)`, e adicionar as relations inversas (`one(clientes)`) para as duas novas tabelas.

Depois GERAR a migration (offline, não conecta no banco):
`npx drizzle-kit generate`

REGRA CRÍTICA DE MIGRATION (o snapshot está DESSINCRONIZADO):
- O snapshot `drizzle/meta/0000_snapshot.json` só conhece 4 tabelas (clientes, contratos, profiles, transacoes) e NÃO conhece `ad_accounts`, `campaign_insights` nem o enum `plataforma` (foram criados no banco via push, sem migration).
- Portanto o `generate` vai emitir no .sql, ALÉM dos objetos novos, CREATE TYPE `plataforma` + CREATE TABLE `ad_accounts` + CREATE TABLE `campaign_insights` (que JÁ EXISTEM no banco). Se aplicados, quebram com "already exists".
- AÇÃO OBRIGATÓRIA: abrir o `drizzle/0001_*.sql` gerado e REMOVER manualmente todas as statements referentes a objetos que já existem no banco (o CREATE TYPE "plataforma", o CREATE TABLE "ad_accounts", o CREATE TABLE "campaign_insights" e quaisquer ALTER/constraint desses). MANTER APENAS as statements dos objetos NOVOS: `CREATE TYPE "public"."frequencia_checklist"`, `CREATE TABLE "checklist_items"`, `CREATE TABLE "acompanhamentos"`, `ALTER TABLE "clientes" ADD COLUMN "usa_asaas"`, e as foreign keys / índices dessas 2 tabelas novas.
- Deixar o `drizzle/meta/0001_snapshot.json` e `_journal.json` como o drizzle gerou (o snapshot reconciliar a realidade é desejável).
- NÃO APLICAR: NÃO rodar `drizzle-kit migrate`, `drizzle-kit push`, nem qualquer comando que conecte no banco. Apenas deixar o .sql commitado. O orquestrador aplica depois de forma controlada.

Confirmar que a rota é dinâmica (`[id]`, sem generateStaticParams / sem dynamic estático) — já verificado: NÃO é pré-renderizada no build, então `npm run build` passa mesmo sem as tabelas existirem no banco ainda.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit && ls drizzle/0001_*.sql && grep -c "checklist_items\|acompanhamentos\|usa_asaas" drizzle/0001_*.sql</automated>
  </verify>
  <done>schema.ts tem as 2 tabelas + coluna usaAsaas + enum; existe drizzle/0001_*.sql contendo APENAS objetos novos (checklist_items, acompanhamentos, usa_asaas, frequencia_checklist), sem CREATE TABLE de ad_accounts/campaign_insights; nenhum comando aplicou a migration; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Server actions de Checklist e Acompanhamento (novos arquivos)</name>
  <files>src/actions/checklist.ts, src/actions/acompanhamento.ts</files>
  <action>
Criar `src/actions/checklist.ts` (`'use server'`), todas com guard `getCurrentUser()` (retornar `{ error }` ou `[]` se não logado) e `revalidatePath('/clientes/' + clienteId)`:
- `getChecklistDoCliente(clienteId: string)`: `db.query.checklistItems.findMany` where `clienteId`, orderBy `createdAt` asc. Retorna a lista.
- `addChecklistItem(clienteId: string, tarefa: string, frequencia: 'diaria'|'semanal'|'mensal')`: validar tarefa não-vazia e frequencia ∈ enum (inline, sem lib nova). Insert. Revalidate.
- `toggleChecklistItem(id: string, clienteId: string, concluido: boolean)`: `db.update(checklistItems).set({ concluido, updatedAt: new Date() }).where(eq(id))`. Revalidate.
- `deleteChecklistItem(id: string, clienteId: string)`: delete where id. Revalidate.
(clienteId é passado em toggle/delete só para o revalidatePath da ficha.)

Criar `src/actions/acompanhamento.ts` (`'use server'`), guard + revalidate:
- `getAcompanhamentosDoCliente(clienteId: string)`: findMany where clienteId, orderBy `createdAt` desc. Retorna lista.
- `addAcompanhamento(clienteId: string, nota: string)`: `const user = await getCurrentUser()`; se !user retornar `{ error }`; validar nota não-vazia (trim); insert `{ clienteId, autorId: user.id, autorNome: user.nome, nota }`. Revalidate `/clientes/${clienteId}`.

Importar `checklistItems` e `acompanhamentos` de `@/lib/db/schema`, `db` de `@/lib/db`, `eq` de `drizzle-orm`, `revalidatePath` de `next/cache`, `getCurrentUser` de `@/lib/auth/session`.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit</automated>
  </verify>
  <done>Ambos arquivos existem, exportam as funções listadas, todas com guard de sessão + revalidatePath; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Server actions de Cobrança, Contas e Alertas do cliente</name>
  <files>src/actions/financeiro.ts, src/actions/trafego.ts, src/actions/alertas.ts</files>
  <action>
Estender `src/actions/financeiro.ts` (guard getCurrentUser + revalidatePath('/clientes/'+clienteId) onde aplicável):
- `getCobrancasDoCliente(clienteId: string)`: select de `transacoes` where `clienteId = clienteId AND tipo = 'receita'`, orderBy `data` desc. Retornar id, descricao, valor, data, status, diaVencto.
- `updateTransacaoStatus(id: string, clienteId: string, status: 'pago'|'pendente'|'vencido')`: guard; `db.update(transacoes).set({ status, updatedAt: new Date() }).where(eq(transacoes.id, id))`; revalidate `/clientes/${clienteId}` e `/financeiro`.
- `createCobranca(clienteId: string, input: { descricao: string; valor: number; data: string; diaVencto?: number })`: guard; validar (valor>0, data no formato YYYY-MM-DD, descricao não-vazia) inline; insert em `transacoes` com `tipo:'receita', categoria:'mensalidade', clienteId, valor: valor.toFixed(2), data, status:'pendente', diaVencto: diaVencto ?? null`. Revalidate `/clientes/${clienteId}` e `/financeiro`.
- `setUsaAsaas(clienteId: string, usaAsaas: boolean)`: guard; `db.update(clientes).set({ usaAsaas, updatedAt: new Date() }).where(eq(clientes.id, clienteId))`; revalidate `/clientes/${clienteId}`. (Importar `clientes` no arquivo.)

Estender `src/actions/trafego.ts`:
- `getContasDoCliente(clienteId: string)`: guard; select de `adAccounts` where `clienteId` (todas plataformas), retornar id, nome, metaAccountId, plataforma, accountStatus, ativo, ordenado por nome.
- Ajustar `vincularContaAoCliente`: quando `clienteId` não-nulo, adicionar `revalidatePath('/clientes/' + clienteId)` ALÉM do `revalidatePath('/campanhas')` já existente (não quebrar o comportamento atual de /campanhas).

Estender `src/actions/alertas.ts`:
- `getAlertasDoCliente(clienteId: string): Promise<Alerta[]>`: chamar `getAlertas()` e `return todos.filter(a => a.clienteId === clienteId)`. (Reuso simples — o motor já popula clienteId em contrato/transacao/cliente.)
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit</automated>
  </verify>
  <done>financeiro.ts exporta getCobrancasDoCliente/updateTransacaoStatus/createCobranca/setUsaAsaas; trafego.ts exporta getContasDoCliente e vincularContaAoCliente revalida a ficha; alertas.ts exporta getAlertasDoCliente; tsc limpo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Componentes client persistentes (checklist, acompanhamento, cobrança, vincular conta)</name>
  <files>src/components/ficha/checklist-cliente.tsx, src/components/ficha/acompanhamento-form.tsx, src/components/ficha/cobranca-cliente.tsx, src/components/ficha/vincular-conta-ficha.tsx</files>
  <action>
Todos client components (`'use client'`), usar `useTransition` + `router.refresh()` (ou confiar no revalidatePath), `toast` (sonner) para feedback, PT-BR, preservando a linguagem visual premium (mesmos primitivos shadcn já usados: Card, Badge, Button, Input, Checkbox, Textarea, Select se existir).

1. REESCREVER `checklist-cliente.tsx` para persistir (não mais estado local puro):
   - Props: `{ clienteId: string; itens: { id: string; tarefa: string; frequencia: 'diaria'|'semanal'|'mensal'; concluido: boolean }[] }`.
   - Lista com Checkbox chamando `toggleChecklistItem(id, clienteId, !concluido)` via startTransition; botão excluir chamando `deleteChecklistItem(id, clienteId)`; form (Input tarefa + select de frequência + botão) chamando `addChecklistItem(clienteId, tarefa, frequencia)`.
   - Badge de frequência: rótulos legíveis (diaria→"Diária", semanal→"Semanal", mensal→"Mensal").
   - Estado vazio: mensagem "Nenhum item de checklist para este cliente." + o form de adicionar sempre visível.

2. NOVO `acompanhamento-form.tsx`:
   - Props: `{ clienteId: string }`. Textarea HABILITADO + botão "Adicionar nota"; on submit chama `addAcompanhamento(clienteId, nota)` em startTransition; limpa o textarea e `router.refresh()` no sucesso; toast de erro se `{ error }`.

3. NOVO `cobranca-cliente.tsx`:
   - Props: `{ clienteId: string; usaAsaas: boolean; cobrancas: { id, descricao, valor, data, status, diaVencto }[] }`.
   - Lista de cobranças: cada linha mostra descrição/valor formatado (Intl BRL)/data (dd/MM/yyyy) e um controle para mudar status (3 botões ou um Select) chamando `updateTransacaoStatus(id, clienteId, status)`. Reusar as cores de STATUS_COBRANCA (pago=success, pendente=warning, vencido=destructive).
   - Toggle "Usa Asaas" (Checkbox/Switch) chamando `setUsaAsaas(clienteId, valor)`.
   - Form "Adicionar cobrança" (descricao, valor, data, diaVencto opcional) chamando `createCobranca(clienteId, {...})`.
   - Estado vazio: "Nenhuma cobrança registrada." + form visível.

4. NOVO `vincular-conta-ficha.tsx`:
   - Props: `{ clienteId: string; contasNaoVinculadas: { id, nome, metaAccountId }[] }`.
   - Select (ou lista com botões) de contas não-vinculadas → botão "Vincular" chamando `vincularContaAoCliente(contaId, clienteId)` em startTransition + toast + refresh.
   - Se `contasNaoVinculadas` vazio: mostrar texto discreto "Nenhuma conta de anúncio disponível para vincular."

Se o primitivo Select do shadcn não existir no repo, usar `<select>` nativo estilizado com classes Tailwind (não adicionar dependência nem componente novo de UI fora do que já existe).
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit</automated>
  </verify>
  <done>Os 4 componentes existem, são client components, chamam as server actions corretas e dão feedback; checklist-cliente reescrito para persistir; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 5: Religar a página da ficha a dados reais (remover mock)</name>
  <files>src/app/(app)/clientes/[id]/page.tsx</files>
  <action>
Editar `src/app/(app)/clientes/[id]/page.tsx` REMOVENDO todo uso de mock e preservando EXATAMENTE o layout premium (cabeçalho, faixa de 4 KPIs, Tabs com as 4 abas e os títulos/emojis atuais):

Remover: `import { getMockDaFicha }`, `import { MockNotice }`, a chamada `getMockDaFicha(...)`, e todos os `<MockNotice>` desta ficha. (NÃO alterar o mock em si nem as rotas soltas /checklist e /acompanhamento — fora de escopo.)

Carregar dados reais (Promise.all onde fizer sentido) — importar as actions das tasks 2/3 e o aggregate:
- `getContratosDoCliente(id)` (já usado — preservar contratoAtual/historico/exclusões admin-only com AlertDialog e copy idêntica).
- `getCobrancasDoCliente(id)`, `getContasDoCliente(id)`, `getResumoCliente(id, 30)`, `getContasNaoVinculadas()`, `getChecklistDoCliente(id)`, `getAcompanhamentosDoCliente(id)`, `getAlertasDoCliente(id)`, `getCurrentUser()`.

KPIs reais (manter os 4 StatCards e ícones atuais):
- "MRR do cliente": `contratoAtual ? Number(contratoAtual.valorMensal)` formatado, senão '—'.
- "Próxima cobrança": a cobrança (de getCobrancasDoCliente) com status 'pendente'/'vencido' de data futura/mais próxima → mostrar `formatarData(data)`; helper = status. Se nenhuma, '—'.
- "Verba rodando": `resumo?.temDados ? formatadorMoeda.format(resumo.totais.spend)` senão '—'; helper "últimos 30 dias".
- "Status da conta": derivar de `getContasDoCliente`. Mapear Meta accountStatus (1→'Ativa'/success; 2/3→'Com restrição'/warning; outros→'Inativa'/danger). Se nenhuma conta vinculada, '—' cor primary. Manter a estrutura STATUS_CONTA/cores semânticas.

Cabeçalho: adicionar, logo abaixo do bloco de status/contato (ou entre o header e os KPIs), uma faixa de ALERTAS do cliente quando `alertas.length > 0`: uma lista/box com borda colorida por severidade (critico→destructive, atencao→warning, info→muted/primary) mostrando `titulo` + `detalhe`. Se vazio, não renderizar nada.

Abas:
- "📄 Contrato & Cobrança": manter Contrato Atual + Histórico + `<ContratoForm />` EXATAMENTE como estão (incl. AlertDialog admin-only e copy). SUBSTITUIR o bloco "💳 Cobrança" mock por `<CobrancaCliente clienteId={id} usaAsaas={cliente.usaAsaas} cobrancas={cobrancas} />`.
- "📊 Contas de anúncio": se `contasDoCliente.length === 0`, estado vazio com CTA usando `<VincularContaFicha clienteId={id} contasNaoVinculadas={naoVinculadas} />`. Se houver contas: renderizar as contas vinculadas + a performance real do `resumo` (spend total, contasUnificadas, e ranking de campanhas do `resumo.ranking` reaproveitando o visual de cards de campanha existente) + também o `<VincularContaFicha />` para vincular contas adicionais.
- "✅ Checklist": `<ChecklistCliente clienteId={id} itens={checklist.map(...frequencia/concluido...)} />` (sem MockNotice).
- "📝 Acompanhamento": lista real de notas (Avatar com inicial de `autorNome`, `autorNome`, data de `createdAt` formatada, `nota`) + `<AcompanhamentoForm clienteId={id} />` no lugar do Textarea disabled.

Preservar `notFound()`, `getCurrentUser`, `isAdmin`, e todo comportamento real já existente. Toda a UI em português.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -15</automated>
  </verify>
  <done>page.tsx não importa getMockDaFicha nem MockNotice; KPIs/abas/alertas usam dados reais via as novas actions; `npx tsc --noEmit` limpo, `npm run build` sucesso, `npm run lint` sem erros NOVOS (conhecidos toleráveis: ui/sidebar.tsx, hooks/use-mobile.ts, o `any` em sync-meta-ads.ts).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo após cada task e no final.
- `npm run build` conclui com sucesso (rota `[id]` é dinâmica — não pré-renderiza, passa sem as tabelas novas existirem no banco).
- `npm run lint` sem erros NOVOS além dos conhecidos (ui/sidebar.tsx, hooks/use-mobile.ts, any em sync-meta-ads.ts).
- `drizzle/0001_*.sql` existe, é aditivo (CREATE TYPE/TABLE + ADD COLUMN), NÃO contém CREATE TABLE de ad_accounts/campaign_insights, e NÃO foi aplicado.
- Grep confirma que page.tsx não referencia mais getMockDaFicha nem MockNotice.
</verification>

<success_criteria>
- Ficha do cliente 100% em dados reais: sem getMockDaFicha, sem MockNotice.
- Cobrança editável e persistida (status, adicionar, usa Asaas).
- Contas de anúncio reais + vínculo pela ficha + performance agregada real.
- Checklist e acompanhamento persistem no banco.
- Alertas do cliente aparecem quando existem.
- Migration aditiva gerada e commitada, NÃO aplicada.
- Sem novas dependências. UI em português. Visual premium preservado.
</success_criteria>

<output>
Após a conclusão, criar `.planning/quick/260711-tg5-ficha-do-cliente-real-ligar-a-ficha-aos-/260711-tg5-SUMMARY.md`.
</output>
