---
phase: quick-260720-re3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/financeiro/cac.ts
  - src/lib/financeiro/cac.test.ts
  - src/actions/financeiro.ts
  - src/app/(app)/financeiro/aquisicao-form.tsx
autonomous: true
requirements: [quick-260720-re3]
must_haves:
  truths:
    - "Cliente cuja oportunidade no CRM tem origem meta_lead_ad é contado no canal meta_ads do CAC (não mais só pelo texto livre)"
    - "Cliente com origem CRM não-paga (landing_page/whatsapp/evento/parceria/manual/outro) cai na reserva do texto livre; 'Amigo indicou' vira indicação"
    - "Cliente sem vínculo no CRM continua classificado só pelo texto livre (comportamento atual preservado)"
    - "Cliente de Google (não existe origem Google no CRM) é captado pela reserva de texto livre (keyword 'google')"
    - "Botão '+ Lançar investimento' abre um Dialog centralizado com o formulário; o histórico de lançamentos continua visível na aba, fora do Dialog"
  artifacts:
    - path: "src/lib/financeiro/cac.ts"
      provides: "canalDaOrigemCrm (mapa CRM→canal) + resolverCanalCliente (cadeia CRM→reserva) + premissa da atribuição"
      contains: "canalDaOrigemCrm"
    - path: "src/lib/financeiro/cac.test.ts"
      provides: "Testes RED→GREEN do mapa e da reserva"
      contains: "canalDaOrigemCrm"
    - path: "src/actions/financeiro.ts"
      provides: "getCacAquisicao resolve o canal via origem do CRM (queries sequenciais) com reserva no texto livre"
      contains: "origemCrm"
    - path: "src/app/(app)/financeiro/aquisicao-form.tsx"
      provides: "Formulário da aba Aquisição em Dialog centralizado, histórico fora do Dialog"
      contains: "Dialog"
  key_links:
    - from: "src/actions/financeiro.ts (getCacAquisicao)"
      to: "crm_oportunidades / crm_contatos por cliente_id"
      via: "queries SEQUENCIAIS (fora do Promise.all), em try/catch de degradação"
      pattern: "crmOportunidades|crmContatos"
    - from: "src/lib/financeiro/cac.ts (agregar)"
      to: "resolverCanalCliente"
      via: "cadeia canalDaOrigemCrm(origemCrm) ?? classificarCanal(origem)"
      pattern: "resolverCanalCliente"
    - from: "src/app/(app)/financeiro/aquisicao-form.tsx (botão)"
      to: "Dialog"
      via: "useState aberto + DialogContent centralizado"
      pattern: "setAberto"
---

<objective>
Melhorar o CAC do Financeiro em duas frentes:

1. **Atribuição de canal por origem estruturada do CRM (híbrida).** Hoje `getCacAquisicao` classifica cada cliente só pelo texto livre `clientes.origem_cliente`. Passa a descobrir o canal PRIMEIRO pela origem estruturada do CRM (oportunidade vinculada → contato vinculado), mapeada por uma função pura nova e testável, com RESERVA no texto livre quando não há vínculo ou a origem estruturada não identifica um canal pago.

2. **UI Dialog.** O formulário inline da aba Aquisição (`aquisicao-form.tsx`) vira um Dialog centralizado no mesmo padrão de `transacao-form.tsx`: um botão "+ Lançar investimento" abre o pop-up; o histórico de lançamentos continua visível fora do Dialog.

Purpose: casar clientes captados pelo CRM (Meta lead ads, indicação, prospecção, Instagram orgânico) ao canal certo do CAC sem depender só de como alguém digitou "Como conheceu a agência?"; e alinhar a UI da aba ao padrão de formulário do resto do Financeiro.
Output: `cac.ts` com `canalDaOrigemCrm` + `resolverCanalCliente` sob TDD; `getCacAquisicao` lendo origem do CRM em queries sequenciais; aba Aquisição com form em Dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contratos existentes que o executor consome direto — sem explorar o codebase. -->

De src/lib/financeiro/cac.ts (módulo PURO — zero import de db/crm/react):
```typescript
export const CANAIS_AQUISICAO = ['meta_ads','google_ads','indicacao','organico','prospeccao','outro'] as const
export type CanalAquisicao = (typeof CANAIS_AQUISICAO)[number]
export function classificarCanal(origem: string | null): CanalAquisicao   // reserva do texto livre (keywords → canal, fallback 'outro')
export type ClienteGanho = { origem: string | null; inicio: string | null }
// agregar() hoje faz: const canal = classificarCanal(cliente.origem)
export const PREMISSA_CAC = '...'
```

De src/lib/crm/origem.ts — valores canônicos de crm_*.origem:
`meta_lead_ad | indicacao | prospeccao_fria | instagram | landing_page | whatsapp | evento | parceria | manual | outro`

De src/lib/db/schema.ts:
- `crmOportunidades`: colunas `clienteId` (uuid, nullable), `origem` (text, nullable), `updatedAt`, `createdAt`
- `crmContatos`: colunas `clienteId` (uuid, nullable), `origem` (text notNull default 'manual'), `updatedAt`, `createdAt`
- `clientes`: `origemCliente` (text livre), `id`

De src/actions/financeiro.ts (getCacAquisicao, ~714-804): já roda SEQUENCIAL fora do Promise.all da página; lê `linhasClientes` (id, status, origemCliente, createdAt) e `linhasContratos` (agregado por clienteId com array_agg ordenado); monta `clientesGanhos: ClienteGanho[]` a partir de `c.origemCliente`. Padrão de leitura "mais recente por cliente" já usado: `(array_agg(col order by data desc))[1]`.

De src/app/(app)/financeiro/transacao-form.tsx — PADRÃO de Dialog a replicar:
```typescript
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog'
const [aberto, setAberto] = useState(false)
// botão-gatilho: <Button onClick={() => setAberto(true)}><Plus/> Adicionar transação</Button>
// <Dialog open={aberto} onOpenChange={(open) => (open ? setAberto(true) : handleCancel())}>
//   <DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-2xl"> ... </DialogContent>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frente 1 (lógica pura) — canalDaOrigemCrm + resolverCanalCliente sob TDD</name>
  <files>src/lib/financeiro/cac.ts, src/lib/financeiro/cac.test.ts</files>
  <behavior>
    Escrever os testes RED PRIMEIRO em cac.test.ts, depois implementar (GREEN). Não alterar nenhum dos 19 testes existentes — eles devem continuar verdes (regressão da reserva).

    canalDaOrigemCrm(origemCrm: string | null): CanalAquisicao | null — mapa CRM→canal:
    - Test: 'meta_lead_ad' → 'meta_ads'
    - Test: 'indicacao' → 'indicacao'
    - Test: 'prospeccao_fria' → 'prospeccao'
    - Test: 'instagram' → 'organico'  (Instagram ORGÂNICO; o Meta PAGO é meta_lead_ad)
    - Test: 'landing_page' | 'whatsapp' | 'evento' | 'parceria' | 'manual' | 'outro' → null (não identifica canal — deixa a reserva decidir)
    - Test: null | '' | 'valor_desconhecido' → null

    resolverCanalCliente(origemCrm: string | null, origemTextoLivre: string | null): CanalAquisicao — a CADEIA/RESERVA:
    - Test (com vínculo pago): resolverCanalCliente('meta_lead_ad', 'Amigo') → 'meta_ads' (CRM vence)
    - Test (CRM não-paga cai no texto livre): resolverCanalCliente('landing_page', 'Foi um amigo que indicou') → 'indicacao'
    - Test (sem vínculo CRM → só texto livre): resolverCanalCliente(null, 'Google') → 'google_ads'
    - Test (Instagram orgânico via CRM): resolverCanalCliente('instagram', 'sei lá') → 'organico'
    - Test (nada resolve): resolverCanalCliente(null, null) → 'outro' e resolverCanalCliente('manual', 'xxxx') → 'outro'
  </behavior>
  <action>
    Em src/lib/financeiro/cac.ts:

    1. Adicionar a função pura exportada `canalDaOrigemCrm(origemCrm: string | null): CanalAquisicao | null` com um mapa explícito (Record parcial) dos valores canônicos de origem do CRM:
       meta_lead_ad→'meta_ads', indicacao→'indicacao', prospeccao_fria→'prospeccao', instagram→'organico'.
       Qualquer outro valor (landing_page, whatsapp, evento, parceria, manual, outro, null, desconhecido) → null.
       Documentar em comentário: Google Ads NÃO existe como origem no CRM — por isso o híbrido; cliente de Google é captado só pela reserva de texto livre (keyword 'google' já existe em classificarCanal).

    2. Adicionar a função pura exportada `resolverCanalCliente(origemCrm: string | null, origemTextoLivre: string | null): CanalAquisicao` = `canalDaOrigemCrm(origemCrm) ?? classificarCanal(origemTextoLivre)`. É a cadeia/reserva: origem estruturada do CRM primeiro; se null (sem vínculo OU origem que não identifica canal), cai no classificador do texto livre já existente; classificarCanal garante o fallback 'outro'.

    3. Estender o tipo `ClienteGanho`: adicionar campo OPCIONAL `origemCrm?: string | null` (origem estruturada resolvida do CRM — undefined/null = sem vínculo, cai na reserva). Manter `origem` como o texto livre da reserva. Isso preserva os 19 testes existentes (que passam só `origem`).

    4. Em `agregar()`, trocar `const canal = classificarCanal(cliente.origem)` por `const canal = resolverCanalCliente(cliente.origemCrm ?? null, cliente.origem)`. Nenhuma outra linha muda.

    5. Estender a documentação da premissa: adicionar uma constante exportada `PREMISSA_CAC_ATRIBUICAO` (string) explicando a nova cadeia — "canal atribuído por origem estruturada do CRM (oportunidade vinculada → contato vinculado, mapeadas por canalDaOrigemCrm); reserva no texto livre clientes.origem_cliente via classificarCanal quando não há vínculo ou a origem estruturada não identifica canal pago; Google não existe no CRM, só chega pela reserva; fallback final 'outro'." Manter PREMISSA_CAC intacta.

    Não importar db/crm/react no arquivo (regra do módulo puro): as funções recebem as origens já lidas.
  </action>
  <verify>
    <automated>cd D:/projetos/projeto_agencia_jsr && npx vitest run src/lib/financeiro/cac.test.ts</automated>
  </verify>
  <done>canalDaOrigemCrm e resolverCanalCliente exportados e cobertos por testes novos; os 19 testes anteriores seguem verdes; agregar usa resolverCanalCliente; PREMISSA_CAC_ATRIBUICAO exportada; nenhum import de db/crm no módulo.</done>
</task>

<task type="auto">
  <name>Task 2: Frente 1 (wiring) — getCacAquisicao resolve o canal via origem do CRM (sequencial)</name>
  <files>src/actions/financeiro.ts</files>
  <action>
    Em getCacAquisicao (~714-804), DEPOIS das leituras sequenciais existentes de linhasClientes e linhasContratos (NUNCA dentro do Promise.all da página — regra do pool max=5), acrescentar a resolução da origem do CRM:

    1. Importar `crmOportunidades` e `crmContatos` de '@/lib/db/schema' (adicionar ao import existente na linha 8).

    2. Ler a ORIGEM da oportunidade mais relevante por cliente — query SEQUENCIAL, agregada, dentro de um try/catch (degradação graciosa: se a tabela/coluna não existir — migrations do CRM pendentes — logar e seguir com mapa vazio, caindo 100% na reserva de texto livre). Escolher a MAIS RECENTE por updatedAt (fallback createdAt) usando o mesmo padrão já presente no arquivo:
       ```
       select
         clienteId: crmOportunidades.clienteId,
         origem: sql<string | null>`(array_agg(${crmOportunidades.origem} order by ${crmOportunidades.updatedAt} desc, ${crmOportunidades.createdAt} desc))[1]`
       from crmOportunidades
       where clienteId is not null   // use isNotNull(crmOportunidades.clienteId)
       group by clienteId
       ```
       Montar `origemOppPorCliente = new Map(linhas.map(l => [l.clienteId, l.origem]))`. Documentar em comentário a decisão "mais recente por updatedAt".

    3. Ler a ORIGEM do contato vinculado por cliente — outra query SEQUENCIAL, mesmo try/catch (pode ser o mesmo bloco try). Mesmo padrão array_agg mais recente por updatedAt, group by clienteId, filtrando clienteId not null. Montar `origemContatoPorCliente`.

    4. Ao montar `clientesGanhos`, definir `origemCrm` pela cadeia de VÍNCULO (oportunidade primeiro, senão contato): `const origemCrm = origemOppPorCliente.get(c.id) ?? origemContatoPorCliente.get(c.id) ?? null`. Passar no objeto: `{ origem: c.origemCliente, origemCrm, inicio: ... }`. O módulo puro (resolverCanalCliente dentro de agregar) faz o resto da cadeia (mapa CRM → reserva de texto livre → 'outro'). NÃO alterar a montagem de `vidas` (LTV independe da origem).

    Manter a ordem: as novas queries do CRM são SEQUENCIAIS após as existentes, nunca engordam o Promise.all da página. Nenhuma migration nova (só leitura de tabelas existentes).
  </action>
  <verify>
    <automated>cd D:/projetos/projeto_agencia_jsr && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "financeiro\.ts|cac\.ts" || echo "sem erros de tipo em financeiro/cac"</automated>
  </verify>
  <done>getCacAquisicao lê crm_oportunidades e crm_contatos em queries sequenciais (fora do Promise.all), em try/catch de degradação; origemCrm resolvido por oportunidade-mais-recente → contato → null é injetado em cada ClienteGanho; tipos compilam; nenhuma migration nova.</done>
</task>

<task type="auto">
  <name>Task 3: Frente 2 (UI) — aquisicao-form vira Dialog centralizado, histórico fora</name>
  <files>src/app/(app)/financeiro/aquisicao-form.tsx</files>
  <action>
    Transformar o formulário inline de investimento em um Dialog centralizado no padrão de transacao-form.tsx (ver <interfaces>), preservando TODA a lógica existente (competência, valores por canal pré-preenchidos do histórico, onSalvar com toasts e router.refresh).

    1. Importar Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter de '@/components/ui/dialog' e o ícone Plus de 'lucide-react'.

    2. Adicionar `const [aberto, setAberto] = useState(false)`.

    3. Substituir o Card do formulário (linhas ~104-156) por:
       - Um botão-gatilho no topo da aba: `<Button type="button" onClick={() => setAberto(true)}><Plus className="mr-2 size-4" /> Lançar investimento</Button>` (texto pt-BR com acento).
       - Um `<Dialog open={aberto} onOpenChange={(open) => (open ? setAberto(true) : (isPending ? undefined : setAberto(false)))}>` com `<DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-2xl">`, `<DialogHeader><DialogTitle className="flex items-center gap-2"><Megaphone className="size-4" /> Investimento em aquisição</DialogTitle></DialogHeader>`, o parágrafo explicativo, o campo Competência (type="month") e a grade de inputs por canal (CANAIS_AQUISICAO.map, exatamente como hoje) dentro do DialogContent.
       - Um `<DialogFooter className="gap-2">` com botão "Cancelar" (variant="outline", onClick fecha se !isPending) e o botão "Salvar lançamento" (onClick={onSalvar}, disabled={isPending}, texto "Salvando..." durante o pending). Após sucesso em onSalvar, fechar o Dialog (setAberto(false)) além do router.refresh() já existente.

    4. MANTER o segundo Card "Histórico de lançamentos" (linhas ~158-194) FORA do Dialog, visível na aba como hoje. A estrutura vira: `<div className="space-y-6">` → botão-gatilho + `<Dialog>...</Dialog>` → Card do histórico.

    5. Preservar competenciaAtual, valoresDaCompetencia, o efeito de recarga por troca de competência, valorDoCanal e onSalvar sem mudança de comportamento. Todo texto visível em pt-BR com acentos.
  </action>
  <verify>
    <automated>cd D:/projetos/projeto_agencia_jsr && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "aquisicao-form" || echo "sem erros de tipo em aquisicao-form"</automated>
  </verify>
  <done>A aba Aquisição mostra um botão "Lançar investimento" que abre um Dialog centralizado com competência + inputs por canal e footer Cancelar/Salvar; o histórico de lançamentos continua visível fora do Dialog; onSalvar fecha o Dialog no sucesso; lógica de valores/competência intacta; compila sem erros.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/financeiro/cac.test.ts` — todos verdes (19 antigos + novos do mapa e da reserva).
- `npx tsc --noEmit` — sem erros novos em cac.ts, financeiro.ts, aquisicao-form.tsx.
- Revisão manual: getCacAquisicao mantém as queries do CRM SEQUENCIAIS (fora do Promise.all) e em try/catch de degradação; nenhuma migration nova foi criada.
- Revisão manual: a aba Aquisição abre o form em Dialog centralizado e mantém o histórico visível fora dele.
</verification>

<success_criteria>
- Frente 1: um cliente com oportunidade CRM origem meta_lead_ad conta em meta_ads; origem CRM não-paga (ex.: landing_page) cai na reserva de texto livre; cliente sem vínculo CRM permanece só no texto livre; Google chega só pela reserva. Tudo coberto por testes puros.
- Frente 1: função pura sem import de db/crm; leitura no banco só na action, sequencial (regra do pool).
- Frente 2: formulário da aba Aquisição em Dialog centralizado (padrão transacao-form), histórico fora do Dialog, textos pt-BR com acentos.
- Nenhuma migration nova; getCacAquisicao continua degradando graciosamente.
</success_criteria>

<output>
After completion, create `.planning/quick/260720-re3-cac-via-origem-do-crm-form-dialog-na-aba/260720-re3-SUMMARY.md`
</output>
