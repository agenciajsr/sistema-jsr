---
quick_id: 260721-xa1
phase: quick
plan: 260721-xa1
type: execute
wave: 1
depends_on: []
title: Google Ads Parte 2c — mostrar Google nas telas + vincular conta a cliente
autonomous: true
requirements: [GOOGLE-ADS-2C]
files_modified:
  - src/lib/trafego/aggregate.ts
  - src/lib/trafego/painel.ts
  - src/lib/trafego/acoes-dia.ts
  - src/actions/trafego.ts
  - src/lib/relatorios/gerar-relatorio.ts
  - src/actions/relatorios.ts
  - src/actions/relatorio-configs.ts
  - src/lib/saude/avaliar-campanhas.ts
  - src/lib/alertas/regras-campanha.ts

must_haves:
  truths:
    - "A conta Google (ad_accounts plataforma='google') pode ser vinculada a um cliente pela UI existente"
    - "Um cliente que tenha uma conta Google vinculada aparece na lista de /campanhas e nos cards do dashboard"
    - "Nenhum número do Meta muda (a única conta Google linkada está vazia — 0 campanhas)"
    - "Relatórios (client-facing) NÃO passam a exibir métricas Google não validadas"
  artifacts:
    - path: "src/lib/trafego/aggregate.ts"
      provides: "Queries de campanhas/dashboard unificadas (meta+google)"
      contains: "listarClientesComContas"
    - path: "src/actions/trafego.ts"
      provides: "getContasNaoVinculadas sem filtro de plataforma (conta Google linkável)"
      contains: "getContasNaoVinculadas"
  key_links:
    - from: "src/components/ficha/vincular-conta-ficha.tsx"
      to: "getContasNaoVinculadas / vincularContaAoCliente"
      via: "conta Google agora listada no dropdown de vínculo"
      pattern: "vincularContaAoCliente"
---

<objective>
Fazer o Google Ads aparecer nas telas operacionais (Campanhas, Dashboard, /trafego) e permitir vincular a conta Google já sincronizada (`ad_accounts` plataforma='google', `cliente_id = null`) a um cliente, REUSANDO a UI/action de vínculo que já existe.

Purpose: A Parte 2b já grava contas e insights do Google nas MESMAS tabelas do Meta (mesmo formato: `spend`, `actions=[{action_type:'lead'}]`, `actionValues=[{purchase}]`). Falta apenas remover os filtros `plataforma='meta'` das queries de EXIBIÇÃO para o Google encaixar. É uma mudança ESTRUTURAL — não valida números (isso é o PASSO B).

Output: 9 queries alteradas (2 tarefas). Trabalho é cirúrgico: cada filtro de plataforma foi decidido explicitamente como UNIFICAR ou MANTER (ver tabela na Task 1/Task 2). Nada de adivinhação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260721-wyt-google-ads-parte-2b-sync-engine-contas-c/260721-wyt-SUMMARY.md
@src/lib/trafego/aggregate.ts
@src/actions/trafego.ts

<decisao_de_design>
Mapeei TODOS os 14 sites que filtram `eq(adAccounts.plataforma, 'meta')`. Decisão por site:

UNIFICAR (remover o filtro de plataforma) — caminho de EXIBIÇÃO operacional interna (Campanhas/Dashboard/Trafego + vínculo). Schema e formato são compartilhados; a agência quer visão consolidada por cliente. A única conta Google linkada está VAZIA → nenhum número Meta muda hoje:
  1. src/lib/trafego/aggregate.ts ~L110  (getInvestido30dPorCliente — investido nos cards de /campanhas e dashboard)
  2. src/lib/trafego/aggregate.ts ~L135  (listarClientesComContas — lista de clientes de /campanhas/dashboard)
  3. src/lib/trafego/aggregate.ts ~L250  (getResumoCliente — resumo por cliente)
  4. src/lib/trafego/aggregate.ts ~L532  (métricas de intervalo/comparação por cliente)
  5. src/lib/trafego/painel.ts   ~L247  (getResumoLandingPorCliente)
  6. src/lib/trafego/painel.ts   ~L357  (getPainelCampanhas — painel detalhado de /campanhas)
  7. src/lib/trafego/acoes-dia.ts ~L266 (ações do dia por cliente)
  8. src/actions/trafego.ts       ~L59  (getTrafegoData — tela /trafego)
  9. src/actions/trafego.ts       ~L171 (getContasNaoVinculadas — SEM isto a conta Google não aparece no dropdown de vínculo)

MANTER 'meta' (adiar para o PASSO B — validação de métricas) — apenas DOCUMENTAR com comentário, SEM mudar lógica:
  A. src/lib/relatorios/gerar-relatorio.ts ~L263 — relatório semanal é CLIENT-FACING (copy-paste p/ cliente). Regra CLAUDE.md: NUNCA número financeiro errado p/ cliente. Mapeamento Google ainda NÃO validado → não pode vazar p/ relatório.
  B. src/actions/relatorios.ts ~L65 — caminho de geração de relatório (mesmo motivo de A).
  C. src/actions/relatorio-configs.ts ~L333 — config de relatório (mesmo motivo).
  D. src/lib/saude/avaliar-campanhas.ts ~L343 — `buscarAdsStatus` lê status de ANÚNCIO (effective_status DISAPPROVED/WITH_ISSUES) de ad_insights, que é populado SÓ pelo sync do Meta (Google não grava ad_insights). Unificar aqui não traz dado Google, só ruído.
  E. src/lib/alertas/regras-campanha.ts ~L275 — alertas proativos avaliam métricas contra metas; disparar em métricas Google não validadas geraria alertas errados. Adiar p/ PASSO B.

NÃO MEXER (fora de escopo — são o sync do Meta, não telas): src/lib/meta/sync.ts, src/lib/inngest/functions/sync-meta-ads.ts. E src/lib/google/ads-sync.ts L157 é o sync do Google (filtra 'google' de propósito).

NÃO PRECISA MUDAR (já plataforma-agnóstico): src/actions/trafego.ts `vincularContaAoCliente` (L226 — filtra só por `adAccounts.id`) e `getContasDoCliente` (L190 — sem filtro de plataforma, já retorna 'meta'|'google'). Confirme que continuam agnósticos.
</decisao_de_design>

<interfaces>
Formato que o Google já grava (Parte 2b) — as telas assumem formato Meta e devem encaixar:
- campaign_insights.spend = cost_micros/1e6 (mesma unidade do Meta)
- campaign_insights.actions = [{action_type:'lead'}] (conversions do Google → contam como "lead" na máquina de resultados existente)
- campaign_insights.actionValues = [{action_type:'purchase', value}] (conversions_value → ROAS)
- ctr já em % (×100), average_cpc/cpm em /1e6 — mesmas unidades do Meta
Ou seja: parseActions e os cálculos de spend/cpc/cpm/ctr NÃO precisam de código novo — só precisam NÃO quebrar com linhas Google.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unificar as 9 queries de exibição (remover filtro plataforma='meta')</name>
  <files>src/lib/trafego/aggregate.ts, src/lib/trafego/painel.ts, src/lib/trafego/acoes-dia.ts, src/actions/trafego.ts</files>
  <action>
Em cada um dos 9 sites da lista UNIFICAR (ver `<decisao_de_design>`), remova APENAS o predicado `eq(adAccounts.plataforma, 'meta')` de dentro do `and(...)`, mantendo os outros predicados (`eq(adAccounts.ativo, true)`, `eq(adAccounts.clienteId, ...)`, `gte(campaignInsights.date, ...)` etc.) intactos.

Guie-se pela FUNÇÃO (os números de linha podem deslocar):
- aggregate.ts → getInvestido30dPorCliente, listarClientesComContas, getResumoCliente, e a função de métricas de intervalo/comparação (~L532). São 4 ocorrências.
- painel.ts → getResumoLandingPorCliente (~L247) e getPainelCampanhas (~L357). 2 ocorrências.
- acoes-dia.ts → a query de contas do cliente (~L266). 1 ocorrência.
- actions/trafego.ts → getTrafegoData (~L59) e getContasNaoVinculadas (~L171). 2 ocorrências.

Regras:
- Quando o `and(...)` ficar com um único predicado após a remoção, mantenha o `and(...)` OU simplifique para o predicado direto — o que o lint aceitar; NÃO altere a semântica dos demais predicados.
- Em `listarClientesComContas`, atualize o comentário/JSDoc que diz "ao menos uma conta Meta ativa" para "ao menos uma conta ativa (Meta ou Google)".
- Em `getContasNaoVinculadas`, atualize o JSDoc "Contas Meta ativas ainda sem cliente" para "Contas ativas (qualquer plataforma) ainda sem cliente" — este é o que faz a conta Google aparecer no dropdown de vínculo (VincularContaFicha / contas-nao-vinculadas.tsx).
- NÃO toque em `vincularContaAoCliente` nem em `getContasDoCliente` (já agnósticos) — apenas confirme visualmente que continuam sem filtro de plataforma.
- Se algum `import` de `eq`/`and` ficar sem uso após a remoção, o lint apontará — só remova import se realmente ficar órfão (improvável, todos são reusados).

CUIDADO (não quebrar Meta): a única conta Google linkada está vazia (0 campanhas), então nenhuma linha Google entra nas agregações hoje — os números Meta são idênticos. A mudança é estrutural (encaixa quando houver dado Google).
  </action>
  <verify>
    <automated>cd D:/projetos/projeto_agencia_jsr && npx tsc --noEmit && npx eslint src/lib/trafego src/actions/trafego.ts --max-warnings=0 && npx vitest run</automated>
  </verify>
  <done>tsc sem erros, eslint 0, suíte de testes verde (regressão: nenhum número Meta mudou). Nenhum `eq(adAccounts.plataforma, 'meta')` resta em aggregate.ts, painel.ts, acoes-dia.ts ou nas funções getTrafegoData/getContasNaoVinculadas de actions/trafego.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar as 5 queries que MANTÊM 'meta' + confirmar parse Google</name>
  <files>src/lib/relatorios/gerar-relatorio.ts, src/actions/relatorios.ts, src/actions/relatorio-configs.ts, src/lib/saude/avaliar-campanhas.ts, src/lib/alertas/regras-campanha.ts</files>
  <action>
Nos 5 sites da lista MANTER (A–E em `<decisao_de_design>`), NÃO altere a lógica. Adicione, na linha imediatamente acima do `eq(adAccounts.plataforma, 'meta')` (ou acima do `.where(and(...))`), um comentário curto explicando a decisão, para um futuro dev não achar que foi esquecido:

- relatorios/gerar-relatorio.ts, actions/relatorios.ts, actions/relatorio-configs.ts:
  `// Google Ads mantido fora: relatório é client-facing e o mapeamento de métricas Google ainda não foi validado (PASSO B). Ver quick 260721-xa1.`
- saude/avaliar-campanhas.ts:
  `// Só Meta: ad_insights (status/reprovação de anúncio) é populado apenas pelo sync do Meta; Google não grava ad_insights. Ver quick 260721-xa1.`
- alertas/regras-campanha.ts:
  `// Google Ads mantido fora: alertas avaliam métricas contra metas; métricas Google ainda não validadas (PASSO B). Ver quick 260721-xa1.`

Depois, CONFIRME (leitura, sem alterar) que o parse das métricas encaixa com linhas Google — abra o(s) helper(s) de parse de `actions`/`actionValues` (ex.: `parseActions`) usados por aggregate.ts/painel.ts e verifique que:
  1. lidam com `actions = [{action_type:'lead'}]` e `actionValues = [{action_type:'purchase', value}]` (o formato que o Google grava) sem lançar exceção;
  2. cálculos de spend/cpc/cpm/ctr não assumem nada exclusivo do Meta (unidades já normalizadas no sync do Google).
Se encontrar QUALQUER ponto onde uma linha Google quebraria o parse ou produziria número inválido, PARE e reporte no SUMMARY (não invente correção fora de escopo) — mas o esperado é que encaixe sem mudança de código.
  </action>
  <verify>
    <automated>cd D:/projetos/projeto_agencia_jsr && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Os 5 sites têm comentário explicando a decisão de MANTER (lógica intacta, ainda filtram 'meta'). Parse de actions/actionValues confirmado compatível com o formato Google (ou incompatibilidade reportada no SUMMARY). tsc e testes verdes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → sem erros
- `npx eslint` → 0 warnings nas pastas tocadas
- `npx vitest run` → suíte verde (mesma contagem da 2b: ~649/0), provando que nenhum número Meta regrediu
- Grep de sanidade: `eq(adAccounts.plataforma, 'meta')` NÃO deve mais existir em aggregate.ts, painel.ts, acoes-dia.ts, nem em getTrafegoData/getContasNaoVinculadas; DEVE continuar existindo (com comentário) nos 5 sites de relatórios/saúde/alertas.
</verification>

<success_criteria>
- As 9 queries de exibição unificadas (meta+google); Google encaixa quando houver dado.
- Conta Google aparece no dropdown de vínculo (getContasNaoVinculadas) e pode ser vinculada a um cliente via a action existente (vincularContaAoCliente, já agnóstica).
- As 5 queries client-facing/Meta-específicas permanecem filtradas em 'meta', com comentário justificando (adiado ao PASSO B).
- Zero mudança em números do Meta (conta Google linkada está vazia).
</success_criteria>

<output>
Após completar, criar `.planning/quick/260721-xa1-google-ads-parte-2c-mostrar-google-ads-n/260721-xa1-SUMMARY.md` registrando: os 9 sites unificados, os 5 mantidos+comentados, e o resultado da conferência do parse Google (encaixou / ou incompatibilidade encontrada). Atualizar STATE.md (Google Ads Parte 2c concluída).
</output>
