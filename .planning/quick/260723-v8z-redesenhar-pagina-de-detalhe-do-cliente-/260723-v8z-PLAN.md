---
phase: quick-260723-v8z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/clientes/[id]/page.tsx
  - src/components/ficha/visao-geral-cliente.tsx
autonomous: true
requirements: [QUICK-V8Z]
must_haves:
  truths:
    - "Ficha do cliente abre com header em card: avatar grande com iniciais, nome + badge de status, nicho, linha de contatos com ícones, e colunas 'Cliente desde' / 'Responsável' / 'Status' separadas por divisores verticais"
    - "Aba 'Visão geral' é a aba padrão e mostra grid de cards: Dados de cadastro, Acessos e contas, Observações, Atividades recentes, Próximas atividades"
    - "Todas as 9 abas existentes continuam presentes e funcionando (Contrato & Cobrança, Faturas, Contas de anúncio, Checklist, Tarefas, Onboarding, Retenção, Acompanhamento, Documentos)"
    - "Excluir cliente segue admin-only; faixa de alertas e KPIs continuam visíveis"
    - "Nenhum dado mock — todos os campos vêm do cliente/queries já carregados"
  artifacts:
    - path: "src/app/(app)/clientes/[id]/page.tsx"
      provides: "Header rico em card + Tabs com defaultValue='visao-geral'"
    - path: "src/components/ficha/visao-geral-cliente.tsx"
      provides: "Server component da aba Visão geral (grid de cards + histórico)"
      min_lines: 80
  key_links:
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "src/components/ficha/visao-geral-cliente.tsx"
      via: "import + render dentro de <TabsContent value='visao-geral'>"
      pattern: "VisaoGeralCliente"
---

<objective>
Redesenhar a página de detalhe do cliente (`src/app/(app)/clientes/[id]/page.tsx`) seguindo o mockup `prints_aleatorios/modelo_cliente_novo.png`: header rico em card + nova aba padrão "Visão geral" com grid de cards, preservando 100% das abas e funcionalidades existentes.

Purpose: A ficha atual tem cabeçalho pobre (texto solto) e cai direto na aba de contrato; o mockup traz uma visão de resumo do cliente num relance.
Output: Header em card no estilo do mockup + componente `VisaoGeralCliente` como aba default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@prints_aleatorios/modelo_cliente_novo.png  (mockup — LER a imagem antes de codar)
@src/app/(app)/clientes/[id]/page.tsx
@src/lib/db/schema.ts (tabela clientes, linhas 21-84)

<interfaces>
Campos REAIS de `clientes` disponíveis para os cards (schema.ts):
`nome, nicho, status, contatoNome, contatoTelefone, contatoEmail, notas, modoCobranca, tipoPessoa, documento, razaoSocial, endereco, cidade, estado, instagram, siteUrl, formaPagamento, diaPagamento, servicosContratados (jsonb), verbaMensal, ticketMedio, origemCliente, objetivoPrincipal, linkDrive, createdAt`

Dados já carregados na page (reusar — NÃO criar queries novas):
- `contratoAtual, historico` (getContratosDoCliente) — valorMensal, dataInicio, dataVencimento
- `contasDoCliente` (getContasDoCliente) — `{ id, nome, plataforma: 'meta'|'google', metaAccountId, accountStatus }`
- `acompanhamentos` — `{ id, autorNome, nota, createdAt }`
- `tarefasFicha` (getTarefasDoClienteFicha) — `{ abertas, historico }`; abertas têm título/data
- `docsCliente, alertas, resumo, cobrancas, faturas`

Mockup (modelo_cliente_novo.png):
- Card header: avatar grande quadrado com iniciais, nome + badge "Ativo", subtítulo (nicho), linha com ícones Phone/Mail/Instagram/MapPin; à direita colunas com divisores verticais: "Cliente desde" (ícone calendário + data), "Responsável", "Status" (badge). Botões no topo direito fora do card: "Editar cliente" etc.
- Aba "Visão geral" (primeira, ativa): grid 3 colunas — col 1: "Dados de cadastro" (lista label/valor) + "Observações"; col 2: "Acessos e contas" (linhas com ícone, nome, id, link "Acessar") + "Atividades recentes" (timeline); col 3: "Pastas e documentos" + "Próximas atividades". Rodapé: card horizontal "Histórico do cliente" (marcos com datas).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header rico em card na ficha do cliente</name>
  <files>src/app/(app)/clientes/[id]/page.tsx</files>
  <action>
    Substituir o cabeçalho atual (h1 + badge + parágrafos soltos, linhas ~273-333) por um Card no estilo do mockup:
    - Linha superior FORA do card (ou no topo): BotaoVoltar mantido + à direita botões "Editar cliente" (Link para /clientes/[id]/editar), "Pasta do Drive" (se linkDrive) e "Excluir cliente" (AlertDialog atual, admin-only por D-03 — manter a Server Action inline `excluirClienteAction` e a copy exata intactas).
    - Dentro do Card: Avatar grande (size ~16, rounded, AvatarFallback com 2 iniciais do nome — helper simples: primeiras letras das 2 primeiras palavras), nome em text-2xl font-semibold + Badge de status (usar STATUS_LABEL/STATUS_COLOR existentes, D-10), abaixo o nicho (NICHO_LABEL), e linha de contatos com ícones lucide (Phone, Mail, Instagram, MapPin) mostrando SOMENTE campos preenchidos: contatoTelefone, contatoEmail, instagram, cidade/estado.
    - À direita (hidden em mobile, `hidden lg:flex`), colunas separadas por `border-l` verticais: "Cliente desde" = format(cliente.createdAt, 'dd/MM/yyyy') com ícone CalendarDays; "Responsável" = cliente.contatoNome ?? '—'; "Status" = badge de status. NÃO inventar coluna "Plano" (não existe no schema).
    - `cliente.notas` SAI do header (vai para o card Observações na Task 2).
    - Faixa de alertas e grid de StatCards permanecem logo abaixo do header, inalterados.
    Não alterar nenhuma query nem a ordem sequencial fora do Promise.all.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Header renderiza como card com avatar/iniciais, contatos com ícones e colunas Cliente desde/Responsável/Status; Excluir cliente segue admin-only com a mesma copy; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Aba "Visão geral" como default com grid de cards</name>
  <files>src/components/ficha/visao-geral-cliente.tsx, src/app/(app)/clientes/[id]/page.tsx</files>
  <action>
    Criar `src/components/ficha/visao-geral-cliente.tsx` — componente de SERVIDOR (sem 'use client', sem queries próprias; recebe tudo por props) que renderiza grid `grid gap-4 lg:grid-cols-3`:
    1. **Dados de cadastro** (Card): linhas label/valor (label muted à esquerda, valor à direita) apenas com campos reais e preenchidos: Segmento (NICHO_LABEL), Objetivo principal, Valor mensal do contrato (contratoAtual?.valorMensal formatado em BRL), Verba mensal, Ticket médio, Forma de pagamento/dia, Modo de cobrança ('automatico_asaas' → 'Automático (Asaas)', senão 'Manual/Pix'), Como nos conheceu (origemCliente), Data de cadastro (createdAt). Campos nulos → omitir a linha.
    2. **Acessos e contas** (Card): uma linha por conta de `contasDoCliente` com ícone (lucide `Facebook` para meta / `Chrome` ou `BarChart3` para google — escolher ícones já usados no repo se houver), nome + id (metaAccountId) e badge de plataforma; se `cliente.linkDrive`, linha "Pasta do Drive" com link `Acessar` (a target=_blank, ExternalLink icon). Vazio → texto muted "Nenhuma conta vinculada".
    3. **Observações** (Card): `cliente.notas` em parágrafo (whitespace-pre-wrap) ou "Sem observações".
    4. **Atividades recentes** (Card): últimos 5 `acompanhamentos` (autorNome, nota truncada, data dd/MM HH:mm) — mesmo shape usado na aba Acompanhamento. Vazio → texto muted.
    5. **Próximas atividades** (Card): até 5 de `tarefasFicha.abertas` (título + data). Vazio → texto muted.
    6. Rodapé fora do grid (col-span full): Card horizontal **Histórico do cliente** com marcos reais: "Cliente criado" (createdAt) e um marco por contrato do histórico ("Contrato iniciado" + dataInicio) — só renderizar o card se houver ao menos 1 marco.
    Tipar props com os tipos inferidos já existentes na page (passar arrays como estão; definir types locais leves se necessário). NADA de mock.

    Na page: adicionar `<TabsTrigger value="visao-geral">🏠 Visão geral</TabsTrigger>` como PRIMEIRA aba, mudar `defaultValue` do Tabs de "contrato" para "visao-geral", e adicionar `<TabsContent value="visao-geral">` renderizando `<VisaoGeralCliente ... />` com os dados já carregados (cliente, contratoAtual, registrosAnteriores, contasDoCliente, acompanhamentos, tarefasFicha.abertas). As 9 abas existentes permanecem intactas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>Aba "Visão geral" é a default, mostra os 5 cards + histórico com dados reais; todas as abas anteriores funcionam; tsc limpo e suíte vitest verde (sem regressão).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sem erros.
- Suíte vitest inteira verde (630+ testes, regressão zero).
- Grep: `defaultValue="visao-geral"` presente; todos os 10 TabsTrigger presentes; nenhum literal mock (nomes/valores inventados) em visao-geral-cliente.tsx.
</verification>

<success_criteria>
- Header da ficha segue o mockup (card com avatar, contatos com ícones, colunas com divisores).
- "Visão geral" é a aba padrão com grid de cards usando só dados reais já carregados.
- Nenhuma funcionalidade existente removida; nenhuma query nova pesada; D-03 e D-10 respeitados.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-v8z-redesenhar-pagina-de-detalhe-do-cliente-/260723-v8z-SUMMARY.md`
</output>
