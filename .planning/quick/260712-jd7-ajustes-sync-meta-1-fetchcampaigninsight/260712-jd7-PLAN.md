---
phase: quick-260712-jd7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/date-br.ts
  - src/lib/date-br.test.ts
  - src/lib/meta/client.ts
  - src/lib/trafego/aggregate.ts
  - src/components/trafego/sync-button.tsx
autonomous: true
requirements: [SYNC-META-JD7]
must_haves:
  truths:
    - "O sync da Meta busca dados ATÉ o dia de hoje (fuso de Brasília), não parando 1-2 dias atrás"
    - "Os filtros de período 'Hoje' e 'Ontem' em /campanhas passam a mostrar dados (as strings de data batem com o fuso da conta)"
    - "O botão 'Sincronizar agora' exige um cliente selecionado — sem cliente mostra aviso e não dispara sync"
    - "O sync automático agendado (cron 6h/Inngest) continua puxando TODAS as contas, inalterado"
  artifacts:
    - path: "src/lib/date-br.ts"
      provides: "hojeBrasilia() e dataMenosDias() — datas 'YYYY-MM-DD' no fuso America/Sao_Paulo"
      exports: ["hojeBrasilia", "dataMenosDias"]
    - path: "src/lib/meta/client.ts"
      provides: "fetchCampaignInsights e fetchAdInsights com time_range até hoje-BR (sem date_preset)"
      contains: "time_range"
    - path: "src/lib/trafego/aggregate.ts"
      provides: "getResumoCliente calculando hoje/ontem/7d/30d no fuso de Brasília"
      contains: "hojeBrasilia"
    - path: "src/components/trafego/sync-button.tsx"
      provides: "Guard: sync manual só com cliente selecionado"
  key_links:
    - from: "src/lib/meta/client.ts"
      to: "src/lib/date-br.ts"
      via: "import hojeBrasilia, dataMenosDias"
      pattern: "from '@/lib/date-br'"
    - from: "src/lib/trafego/aggregate.ts"
      to: "src/lib/date-br.ts"
      via: "import hojeBrasilia, dataMenosDias"
      pattern: "from '@/lib/date-br'"
---

<objective>
Corrigir dois problemas do sync da Meta, sem migration e sem novas dependências:

1. "Hoje/Ontem" sempre vazios. Causa dupla: (a) o fetch usa `date_preset='last_30d'`, que NÃO inclui o dia de hoje; (b) o filtro de período em `getResumoCliente` calcula "hoje" no fuso do SERVIDOR (UTC na Vercel), que pode não bater com a data gravada (fuso da conta = Brasília). Solução: janela `time_range` até hoje-BR no fetch + base "hoje" em fuso de Brasília no agregador, ambos usando um helper de data compartilhado.

2. O botão "Sincronizar agora" puxa TODAS as contas quando nenhum cliente está selecionado, abusando do limite da API da Meta. Solução: exigir cliente selecionado no botão manual (o cron agendado continua igual).

Purpose: dados de tráfego atualizados até hoje e proteção contra sync acidental de todas as contas.
Output: helper `date-br.ts` (+ teste), `client.ts` com `time_range`, `aggregate.ts` em fuso BR, `sync-button.tsx` com guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Arquivos-alvo (já lidos pelo planner):
@src/lib/meta/client.ts
@src/lib/trafego/aggregate.ts
@src/components/trafego/sync-button.tsx
@src/actions/trafego.ts
@src/app/(app)/campanhas/page.tsx

<interfaces>
<!-- Contratos que o executor deve respeitar. Não precisa explorar o codebase. -->

Callers de fetchCampaignInsights / fetchAdInsights (chamam SEM segundo argumento):
- src/app/api/sync-meta/route.ts  → `fetchCampaignInsights(account.metaAccountId)`, `fetchAdInsights(account.metaAccountId)`
- src/lib/inngest/functions/sync-meta-ads.ts → idem
Portanto a mudança de assinatura deve manter a chamada com 1 argumento funcionando.

metaFetch(path, params: Record<string,string>) → serializa cada valor com URLSearchParams.set(k, v).
A Meta aceita `time_range` como JSON string: time_range={"since":"2026-06-12","until":"2026-07-12"}.

campaign_insights.date e ad_insights.dateStart são strings 'YYYY-MM-DD' gravadas de insight.date_start (fuso da conta = Brasília). Por isso a base de comparação em getResumoCliente precisa ser em fuso de Brasília.

getResumoCliente hoje usa `date-fns` (format, subDays) SÓ dentro de getResumoCliente. getMetricasIntervalo NÃO usa date-fns. Ao migrar, remover o import de date-fns se ficar sem uso (evitar lint de import não usado).

sync-button.tsx: `clienteId = searchParams.get('cliente') ?? undefined`. Já importa `toast` de 'sonner'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper de data em fuso de Brasília (date-br.ts) + aplicar time_range no client.ts</name>
  <files>src/lib/date-br.ts, src/lib/date-br.test.ts, src/lib/meta/client.ts</files>
  <behavior>
    - hojeBrasilia() retorna string no formato 'YYYY-MM-DD' (10 chars, regex ^\d{4}-\d{2}-\d{2}$)
    - dataMenosDias(0) === hojeBrasilia()
    - dataMenosDias(30, '2026-07-12') === '2026-06-12' (aritmética de dias correta, sem drift de DST)
    - dataMenosDias(1, '2026-03-01') === '2026-02-28' (atravessa mês)
  </behavior>
  <action>
    Criar `src/lib/date-br.ts` com duas funções puras (sem dependências novas):

    ```ts
    /** Data de HOJE no fuso de Brasília, formato 'YYYY-MM-DD'. en-CA => ISO. */
    export function hojeBrasilia(): string {
      return new Date().toLocaleDateString('en-CA', { timeZone: 'America/Sao_Paulo' })
    }

    /** Subtrai n dias de uma data-base 'YYYY-MM-DD' (default: hoje-BR), retorna 'YYYY-MM-DD'.
     *  Usa meio-dia UTC como âncora para não sofrer com bordas de fuso/DST. */
    export function dataMenosDias(n: number, base: string = hojeBrasilia()): string {
      const d = new Date(`${base}T12:00:00Z`)
      d.setUTCDate(d.getUTCDate() - n)
      return d.toISOString().slice(0, 10)
    }
    ```

    Criar `src/lib/date-br.test.ts` (vitest) cobrindo os casos do bloco <behavior>.

    Em `src/lib/meta/client.ts`:
    - Importar `{ hojeBrasilia, dataMenosDias } from '@/lib/date-br'`.
    - `fetchCampaignInsights`: REMOVER o parâmetro `datePreset` e a linha `date_preset`. Calcular `const until = hojeBrasilia()` e `const since = dataMenosDias(30, until)`. Passar `time_range: JSON.stringify({ since, until })`. MANTER `time_increment: '1'` (linhas diárias, agora incluindo hoje e ontem). Assinatura fica `fetchCampaignInsights(adAccountId: string)`.
    - `fetchAdInsights`: idem — REMOVER `datePreset`/`date_preset`, usar `time_range` since=hoje-30d until=hoje. NÃO adicionar time_increment (continua uma linha agregada por anúncio). Assinatura `fetchAdInsights(adAccountId: string)`.
    - Adicionar comentário curto acima de cada função: janela até hoje-BR; o dado de "hoje" na Meta é PARCIAL e muda ao longo do dia (esperado).
    - Atualizar os comentários/JSDoc que dizem "date_preset" para refletir `time_range`.
    - NÃO alterar metaFetch (ele já faz URLSearchParams.set com o valor já-stringificado).
  </action>
  <verify>
    <automated>npx vitest run src/lib/date-br.test.ts</automated>
  </verify>
  <done>date-br.ts exporta hojeBrasilia/dataMenosDias; teste passa; client.ts usa time_range (sem date_preset) em ambas as funções, callers de 1 argumento continuam válidos (tsc limpo).</done>
</task>

<task type="auto">
  <name>Task 2: getResumoCliente calcula período em fuso de Brasília</name>
  <files>src/lib/trafego/aggregate.ts</files>
  <action>
    Em `src/lib/trafego/aggregate.ts`, dentro de `getResumoCliente`:
    - Importar `{ hojeBrasilia, dataMenosDias } from '@/lib/date-br'`.
    - Substituir `const hoje = new Date()` e `const hojeStr = format(hoje, 'yyyy-MM-dd')` por `const hojeStr = hojeBrasilia()`.
    - No switch de período, trocar os cálculos por strings-BR, mantendo a semântica de dataMinima/dataMaxima:
      - 'hoje': `dataMinima = hojeStr`
      - 'ontem': `const ontemStr = dataMenosDias(1, hojeStr); dataMinima = ontemStr; dataMaxima = ontemStr`
      - '7d': `dataMinima = dataMenosDias(7, hojeStr)`
      - '30d' (default): `dataMinima = dataMenosDias(30, hojeStr)`
    - Remover o import `import { format, subDays } from 'date-fns'` se ficar sem uso no arquivo (confirmar que nenhuma outra função do arquivo usa format/subDays — getMetricasIntervalo não usa). Se ficar sem uso, apagar a linha para evitar lint de import não utilizado.
    - NÃO alterar `getMetricasIntervalo` (recebe intervalo explícito) nem a query/condições (só a base "hoje" muda de fuso).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>getResumoCliente usa hojeBrasilia/dataMenosDias para hoje/ontem/7d/30d; import date-fns removido se sem uso; tsc limpo; sem novos erros de lint.</done>
</task>

<task type="auto">
  <name>Task 3: Botão manual exige cliente selecionado</name>
  <files>src/components/trafego/sync-button.tsx</files>
  <action>
    Em `src/components/trafego/sync-button.tsx`, no início de `handleSync`, antes de `setSyncing(true)`:
    - Se `!clienteId` (nenhum `?cliente` na URL), disparar um toast de aviso em português e retornar SEM sincronizar:
      `toast.warning('Selecione um cliente antes de sincronizar — assim você puxa só as contas dele e não abusa do limite da Meta.')`
      (se `toast.warning` não existir na versão do sonner instalada, usar `toast.error` com o mesmo texto.)
      `return`
    - Quando houver `clienteId`, manter o comportamento atual intacto (triggerMetaSync(clienteId), toast.info, polling de status, router.refresh).
    - NÃO alterar o sync automático (cron/Inngest) — este ajuste é só no botão manual.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sem cliente selecionado, o clique mostra aviso em PT e não chama triggerMetaSync; com cliente, comportamento atual preservado (polling, refresh).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo.
- `npm test` (vitest) passa, incluindo o novo date-br.test.ts e os testes existentes de aggregate.
- `npm run build` sem erros novos.
- `npm run lint` sem erros NOVOS (conhecidos toleráveis: ui/sidebar.tsx, hooks/use-mobile.ts, o `any` de sync-meta-ads.ts, itens já em deferred-items.md).
- Nenhuma migration, nenhuma dependência nova.
</verification>

<success_criteria>
- fetchCampaignInsights/fetchAdInsights usam `time_range` com `until = hoje-BR` (sem `date_preset`); campaign mantém `time_increment: '1'`.
- getResumoCliente calcula hoje/ontem/7d/30d no fuso America/Sao_Paulo via helper compartilhado.
- Botão "Sincronizar agora" exige cliente selecionado; cron/Inngest inalterado.
- Toda a UI/textos em português; demais comportamentos (per-client sync, polling) preservados.
</success_criteria>

<output>
Após completar, criar `.planning/quick/260712-jd7-ajustes-sync-meta-1-fetchcampaigninsight/260712-jd7-SUMMARY.md`.
</output>
