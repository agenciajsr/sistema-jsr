---
phase: quick-260719-s3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0037_crm_followup.sql
  - scripts/aplicar-migration-0037.ts
  - src/lib/db/schema.ts
  - src/lib/crm/followup.ts
  - src/lib/crm/followup.test.ts
  - src/lib/crm/temperatura.ts
  - src/lib/crm/temperatura.test.ts
  - src/lib/crm/dados.ts
  - src/actions/crm.ts
  - src/components/crm/card-oportunidade.tsx
  - src/components/crm/crm-view.tsx
  - src/components/crm/kanban-followup.tsx
autonomous: true
requirements: [FUP-01, FUP-02, FUP-03, FUP-04, FUP-05, FUP-06]
must_haves:
  truths:
    - "Existe (após aplicar a migration) a etapa 'Follow-up' no pipeline Vendas, logo após 'Contato Feito', sem duplicar em re-execução"
    - "Mover um card para a etapa Follow-up no kanban de vendas o coloca em D1; ele aparece na aba Follow-up (colunas D1..D6 + Perdido)"
    - "Arrastar D(n)→D(n+1) carimba ultimo_followup_em e incrementa o nível — card único, sem duplicação"
    - "Card em 'Contato Feito' há 24h sem follow-up mostra selo '⏰ Follow-up pendente'; dentro do fluxo os prazos são crescentes (48h/72h/5d/7d/14d); D6 vencido mostra 'Follow-ups esgotados' (mover pra Perdido é manual)"
    - "Cards mostram chip 🔥/🧊 conforme a origem (meta_lead_ad/landing_page = quente; prospeccao_fria/whatsapp = frio), inclusive leads antigos"
  artifacts:
    - path: "drizzle/0037_crm_followup.sql"
      provides: "Colunas followup_nivel/ultimo_followup_em + seed idempotente da etapa Follow-up"
    - path: "scripts/aplicar-migration-0037.ts"
      provides: "Aplicação manual em transação via DIRECT_URL conferindo information_schema"
    - path: "src/lib/crm/followup.ts"
      provides: "Regras puras de prazo/pendência D1-D6 + detecção das etapas por nome"
      exports: ["PRAZOS_FOLLOWUP_HORAS", "ehEtapaFollowup", "ehEtapaContatoFeito", "pendenciaFollowup"]
    - path: "src/lib/crm/temperatura.ts"
      provides: "Helper puro origem→temperatura"
      exports: ["temperaturaOrigem"]
    - path: "src/components/crm/kanban-followup.tsx"
      provides: "Visão kanban D1..D6 + Perdido"
  key_links:
    - from: "src/actions/crm.ts (moverOportunidade)"
      to: "crm_oportunidades.followup_nivel"
      via: "entrada no fluxo ao mover para etapa Follow-up (ehEtapaFollowup)"
      pattern: "ehEtapaFollowup"
    - from: "src/components/crm/kanban-followup.tsx"
      to: "avancarFollowup em src/actions/crm.ts"
      via: "onDragEnd D(n)→D(n+1)"
      pattern: "avancarFollowup"
    - from: "src/lib/crm/dados.ts (getCrmVisaoGeral)"
      to: "src/lib/crm/followup.ts + temperatura.ts"
      via: "campos followupNivel/pendenciaFollowup/temperatura no OportunidadeCard"
      pattern: "pendenciaFollowup|temperaturaOrigem"
---

<objective>
Sistema de follow-up no CRM de vendas: etapa "Follow-up" seedada no pipeline Vendas (após "Contato Feito"), aba "Follow-up" com kanban D1..D6 + Perdido derivado de `followup_nivel` (card ÚNICO, sem clone), pendências com prazos crescentes na família visual do SLA de 1º contato, e chip de temperatura 🔥/🧊 automático por origem.

Purpose: leads que não respondem ao 1º contato hoje se perdem sem cadência; follow-up estruturado com prazos visíveis é estratégico para vendas (decisão do usuário, CONTEXT 260719-s3a).
Output: migration 0037 GERADA (aplicação manual), módulos puros testados, actions, aba nova no /crm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260719-s3a-crm-follow-up-etapa-visao-d1-d6-pendenci/260719-s3a-CONTEXT.md
@src/lib/db/schema.ts (crmOportunidades linhas ~838-881; crmEtapas ~826)
@src/lib/crm/dados.ts (getCrmVisaoGeral — fonte ÚNICA da /crm; padrão de query sequencial em try/catch p/ coluna de migration pendente, passo 15)
@src/lib/crm/sla-contato.ts (família visual/matemática que a pendência de follow-up imita)
@src/lib/crm/reuniao.ts (padrão de detecção de etapa por NOME normalizado — ehEtapaReuniaoAgendada)
@src/lib/crm/origem.ts (chaves REAIS de origem)
@src/actions/crm.ts (moverOportunidade ~488; carimbarPrimeiroContato)
@src/components/crm/crm-view.tsx (TabsList kanban/lista ~574-604)
@src/components/crm/kanban-crm.tsx (DndContext, onDragEnd, moverParaPerdido com MotivoPerdaDialog)
@src/components/crm/card-oportunidade.tsx (onde entram chip de temperatura e selo de pendência)
@drizzle/0019_jittery_maestro.sql (linhas 180-199 — padrão de seed idempotente NOT EXISTS)
@scripts/aplicar-migration-0034.ts (padrão de script de aplicação manual)

<interfaces>
Chaves reais de origem (src/lib/crm/origem.ts): manual, whatsapp, landing_page, meta_lead_ad, indicacao, prospeccao_fria, instagram, evento, parceria, outro.
Etapas seedadas do pipeline Vendas (0019): 'Novo Lead'(0), 'Contato Feito'(1), 'Qualificado'(2), 'Reunião Agendada'(3), 'Proposta Enviada'(4), 'Negociação'(5).
`OportunidadeCard` (src/lib/crm/dados.ts) é o contrato do card; `montarCard` é o único ponto de montagem; insumos agregados entram via `InsumosCard`.
`moverOportunidade(id, etapaId, ordemNaEtapa?)` já busca `etapaPara.nome` — ponto de hook da entrada no fluxo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0037 + schema + módulos puros followup/temperatura sob TDD</name>
  <files>drizzle/0037_crm_followup.sql, scripts/aplicar-migration-0037.ts, src/lib/db/schema.ts, src/lib/crm/followup.ts, src/lib/crm/followup.test.ts, src/lib/crm/temperatura.ts, src/lib/crm/temperatura.test.ts</files>
  <behavior>
    followup.test.ts (Vitest, mesmo estilo de sla-contato/reuniao):
    - ehEtapaFollowup('Follow-up')/('follow up')/('FOLLOW-UP ')=true; 'Contato Feito'/'Qualificado'=false (normaliza acento/caixa/espaços/hífen)
    - ehEtapaContatoFeito('Contato Feito')/('contato feito')=true; 'Follow-up'=false
    - PRAZOS_FOLLOWUP_HORAS = {1:48, 2:72, 3:120, 4:168, 5:336, 6:336} (prazo APÓS o nível n; nível 6 usa 14d para "esgotados")
    - pendenciaFollowup(params, agora): em Contato Feito (nivel null) com base < 24h → null; ≥ 24h em ponto → {tipo:'pendente', texto:'Follow-up pendente'}
    - nivel 1 com ultimoFollowupEm há 47h → null; há 48h → pendente; nivel 3 há 119h → null; há 120h → pendente (idem 5d/7d)
    - nivel 6 com ultimoFollowupEm há 14d → {tipo:'esgotado', texto:'Follow-ups esgotados'}; antes de 14d → null
    - status !== 'aberta' → sempre null (ganho/perdido não pendem)
    temperatura.test.ts:
    - temperaturaOrigem: 'meta_lead_ad'/'landing_page' → 'quente'; 'prospeccao_fria'/'whatsapp' → 'frio'; 'manual'/'indicacao'/'instagram'/null/undefined/desconhecida → null
  </behavior>
  <action>
    1. `src/lib/crm/followup.ts` — módulo PURO (zero import de db/react/next), cabeçalho comentado em pt-BR citando a decisão (CONTEXT 260719-s3a): prazos CRESCENTES D1→D2 48h, D2→D3 72h, D3→D4 5d, D4→D5 7d, D5→D6 14d, D6 vencido 14d = esgotado; NUNCA perde lead automaticamente. Exportar: `PRAZOS_FOLLOWUP_HORAS`, `ehEtapaFollowup(nome)`, `ehEtapaContatoFeito(nome)` (mesma normalização NFD de reuniao.ts, tolerando 'follow up'/'follow-up'/'followup'), e `pendenciaFollowup(p: { status: string; etapaNome: string | null; followupNivel: number | null; ultimoFollowupEm: Date | string | null; baseContatoFeito: Date | string | null }, agora?: Date): { tipo: 'pendente' | 'esgotado'; texto: string } | null`. Regras: só status 'aberta'; se etapa é Contato Feito e followupNivel null → pendente após 24h desde baseContatoFeito; se etapa é Follow-up e followupNivel 1-5 → pendente após PRAZOS[nivel] desde ultimoFollowupEm; nivel 6 → 'esgotado' após 336h. Limite EM PONTO conta como vencido (mesma convenção de estourouSla).
    2. `src/lib/crm/temperatura.ts` — módulo PURO: `type Temperatura = 'quente' | 'frio'`; `temperaturaOrigem(origem: string | null | undefined): Temperatura | null` com Record explícito (quente: meta_lead_ad, landing_page; frio: prospeccao_fria, whatsapp — chaves REAIS de origem.ts). DECISÃO REGISTRADA: temperatura é DERIVADA NA LEITURA (função pura da origem), sem coluna nova nem backfill — leads antigos ganham o chip automaticamente e a origem editada na ficha (quick 260719-qf5 propaga p/ oportunidades) mantém tudo consistente. Documentar isso no cabeçalho.
    3. RED→GREEN: escrever os testes acima primeiro, ver falhar, implementar.
    4. `src/lib/db/schema.ts` — em `crmOportunidades`, adicionar (com comentários pt-BR): `followupNivel: integer('followup_nivel')` (1-6; null = fora do fluxo; NUNCA zerado ao sair da etapa — histórico) e `ultimoFollowupEm: timestamp('ultimo_followup_em', { withTimezone: true })`.
    5. `drizzle/0037_crm_followup.sql` — GERAR À MÃO (drizzle-kit generate está quebrado por colisão de snapshots — decisão quick-260717-qq6), separando por `--> statement-breakpoint`: (a) `ALTER TABLE "crm_oportunidades" ADD COLUMN IF NOT EXISTS "followup_nivel" integer;` (b) idem `"ultimo_followup_em" timestamp with time zone;` (c) seed idempotente da etapa: num único statement guardado por NOT EXISTS sobre o pipeline padrão do workspace 'jsr' — se não existir etapa cujo nome normalizado seja 'follow-up' no pipeline Vendas padrão: `UPDATE crm_etapas SET ordem = ordem + 1 WHERE pipeline_id = <vendas> AND ordem >= 2` e depois `INSERT INTO crm_etapas (pipeline_id, nome, ordem, probabilidade) VALUES (<vendas>, 'Follow-up', 2, 25)` (após 'Contato Feito' ordem 1). Use CTE/subselect no padrão do 0019 (JOIN workspaces w ON w.slug='jsr', pipeline padrao=true). Como UPDATE+INSERT precisam ser atômicos e condicionais, é aceitável fazer o seed SÓ no script TS (transação) e deixar no .sql um comentário apontando para o script — escolha a forma mais segura, mas o SQL das colunas fica no .sql obrigatoriamente.
    6. `scripts/aplicar-migration-0037.ts` — padrão de aplicar-migration-0034.ts: lê `DIRECT_URL`, `sql.begin()` (rollback total), CONFERE ANTES via information_schema se as colunas já existem e via SELECT se a etapa 'Follow-up' já existe no pipeline Vendas padrão (re-rodar é seguro/idempotente); loga o que fez/pulou em pt-BR. Uso: `npx tsx --env-file=.env.local scripts/aplicar-migration-0037.ts`. NUNCA rodar drizzle-kit migrate. NÃO aplicar a migration — o usuário aplica na mão.
  </action>
  <verify>
    <automated>npx vitest run src/lib/crm/followup.test.ts src/lib/crm/temperatura.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Módulos puros verdes sob TDD; migration 0037 gerada (NÃO aplicada) com colunas aditivas + seed idempotente; script de aplicação segue o padrão do projeto.</done>
</task>

<task type="auto">
  <name>Task 2: Dados + actions — campos no card, entrada no fluxo e avancarFollowup</name>
  <files>src/lib/crm/dados.ts, src/actions/crm.ts</files>
  <action>
    1. `src/lib/crm/dados.ts`:
       - `OportunidadeCard` ganha: `followupNivel: number | null`, `ultimoFollowupEm: string | null` (ISO), `temperatura: 'quente' | 'frio' | null`, `pendenciaFollowup: { tipo: 'pendente' | 'esgotado'; texto: string } | null`.
       - Query SEQUENCIAL SEPARADA em try/catch (mesmo padrão do passo 15 do SLA — as colunas são da migration 0037 pendente; NÃO entram em CAMPOS_CARD para o board não quebrar junto): buscar `{ id, followupNivel, ultimoFollowupEm }` das oportunidades ABERTAS do pipeline; guardar em Map dentro de `InsumosCard` (ex.: `followupPorOportunidade`). No catch, Map vazio + console.error citando "migration 0037 pendente?".
       - `montarCard` precisa do NOME da etapa: montar um `Map<etapaId, nome>` a partir de `etapas` (já carregadas no passo 3) e passar via `InsumosCard`. Preencher `temperatura: temperaturaOrigem(o.origem)` e `pendenciaFollowup: pendenciaFollowup({ status, etapaNome, followupNivel, ultimoFollowupEm, baseContatoFeito: primeiroContatoEm ?? createdAt }, agora)`. Para `baseContatoFeito`: a base da pendência de 24h em Contato Feito é `primeiro_contato_em` (carimbado ao mover o card — quick 260717-qq6), com fallback `createdAt`; para isso a MESMA query try/catch acima também seleciona `primeiroContatoEm` (ela já vive num try/catch, então mantém a degradação graciosa). Cards fechados: campos null (pendência só existe em aberta — o módulo puro já garante).
    2. `src/actions/crm.ts`:
       - Em `moverOportunidade`, após o update e o `carimbarPrimeiroContato(id)`: se `ehEtapaFollowup(etapaPara.nome)`, num try/catch próprio (degradação graciosa à coluna ausente) fazer `UPDATE ... SET followup_nivel = 1, ultimo_followup_em = now WHERE id = :id AND followup_nivel IS NULL` (só entra em D1 se null — voltar pra etapa depois de já ter nível NÃO rebaixa; comentário pt-BR explicando). Sair da etapa/ganhar/perder NÃO zera nada (histórico preservado; o card só some da visão).
       - Nova action `avancarFollowup(id: string)`: mesmas guardas de sessão/workspace do padrão do arquivo; carrega a oportunidade (workspace, status, etapaId, followupNivel); recusa se status !== 'aberta', se a etapa atual não é Follow-up (buscar nome via crmEtapas), se followupNivel null ou >= 6. Atualiza `followupNivel = nivel + 1`, `ultimoFollowupEm = new Date()`, `updatedAt`. Registra `registrarAtividadeCrm` tipo 'followup' (campo: 'followup', de: `D${nivel}`, para: `D${nivel+1}`) — se o tipo de atividade for validado em algum lugar, seguir o padrão dos tipos existentes (ex.: 'mudanca_etapa'). `revalidatePath('/crm')`. Erros em pt-BR.
    3. Queries sempre SEQUENCIAIS (pool max=5, max_pipeline=1) — nunca Promise.all dentro da função.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/crm src/lib/tarefas</automated>
  </verify>
  <done>getCrmVisaoGeral entrega temperatura + pendência + nível de follow-up com degradação graciosa sem a migration; mover para Follow-up seta D1 idempotente; avancarFollowup incrementa nível com carimbo e atividade.</done>
</task>

<task type="auto">
  <name>Task 3: UI — chip 🔥/🧊 e selo de pendência no card + aba Follow-up com kanban D1..D6 + Perdido</name>
  <files>src/components/crm/card-oportunidade.tsx, src/components/crm/crm-view.tsx, src/components/crm/kanban-followup.tsx</files>
  <action>
    1. `card-oportunidade.tsx`:
       - Chip de temperatura ao lado do #N no cabeçalho (ou junto à linha azul): 🔥 com classes âmbar/laranja pastel OU 🧊 com azul/cyan pastel — SEMPRE com variante `dark:` (memória dark-mode: badges pastel precisam de variante dark). `temperatura === null` → não renderiza nada (sem placeholder).
       - Selo de pendência de follow-up na MESMA família visual do SLA (bloco `aguardandoSla` existente): quando `pendenciaFollowup?.tipo === 'pendente'` → linha "⏰ Follow-up pendente" em âmbar (ícone AlertTriangle ou Clock do lucide + emoji não obrigatório — seguir o estilo do texto do SLA); quando `'esgotado'` → "Follow-ups esgotados" em vermelho + `ring-1 ring-red-500/40 dark:ring-red-400/40` no card (mesmo destaque do slaEstourado). Precedência para não empilhar 3 avisos: SLA de 1º contato > pendência de follow-up > semContato (estender a condição `!aguardandoSla` já existente).
    2. `kanban-followup.tsx` (novo, client): visão kanban de 7 colunas fixas — D1..D6 + Perdido. Recebe `colunas` (do pipeline de VENDAS), `colunasFechadas` e `oportunidadesVisiveis` (mesmo contrato do KanbanCrm). Deriva: cards da etapa cujo nome passa em `ehEtapaFollowup` agrupados por `followupNivel` (1..6; nível null nessa etapa cai em D1 visualmente — comentar); coluna Perdido = `colunasFechadas` chave 'perdido' (fixa, existente — NUNCA etapa nova). Cabeçalho de cada coluna: "D1 · 48h", "D2 · 72h", "D3 · 5d", "D4 · 7d", "D5 · 14d", "D6 · final" (derivar dos PRAZOS_FOLLOWUP_HORAS, não hardcode duplicado) + contagem. DnD com `DndContext` (mesmos sensores/activationConstraint por distância de kanban-crm.tsx): permitir SOLTAR apenas em D(nivel+1) (adjacente à frente) → chama `avancarFollowup(id)` com atualização otimista + rollback em erro (padrão do onDragEnd existente); soltar em 'perdido' → reusar o fluxo de motivo de perda: importar e renderizar o mesmo `MotivoPerdaDialog`/`moverParaPerdido` usados em kanban-crm.tsx (cancelar NÃO move). Qualquer outro destino = no-op. Reusar `CardOportunidade` para o card (card único, mesma aparência). Estado vazio honesto: "Nenhum lead na etapa Follow-up" quando a etapa não existe ainda (migration não aplicada) ou está vazia — sem dado falso.
    3. `crm-view.tsx`: adicionar `TabsTrigger value="followup"` ("Follow-up", ícone lucide ex.: `IterationCw`/`Repeat2`) na TabsList e `TabsContent value="followup"` renderizando `<KanbanFollowup colunas={dados.colunas} colunasFechadas={dados.colunasFechadas} oportunidadesVisiveis={oportunidadesVisiveis} />`. A TabsList já tem overflow-x-auto para mobile (quick 260717-is7) — manter. Se o kanban-followup pausar polling durante drag, expor `onArrastandoChange` como o KanbanCrm e ligar em `setArrastando`.
    4. Tudo em pt-BR, dark mode em todas as cores novas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/crm src/lib/tarefas</automated>
  </verify>
  <done>Aba Follow-up funcional com D1..D6 + Perdido sobre o MESMO card; drag adjacente incrementa nível via avancarFollowup; card do kanban de vendas mostra chip de temperatura e selos de pendência/esgotado na família visual do SLA.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo.
- `npx vitest run src/lib/crm src/lib/tarefas` verde (novos testes de followup/temperatura inclusos).
- Migration 0037 GERADA e NÃO aplicada; script idempotente pronto (`npx tsx --env-file=.env.local scripts/aplicar-migration-0037.ts`) — usuário aplica na mão.
- Sem a migration aplicada, /crm continua funcionando (degradação graciosa: sem nível/pendência, aba Follow-up com estado vazio honesto; temperatura funciona pois é derivada).
- NÃO fazer push para master (teste local primeiro — decisão do usuário).
</verification>

<success_criteria>
- Etapa "Follow-up" seedada idempotente após "Contato Feito" no pipeline Vendas (via migration/script).
- Card único: aba Follow-up é VISÃO por followup_nivel; entrar na etapa seta D1; sair não apaga histórico.
- Prazos crescentes 24h/48h/72h/5d/7d/14d com selos na família do SLA; D6 vencido = destaque, decisão humana.
- Temperatura derivada da origem (decisão registrada: sem coluna/backfill) com chip 🔥/🧊 no card, dark mode ok.
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260719-s3a-crm-follow-up-etapa-visao-d1-d6-pendenci/260719-s3a-SUMMARY.md`
</output>
