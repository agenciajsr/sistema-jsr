---
phase: quick-260715-oz9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/session.ts
  - src/app/error.tsx
  - src/lib/db/index.ts
  - src/app/(app)/financeiro/page.tsx
autonomous: true
requirements: [QUICK-OZ9]
must_haves:
  truths:
    - "Um soluço do pooler durante a validação de sessão NÃO derruba a página na 1ª tentativa — getCurrentUser tenta de novo antes de lançar"
    - "Se o layout do grupo (app) lançar erro, o usuário vê uma tela de erro em PORTUGUÊS com botão de recarregar, nunca a tela preta padrão do Next em inglês"
    - "Um travamento no /financeiro não entope o pool a ponto de derrubar getCurrentUser de todas as outras páginas na mesma instância"
  artifacts:
    - path: "src/lib/auth/session.ts"
      provides: "getCurrentUser com retry (withRetry) na revalidação de sessão"
      contains: "withRetry"
    - path: "src/app/error.tsx"
      provides: "Error boundary de raiz em português, estilos standalone"
      min_lines: 25
    - path: "src/lib/db/index.ts"
      provides: "Pool com max ajustado e comentário atualizado documentando a decisão"
  key_links:
    - from: "src/lib/auth/session.ts"
      to: "src/lib/utils/with-retry.ts"
      via: "import { withRetry }"
      pattern: "withRetry"
    - from: "src/app/error.tsx"
      to: "erros lançados por src/app/(app)/layout.tsx"
      via: "error boundary do segmento raiz (captura erros de layouts filhos)"
      pattern: "'use client'"
---

<objective>
Corrigir a cascata de travamento confirmada em produção (15/jul/2026): /financeiro entope o pool (max=3) com queries abandonadas da 1ª tentativa do withRetry → a instância Fluid Compute reutilizada mantém o pool congestionado → getCurrentUser (sem retry, timeout 8s) estoura em TODAS as páginas → o erro do layout (app) não é capturado por nenhum error boundary → usuário vê tela preta do Next em inglês até a instância reciclar.

Purpose: sistema inteiro não pode "cair" por causa de uma página pesada; e quando cair, a mensagem deve ser em português com recuperação clara.
Output: retry na sessão, error boundary de raiz, pool com mais folga + retry do financeiro menos agressivo com o pool.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/auth/session.ts
@src/lib/utils/with-retry.ts
@src/lib/utils/with-timeout.ts
@src/lib/db/index.ts
@src/app/(app)/layout.tsx
@src/app/(app)/error.tsx
@src/app/(app)/financeiro/page.tsx

Diagnóstico já confirmado pelo orquestrador — NÃO re-investigar:
1. getCurrentUser tem withTimeout de 8s mas nenhum retry (diferente das cargas de dados).
2. error.tsx do grupo (app) NÃO captura erro lançado pelo layout do MESMO segmento (regra do App Router); não existe src/app/error.tsx nem global-error.tsx.
3. withRetry não cancela as queries da 1ª tentativa (comentário no próprio arquivo admite); com pool max=3 e 9 queries do financeiro, a 2ª tentativa disputa o pool com as abandonadas e a instância reutilizada fica com o pool entupido.

Regras invioláveis do projeto:
- max_pipeline DEVE permanecer 1 (0 quebra db.transaction — UNSAFE_TRANSACTION, provado 15/jul; >1 pipelina e trava no Supavisor, provado 14/jul).
- NUNCA rodar drizzle-kit migrate (sem migration neste plano de qualquer forma).
- Todo texto visível ao usuário e todos os comentários em português.
- Commits atômicos por task.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retry na validação de sessão (getCurrentUser)</name>
  <files>src/lib/auth/session.ts</files>
  <action>
Em `getCurrentUser`, substituir os dois `withTimeout` diretos por UMA chamada `withRetry` envolvendo a revalidação completa (auth.getUser + profiles.findFirst juntos numa factory), espelhando o padrão já usado no /financeiro:

- Importar `withRetry` de `@/lib/utils/with-retry` (remover o import de `withTimeout` se ficar sem uso).
- Factory `revalidarSessao` que: (a) chama `supabase.auth.getUser()`; se `!user`, retorna `null` imediatamente (curto-circuito — usuário deslogado NÃO é erro e não deve gastar retry); (b) busca `db.query.profiles.findFirst(...)`; se `!profile`, retorna `null`; (c) senão retorna o objeto `CurrentUser`.
- Chamar `withRetry(revalidarSessao, { timeoutMs: 5_000, retryTimeoutMs: 8_000, label: 'sessao' })` — 1ª tentativa curta (5s) para falhar rápido no pool entupido, retry com 8s (equivalente ao teto atual).
- Semântica preservada: retorna `null` quando não logado/sem profile; lança SÓ quando as DUAS tentativas falharem (o erro sobe para o error boundary).
- Atualizar os comentários existentes (que são ricos e devem ser mantidos): explicar que o retry existe porque, na cascata de 15/jul/2026, o pool entupido pelo /financeiro derrubava a sessão de TODAS as páginas na 1ª tentativa, enquanto Supabase Auth e o banco estavam saudáveis por fora — a 2ª tentativa costuma passar. Manter o `cache()` e sua explicação intactos.
- Comentários e constantes em português. A constante `AUTH_TIMEOUT_MS` pode virar duas (`SESSAO_TIMEOUT_MS = 5_000`, `SESSAO_RETRY_TIMEOUT_MS = 8_000`) ou opções inline documentadas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>getCurrentUser usa withRetry (5s → 8s), retorna null para deslogado sem gastar retry, lança apenas após 2 falhas; tipos ok e suíte (237 testes) verde.</done>
</task>

<task type="auto">
  <name>Task 2: Error boundary de raiz em português (src/app/error.tsx)</name>
  <files>src/app/error.tsx</files>
  <action>
Criar `src/app/error.tsx` (client component) para capturar erros lançados pelo layout do grupo `(app)` — que o `src/app/(app)/error.tsx` NÃO captura por ser do mesmo segmento.

- `'use client'` no topo; assinatura padrão `{ error, reset }` com `error: Error & { digest?: string }`.
- Texto em português, mesma mensagem/tom do `src/app/(app)/error.tsx` ("Sistema instável no momento", "Não conseguimos validar sua sessão agora...", botão "Recarregar" chamando `reset`).
- IMPORTANTE — estilos standalone: este boundary renderiza DENTRO do root layout (globals.css e ThemeProvider presentes), então classes Tailwind base funcionam; porém NÃO depender de componentes do grupo (app) nem de contexto de sidebar. Usar `<button>` nativo estilizado com classes Tailwind simples (evitar tokens que dependam de tema podendo estar sem provider — preferir classes neutras como `bg-zinc-900 text-white dark:...` ou usar os tokens do design system que vivem no globals.css, ex.: `bg-primary text-primary-foreground`, que existem independentemente do grupo). Pode usar ícones lucide-react (dependência global) para manter coerência visual (AlertTriangle, RefreshCw).
- Comentário em português explicando POR QUE este arquivo existe: error.tsx de um segmento não captura erro do layout do MESMO segmento; sem este arquivo, erro no layout (app) (ex.: timeout de sessão) mostrava a tela preta padrão do Next em inglês ("This page couldn't load") — visto em produção 15/jul/2026.
- Avaliar `global-error.tsx`: o root layout (`src/app/layout.tsx`) é puramente estático (fontes + providers, sem async/fetch) e praticamente não pode falhar em runtime — NÃO criar global-error.tsx; registrar essa decisão no comentário do novo error.tsx (uma linha).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>src/app/error.tsx existe, client component em português, funciona sem depender do grupo (app); build passa; decisão sobre global-error.tsx documentada em comentário.</done>
</task>

<task type="auto">
  <name>Task 3: Mitigar pool entupido (max=5 + retry do financeiro com folga)</name>
  <files>src/lib/db/index.ts, src/app/(app)/financeiro/page.tsx</files>
  <action>
Duas mudanças coordenadas para a 2ª tentativa do withRetry não disputar o pool com as queries abandonadas da 1ª:

1. `src/lib/db/index.ts`: elevar `max: 3` → `max: 5`. Manter `statement_timeout: 12_000` como está (reduzir agora encurtaria queries legítimas do financeiro que rodam perto do teto — mais arriscado que subir o max; o statement_timeout já garante que queries abandonadas morrem em ≤12s e devolvem a conexão). Atualizar o comentário rico do arquivo (mantendo TODO o histórico existente, inclusive as notas de max_pipeline): explicar que max=5 dá folga para a 2ª tentativa do withRetry rodar mesmo com até 12s de queries órfãs da 1ª ocupando conexões; Supavisor limita 200 clients e o app roda em pouquíssimas instâncias Fluid Compute (região gru1), então 5/instância é seguro. NÃO tocar em `max_pipeline: 1`.

2. `src/app/(app)/financeiro/page.tsx`: na chamada `withRetry(carregarDados, ...)`, adicionar `delayMs: 3_000` — espera 3s entre as tentativas para dar tempo de parte das queries órfãs da 1ª tentativa concluírem (são selects que terminam sozinhos) ou serem mortas pelo statement_timeout, liberando conexões antes da 2ª tentativa. Atualizar o comentário da "Estratégia anti-travamento" (item 3) explicando o delay. Como os lotes viram 4+5 num pool de 5, manter os 2 lotes sequenciais (decisão registrada em STATE.md: nada de paralelismo além do padrão atual).

Commit desta task separado das anteriores.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Pool com max=5 e comentário atualizado preservando o histórico (max_pipeline: 1 intocado); withRetry do financeiro com delayMs: 3000 documentado; build verde.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npm test` verdes (suíte existente ~237 testes).
- `npm run build` compila (valida error.tsx de raiz e páginas server).
- `src/lib/auth/session.ts` importa e usa `withRetry`; nenhuma chamada solta de `withTimeout` restante sem retry na sessão.
- `src/app/error.tsx` existe com `'use client'` e texto em português.
- `src/lib/db/index.ts`: `max: 5`, `max_pipeline: 1` intocado, comentário atualizado.
- Smoke manual pós-deploy (usuário): abrir /financeiro e navegar para outras páginas — nenhuma tela preta em inglês; em falha total, tela em português com "Recarregar".
</verification>

<success_criteria>
- Soluço transitório do pool não derruba mais a sessão na 1ª tentativa (retry 5s→8s em getCurrentUser).
- Erro do layout (app) cai no error boundary de raiz em português, nunca na tela padrão do Next.
- 2ª tentativa do financeiro tem folga real de conexões (max=5 + delay de 3s), quebrando a cascata financeiro → todas as páginas.
- Regras preservadas: max_pipeline=1, sem drizzle-kit migrate, textos/comentários em português, 3 commits atômicos.
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260715-oz9-corrigir-cascata-de-travamento-retry-na-/260715-oz9-SUMMARY.md`
</output>
