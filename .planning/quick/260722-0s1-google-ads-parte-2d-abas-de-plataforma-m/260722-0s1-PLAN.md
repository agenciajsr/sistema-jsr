---
phase: quick
plan: 260722-0s1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/trafego/painel.ts
  - src/lib/trafego/aggregate.ts
  - src/components/trafego/seletor-plataforma.tsx
  - src/components/trafego/tabela-niveis.tsx
  - src/components/trafego/landing-clientes.tsx
  - src/app/(app)/campanhas/page.tsx
autonomous: true
requirements: [GADS-2D]
must_haves:
  truths:
    - "Cliente com UMA plataforma (só Meta ou só Google) abre o painel direto, sem abas, idêntico a hoje"
    - "Cliente com as DUAS plataformas mostra abas [Meta] [Google] [Compilado] no topo do painel, controladas por ?plataforma="
    - "Aba padrão quando o cliente tem as duas plataformas é Meta"
    - "Aba Meta mostra só dados de contas plataforma='meta'; nenhum número Meta muda em relação a hoje"
    - "Aba Google mostra KPIs, gráfico por dia, tabela só de CAMPANHAS e funil; oculta Conjuntos/Anúncios, Demografia, Regiões e Criativos com nota 'Em breve para Google Ads'"
    - "Aba Compilado replica o comportamento unificado atual (meta + google somados)"
    - "Um badge discreto (Meta / Google / Compilado) indica qual plataforma está sendo vista"
    - "Cards da tela inicial mostram o investido 30d dividido por plataforma (Meta R$X · Google R$Y)"
  artifacts:
    - path: "src/lib/trafego/painel.ts"
      provides: "getPainelCampanhas com 3º parâmetro plataforma? que filtra o conjunto de contas do cliente"
      contains: "plataforma"
    - path: "src/lib/trafego/aggregate.ts"
      provides: "getInvestido30dPorCliente por-plataforma + getPlataformasDoCliente"
      contains: "getPlataformasDoCliente"
    - path: "src/components/trafego/seletor-plataforma.tsx"
      provides: "Seletor de abas Meta/Google/Compilado (client) + BadgePlataforma"
      min_lines: 40
    - path: "src/app/(app)/campanhas/page.tsx"
      provides: "Resolução de abas por plataforma e ocultação das seções sem dado no Google"
      contains: "plataforma"
  key_links:
    - from: "src/app/(app)/campanhas/page.tsx"
      to: "getPainelCampanhas"
      via: "passa a plataforma resolvida (meta|google|undefined) como 3º argumento"
      pattern: "getPainelCampanhas\\(cliente"
    - from: "src/app/(app)/campanhas/page.tsx"
      to: "getPlataformasDoCliente"
      via: "descobre as plataformas do cliente selecionado para decidir se mostra abas"
      pattern: "getPlataformasDoCliente"
    - from: "src/components/trafego/seletor-plataforma.tsx"
      to: "/campanhas?plataforma="
      via: "router.push preservando cliente e periodo (espelha SeletorCampanhas)"
      pattern: "plataforma"
---

<objective>
Adicionar abas de plataforma [Meta] [Google] [Compilado] no painel de um cliente em /campanhas, controladas pelo searchParam `?plataforma=meta|google|compilado` (mesmo padrão server-render do `?periodo=`). Reutiliza 100% dos componentes de exibição existentes — só troca a fonte de dados por plataforma e adiciona um seletor de abas + um badge.

Purpose: dar visibilidade separada de Meta e Google (que a Parte 2b passou a sincronizar) sem mexer nos números do Meta e sem expandir o sync do Google (Parte 2e). Cliente com uma só plataforma continua idêntico a hoje.

Output: camada de dados plataforma-aware por parâmetro, seletor de abas, split de investido por plataforma nos cards, e ocultação graciosa das seções que o Google ainda não alimenta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260721-xa1-google-ads-parte-2c-mostrar-google-ads-n/260721-xa1-SUMMARY.md

# Fatos verificados nos arquivos-alvo (usar diretamente, não reexplorar):

- `adAccounts.plataforma` é um enum NOT NULL `'meta' | 'google'` (schema.ts L183-188).
- `getPainelCampanhas(clienteId, periodo)` (painel.ts L334) hoje seleciona as contas do cliente
  em L351-359: `.where(and(eq(adAccounts.clienteId, clienteId), eq(adAccounts.ativo, true)))`.
  `contasUnificadas` = `contas.length` (L708). É AQUI que o filtro de plataforma entra.
- `getInvestido30dPorCliente()` (aggregate.ts L96) retorna hoje `Map<string, number>`.
  Faz 1 query com innerJoin adAccounts + groupBy `adAccounts.clienteId`. ÚNICO caller: campanhas/page.tsx L100.
- `getContasDoCliente` já tipa `plataforma: 'meta' | 'google'` (actions/trafego.ts L177-205) — modelo do enum.
- `SeletorCampanhas` (seletor-campanhas.tsx) é um client component que usa
  `useRouter`/`useSearchParams`, lê `cliente` FRESCO da URL e faz `router.push('/campanhas?...')`
  preservando o cliente. O seletor de abas espelha ESSE padrão, preservando cliente E periodo.
- `TabelaNiveis` (tabela-niveis.tsx L144, L272-278) SEMPRE renderiza 3 TabsTrigger
  (Campanhas/Conjuntos/Anúncios). Para "só campanha" é preciso um prop novo `soloCampanhas`.
- `LandingClientes` (landing-clientes.tsx L66) recebe `investido30d: Map<string, number>`
  e usa `investido30d.get(c.id) ?? 0` (L80) para o card "Investido" e o flag `ativo`.
- Na página, `painel` é chamado SEQUENCIALMENTE (pool max=5, maxDuration=60) — nunca Promise.all interno.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Camada de dados plataforma-aware (painel.ts + aggregate.ts)</name>
  <files>src/lib/trafego/painel.ts, src/lib/trafego/aggregate.ts</files>
  <action>
Três mudanças cirúrgicas, sem remover nada:

1. **painel.ts — `getPainelCampanhas` ganha 3º parâmetro opcional `plataforma`:**
   Assinatura: `export async function getPainelCampanhas(clienteId: string, periodo: Periodo = '30d', plataforma?: 'meta' | 'google'): Promise<PainelCampanhas | null>`.
   No select de contas (L351-359), adicionar o predicado condicional DENTRO do `and(...)`:
   ```
   .where(
     and(
       eq(adAccounts.clienteId, clienteId),
       eq(adAccounts.ativo, true),
       ...(plataforma ? [eq(adAccounts.plataforma, plataforma)] : []),
     ),
   )
   ```
   NADA MAIS muda. `plataforma` ausente = todas as contas do cliente (comportamento idêntico ao de hoje = aba Compilado). `contasUnificadas` (L708 = `contas.length`) passa a refletir só as contas da plataforma filtrada — é o comportamento correto para o texto "X contas unificadas" por aba.

2. **aggregate.ts — `getInvestido30dPorCliente` passa a devolver por-plataforma:**
   Mudar o retorno para `Promise<Map<string, { meta: number; google: number }>>`.
   No select (L101-114) adicionar `plataforma: adAccounts.plataforma` e incluí-la no `.groupBy(adAccounts.clienteId, adAccounts.plataforma)`.
   Montar o mapa aninhado:
   ```
   const mapa = new Map<string, { meta: number; google: number }>()
   for (const r of rows) {
     if (!r.clienteId) continue
     const atual = mapa.get(r.clienteId) ?? { meta: 0, google: 0 }
     if (r.plataforma === 'google') atual.google += Number(r.investido) || 0
     else atual.meta += Number(r.investido) || 0
     mapa.set(r.clienteId, atual)
   }
   ```
   Único caller é a página (Task 3) — atualizada na mesma entrega.

3. **aggregate.ts — nova query leve `getPlataformasDoCliente`:**
   ```
   export async function getPlataformasDoCliente(clienteId: string): Promise<('meta' | 'google')[]> {
     const user = await getCurrentUser()
     if (!user) return []
     const rows = await db
       .selectDistinct({ plataforma: adAccounts.plataforma })
       .from(adAccounts)
       .where(and(eq(adAccounts.clienteId, clienteId), eq(adAccounts.ativo, true)))
     return rows.map((r) => r.plataforma)
   }
   ```
   Decide se o cliente tem 1 ou 2 plataformas (base em CONTAS existentes, não em gasto — cliente com conta Google vazia ainda conta como "tem Google"). Uma query distinct barata, chamada só quando há cliente selecionado.
  </action>
  <verify>
    <automated>cd /d/projetos/projeto_agencia_jsr && npx tsc --noEmit</automated>
  </verify>
  <done>getPainelCampanhas aceita plataforma? e filtra as contas; getInvestido30dPorCliente devolve {meta,google} por cliente; getPlataformasDoCliente existe e é exportada; tsc sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Seletor de abas + badge de plataforma (novo componente client)</name>
  <files>src/components/trafego/seletor-plataforma.tsx</files>
  <action>
Criar o componente espelhando `SeletorCampanhas` (mesmo padrão useRouter/useSearchParams, links server-render, sem estado global). Exportar DOIS itens do mesmo arquivo:

1. **`SeletorPlataforma`** — abas [Meta] [Google] [Compilado].
   Props: `{ plataformaAtual: 'meta' | 'google' | 'compilado' }`.
   `'use client'` no topo. Usar `Tabs`/`TabsList`/`TabsTrigger` de `@/components/ui/tabs` (mesmo kit que TabelaNiveis usa) com `value={plataformaAtual}` e `onValueChange={trocarPlataforma}`.
   `trocarPlataforma(p: string)`: monta `URLSearchParams` preservando `cliente` e `periodo` lidos FRESCOS de `useSearchParams()` (mesma lógica defensiva do SeletorCampanhas que preserva o cliente), define `params.set('plataforma', p)` e `router.push('/campanhas?'+params.toString())`.
   Rótulos: Meta, Google, Compilado (values 'meta' | 'google' | 'compilado').

2. **`BadgePlataforma`** — selinho discreto.
   Props: `{ plataforma: 'meta' | 'google' | 'compilado' }`.
   Renderiza um `<span>` pequeno (classe estilo badge: `inline-flex items-center gap-1 rounded-md px-2 py-0.5 text-xs font-medium ring-1 ring-inset`) com rótulo "Meta", "Google" ou "Compilado". Tons discretos por plataforma (ex.: Meta azul via `bg-primary/10 text-primary ring-primary/15`; Google âmbar/neutro; Compilado neutro/muted). Pode ser componente puro (sem hooks) mas fica no mesmo arquivo 'use client' sem problema.

NÃO criar nenhum componente de VISUALIZAÇÃO novo — só este seletor e o badge.
  </action>
  <verify>
    <automated>cd /d/projetos/projeto_agencia_jsr && npx tsc --noEmit && npx eslint src/components/trafego/seletor-plataforma.tsx --max-warnings=0</automated>
  </verify>
  <done>Arquivo exporta SeletorPlataforma e BadgePlataforma; troca de aba preserva cliente+periodo e altera só ?plataforma; tsc + eslint limpos.</done>
</task>

<task type="auto">
  <name>Task 3: Integrar abas na página + soloCampanhas na TabelaNiveis + split nos cards</name>
  <files>src/app/(app)/campanhas/page.tsx, src/components/trafego/tabela-niveis.tsx, src/components/trafego/landing-clientes.tsx</files>
  <action>
**A) tabela-niveis.tsx — prop `soloCampanhas`:**
Adicionar `soloCampanhas?: boolean` ao `TabelaNiveisProps` (L135-142) e à desestruturação (L144). Quando `soloCampanhas` for true, NÃO renderizar os TabsTrigger de Conjuntos e Anúncios (L275-276) — envolver ambos em `{!soloCampanhas && (<>...</>)}`. Como o nível inicial já é `'campanhas'` (L145) e não há mais como trocar, a tabela fica travada em campanhas. Resto intocado.

**B) landing-clientes.tsx — split por plataforma:**
Mudar o tipo do prop `investido30d` (L59) para `Map<string, { meta: number; google: number }>`.
Dentro do `.map` (L80): substituir `const investido = investido30d.get(c.id) ?? 0` por:
```
const inv = investido30d.get(c.id) ?? { meta: 0, google: 0 }
const total = inv.meta + inv.google
```
Trocar `const ativo = investido > 0` por `const ativo = total > 0`.
No card "Investido" (L127-133): o valor mostrado passa a ser `formatadorMoeda.format(r?.spend ?? total)`.
Adicionar, logo abaixo do bloco do objetivo (após L125, antes do grid de métricas), uma linha discreta de split QUANDO houver Google (`inv.google > 0`):
```
{inv.google > 0 && (
  <p className="text-xs text-muted-foreground">
    Meta {formatadorMoeda.format(inv.meta)} · Google {formatadorMoeda.format(inv.google)}
  </p>
)}
```
(Cliente só-Meta continua sem a linha — visual idêntico a hoje.)

**C) campanhas/page.tsx — resolução das abas:**
- Importar `getPlataformasDoCliente` de `@/lib/trafego/aggregate` e `SeletorPlataforma, BadgePlataforma` de `@/components/trafego/seletor-plataforma`.
- Tipar searchParams: `Promise<{ cliente?: string; periodo?: string; plataforma?: string }>`.
- Após ler `sp`, adicionar constante de validação:
  `const PLATAFORMAS_VALIDAS = ['meta', 'google', 'compilado'] as const` (topo do arquivo, junto de PERIODOS_VALIDOS).
- **Descobrir plataformas ANTES do painel** (precisa do filtro resolvido para chamar getPainelCampanhas). Substituir a linha 72 por esta sequência SEQUENCIAL:
  ```
  const plataformasCliente = cliente ? await getPlataformasDoCliente(cliente) : []
  const temAbas = plataformasCliente.length >= 2
  // Aba ativa: só faz sentido com 2 plataformas; default 'meta'.
  const plataformaParam = PLATAFORMAS_VALIDAS.includes(sp.plataforma as (typeof PLATAFORMAS_VALIDAS)[number])
    ? (sp.plataforma as 'meta' | 'google' | 'compilado')
    : 'meta'
  const abaAtiva: 'meta' | 'google' | 'compilado' | null = temAbas ? plataformaParam : null
  // Filtro passado ao painel: 'compilado' e cliente-de-1-plataforma => undefined (todas).
  const plataformaFiltro: 'meta' | 'google' | undefined =
    abaAtiva === 'meta' || abaAtiva === 'google' ? abaAtiva : undefined
  const painel = cliente ? await getPainelCampanhas(cliente, periodo, plataformaFiltro) : null
  ```
  IMPORTANTE (REGRA DURA): cliente com 1 só plataforma → `plataformaFiltro=undefined` → getPainelCampanhas seleciona as únicas contas que existem = comportamento byte-a-byte idêntico ao de hoje. Cliente com 2 e aba Meta → filtra 'meta'.
- `const ehGoogle = abaAtiva === 'google'`.
- **Cabeçalho do painel** (bloco L191-201, "Cliente com dados"): dentro do `<div className="flex flex-wrap items-center gap-3">` que mostra "X contas unificadas", adicionar:
  - `<BadgePlataforma plataforma={abaAtiva ?? (plataformasCliente[0] ?? 'meta')} />` (single-platform mostra a plataforma real do cliente; com abas mostra a aba ativa).
  - `{temAbas && <SeletorPlataforma plataformaAtual={abaAtiva ?? 'meta'} />}` — renderizado só quando há 2 plataformas.
- **Ocultar seções sem dado no Google:** dentro do mesmo bloco de painel completo:
  - `<TabelaNiveis ... soloCampanhas={ehGoogle} />` (adicionar o prop; demais props intactos).
  - Envolver `DemografiaSection`, `RegioesSection` e `CriativosCampeoes` em `{!ehGoogle && (...)}`.
  - Quando `ehGoogle`, renderizar UMA nota discreta no lugar dessas seções:
    ```
    {ehGoogle && (
      <Card className="border-none p-6 text-center shadow-[var(--shadow-sm)]">
        <p className="text-sm text-muted-foreground">
          Conjuntos, anúncios, demografia e regiões: em breve para Google Ads.
        </p>
      </Card>
    )}
    ```
  - `GradeKpis`, `GraficoPerformance`, `FunilConversao` e a `TabelaNiveis` (nível campanha) permanecem visíveis nas 3 abas. Herói/semáforo/metas continuam por cliente — intocados.
- **Cards da tela inicial:** o prop `investido30d={investido30d ?? new Map()}` continua; o tipo agora flui como `Map<string, {meta,google}>`. Nenhuma outra mudança na chamada.
  </action>
  <verify>
    <automated>cd /d/projetos/projeto_agencia_jsr && npx tsc --noEmit && npx eslint "src/app/(app)/campanhas/page.tsx" src/components/trafego/tabela-niveis.tsx src/components/trafego/landing-clientes.tsx --max-warnings=0 && npx vitest run</automated>
  </verify>
  <done>
- Cliente com 1 plataforma: sem abas, painel direto, badge com a plataforma real.
- Cliente com 2 plataformas: abas Meta/Google/Compilado, default Meta, badge da aba ativa.
- Aba Google: KPIs + gráfico + tabela só-campanhas + funil visíveis; Conjuntos/Anúncios/Demografia/Regiões/Criativos ocultos com a nota "em breve para Google Ads".
- Cards mostram "Meta R$X · Google R$Y" quando o cliente tem gasto Google.
- tsc + eslint limpos; vitest = 649 passando (nenhuma lógica pura mudou).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → 0 erros.
- `npx eslint` nos 4 arquivos estruturalmente alterados → 0 issues.
- `npx vitest run` → 649/649 (baseline da Parte 2c mantido — REGRA DURA: nenhum número Meta muda; nenhuma função pura testada foi tocada).
- Checagem manual de código (não teste): confirmar que a aba Meta chama `getPainelCampanhas(cliente, periodo, 'meta')` e que o predicado `eq(adAccounts.plataforma, plataforma)` só entra no `and(...)` quando `plataforma` está definido (Compilado e cliente-de-1-plataforma passam undefined → contas idênticas às de hoje).
</verification>

<success_criteria>
- Abas [Meta] [Google] [Compilado] aparecem SOMENTE para clientes com as duas plataformas; default Meta.
- Cliente de uma só plataforma: idêntico a hoje (sem abas, filtro undefined).
- Aba Google mostra só o que a Parte 2b grava (KPIs, gráfico, campanhas, funil); resto oculto com nota "em breve".
- Aba Compilado = comportamento unificado atual.
- Badge de plataforma sempre visível no painel.
- Cards da tela inicial com split Meta/Google.
- Nenhum número Meta regride; vitest 649 verde.
- Sync do Google NÃO expandido (fora de escopo — Parte 2e).
</success_criteria>

<output>
After completion, create `.planning/quick/260722-0s1-google-ads-parte-2d-abas-de-plataforma-m/260722-0s1-SUMMARY.md`
</output>
