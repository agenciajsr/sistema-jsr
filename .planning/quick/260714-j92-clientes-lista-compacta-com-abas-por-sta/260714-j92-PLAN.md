---
phase: quick-260714-j92
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0014_*.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0014_snapshot.json
  - src/lib/validations/cliente.ts
  - src/components/cliente-form.tsx
  - src/app/(app)/clientes/[id]/page.tsx
  - src/components/cliente-card.tsx
  - src/lib/clientes/agregar.ts
  - src/lib/clientes/agregar.test.ts
  - src/lib/clientes/lista.ts
  - src/app/(app)/clientes/page.tsx
  - src/app/(app)/clientes/clientes-lista.tsx
autonomous: true
requirements: [260714-j92]

must_haves:
  truths:
    - "O enum cliente_status passa a aceitar 'aguardando_inicio' e 'em_aviso' (migration GERADA em drizzle/, NAO aplicada)"
    - "O formulario de cliente (modo editar) oferece os 5 status: Ativo, Aguardando Inicio, Em Aviso, Pausado, Encerrado"
    - "A pagina /clientes mostra uma LISTA compacta (1 linha por cliente), nao mais cards grandes"
    - "A lista tem abas com contador na ordem: Ativos, Aguardando Inicio, Em Aviso, Pausados, Inativos, Todos"
    - "A lista tem busca por nome que filtra instantaneamente no client"
    - "Cada linha mostra Nome, Status (badge), Alertas, Responsavel, Mensalidade, LT (dias), LTV e Invest. (30d)"
    - "Clicar na linha navega para /clientes/[id]; o menu ⋯ leva para /clientes/[id]/editar sem disparar a navegacao da linha"
    - "getClientesLista() executa poucas queries AGREGADAS SEQUENCIAIS (sem N+1, sem Promise.all interno) e nunca lanca"
    - "Estado vazio aparece quando nao ha clientes e quando a busca/aba nao retorna resultado"
    - "As consultas existentes que filtram status='ativo' (dashboard, financeiro, relatorios, alertas) permanecem intactas"
    - "npm run build, npx tsc --noEmit e npx vitest run passam"
  artifacts:
    - path: "drizzle/0014_*.sql"
      provides: "Migration aditiva: ALTER TYPE cliente_status ADD VALUE (aguardando_inicio, em_aviso)"
      contains: "ADD VALUE"
    - path: "src/lib/clientes/agregar.ts"
      provides: "Logica pura: calculo de LT em dias, merge dos agregados por cliente, filtro/contagem por status"
      exports: ["calcularLtDias", "montarLinhas", "filtrarClientes", "contarPorStatus", "STATUS_ORDEM", "STATUS_LABEL"]
    - path: "src/lib/clientes/agregar.test.ts"
      provides: "Testes vitest da logica pura (LT com/sem contrato, merge com agregados faltando, filtro e contagem)"
      contains: "describe("
    - path: "src/lib/clientes/lista.ts"
      provides: "getClientesLista(): 6 queries agregadas sequenciais + merge em memoria via agregar.ts"
      exports: ["getClientesLista"]
      contains: "export async function getClientesLista"
    - path: "src/app/(app)/clientes/page.tsx"
      provides: "Server component enxuto: chama getClientesLista() e passa para o client component"
      contains: "getClientesLista"
    - path: "src/app/(app)/clientes/clientes-lista.tsx"
      provides: "Client component: busca, abas com contador, tabela compacta, linha clicavel, estado vazio"
      exports: ["ClientesLista"]
      min_lines: 100
  key_links:
    - from: "src/app/(app)/clientes/page.tsx"
      to: "src/lib/clientes/lista.ts"
      via: "await getClientesLista()"
      pattern: "getClientesLista\\(\\)"
    - from: "src/app/(app)/clientes/page.tsx"
      to: "src/app/(app)/clientes/clientes-lista.tsx"
      via: "<ClientesLista clientes={...} />"
      pattern: "<ClientesLista"
    - from: "src/lib/clientes/lista.ts"
      to: "src/lib/clientes/agregar.ts"
      via: "montarLinhas(...)"
      pattern: "montarLinhas"
    - from: "src/app/(app)/clientes/clientes-lista.tsx"
      to: "/clientes/[id]"
      via: "router.push no onClick da TableRow"
      pattern: "router\\.push\\(`/clientes/"
---

<objective>
Trocar a pagina /clientes (hoje cards grandes + N+1 via getContratosDoCliente por cliente) por uma **lista compacta** com busca e abas por status, e criar os **status novos** que preparam o CRM (`aguardando_inicio`, `em_aviso`).

Purpose: a tela de clientes e a porta de entrada da operacao. Hoje ela e lenta (1 query por cliente) e mostra pouca informacao por scroll. A lista compacta da leitura imediata da carteira (mensalidade, LTV, tempo de casa, investimento, alertas) e os status novos abrem espaco para o funil de CRM sem quebrar nada do que ja existe.

Output: migration 0014 GERADA (nao aplicada), modulo puro testado (`src/lib/clientes/agregar.ts`), funcao de dados sem N+1 (`src/lib/clientes/lista.ts`), pagina server enxuta + client component com a lista.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@src/app/(app)/clientes/page.tsx
@src/components/cliente-card.tsx
@src/app/(app)/clientes/[id]/page.tsx
@src/lib/db/schema.ts
@src/lib/validations/cliente.ts
@src/components/cliente-form.tsx
@src/lib/financeiro/calculos.ts
@src/lib/financeiro/calculos.test.ts
@src/app/(app)/financeiro/contas-table.tsx
@src/app/(app)/financeiro/visao-analitica.tsx
@src/lib/date-br.ts
@src/lib/db/index.ts

<interfaces>
<!-- Contratos ja existentes no codigo. Use direto — nao explore o codebase. -->

De `src/lib/date-br.ts`:
```typescript
export function hojeBrasilia(): string                 // 'YYYY-MM-DD' no fuso America/Sao_Paulo
export function dataMenosDias(n: number, base?: string): string  // 'YYYY-MM-DD'
```

De `src/lib/db/schema.ts` (colunas relevantes):
```typescript
clientes:         { id, nome, status: 'ativo'|'pausado'|'encerrado', gestorId (-> profiles.id), createdAt: Date }
profiles:         { id, nome }
contratos:        { clienteId, dataInicio: string 'YYYY-MM-DD', dataVencimento: string, valorMensal: numeric(string) }
transacoes:       { clienteId (nullable), tipo: 'receita'|'despesa', status: 'pago'|'pendente'|'vencido', valor: numeric(string) }
alertas:          { clienteId (nullable), status: text 'novo'|'lido'|'resolvido' }   // index alertas_status_idx
adAccounts:       { id, clienteId (nullable) }
campaignInsights: { adAccountId (-> adAccounts.id), date: string 'YYYY-MM-DD', spend: numeric(string) }
```

De `src/lib/db/index.ts` — restricoes que MOLDAM este plano:
```
pool max: 3 | max_pipeline: 0 | statement_timeout: 12s
=> queries paralelas na MESMA funcao enfileiram e podem travar. SEQUENCIAL sempre.
```

Componentes shadcn ja instalados em `src/components/ui/`:
`table.tsx` (Table, TableBody, TableCell, TableHead, TableHeader, TableRow), `tabs.tsx` (Tabs, TabsContent, TabsList, TabsTrigger), `dropdown-menu.tsx`, `badge.tsx` (variants: default | secondary | destructive | outline | ghost | link), `input.tsx`, `card.tsx`, `button.tsx`.

Tokens de cor semantica ja definidos em `src/app/globals.css`:
`text-chart-success` / `bg-chart-success` (#16a34a), `text-chart-warning` / `bg-chart-warning` (#d97706), `primary`, `muted`, `destructive`.
</interfaces>

<armadilhas_conhecidas>
1. **Pool max=3 + max_pipeline=0** (STATE.md, quick 260713-usi e 260714-ita): NUNCA use `Promise.all` dentro de `getClientesLista()`. Queries paralelas na mesma conexao penduram para sempre. Sequencial + merge em JS.
2. **Fuso horario**: `new Date()` no server roda em UTC na Vercel — a data vira o dia seguinte a partir das 21h BR. Use SEMPRE `hojeBrasilia()` e compare datas como string ISO ('YYYY-MM-DD' ordena lexicograficamente).
3. **Dinheiro**: colunas `numeric` voltam como **string** no postgres.js. Sempre `Number(...)`. Nunca somar strings.
4. **Records exaustivos quebram o tsc**: `Record<Cliente['status'], string>` existe em `src/components/cliente-card.tsx` e `src/app/(app)/clientes/[id]/page.tsx`. Adicionar valores ao enum QUEBRA esses arquivos — a Task 1 tem que atualizar os dois.
5. **Migration NUNCA aplicada**: apenas `npx drizzle-kit generate`. O orquestrador aplica depois de forma controlada.
</armadilhas_conhecidas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status novos (migration aditiva GERADA + schema + validacao + form + Records exaustivos)</name>
  <files>src/lib/db/schema.ts, drizzle/0014_*.sql, drizzle/meta/*, src/lib/validations/cliente.ts, src/components/cliente-form.tsx, src/app/(app)/clientes/[id]/page.tsx, src/components/cliente-card.tsx</files>
  <action>
Antes de tudo: se o worktree estiver atras do master e for ancestral limpo, rodar `git merge --ff-only master`. Se nao for fast-forward, PARAR e reportar (nao forcar merge).

1. `src/lib/db/schema.ts` — estender o enum, mantendo a ORDEM (novos valores no fim; `ALTER TYPE ADD VALUE` so acrescenta):
   ```ts
   export const clienteStatusEnum = pgEnum('cliente_status', ['ativo', 'pausado', 'encerrado', 'aguardando_inicio', 'em_aviso'])
   ```
   Comentario acima do enum: `aguardando_inicio` = lead que ja fechou contrato mas esta em onboarding; `em_aviso` = cliente ativo porem sinalizado.

2. Gerar a migration: `npx drizzle-kit generate`. Deve produzir `drizzle/0014_*.sql` com `ALTER TYPE "public"."cliente_status" ADD VALUE 'aguardando_inicio';` e `... ADD VALUE 'em_aviso';`.
   **NAO APLICAR** — nao rodar `drizzle-kit push`, `migrate`, nem SQL manual no banco. Deixar o .sql + os arquivos de `drizzle/meta/` no repo.
   Conferir que o .sql gerado contem APENAS os dois ADD VALUE (nenhum DROP/ALTER inesperado). Se aparecer qualquer coisa alem disso, PARAR e reportar.

3. `src/lib/validations/cliente.ts` — `status: z.enum(['ativo', 'pausado', 'encerrado', 'aguardando_inicio', 'em_aviso']).default('ativo')`.

4. `src/components/cliente-form.tsx` — no `ClienteFormEditar`, secao 1, Select de status (hoje linhas ~660-664): adicionar `<SelectItem value="aguardando_inicio">Aguardando Inicio</SelectItem>` e `<SelectItem value="em_aviso">Em Aviso</SelectItem>`, na ordem Ativo, Aguardando Inicio, Em Aviso, Pausado, Encerrado. (O `ClienteFormCriar` nao tem select de status — usa o default 'ativo'. Deixar como esta.)

5. `src/app/(app)/clientes/[id]/page.tsx` — os `Record<Cliente['status'], string>` STATUS_LABEL e STATUS_COLOR (linhas ~52-63) viram exaustivos-quebrados com o enum novo. Adicionar:
   - STATUS_LABEL: `aguardando_inicio: 'Aguardando Inicio'`, `em_aviso: 'Em Aviso'`
   - STATUS_COLOR: `aguardando_inicio: '#2563EB'` (azul), `em_aviso: '#D97706'` (ambar)

6. `src/components/cliente-card.tsx` — mesmos dois Records (linhas ~12-22). Adicionar as mesmas entradas. (Este arquivo sera DELETADO na Task 3; aqui so mantemos o tsc verde entre as tasks.)

NAO tocar em: `src/lib/alertas/calcular.ts`, `src/lib/alertas/avaliar.ts`, `src/lib/ai/copilot.ts`, dashboard, financeiro, relatorios. Os status novos sao ADITIVOS — a semantica de "cliente ativo" nas outras telas nao muda (decisao a registrar no SUMMARY).
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && ls drizzle/0014_*.sql && grep -c "ADD VALUE" drizzle/0014_*.sql && npx tsc --noEmit</automated>
  </verify>
  <done>drizzle/0014_*.sql existe com exatamente 2 linhas ADD VALUE e NAO foi aplicado no banco; enum, Zod, form e os dois Records exaustivos aceitam os 5 status; `npx tsc --noEmit` passa sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getClientesLista() — 6 queries agregadas sequenciais + modulo puro testado</name>
  <files>src/lib/clientes/agregar.ts, src/lib/clientes/agregar.test.ts, src/lib/clientes/lista.ts</files>
  <behavior>
Testes em `src/lib/clientes/agregar.test.ts` (vitest, `import { describe, it, expect } from 'vitest'` — o projeto usa `globals: false`), espelhando o estilo de `src/lib/financeiro/calculos.test.ts`:

`calcularLtDias(dataInicioMaisAntiga, createdAtIso, hoje)`:
- Test 1: contrato mais antigo em '2026-06-14', hoje '2026-07-14' => 30
- Test 2: sem contrato (null) => usa createdAt '2026-07-04' com hoje '2026-07-14' => 10
- Test 3: comeca hoje => 0
- Test 4: data futura (contrato comeca amanha) => 0 (nunca negativo)

`montarLinhas(base, mensalidades, inicios, ltvs, alertas, investimentos, hoje)`:
- Test 5: cliente presente em todos os agregados => todos os campos preenchidos e numericos (Number, nao string)
- Test 6: cliente ausente dos agregados => mensalidade 0, ltv 0, alertasAbertos 0, investimento30d 0 (nunca undefined/NaN)
- Test 7: agregado orfao (clienteId que nao esta na base) => ignorado, nao cria linha fantasma
- Test 8: `responsavelNome` null quando nao ha gestor
- Test 9: valores numeric chegando como string ('1500.00') => viram number 1500

`filtrarClientes(linhas, { busca, aba })`:
- Test 10: aba 'ativo' devolve so status ativo; aba 'todos' devolve tudo
- Test 11: busca 'ac' casa 'Acme' e 'Farmacia' (case-insensitive, substring)
- Test 12: busca com espacos nas pontas e busca vazia => sem filtro de nome
- Test 13: busca + aba combinam (AND)

`contarPorStatus(linhas)`:
- Test 14: conta por status e o total; status sem cliente => 0 (chave presente, nao undefined)
  </behavior>
  <action>
**A) `src/lib/clientes/agregar.ts` — modulo PURO** (zero import de `db`, `auth`, `react`, `next`; mesma filosofia de `src/lib/financeiro/calculos.ts`). Define os tipos localmente (nao importar do schema) para permanecer importavel tambem pelo client component:

```ts
export type ClienteStatus = 'ativo' | 'aguardando_inicio' | 'em_aviso' | 'pausado' | 'encerrado'

export type ClienteLinha = {
  id: string
  nome: string
  status: ClienteStatus
  responsavelNome: string | null
  mensalidade: number
  ltDias: number
  ltv: number
  alertasAbertos: number
  investimento30d: number
}

// Ordem EXATA das abas (D-01 do pedido)
export const STATUS_ORDEM: ClienteStatus[] = ['ativo', 'aguardando_inicio', 'em_aviso', 'pausado', 'encerrado']
export const STATUS_LABEL: Record<ClienteStatus, string> = {
  ativo: 'Ativos',
  aguardando_inicio: 'Aguardando Inicio',
  em_aviso: 'Em Aviso',
  pausado: 'Pausados',
  encerrado: 'Inativos',
}
```
Exports: `calcularLtDias`, `montarLinhas`, `filtrarClientes`, `contarPorStatus`.
- `calcularLtDias(dataInicioMaisAntiga: string | null, createdAtIso: string, hoje: string): number` — base = `dataInicioMaisAntiga ?? createdAtIso.slice(0, 10)`. Diferenca em dias ancorada em meio-dia UTC (`new Date(\`${data}T12:00:00Z\`)`) para nao sofrer com DST — mesmo truque de `dataMenosDias` em `src/lib/date-br.ts`. `Math.max(0, ...)`.
- `montarLinhas(...)` — indexa cada agregado num `Map<clienteId, valor>` e itera a base (a base manda; agregado orfao e ignorado). Todo valor monetario passa por `Number(...)` com fallback 0 (`Number(x ?? 0) || 0` para blindar NaN).
- `filtrarClientes(linhas, { busca, aba })` — `aba: ClienteStatus | 'todos'`; busca `.trim().toLowerCase()` com `includes` sobre `nome.toLowerCase()`.
- `contarPorStatus(linhas)` — devolve `Record<ClienteStatus, number> & { todos: number }`, inicializando TODAS as chaves em 0.

**B) `src/lib/clientes/agregar.test.ts`** — os 14 testes do bloco <behavior>. Escrever ANTES da implementacao (RED), depois implementar ate ficar verde.

**C) `src/lib/clientes/lista.ts` — funcao de dados**. Modulo server comum (NAO adicionar `'use server'`: e chamado direto pelo Server Component, nao pelo client — evita expor endpoint desnecessario). Importa `db`, `hojeBrasilia`, `dataMenosDias`, `montarLinhas`.

```ts
export async function getClientesLista(): Promise<ClienteLinha[]>
```
Regras inegociaveis:
- **6 queries AGREGADAS, uma de cada vez, com `await` sequencial. ZERO `Promise.all`.** Comentario no topo explicando o porque (pool max=3, max_pipeline=0 — quicks 260713-usi e 260714-ita).
- `const hoje = hojeBrasilia()` / `const ha30dias = dataMenosDias(30)`. NUNCA `new Date()` para "hoje".
- Todo o corpo dentro de `try/catch`. Em erro: `console.error('[getClientesLista]', e)` e `return []` — nunca lanca.

As 6 queries (drizzle, seguindo o estilo de `getVisaoAnalitica` em `src/actions/financeiro.ts`):
1. **Base** — `select({ id, nome, status, createdAt, responsavelNome: profiles.nome }).from(clientes).leftJoin(profiles, eq(clientes.gestorId, profiles.id)).orderBy(asc(clientes.nome))`
2. **Mensalidade (contrato vigente hoje)** — `select({ clienteId, valor: sql<string>\`sum(${contratos.valorMensal})\` }).from(contratos).where(and(lte(contratos.dataInicio, hoje), gte(contratos.dataVencimento, hoje))).groupBy(contratos.clienteId)`
3. **Inicio do relacionamento** — `select({ clienteId, inicio: sql<string>\`min(${contratos.dataInicio})\` }).from(contratos).groupBy(contratos.clienteId)`
4. **LTV** — `select({ clienteId, total: sql<string>\`sum(${transacoes.valor})\` }).from(transacoes).where(and(eq(transacoes.tipo, 'receita'), eq(transacoes.status, 'pago'), isNotNull(transacoes.clienteId))).groupBy(transacoes.clienteId)`
5. **Alertas abertos** — `select({ clienteId, total: sql<number>\`count(*)::int\` }).from(alertas).where(and(eq(alertas.status, 'novo'), isNotNull(alertas.clienteId))).groupBy(alertas.clienteId)`
6. **Investimento 30d** — `select({ clienteId: adAccounts.clienteId, total: sql<string>\`sum(${campaignInsights.spend})\` }).from(campaignInsights).innerJoin(adAccounts, eq(campaignInsights.adAccountId, adAccounts.id)).where(and(gte(campaignInsights.date, ha30dias), lte(campaignInsights.date, hoje), isNotNull(adAccounts.clienteId))).groupBy(adAccounts.clienteId)`

Fecha com `return montarLinhas(base, mensalidades, inicios, ltvs, alertasPorCliente, investimentos, hoje)` — nenhuma matematica mora aqui, so I/O.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx vitest run src/lib/clientes/agregar.test.ts && npx tsc --noEmit && ! grep -q "Promise.all" src/lib/clientes/lista.ts && ! grep -q "new Date()" src/lib/clientes/lista.ts && echo "OK: sem Promise.all e sem new Date()"</automated>
  </verify>
  <done>14 testes passam; `getClientesLista()` faz 6 queries sequenciais agregadas (grep confirma ausencia de `Promise.all` e de `new Date()` em lista.ts), devolve `[]` em erro; `agregar.ts` nao importa db/react; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 3: UI — pagina server enxuta + lista compacta com busca, abas e linha clicavel</name>
  <files>src/app/(app)/clientes/page.tsx, src/app/(app)/clientes/clientes-lista.tsx, src/components/cliente-card.tsx</files>
  <action>
**A) `src/app/(app)/clientes/page.tsx`** — reescrever como Server Component enxuto:
```tsx
import { getClientesLista } from '@/lib/clientes/lista'
import { ClientesLista } from './clientes-lista'

export const maxDuration = 60   // manter o backstop existente

export default async function ClientesPage() {
  const clientes = await getClientesLista()
  return (
    <div className="space-y-8">
      <div className="flex items-center justify-between gap-4">
        <h1 className="text-[28px] leading-tight font-semibold">Clientes</h1>
        <Button asChild><Link href="/clientes/novo">Cadastrar Cliente</Link></Button>
      </div>
      <ClientesLista clientes={clientes} />
    </div>
  )
}
```
Remover os imports de `getContratosDoCliente`, `ClienteCard`, `db` e o `Promise.all` (o N+1 morre aqui).

**B) `src/app/(app)/clientes/clientes-lista.tsx`** — novo, `'use client'`. Props: `{ clientes: ClienteLinha[] }`.
- Estado: `const [busca, setBusca] = useState('')`, `const [aba, setAba] = useState<ClienteStatus | 'todos'>('ativo')`.
- `const contagens = useMemo(() => contarPorStatus(clientes), [clientes])` e `const visiveis = useMemo(() => filtrarClientes(clientes, { busca, aba }), [clientes, busca, aba])` — importados de `@/lib/clientes/agregar` (puro, seguro no client).
- **Busca**: `<Input placeholder="Buscar cliente..." value={busca} onChange={...} className="max-w-sm" />` com icone `Search` do lucide-react se couber no padrao. Filtro 100% client (≈45 clientes).
- **Abas**: `Tabs` do shadcn com `value={aba} onValueChange={setAba}`. `TabsList` gerada de `STATUS_ORDEM.map(...)` + a aba final `todos`. Rotulo com contador: `{STATUS_LABEL[s]} ({contagens[s]})`, e `Todos ({contagens.todos})`. Ordem EXATA: Ativos · Aguardando Inicio · Em Aviso · Pausados · Inativos · Todos. Renderizar a tabela UMA vez abaixo do `TabsList` (a lista ja e filtrada por `aba`) — nao duplicar a tabela em 6 `TabsContent`.
- **Tabela**: `Card` envolvendo um `<div className="overflow-x-auto">` com o `Table` do shadcn (mesmo padrao de `src/app/(app)/financeiro/contas-table.tsx`). Colunas, nesta ordem: Nome · Status · Alertas · Responsavel · Mensalidade · LT · LTV · Invest. (30d) · (⋯ sem header).
  - Nome: `font-medium`, `truncate`.
  - Status: `<Badge>` com as cores semanticas do projeto — mapa local no arquivo:
    `ativo` → `bg-chart-success text-white`; `aguardando_inicio` → `bg-primary text-primary-foreground`; `em_aviso` → `bg-chart-warning text-white`; `pausado` → `bg-muted text-muted-foreground`; `encerrado` → `variant="destructive"`.
    Rotulo singular no badge (Ativo / Aguardando Inicio / Em Aviso / Pausado / Inativo) — o plural fica so nas abas.
  - Alertas: se `alertasAbertos > 0`, badge/numero em `text-destructive` com `font-medium`; se 0, `—` em `text-muted-foreground`.
  - Responsavel: `responsavelNome ?? '—'`.
  - Mensalidade / LTV / Invest.: `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`, `className="tabular-nums"`, alinhados a direita (`text-right` no `TableHead` e no `TableCell`).
  - LT: `{ltDias}d`, `tabular-nums`.
- **Linha clicavel**: `const router = useRouter()`; `<TableRow onClick={() => router.push(\`/clientes/${c.id}\`)} className="cursor-pointer hover:bg-muted/40">`. Acessibilidade minima: `tabIndex={0}` + `onKeyDown` disparando o mesmo push em Enter.
- **Menu ⋯**: `DropdownMenu` do shadcn na ultima celula. **CRITICO**: `<TableCell onClick={(e) => e.stopPropagation()}>` envolvendo o trigger, para o ⋯ nao disparar a navegacao da linha. Item: "Editar" → `router.push(\`/clientes/${c.id}/editar\`)`.
- **Estados vazios** (dois casos distintos, dentro do Card):
  - `clientes.length === 0` → bloco `rounded-xl border border-dashed p-12 text-center` com "Nenhum cliente cadastrado ainda" + botao "Cadastrar Cliente" (preserva a copy atual da pagina).
  - `clientes.length > 0 && visiveis.length === 0` → "Nenhum cliente encontrado com esses filtros."

**C) Deletar `src/components/cliente-card.tsx`** — a pagina era seu unico consumidor. Confirmar com `grep -rn "cliente-card\|ClienteCard" src/` (deve voltar vazio) ANTES de remover.

NAO alterar `src/app/(app)/clientes/[id]/page.tsx` (a ficha) nem `.../[id]/editar/page.tsx` — a lista so navega para elas.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && ! grep -rq "ClienteCard" src/ && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>/clientes renderiza lista compacta com busca, 6 abas com contador na ordem pedida e linha clicavel; ⋯ nao dispara a navegacao da linha; cliente-card.tsx removido sem referencias orfas; `npm run build`, `npx tsc --noEmit` e `npx vitest run` passam.</done>
</task>

</tasks>

<verification>
Verificacao final OBRIGATORIA (nao concluir com build quebrado):
```bash
npx tsc --noEmit     # sem erros de tipo (inclui os Records exaustivos de status)
npx vitest run       # todos os testes, incluindo os 14 novos de agregar.test.ts
npm run build        # build de producao limpo
```
Checagens de regressao especificas:
- `grep -n "Promise.all" src/lib/clientes/lista.ts` => vazio (pool max=3)
- `grep -n "new Date()" src/lib/clientes/lista.ts` => vazio (fuso: so hojeBrasilia())
- `git diff --stat` NAO inclui: `src/lib/alertas/*`, `src/lib/ai/copilot.ts`, `src/app/(app)/dashboard/*`, `src/app/(app)/financeiro/*`, `src/app/(app)/relatorios/*` — a semantica de `status='ativo'` nas outras telas fica intacta.
- Migration: `drizzle/0014_*.sql` existe no repo e o banco NAO foi tocado (nenhum `drizzle-kit push`/`migrate` foi executado).
- Commit com arquivos ESPECIFICOS (`git add <paths>`), NUNCA `git add -A`.
</verification>

<success_criteria>
- Enum `cliente_status` com 5 valores no schema + migration 0014 aditiva GERADA e NAO aplicada
- Form de edicao oferece os 5 status; Zod aceita os 5
- /clientes = lista compacta (1 linha/cliente) com busca por nome e abas com contador (Ativos · Aguardando Inicio · Em Aviso · Pausados · Inativos · Todos)
- Colunas: Nome · Status · Alertas · Responsavel · Mensalidade · LT · LTV · Invest. (30d) · ⋯
- Linha navega para /clientes/[id]; ⋯ navega para /clientes/[id]/editar sem propagar o clique
- `getClientesLista()`: 6 queries agregadas SEQUENCIAIS, sem N+1, sem Promise.all, nunca lanca
- Logica pura extraida e coberta por 14 testes vitest
- Consultas de `status='ativo'` das outras telas inalteradas
- build + tsc + vitest verdes
</success_criteria>

<output>
Ao concluir, criar `.planning/quick/260714-j92-clientes-lista-compacta-com-abas-por-sta/260714-j92-SUMMARY.md`.

Registrar OBRIGATORIAMENTE no SUMMARY:
1. **Decisao**: os status novos (`aguardando_inicio`, `em_aviso`) sao ADITIVOS. As consultas existentes que filtram `status='ativo'` (dashboard, financeiro, relatorios, alertas) NAO foram alteradas — mudar a semantica de "cliente ativo" agora alteraria numeros de outras telas sem o usuario pedir. Fica como decisao explicita a revisitar quando o CRM entrar.
2. **Pendencia**: `drizzle/0014_*.sql` GERADA mas NAO aplicada — o orquestrador aplica de forma controlada. Ate la, gravar um cliente com os status novos falha no banco.
3. **Padrao reforcado**: funcao de dados = poucas queries agregadas sequenciais + merge em memoria via modulo puro testado (pool max=3, max_pipeline=0). Mesmo padrao do quick 260714-ita.
4. Nome exato do arquivo de migration gerado e arquivos removidos (`src/components/cliente-card.tsx`).
</output>
</content>
