---
phase: quick-260715-h9z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0022_crm_ficha_lead.sql
  - scripts/aplicar-migration-0022.ts
  - src/lib/db/schema.ts
  - src/lib/crm/dados.ts
  - src/lib/validations/crm.ts
  - src/actions/crm-lead.ts
  - src/actions/crm-tags.ts
  - src/actions/crm-atividades.ts
  - src/components/crm/card-oportunidade.tsx
  - src/components/crm/ficha-lead.tsx
  - src/components/crm/criar-atividade-dialog.tsx
  - src/components/crm/kanban-crm.tsx
autonomous: true
requirements: [QUICK-H9Z]
must_haves:
  truths:
    - "Card do Kanban segue a imagem03: avatar com inicial, nome, linha azul 'Serviço - [ORIGEM]', #N, linhas atendente/valor/data/atividades, tags no rodapé, SEM badge de mensalidade"
    - "Botão WhatsApp no card abre https://wa.me/55<dígitos> em nova aba sem disparar drag nem abrir a ficha; oculto quando o lead não tem telefone"
    - "Clicar no card abre a ficha completa do lead (painel esquerdo + painel direito com abas Histórico/Atividades/Informações do Negócio)"
    - "Foto do lead pode ser enviada (lápis sobre o avatar) e persiste em foto_url via Supabase Storage"
    - "Nome editável inline, notas salvam, atendente selecionável, métricas e notas recolhíveis, abas Perfil/Endereço/Campos adicionais mostram o cadastro"
    - "Histórico mostra a linha do tempo real (lead criado, negócio #N criado, tags adicionadas, mudança de etapa, ganho/perda)"
    - "Aba Atividades lista atividades por dia e o modal 'Criar atividade' (igual cria_atividade.png) cria uma atividade com início/fim, tipo e prioridade"
    - "Aba Informações do Negócio mostra #N, valor total, data de criação e a Pipeline Completa com a etapa atual marcada"
  artifacts:
    - path: "drizzle/0022_crm_ficha_lead.sql"
      provides: "foto_url em crm_contatos + data_inicio/data_fim/prioridade/descricao em crm_tarefas + bucket/policies de fotos"
    - path: "src/actions/crm-atividades.ts"
      provides: "criarAtividadeCrm e listagem de atividades do lead"
    - path: "src/components/crm/ficha-lead.tsx"
      provides: "Ficha completa em dois painéis (reformulação da existente)"
    - path: "src/components/crm/card-oportunidade.tsx"
      provides: "Card estilo imagem03 com botão WhatsApp e tags"
  key_links:
    - from: "src/components/crm/card-oportunidade.tsx"
      to: "wa.me"
      via: "onClick com stopPropagation + onPointerDown stopPropagation (não vira drag)"
      pattern: "wa\\.me/55"
    - from: "src/components/crm/ficha-lead.tsx"
      to: "src/actions/crm-lead.ts"
      via: "getFichaLead ampliada (tags, dono, foto, atividades, timeline)"
      pattern: "getFichaLead"
    - from: "src/actions/crm-lead.ts"
      to: "@/lib/storage/client"
      via: "uploadFile para a foto do lead"
      pattern: "uploadFile"
---

<objective>
Reformular o card do lead no Kanban do CRM (fiel à imagem03, com botão WhatsApp e tags) e transformar a ficha do lead existente (Sheet simples) na ficha completa de dois painéis das imagens 04-06, com upload real de foto, edição inline, histórico, atividades agendáveis (modal cria_atividade.png) e visão do negócio com jornada da pipeline.

Purpose: o CRM lead-first (quicks 260715-e8w/gmf/1rq) já tem backend e Kanban; falta a experiência de trabalho diário sobre o lead — contato rápido (WhatsApp), contexto completo (ficha) e agenda (atividades).
Output: card novo, ficha nova, migration 0022 aplicada, actions de atividades e foto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@Imagens_referencia_CRM/imagem03.png (card — referência OBRIGATÓRIA)
@Imagens_referencia_CRM/imagem04.png
@Imagens_referencia_CRM/imagem05.png
@Imagens_referencia_CRM/imagem06.png
@Imagens_referencia_CRM/cria_atividade.png (modal Criar atividade)
@src/components/crm/card-oportunidade.tsx
@src/components/crm/ficha-lead.tsx
@src/components/crm/kanban-crm.tsx
@src/components/crm/tags-select.tsx
@src/actions/crm-lead.ts
@src/actions/crm-tags.ts
@src/actions/documentos.ts (padrão de upload com @/lib/storage/client)
@src/lib/crm/dados.ts (getCrmVisaoGeral, OportunidadeCard)
@src/lib/db/schema.ts (crmContatos l.543, crmTags l.584, crmTarefas l.664, crmAtividades l.688)
@src/lib/validations/crm.ts

<interfaces>
Fatos do código que MUDAM o escopo em relação ao pedido literal:

1. **`crm_eventos` NÃO precisa existir** — `crm_atividades` (schema.ts l.688) JÁ é a tabela de timeline
   (tipos: criacao, contato_criado, mudanca_etapa, ganho, perda, reabertura, lead_recebido,
   tarefa_criada, tarefa_concluida), já alimentada por criarLead/mover/ganhar/perder via
   `registrarAtividadeCrm` (src/lib/crm/atividades.ts, helper server NÃO exportado de 'use server').
   Falta apenas registrar o evento de **tag adicionada/removida** (crm-tags.ts / vínculo em criarLead).

2. **Atividades agendáveis NÃO precisam de tabela nova** — `crm_tarefas` (l.664) já tem
   titulo, tipo, notas, dataVencimento, concluida, donoId, oportunidadeId, contatoId.
   Faltam colunas: `data_inicio timestamptz`, `data_fim timestamptz`, `prioridade text`
   (baixa|media|alta). Manter dataVencimento preenchida (= data_fim) para não quebrar a
   heurística "sem contato +7d" de getCrmVisaoGeral.

3. **Storage** — já existe `@/lib/storage/client` com `uploadFile/getSignedUrl/deleteFile`
   (usado por src/actions/documentos.ts). Seguir esse padrão para a foto.

4. **Ficha existente** — `ficha-lead.tsx` é um Sheet com abas Perfil/Negócios/Histórico.
   REFORMULAR este arquivo (não criar outro). `getFichaLead` em crm-lead.ts já devolve
   perfil + negócios + histórico (3 queries sequenciais) — ampliar, não duplicar.

5. **Card** — `OportunidadeCard` (dados.ts l.24) não tem: telefoneNormalizado, tags, número #N.
   Ampliar o select de getCrmVisaoGeral. #N = `row_number() over (partition by workspace
   order by created_at)` por oportunidade (sem migration; número estável por ordem de criação).

6. **Regras do repo**: actions `{data}|{error}` iniciando por getCurrentUser()+getWorkspaceAtual();
   queries SEQUENCIAIS (pool max=3, nunca Promise.all dentro da mesma função); dinheiro nunca
   float (numeric → String no insert, Number na leitura); tudo em português; shadcn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infra e backend — migration 0022 aplicada na mão, colunas novas, actions de foto/atividades/eventos de tag e dados ampliados</name>
  <files>drizzle/0022_crm_ficha_lead.sql, scripts/aplicar-migration-0022.ts, src/lib/db/schema.ts, src/lib/crm/dados.ts, src/lib/validations/crm.ts, src/actions/crm-lead.ts, src/actions/crm-tags.ts, src/actions/crm-atividades.ts</files>
  <action>
    1. **Schema + migration 0022 (SQL manual, sem drizzle-kit generate se preferir escrever à mão — mas o SQL deve ficar em drizzle/0022_crm_ficha_lead.sql):**
       - `crm_contatos`: `foto_url text` (nullable).
       - `crm_tarefas`: `data_inicio timestamptz`, `data_fim timestamptz`, `prioridade text` (nullable; valores 'baixa'|'media'|'alta' validados no Zod, não em CHECK).
       - Bucket de fotos: `insert into storage.buckets (id, name, public) values ('crm-fotos','crm-fotos', true) on conflict (id) do nothing;` + policy de leitura pública e de escrita para authenticated (`create policy ... on storage.objects for insert to authenticated with check (bucket_id = 'crm-fotos')`). Bucket PÚBLICO de propósito: avatar não pode depender de signed URL que expira; guardar a URL pública em foto_url.
       - Refletir as colunas em src/lib/db/schema.ts.
    2. **Aplicar NA MÃO (memória do projeto — NUNCA `drizzle-kit migrate`):** script Node pontual `scripts/aplicar-migration-0022.ts` lendo `DIRECT_URL`, separando por `--> statement-breakpoint`, dentro de `sql.begin()`. ANTES de aplicar, conferir o estado real via information_schema (coluna foto_url já existe? bucket já existe?). Rodar com `npx tsx --env-file=.env.local scripts/aplicar-migration-0022.ts`. Se as policies em storage falharem por permissão, deixar o restante aplicado e reportar no SUMMARY as instruções SQL para o editor do Supabase (fallback documentado, não silencioso).
    3. **getCrmVisaoGeral / OportunidadeCard (src/lib/crm/dados.ts):** adicionar ao card `telefoneNormalizado`, `numero` (#N via `row_number() over (order by created_at)` por workspace no select de oportunidades) e `tags: {id, nome, cor}[]` do CONTATO — uma query agregada extra SEQUENCIAL (join crm_contato_tags × crm_tags pelos contatoIds do kanban, merge em memória), seguindo o padrão de queries agregadas existente. Não aumentar o número de queries por oportunidade.
    4. **getFichaLead (crm-lead.ts) ampliada:** devolver também `fotoUrl`, `donoId`/`donoNome` do contato, `tags` do contato, `atendentes` disponíveis (reusar getProfiles se existir ou query em profiles), `atividades` (crm_tarefas do contato ou de seus negócios, ordenadas por data_inicio/dataVencimento) e, por negócio, `etapaId` + lista de etapas do pipeline (`{id, nome, ordem}`) para desenhar a Pipeline Completa. Métricas: ticket médio = média dos valores dos negócios ganhos; total = soma dos ganhos; ciclo de compra = média em dias entre createdAt e ganhaEm dos ganhos; última compra = dias desde a ganhaEm mais recente — tudo derivado do que a query de negócios já traz (calcular em TS, zerado quando não há ganho).
    5. **Actions novas (padrão {data}|{error}):**
       - `atualizarFotoLead(contatoId, formData)` em crm-lead.ts: valida imagem (tipo image/*, máx ~2MB), `uploadFile` no bucket crm-fotos com path `leads/{contatoId}.{ext}`, monta a URL pública (`${SUPABASE_URL}/storage/v1/object/public/crm-fotos/...`), salva em foto_url, revalidatePath('/crm').
       - `renomearLead(contatoId, nome)` e `salvarNotasLead(contatoId, notas)` e `atualizarAtendenteLead(contatoId, donoId)`: updates parciais pequenos (a edição inline não pode exigir o form inteiro do perfil).
       - `src/actions/crm-atividades.ts` (arquivo novo): `criarAtividadeCrm(input)` validado por `atividadeSchema` novo em validations/crm.ts (titulo obrigatório, donoId, contatoId, oportunidadeId opcional, dataInicio/dataFim ISO com fim > início, tipo dentre ligacao|whatsapp|email|reuniao|followup|outro, prioridade opcional, descricao → coluna notas). Inserir em crm_tarefas com dataVencimento = dataFim; registrar `tarefa_criada` via registrarAtividadeCrm. Também `concluirAtividadeCrm(id)` marcando concluida + evento `tarefa_concluida` (botão simples na lista).
    6. **Eventos de tag no Histórico:** em crm-tags.ts (vincular/desvincular) e no passo 4.5 do criarLead, registrar via registrarAtividadeCrm tipo `tag_adicionada`/`tag_removida` com detalhe = nome da tag. Adicionar os rótulos no ROTULO_ATIVIDADE da ficha.
    7. Rodar `npx vitest run` — se houver testes de dados/validações afetados, atualizar.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Migration 0022 APLICADA no banco (conferida via information_schema), bucket crm-fotos existente (ou fallback documentado), OportunidadeCard com telefoneNormalizado/numero/tags, getFichaLead devolvendo foto/tags/dono/atividades/etapas/métricas, actions de foto, edição parcial e atividades funcionando, eventos de tag no histórico.</done>
</task>

<task type="auto">
  <name>Task 2: Card do Kanban fiel à imagem03 com botão WhatsApp e tags</name>
  <files>src/components/crm/card-oportunidade.tsx, src/components/crm/kanban-crm.tsx</files>
  <action>
    Reescrever o layout do CardOportunidade seguindo a imagem03 (manter dnd-kit e o contrato onAbrirFicha):
    - **Cabeçalho:** avatar circular com a inicial do nome (fundo suave, tipo emerald claro como na referência; se houver fotoUrl no card futuramente não é escopo — só inicial), nome do lead em semibold; logo abaixo do nome, linha AZUL pequena `"{rotuloServico} - [{rotuloOrigem em maiúsculas}]"` (ex.: "Tráfego Pago - [META ADS]"); `#{numero}` no canto superior direito em text-muted.
    - **Linhas com ícones (lucide), uma por linha como na imagem:** User → atendente ("Sem atendente" quando vazio), ícone de dinheiro → valor formatado em BRL (ocultar linha se null), Calendar → data de criação em dd/MM/yyyy, ícone de relógio/atividade → "Sem atividades" quando qtdAtividades = 0, senão "{n} atividade(s)".
    - **REMOVER** o badge de tipoReceita (mensalidade/projeto) e a constante ROTULO_RECEITA.
    - **Rodapé:** badges das tags do lead (cores do sistema CORES_TAG, mesmas classes usadas em tags-select.tsx) — se não houver tags, rodapé não renderiza.
    - **Botão WhatsApp** alinhado à direita (como na imagem, ícone na lateral): renderizar apenas se `telefoneNormalizado` existir. Usar o glifo do WhatsApp (SVG inline pequeno — lucide não tem ícone oficial do WhatsApp; incluir o path do logo). onClick: `e.stopPropagation()` + `window.open('https://wa.me/55' + digitos, '_blank', 'noopener')` onde digitos = telefoneNormalizado só com dígitos (se o normalizado já incluir 55, não duplicar — verificar normalizarTelefone e tratar). CRÍTICO para não brigar com o drag: além do stopPropagation no click, adicionar `onPointerDown={(e) => e.stopPropagation()}` no botão — os listeners do useDraggable ficam no container e o sensor por distância dispara no pointerdown.
    - Preservar: aviso "Não contatado", motivo da perda no card perdido, estilo fantasma `arrastando`.
    - Manter os avisos/comentários em português explicando as decisões (padrão do arquivo).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Card idêntico em estrutura à imagem03: avatar+nome+linha azul+#N, 4 linhas com ícones, tags no rodapé, sem badge de mensalidade; WhatsApp abre wa.me em nova aba sem arrastar o card nem abrir a ficha; oculto sem telefone.</done>
</task>

<task type="auto">
  <name>Task 3: Ficha completa do lead (imagens 04-06) + modal Criar atividade (cria_atividade.png)</name>
  <files>src/components/crm/ficha-lead.tsx, src/components/crm/criar-atividade-dialog.tsx</files>
  <action>
    REFORMULAR ficha-lead.tsx (substituir o Sheet estreito) num painel largo de dois painéis — usar Sheet com `sm:max-w-4xl`/`w-full` ou Dialog largo, o que ficar melhor com o registry atual (dialog.tsx já existe desde o quick gmf). Layout: grid de 2 colunas (esquerda ~340px, direita flex) com scroll independente.

    **Painel ESQUERDO (imagens 04-06):**
    - Topo com faixa de fundo suave + avatar circular grande centralizado: mostra fotoUrl (next/image ou img) ou a inicial; botão-lápis sobreposto (canto inferior direito do avatar) abre `<input type="file" accept="image/*">` oculto → envia FormData para atualizarFotoLead → toast + atualizar estado local.
    - Nome logo abaixo: clicar transforma em Input (estado local); salvar no blur/Enter via renomearLead; Escape cancela.
    - Tags do lead como badges + botão "+" que abre o seletor de tags existente (reusar TagsSelect/popover de tags-select.tsx) chamando as actions de crm-tags.
    - Linha "Atendente": Select com os profiles (dados vindos de getFichaLead), salvando via atualizarAtendenteLead; "Sem atendente" quando null.
    - Seção **Métricas** recolhível (chevron gira; useState, padrão Collapsible do shadcn ou div condicional): grid 2×2 com Ticket médio (BRL), Total (BRL), Ciclo de compra (Xd), Última compra (Xd) — valores vindos prontos de getFichaLead, zerados quando sem dados.
    - Seção **Notas** recolhível: Textarea com o valor de notas + salvar no blur via salvarNotasLead (indicador "Salvando...").
    - Abas **Perfil / Endereço / Campos adicionais** (Tabs shadcn): Perfil = Nome, Empresa, E-mail, Telefone (com bandeirinha 🇧🇷 + botão copiar via navigator.clipboard + toast), Documento, Origem, Site, Data de nascimento — reaproveitar o form RHF+zod existente (leadPerfilSchema/atualizarLead) já presente no arquivo, redistribuindo os campos; Endereço = CEP, endereço, número, complemento, bairro, cidade, estado, país; Campos adicionais = cargo e o que sobrar. NÃO incluir "Executar automação do lead".

    **Painel DIREITO — Tabs com 3 abas** (só as 3; nada de Negócios/Arquivos/Atendimentos placeholders):
    - **Histórico:** timeline vertical do que getFichaLead.historico já traz (agora com tags): ícone por tipo, rótulo em português (ROTULO_ATIVIDADE ampliado com tag_adicionada/tag_removida), detalhe, autor, tempo relativo. Agrupar visualmente por dia (cabeçalho dd/MM/yyyy).
    - **Atividades:** botão "+ Atividade" no topo direito abrindo o modal novo; lista agrupada por dia (data de início) com título, tipo, horário início-fim, prioridade (badge), atendente e checkbox/botão para concluir (concluirAtividadeCrm); vazio = "Nenhuma atividade. Crie a primeira."
    - **Informações do Negócio:** se o lead tem N negócios, seletor simples do negócio (default: o mais recente aberto). Três cards coloridos como na imagem04: Número (#N — usar o numero/posição por createdAt), Valor Total (BRL), Data de Criação (dd/MM/yyyy). Abaixo, **"Pipeline Completa"**: linha horizontal de etapas (etapas do pipeline vindas de getFichaLead + Ganho/Perdido no fim), círculos conectados, marcando a etapa atual (ou Ganho/Perdido conforme status) com destaque. SEM a seção "Produtos e Valores" (não existe catálogo de produtos — não inventar dado).

    **criar-atividade-dialog.tsx (novo, fiel a cria_atividade.png):** Dialog central com título "Criar atividade"; campos: Título (Input), Atendente (Select profiles, default usuário logado), lead exibido (nome fixo do contato) + Negócio (Select dos negócios do lead, opcional), Data/hora início e fim (inputs date + time; default hoje 13:00→13:30) com a duração calculada exibida ("30m"), Tipo de atividade (Select: Ligação/WhatsApp/E-mail/Reunião/Follow-up/Outro), Prioridade (Select: Baixa/Média/Alta), Descrição (Textarea), botão azul largo "Criar atividade". RHF + zodResolver com o atividadeSchema da Task 1; submit → criarAtividadeCrm → toast + recarregar a ficha (reusar o carregar() existente).

    Estados de carregando (Skeleton) e erro preservados. Tudo em português.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Ficha em 2 painéis fiel às imagens 04-06: foto com upload real, nome inline, tags, atendente, Métricas/Notas recolhíveis, 3 abas de cadastro à esquerda; Histórico/Atividades/Informações do Negócio à direita com Pipeline Completa marcando a etapa; modal Criar atividade igual à referência criando em crm_tarefas.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` e `npx vitest run` verdes; `npx next build` sem erro.
- Migration 0022 conferida no banco real (information_schema) ANTES e DEPOIS do script.
- Fluxo manual: criar lead → card na imagem03 (com tag e WhatsApp) → arrastar funciona e o botão WhatsApp não arrasta → abrir ficha → subir foto → renomear inline → criar atividade → ver no Histórico os eventos (lead criado, negócio #1, tag, atividade criada).
</verification>

<success_criteria>
- Card do Kanban visualmente equivalente à imagem03, sem badge de mensalidade, com tags e WhatsApp funcional.
- Ficha completa de dois painéis substituindo a antiga, sem placeholders falsos (sem Produtos e Valores, sem automação, sem Anexos).
- Nenhuma migration pendente ao final; drizzle-kit migrate nunca executado.
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260715-h9z-card-do-lead-estilo-imagem03-com-botao-w/260715-h9z-SUMMARY.md`
</output>
