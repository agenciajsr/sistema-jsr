---
phase: quick-260717-is7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/clientes/[id]/page.tsx
  - src/app/(app)/clientes/clientes-lista.tsx
  - src/components/crm/crm-view.tsx
  - src/components/trafego/abas-campanhas.tsx
  - src/components/trafego/tabela-niveis.tsx
  - src/components/trafego/acoes-do-dia.tsx
  - src/components/trafego/landing-clientes.tsx
  - src/components/agenda/agenda-calendario.tsx
  - src/components/crm/ficha-lead.tsx
autonomous: true
requirements: [MOBILE-01]
must_haves:
  truths:
    - "Em ~360-430px, nenhuma das páginas principais estoura horizontalmente (sem scroll lateral do body)"
    - "Header da ficha do cliente empilha título e botões em telas pequenas sem cortar o botão Excluir"
    - "Barras de abas (TabsList) com muitas abas rolam horizontalmente no mobile em vez de vazar"
    - "Calendário da Agenda é utilizável no mobile via scroll horizontal, sem alterar o desktop"
    - "Visual desktop permanece byte-idêntico em classes fora de breakpoints (só adições mobile-first ou wrappers)"
  artifacts:
    - path: "src/app/(app)/clientes/[id]/page.tsx"
      provides: "Header responsivo + TabsList com overflow"
    - path: "src/components/agenda/agenda-calendario.tsx"
      provides: "Grade grid-cols-7 com scroll horizontal no mobile"
    - path: "src/components/crm/ficha-lead.tsx"
      provides: "Sheet da ficha do lead rolável no mobile"
  key_links:
    - from: "TabsList estreitas"
      to: "padrão já existente em src/app/(app)/financeiro/page.tsx:271"
      via: "className=\"max-w-full justify-start overflow-x-auto\""
      pattern: "overflow-x-auto"
---

<objective>
Varredura de responsividade mobile (~360-430px) nas páginas principais do sistema, corrigindo os pontos concretos encontrados na exploração estática — SEM mudar o visual desktop (só adição de breakpoints Tailwind e wrappers).

Purpose: sistema utilizável no celular pela equipe da JSR.
Output: ajustes de classes/JSX nos arquivos listados; zero mudança de dados/queries/migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

**Estado real verificado na exploração (não retrabalhar o que já está OK):**
- shadcn `src/components/ui/table.tsx` já envolve toda tabela em `overflow-x-auto` — tabelas de /contratos, /financeiro, /clientes, lista CRM etc. NÃO precisam de wrapper extra.
- Kanban CRM (`kanban-crm.tsx:114`) e quadro de Tarefas (`tarefas-quadro.tsx:308`) já têm `overflow-x-auto` com colunas `shrink-0`.
- Grids de KPI do painel, financeiro, funil, contratos, verbas, kpis-crm, grade-kpis já têm breakpoints (`grid-cols-1 sm:... lg:...` ou `grid-cols-2 sm:...`).
- Dialogs shadcn já têm largura responsiva por padrão (`max-w-[calc(100%-2rem)]`); os grandes já têm `max-h-[85-92vh] overflow-y-auto`.
- Headers de /painel, /campanhas, /financeiro, /agenda, /funil, crm-view já usam `flex-wrap` ou `flex-col sm:flex-row`.

**Padrão de referência para TabsList estreita:** `src/app/(app)/financeiro/page.tsx:271` → `<TabsList className="max-w-full justify-start overflow-x-auto">`.

**Restrições (locked):**
- NÃO mudar visual desktop — só classes com prefixo de breakpoint, valores mobile-first equivalentes ou wrappers neutros.
- Texto pt-BR; dark mode intocado; só JSX/classes, zero dados/queries/migrations.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headers e TabsLists que estouram no mobile (clientes, CRM, campanhas)</name>
  <files>src/app/(app)/clientes/[id]/page.tsx, src/app/(app)/clientes/clientes-lista.tsx, src/components/crm/crm-view.tsx, src/components/trafego/abas-campanhas.tsx, src/components/trafego/tabela-niveis.tsx, src/components/trafego/acoes-do-dia.tsx</name>
  <action>
    1. `clientes/[id]/page.tsx:198` — header `flex items-start justify-between gap-4` NÃO empilha: trocar por `flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between`. No bloco de botões (`flex shrink-0 items-center gap-2`, ~linha 222) adicionar `flex-wrap` (3 botões: Drive/Editar/Excluir estouram em 360px).
    2. `clientes/[id]/page.tsx:314` — `<TabsList>` tem ~6 abas (inclui a nova aba Tarefas): aplicar o padrão do financeiro `className="max-w-full justify-start overflow-x-auto"`.
    3. `clientes/clientes-lista.tsx:107` — `<TabsList>` com 6 abas de status (Ativos/Aguardando Início/Em Aviso/Pausados/Inativos/Todos): mesmo padrão `max-w-full justify-start overflow-x-auto`.
    4. `crm-view.tsx:489` — `<TabsList>` (Kanban/Lista/Calendário): verificar se junto na mesma faixa há busca/botões sem wrap; aplicar `max-w-full overflow-x-auto` na TabsList e `flex-wrap` na faixa que a contém se ainda não tiver.
    5. `trafego/abas-campanhas.tsx:29`, `trafego/tabela-niveis.tsx:273`, `trafego/acoes-do-dia.tsx:81` — mesmas TabsLists sem overflow: aplicar `max-w-full justify-start overflow-x-auto`.
    Em todos os casos, NÃO remover classes existentes — apenas adicionar. Conferir visualmente pelo código que nada muda ≥ sm (overflow-x-auto é inerte quando cabe).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Header da ficha do cliente empilha no mobile; todas as TabsLists de 4+ abas têm overflow-x-auto; nenhuma classe desktop removida.</done>
</task>

<task type="auto">
  <name>Task 2: Calendário da Agenda, Sheet da ficha do lead e cards de campanhas no mobile</name>
  <files>src/components/agenda/agenda-calendario.tsx, src/components/crm/ficha-lead.tsx, src/components/trafego/landing-clientes.tsx</files>
  <action>
    1. `agenda-calendario.tsx:161` e `:175` — as duas grades `grid grid-cols-7` (cabeçalho dom–sáb + grade de dias) ficam com células ~48px em 360px, inutilizáveis. Envolver AMBAS num único wrapper `<div className="overflow-x-auto">` com um filho `<div className="min-w-[560px] sm:min-w-0">` contendo os dois blocos (assim cabeçalho e grade rolam juntos e alinhados). Em ≥sm nada muda (min-w-0 + sem overflow efetivo). Manter todas as classes internas intocadas (hoje/hover/dark do quick 260717-dyb).
    2. `ficha-lead.tsx:532-550` — `SheetContent` com `grid h-full min-h-0 grid-cols-1 md:grid-cols-[340px_1fr]` e DOIS filhos `overflow-y-auto`: no mobile (grid-cols-1) as duas linhas dividem a altura e cada painel rola separado num sheet estreito. Corrigir mobile-only: container vira `flex h-full min-h-0 flex-col overflow-y-auto md:grid md:overflow-visible md:grid-cols-[340px_1fr]` e os painéis ganham `md:min-h-0 md:overflow-y-auto` no lugar dos atuais `min-h-0 overflow-y-auto` (rolagem única empilhada no mobile; duas colunas com rolagens independentes preservadas no desktop). Verificar que o `border-r` do painel esquerdo vire `md:border-r` (no mobile empilhado usar `border-b`).
    3. `landing-clientes.tsx:127` — `grid grid-cols-3 gap-2` de mini-stats dentro do card: manter 3 colunas (são números curtos) mas garantir que os valores tenham `truncate`/`min-w-0` se ainda não tiverem, para nomes/valores longos não estourarem o card em 360px.
    Nenhuma alteração de dados, handlers ou textos.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Calendário rola horizontalmente no mobile com cabeçalho alinhado; ficha do lead rola como coluna única no mobile e mantém 2 painéis no desktop; testes existentes passam.</done>
</task>

<task type="auto">
  <name>Task 3: Varredura final de padrões residuais + build de verificação</name>
  <files>src/app/(app)/**/*.tsx, src/components/**/*.tsx (somente ajustes pontuais encontrados)</files>
  <action>
    1. Rodar greps de conferência final e corrigir SÓ o que aparecer fora dos itens já tratados:
       - `grep -rn --include='*.tsx' -E 'grid-cols-[3-9]' src | grep -vE 'sm:|md:|lg:|xl:'` → restam apenas os grid-cols-7 do calendário (já tratados) e casos legítimos; corrigir novos achados com breakpoint base menor.
       - `grep -rn --include='*.tsx' 'justify-between' src/app/(app)/*/page.tsx | grep -v 'flex-wrap\|flex-col'` → headers de página restantes sem empilhamento: adicionar `flex-wrap` onde houver título + 2+ controles.
       - `grep -rn --include='*.tsx' -E 'w-\[[3-9][0-9]{2}px\]' src | grep -v 'shrink-0'` → larguras fixas grandes fora de contêiner com scroll: envolver ou adicionar `max-w-full`.
    2. Não tocar em: `src/components/ui/**` (primitivos shadcn), páginas fora do escopo (equipe, usuarios, documentos, ferramentas, relatorios — só se o grep apontar estouro óbvio e a correção for 1 linha).
    3. Verificação final completa: `npx tsc --noEmit && npx vitest run && npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Greps de varredura não apontam padrões suspeitos não justificados; tsc, vitest e build passam sem erro novo.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sem erros
- `npx vitest run` — suíte completa verde (nenhum teste toca essas classes, mas garante zero regressão de import/JSX)
- `npm run build` compila todas as rotas
- Diff contém APENAS adições de classes com breakpoint/wrappers — nenhuma classe desktop removida ou valor alterado sem equivalente
</verification>

<success_criteria>
Páginas principais (/painel, /crm, /funil, /clientes + ficha, /contratos, /campanhas, /financeiro, /tarefas, /agenda) sem estouro horizontal em ~360-430px; visual desktop inalterado; build e testes verdes.
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260717-is7-responsividade-mobile-geral-nas-paginas-/260717-is7-SUMMARY.md`
</output>
