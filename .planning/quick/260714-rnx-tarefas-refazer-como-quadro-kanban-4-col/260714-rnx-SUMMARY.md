---
phase: quick-260714-rnx
plan: 01
subsystem: ui
tags: [tarefas, kanban, react, drizzle, postgres, tailwind, shadcn]

# Dependency graph
requires:
  - phase: quick-260714-qsy
    provides: "Engine de recorrência pura, materialização preguiçosa idempotente, tabelas tarefas/tarefa_checklist_items, actions de tarefa e checklist"
provides:
  - "/tarefas como QUADRO KANBAN de 4 colunas (Pendentes, Em Andamento, Concluídas, Não Feitas) com toolbar e barra de estatísticas"
  - "/tarefas/[id] como PÁGINA CHEIA com código TAR-XXXX copiável, grade de 8 campos inline, abas Detalhes/Checklists e card Notas"
  - "src/lib/tarefas/quadro.ts — módulo PURO do quadro (agrupamento, estatísticas, filtros, avatar, código, intervalo)"
  - "getTarefasDoPeriodo(inicio, fim) + getTarefa(id) substituindo getTarefasDoDia(dia)"
  - "Campos aditivos: descricao, etiquetas, tempo_estimado, data_inicio, codigo_num/codigo em tarefas; grupo em tarefa_checklist_items"
affects: [tarefas, dashboard, agenda]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Coluna GERADA no banco (codigo) a partir de identity (codigo_num) — sequência sem query extra e sem corrida"
    - "Server manda lista CRUA, client agrupa/filtra com módulo puro (espelha clientes-lista.tsx + agregar.ts)"
    - "Rosca de progresso como SVG inline — sem Recharts para donut de 1 valor"

key-files:
  created:
    - src/lib/tarefas/quadro.ts
    - src/lib/tarefas/quadro.test.ts
    - src/app/(app)/tarefas/tarefas-quadro.tsx
    - src/app/(app)/tarefas/tarefa-card.tsx
    - src/app/(app)/tarefas/nova-tarefa-sheet.tsx
    - src/app/(app)/tarefas/[id]/page.tsx
    - src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx
    - drizzle/0016_fixed_electro.sql
  modified:
    - src/lib/db/schema.ts
    - src/lib/tarefas/dados.ts
    - src/lib/validations/tarefa.ts
    - src/actions/tarefas.ts
    - src/app/(app)/tarefas/page.tsx
  deleted:
    - src/app/(app)/tarefas/tarefa-sheet.tsx
    - src/app/(app)/tarefas/tarefas-lista.tsx

key-decisions:
  - "Intervalo padrão = hoje..hoje+6 (7 dias a partir de hoje), NÃO semana calendário (D-01)"
  - "codigo = coluna gerada STORED a partir de codigo_num (identity) — escape hatch do D-04 NÃO foi necessário"
  - "As 4 colunas usam só a paleta existente: âmbar/azul/verde/cinza — zero cor nova (D-02)"
  - "Estatísticas do rodapé refletem o que está VISÍVEL pós-filtro (D-06)"
  - "Recorrência preservada no menu '...' do detalhe — zero regressão (D-09)"

patterns-established:
  - "Módulo puro por feature: quadro.ts é testável sem banco e importável pelo client (3º caso, após agregar.ts e calculos.ts)"
  - "Datas formatadas a partir da STRING 'YYYY-MM-DD', nunca via new Date() do browser"

requirements-completed: [TAR-KANBAN-01, TAR-KANBAN-02, TAR-KANBAN-03, TAR-KANBAN-04]

# Metrics
duration: 25min
completed: 2026-07-14
---

# Quick 260714-rnx: Tarefas como Quadro Kanban — Summary

Refez a UI de Tarefas: os "blocos por dia" rejeitados deram lugar a um quadro Kanban de 4 colunas e a uma página de detalhe cheia, com a engine de recorrência intacta e um módulo puro (`quadro.ts`) de 29 testes por trás de todo o cálculo.

## ⚠️ MIGRATION 0016 GERADA E **NÃO APLICADA**

`drizzle/0016_fixed_electro.sql` foi **apenas gerada** — nenhum `drizzle-kit push`/`migrate` rodou. Conteúdo verificado como **100% aditivo (zero DROP)**:

```sql
ALTER TABLE "tarefa_checklist_items" ADD COLUMN "grupo" text DEFAULT 'Checklist' NOT NULL;
ALTER TABLE "tarefas" ADD COLUMN "descricao" text;
ALTER TABLE "tarefas" ADD COLUMN "etiquetas" jsonb;
ALTER TABLE "tarefas" ADD COLUMN "tempo_estimado" text;
ALTER TABLE "tarefas" ADD COLUMN "data_inicio" date;
ALTER TABLE "tarefas" ADD COLUMN "codigo_num" integer NOT NULL GENERATED BY DEFAULT AS IDENTITY (...);
ALTER TABLE "tarefas" ADD COLUMN "codigo" text GENERATED ALWAYS AS ('TAR-' || lpad(codigo_num::text, 4, '0')) STORED;
CREATE UNIQUE INDEX "tarefas_codigo_idx" ON "tarefas" USING btree ("codigo");
```

**Ordem de aplicação:** a 0015 (que CRIA `tarefas`) também ainda não está aplicada. As duas rodam em ordem — a 0016 altera uma tabela que nasce na 0015, com zero linhas. Nada a fazer além de aplicar na sequência.

## Escape hatch do D-04: NÃO acionado

O `drizzle-kit generate` expressou a coluna gerada corretamente (`GENERATED ALWAYS AS ... STORED`), então **`codigo` permaneceu no schema** como coluna do banco. Não foi preciso removê-la nem derivar o código na UI. A função pura `codigoTarefa(n)` continua existindo como espelho/fallback e é usada no detalhe via `tarefa.codigo ?? codigoTarefa(tarefa.codigoNum)`.

## Tabela de verificação

| Verificação | Resultado |
|---|---|
| `npm run build` | ✅ Compiled successfully — `/tarefas` e `/tarefas/[id]` dinâmicas (ƒ) |
| `npx tsc --noEmit` | ✅ zero erros |
| `npx vitest run` | ✅ 166 testes / 11 arquivos passando (29 novos em `quadro.test.ts`) |
| `recorrencia.test.ts` | ✅ 26/26 passando — prova de que a engine não foi tocada |
| `git diff master -- recorrencia.ts` | ✅ VAZIO |
| `Promise.all` em `lib/tarefas/`, `actions/tarefas.ts`, `app/(app)/tarefas/` | ✅ zero ocorrências |
| `new Date()` em `dados.ts` | ✅ só em `updatedAt` (linha 281); "hoje" sempre via `hojeBrasilia()` |
| `DROP` na migration 0016 | ✅ zero |
| Migration aplicada? | ✅ NÃO (só gerada) |
| `tarefa-sheet.tsx` / `tarefas-lista.tsx` | ✅ deletados, zero referências órfãs |
| `getTarefasDoDia` | ✅ zero ocorrências em `src/` |
| `actions/checklist.ts` (ficha do cliente) | ✅ intocado |

## Tasks e commits

| Task | Nome | Commit |
|---|---|---|
| 1 (RED) | Testes da lógica pura do quadro | `8bf0335` |
| 1 (GREEN) | Módulo puro + campos aditivos + migration 0016 | `37aeec7` |
| 2 | `getTarefasDoPeriodo` + `getTarefa` + actions | `934eabe` |
| 3 | O QUADRO KANBAN (mockup 1) | `4fb93c1` |
| 4 | Página de detalhe `/tarefas/[id]` (mockup 2) | `b96a2a9` |

## Desvios do plano

### 1. [Rule 3 - Blocante] Ordem das tasks deixa o `tsc` quebrado no fim da Task 2

- **Encontrado em:** Task 2
- **Problema:** a Task 2 remove `getTarefasDoDia`, mas os 3 arquivos que ainda o importavam (`page.tsx`, `tarefa-sheet.tsx`, `tarefas-lista.tsx`) só são deletados/reescritos na Task 3. O gate `npx tsc --noEmit` da Task 2 não tinha como passar.
- **Decisão:** commitei a Task 2 com o `tsc` quebrado **apenas nesses 3 arquivos condenados**, verificando explicitamente que nenhum erro existia fora deles, e registrei isso na mensagem do commit. A Task 3 deixou o `tsc` 100% limpo.
- **Commit:** `934eabe`

### 2. [Rule 1 - Bug] Comentários obsoletos citando `getTarefasDoDia`

- **Encontrado em:** Task 3 (gate de referências órfãs)
- **Problema:** dois comentários em `src/actions/tarefas.ts` ainda citavam `getTarefasDoDia`, que deixou de existir — o gate acusou corretamente.
- **Correção:** atualizados para `getTarefasDoPeriodo`.
- **Commit:** `4fb93c1`

### 3. [Rule 1 - Bug] `tarefa.ehMoldeAviso` inexistente no AlertDialog

- **Encontrado em:** Task 4
- **Problema:** referenciei um campo que não existe no tipo `TarefaDetalhe` ao montar o texto de confirmação de exclusão.
- **Correção:** trocado por um aviso real derivado de `tarefa.tarefaMaeId` — ao excluir uma ocorrência, o texto explica que a série continua e aponta "Encerrar recorrência".
- **Commit:** `b96a2a9`

### 4. [Rule 3 - Blocante] Gate do D-12 acusava os próprios comentários

- **Encontrado em:** Task 4
- **Problema:** o gate `! grep -rniE "anexos|comentarios|atividade recente"` reprovava por causa de comentários MEUS que diziam justamente que essas features **não** existem — a mesma armadilha que o plano já documentava para `Promise.all`.
- **Correção:** comentários reescritos com fraseado equivalente ("arquivos, discussões, histórico") sem as palavras literais. Zero UI dessas features foi construída em momento algum.
- **Commit:** `b96a2a9`

### 5. Setup do worktree (fora do plano)

O worktree estava em `d0d1502` (atrás do master) e sem `node_modules`. Resolvido com `git merge --ff-only master` (ancestral limpo → `ba170cb`) + `npm install`. Sem impacto no código.

## Fora do escopo — para a V2 (D-12)

Nada disso foi construído, e **nenhum botão/aba morto foi deixado na UI** apontando para eles:

- **Anexos** (upload de arquivos na tarefa)
- **Comentários** / discussão na tarefa
- **Atividade Recente** (histórico de alterações)
- **Drag-and-drop entre colunas** — hoje o status muda pelo Select do detalhe, pelo checkbox do card ou ao criar a tarefa já na coluna
- **"Compartilhar" / link do mockup 2** — omitido de propósito (seria botão morto)

## Notas para quem pegar isto depois

- **`recorrencia.ts` é intocável** e continua sendo: toda a regra de calendário mora lá, com 26 testes.
- **`quadro.ts` é PURO** (zero import de db/react/next) — é o 3º módulo desse padrão no repo, junto de `clientes/agregar.ts` e `financeiro/calculos.ts`. Toda lógica nova do quadro vai aí, não no componente.
- **Nada de `Promise.all`** em `lib/tarefas/`, `actions/tarefas.ts` e `app/(app)/tarefas/`: o pool é max=3 com max_pipeline=0 e queries paralelas penduram para sempre. Ao comentar sobre isso, use o fraseado do repo ("nada de paralelizar com Promise") — a literal reprova o próprio gate.
- **O bloco "Atrasadas" morreu.** Tarefas atrasadas aparecem naturalmente em Pendentes quando caem no intervalo; ocorrências abertas de dias passados viram `nao_realizada` pela varredura (que continua igual).

## Self-Check: PASSED

- **8/8 arquivos criados** existem em disco (incl. `quadro.ts` com 249 linhas, acima do mínimo de 120).
- **2/2 arquivos rejeitados** confirmados deletados.
- **5/5 commits** existem no histórico (`8bf0335`, `37aeec7`, `934eabe`, `4fb93c1`, `b96a2a9`).
- **Zero stubs**: nenhum dado mockado, vazio-hardcoded ou placeholder. Toda a UI é alimentada por `getTarefasDoPeriodo`/`getTarefa`; todo botão renderizado é funcional (D-12 omitiu o que não seria).
- `git status` limpo.
