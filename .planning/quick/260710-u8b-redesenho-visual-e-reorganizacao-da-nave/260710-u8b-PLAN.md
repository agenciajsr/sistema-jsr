---
phase: quick
plan: 260710-u8b
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/app-sidebar.tsx
  - src/app/(app)/layout.tsx
  - src/lib/mock/dashboard.ts
  - src/app/globals.css
  - src/app/(app)/painel/page.tsx
autonomous: true
requirements: [REDESIGN-NAV, REDESIGN-PAINEL]

must_haves:
  truths:
    - "O menu de topo mostra 6 áreas (Painel, Clientes, Tráfego & Performance, Financeiro da Agência, Relatórios, Funil) + Usuários (admin)"
    - "Contratos, Checklist, Acompanhamento, Verbas Ads e Alertas NÃO aparecem mais na sidebar, mas as rotas continuam acessíveis por URL"
    - "Um sino no header linka para /alertas com badge de contagem"
    - "O Painel abre com saudação + resumo do dia, 4 KPIs em cards, bloco destacado 'Precisa de você hoje' com severidade colorida, saúde das contas com ponto de status colorido e um gráfico de barras de verba dos últimos 7 dias"
  artifacts:
    - path: "src/components/app-sidebar.tsx"
      provides: "Navegação de 6 áreas + Usuários admin-only"
    - path: "src/app/(app)/layout.tsx"
      provides: "Sino de alertas no header com badge"
    - path: "src/lib/mock/dashboard.ts"
      provides: "Dados de verba diária (7 dias), contagem de contas por cliente e 3 níveis de status"
    - path: "src/app/globals.css"
      provides: "Tokens de fundo suave por severidade (light + dark)"
    - path: "src/app/(app)/painel/page.tsx"
      provides: "Painel redesenhado com KPIs, alertas destacados, saúde das contas e gráfico de barras"
  key_links:
    - from: "src/app/(app)/painel/page.tsx"
      to: "src/lib/mock/dashboard.ts"
      via: "import de verbaDiariaMock/clientesTrafegoMock/alertasMock/financeiroMock"
    - from: "src/app/(app)/painel/page.tsx"
      to: "src/app/globals.css"
      via: "classes bg-alert-danger-soft / bg-alert-warning-soft"
---

<objective>
Incremento 1 do redesenho visual do Sistema JSR: enxugar a navegação de 12 itens
para 6 áreas de topo, mover Alertas para um sino no header e redesenhar o Painel
com direção visual aprovada (cor com significado, emojis nos títulos, KPIs,
alertas destacados por severidade, saúde das contas e gráfico de barras de verba).

Purpose: O dono da agência está insatisfeito com o design neutro e a organização.
Este incremento entrega o esqueleto de navegação limpo e o Painel como vitrine da
nova direção visual.

Output: Sidebar reorganizada, sino de alertas no header, mock estendido, tokens de
severidade em globals.css e Painel redesenhado. Todas as decisões abaixo são LOCKED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contratos existentes que o executor deve reutilizar diretamente. -->

StatCard (src/components/stat-card.tsx) — reutilizar como está, cores disponíveis:
```typescript
type StatCardColor = 'primary' | 'success' | 'warning' | 'danger'
StatCardProps = {
  label: string; value: string; icon: React.ComponentType<{ className?: string }>;
  color?: StatCardColor;
  trend?: { value: string; direction: 'up' | 'down'; positive?: boolean };
  helper?: string
}
```

MockNotice (src/components/mock-notice.tsx) — `<MockNotice>texto</MockNotice>`, manter no Painel.

Mock atual (src/lib/mock/dashboard.ts):
```typescript
ClienteTrafego = { id, nome, nicho, contaStatus: 'ativa'|'problema', verbaGasta, verbaTotal, ultimaSync, campanhas[] }
AlertaMock = { id, tipo, severidade: 'critico'|'atencao', titulo, cliente, detalhe, quando }
FinanceiroClienteMock = { cliente, mrr, diaCobranca, status: 'em dia'|'próximo' }
clientesTrafegoMock (3 clientes), alertasMock (3 alertas), financeiroMock (3), mrrHistoricoMock (6 meses)
```

Padrão de gráfico recharts já usado (src/app/(app)/financeiro/page.tsx) — usar shadcn
ChartContainer/ChartTooltip de '@/components/ui/chart', NÃO Chart.js:
```tsx
import { Bar, BarChart, CartesianGrid, XAxis } from 'recharts'
import { ChartConfig, ChartContainer, ChartTooltip, ChartTooltipContent } from '@/components/ui/chart'
const chartConfig = { verba: { label: 'Verba', color: 'var(--primary)' } } satisfies ChartConfig
// <ChartContainer config={chartConfig} className="h-[220px] w-full"><BarChart data={...}>...
```

Tokens de cor existentes em globals.css: --chart-success, --chart-warning, --chart-danger,
--chart-info, --primary, --destructive. Registrados em @theme inline como bg-chart-success etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganizar navegação da sidebar + sino de alertas no header</name>
  <files>src/components/app-sidebar.tsx, src/app/(app)/layout.tsx</files>
  <action>
LOCKED — implementar exatamente:

1. src/components/app-sidebar.tsx — substituir os três arrays de nav por UM grupo
   de topo com 6 áreas nesta ordem, mantendo o helper NavGroup e o padrão isActive
   existente (pathname === url || startsWith(`${url}/`)):
   - Painel → /painel → ícone LayoutDashboard (usar como 🏠)
   - Clientes → /clientes → ícone Users
   - Tráfego & Performance → /trafego → ícone TrendingUp (absorve /verbas-ads; aponta para /trafego)
   - Financeiro da Agência → /financeiro → ícone Wallet
   - Relatórios → /relatorios → ícone FileText
   - Funil → /funil → ícone Filter
   Usar um único NavGroup com label "Principal" (menu limpo). Manter o grupo
   "Administração" com Usuários (/usuarios, ShieldCheck) exatamente como hoje,
   dentro do `{isAdmin && ...}`.
   REMOVER do menu (rotas continuam existindo, NÃO tocar nas páginas): Contratos,
   Checklist, Acompanhamento, Verbas Ads, Alertas. Remover imports lucide que
   ficaram sem uso (FileSignature, CircleDollarSign, ListChecks, Radar, Bell) —
   deixar apenas os ícones efetivamente usados. Manter SidebarHeader/Footer intactos.

2. src/app/(app)/layout.tsx — adicionar um sino de alertas no header, à esquerda do
   bloco de usuário (dentro do `<div className="flex items-center gap-3">`, antes do
   `<div className="hidden text-right sm:block">`):
   - Importar `Link from 'next/link'`, `Bell from 'lucide-react'` e `alertasMock from '@/lib/mock/dashboard'`.
   - Renderizar um Link para /alertas com um botão-ícone (relativo) contendo o Bell
     (size-5) e um badge de contagem posicionado absoluto no canto superior direito:
     bolinha vermelha (bg-destructive text-destructive-foreground) com
     `alertasMock.length`, tamanho ~size-4, text-[10px], arredondada, só exibir se > 0.
     Ex.: `<Link href="/alertas" className="relative inline-flex ..." aria-label="Alertas">`
     com o Bell e o `<span className="absolute -right-1 -top-1 ...">`.
   - O layout é async Server Component; alertasMock é import estático simples — ok usar direto.
   Não criar dropdown. Não quebrar o botão Sair nem o Avatar existentes.

Referência: task_intent LOCKED (decisões 1 e 2). Português em toda a UI.
  </action>
  <verify>
    <automated>cd "C:\Users\jacso\OneDrive\Documentos\projeto_agencia_jsr" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Sidebar mostra 6 áreas + Usuários (admin); Contratos/Checklist/Acompanhamento/Verbas Ads/Alertas fora do menu; header exibe sino com badge linkando para /alertas; tsc e lint sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Estender mock de dashboard + tokens de severidade em globals.css</name>
  <files>src/lib/mock/dashboard.ts, src/app/globals.css</files>
  <action>
Preparar os "contratos" de dados e cor que o Painel (Task 3) vai consumir. Mudanças MÍNIMAS.

1. src/lib/mock/dashboard.ts:
   - Ampliar o status de conta para 3 níveis: alterar o tipo `contaStatus` de
     ClienteTrafego para `'ativa' | 'atencao' | 'problema'`. Ajustar os 3 clientes:
     Clínica Vitalis = 'ativa' (verde), Método Cresce+ = 'atencao' (âmbar),
     Loja Aurora Moda = 'problema' (vermelho).
   - Adicionar campo `contas: number` em ClienteTrafego (nº de contas de anúncio do
     cliente): Clínica Vitalis = 2, Loja Aurora Moda = 2, Método Cresce+ = 1.
   - Adicionar export `verbaDiariaMock` — verba investida por dia nos últimos 7 dias
     (soma das contas), para o gráfico de barras:
     ```typescript
     export const verbaDiariaMock = [
       { dia: 'Qui', valor: 980 },
       { dia: 'Sex', valor: 1120 },
       { dia: 'Sáb', valor: 640 },
       { dia: 'Dom', valor: 590 },
       { dia: 'Seg', valor: 1340 },
       { dia: 'Ter', valor: 1210 },
       { dia: 'Qua', valor: 1080 },
     ]
     ```
   - NÃO remover campos existentes; outras telas (financeiro, trafego) importam do mesmo arquivo.
     ATENÇÃO: se alguma outra página tratar contaStatus como binário e o tsc acusar,
     ajustar apenas o necessário para compilar (ex.: tratar 'atencao' junto de 'problema').

2. src/app/globals.css — adicionar tokens de fundo suave por severidade, seguindo o
   padrão existente (light em :root, dark em .dark) e registrar em @theme inline:
   - Em @theme inline (após as linhas --color-chart-*):
     ```
     --color-alert-danger-soft: var(--alert-danger-soft);
     --color-alert-warning-soft: var(--alert-warning-soft);
     ```
   - Em :root (após --chart-info):
     ```
     --alert-danger-soft: #fef2f2;
     --alert-warning-soft: #fffbeb;
     ```
   - Em .dark:
     ```
     --alert-danger-soft: #2a1416;
     --alert-warning-soft: #2a2110;
     ```
   Isso habilita `bg-alert-danger-soft` e `bg-alert-warning-soft` no Painel.

Referência: task_intent LOCKED (decisão 3 — "cor com significado" e derivar do mock).
  </action>
  <verify>
    <automated>cd "C:\Users\jacso\OneDrive\Documentos\projeto_agencia_jsr" && npx tsc --noEmit</automated>
  </verify>
  <done>Mock exporta verbaDiariaMock, ClienteTrafego tem `contas` e contaStatus de 3 níveis; globals.css define --alert-danger-soft/--alert-warning-soft em light+dark e registra as classes; tsc sem erros.</done>
</task>

<task type="auto">
  <name>Task 3: Redesenhar o Painel com a nova direção visual</name>
  <files>src/app/(app)/painel/page.tsx</files>
  <action>
Reescrever src/app/(app)/painel/page.tsx (client component — precisa de recharts).
Adicionar `'use client'` no topo. Manter o MockNotice. Português em toda a UI, emojis
nos títulos de seção (pedido explícito do usuário). Estrutura LOCKED:

1. CABEÇALHO com saudação e resumo do dia:
   - `<h1>` "Bom dia, JSR 👋" (ou saudação simples) e subtítulo dinâmico:
     "{N} clientes ativos · {M} pontos precisam de você hoje", onde
     N = clientesTrafegoMock.length e M = alertasMock.length.

2. MockNotice (manter o texto atual explicando dados de exemplo).

3. 4 KPIs com StatCard (grid 1/2/4 cols como hoje), usando dados dos mocks:
   - MRR Total → soma financeiroMock.mrr → color="success", icon Wallet, trend up "8,2%".
   - A receber (7 dias) → soma mrr dos financeiroMock com status === 'próximo' →
     color="primary", icon CalendarClock (ou Calendar), helper "próximos vencimentos".
   - Verba rodando → soma verbaTotal de clientesTrafegoMock → color="warning", icon
     TrendingUp, helper "somando as contas de ads".
   - Contas com problema → clientesTrafegoMock.filter(contaStatus === 'problema').length
     → color="danger", icon AlertTriangle, helper "precisam de atenção imediata".
   Formatar moeda com o Intl.NumberFormat pt-BR/BRL existente.

4. Bloco DESTAQUE "🚩 Precisa de você hoje" (largura total, antes das colunas):
   Card com título e a lista de alertasMock. Cada alerta é uma linha com:
   - Borda esquerda colorida por severidade: crítico = `border-l-4 border-chart-danger`
     + fundo `bg-alert-danger-soft`; atenção = `border-l-4 border-chart-warning` +
     fundo `bg-alert-warning-soft`.
   - Título (font-medium), linha secundária "{cliente} · {detalhe}", e um Badge à direita:
     crítico = variant="destructive" texto "Crítico"; atenção = classe
     `bg-chart-warning/15 text-chart-warning` (ou Badge secondary) texto "Atenção".
   Cor com significado é o ponto central — não usar cinza neutro para severidade.

5. Grid 2 colunas (lg:grid-cols-2) abaixo:
   a) Card "📊 Saúde das contas de anúncio": lista clientesTrafegoMock com um ponto de
      status colorido (span `size-2.5 rounded-full`) — verde `bg-chart-success` (ativa),
      âmbar `bg-chart-warning` (atencao), vermelho `bg-chart-danger` (problema) —
      nome do cliente, subtítulo "{contas} contas · {verbaGasta formatado} de {verbaTotal}
      · sync {ultimaSync}". Usar a palavra "conta"/"contas" conforme cliente.contas.
   b) Card "📈 Verba dos últimos 7 dias": BarChart recharts via ChartContainer/ChartTooltip
      (padrão do financeiro/page.tsx), dados = verbaDiariaMock (dataKey x="dia", y="valor"),
      barras com `fill="var(--color-verba)"` e chartConfig verba→var(--primary),
      `radius={[4,4,0,0]}`, altura h-[220px], CartesianGrid vertical={false}, XAxis
      tickLine/axisLine false. NÃO usar Chart.js.

Imports: 'use client'; recharts (Bar, BarChart, CartesianGrid, XAxis); ChartContainer,
ChartConfig, ChartTooltip, ChartTooltipContent de '@/components/ui/chart'; Card, Badge,
StatCard, MockNotice; ícones lucide necessários; alertasMock, clientesTrafegoMock,
financeiroMock, verbaDiariaMock de '@/lib/mock/dashboard'. Reutilizar StatCard sem alterá-lo
(se um pequeno ajuste for necessário, é permitido, mas provavelmente não é).

Referência: task_intent LOCKED (decisão 3, direção visual aprovada).
  </action>
  <verify>
    <automated>cd "C:\Users\jacso\OneDrive\Documentos\projeto_agencia_jsr" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Painel exibe saudação+resumo, 4 KPIs, bloco "Precisa de você hoje" com severidade colorida (borda + fundo suave), saúde das contas com ponto de status de 3 cores e "N contas", e gráfico de barras de verba (recharts); MockNotice mantido; tsc e lint sem erros.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sem erros em todo o projeto (as outras telas que importam do mock continuam compilando).
- `npm run lint` sem erros.
- Sidebar: 6 áreas de topo + Usuários (admin). Rotas removidas do menu ainda acessíveis por URL (/contratos, /checklist, /acompanhamento, /verbas-ads, /alertas).
- Header: sino com badge → /alertas.
- Painel: KPIs, bloco de alertas por severidade colorida, saúde das contas com status de 3 cores, gráfico de barras recharts.
</verification>

<success_criteria>
- Menu de topo enxuto com as 6 áreas na ordem definida, Usuários admin-only preservado.
- Alertas fora da sidebar e presente como sino no header com contagem.
- Painel redesenhado com cor semântica, emojis nos títulos e gráfico de barras de verba.
- Nenhuma rota existente quebrada; nenhuma dependência nova adicionada.
- Toda a UI em português.
</success_criteria>

<output>
Após completar, criar `.planning/quick/260710-u8b-redesenho-visual-e-reorganizacao-da-nave/260710-u8b-SUMMARY.md`
</output>
