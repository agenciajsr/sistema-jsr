---
phase: quick-260715-la8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0023_relatorios_configuraveis.sql
  - src/lib/relatorios/engine.ts
  - src/lib/relatorios/engine.test.ts
  - src/lib/relatorios/variaveis.ts
  - src/lib/relatorios/variaveis.test.ts
  - src/lib/relatorios/templates-galeria.ts
  - src/lib/relatorios/gerar-relatorio-config.ts
  - src/actions/relatorios.ts
  - src/actions/relatorio-configs.ts
  - src/app/api/cron/relatorios/route.ts
  - src/app/api/cron/relatorios-semanais/route.ts
  - vercel.json
  - src/app/(app)/relatorios/page.tsx
  - src/app/(app)/relatorios/relatorios-content.tsx
  - src/app/(app)/relatorios/novo-relatorio-dialog.tsx
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "Usuário cria uma configuração de relatório (semanal OU mensal) com N blocos de métricas, cada bloco apontando para uma conta de anúncio Meta, com nível conta inteira ou campanhas selecionadas, métricas escolhidas e mensagem com variáveis"
    - "Relatório de cliente com 2 contas sai com bloco por conta + bloco Resumo Compilado somando investimento/resultados"
    - "Cron diário único gera os relatórios devidos hoje (semanais no dia da semana configurado, mensais no dia do mês) — vercel.json continua com exatamente 2 crons"
    - "Usuário vê preview com dados reais/exemplo antes de salvar e copia o texto gerado pronto para WhatsApp (sem integração de API de WhatsApp)"
    - "Toggle ativo/pausado não perde a configuração; campos de destino (número/grupo) e horário ficam salvos para o futuro"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Tabelas relatorio_configs e relatorio_blocos; coluna config_id em relatorios"
      contains: "relatorioConfigs"
    - path: "src/lib/relatorios/engine.ts"
      provides: "Lógica pura: cálculo de período, devidoHoje, montagem de blocos e compilado"
      min_lines: 80
    - path: "src/lib/relatorios/variaveis.ts"
      provides: "Catálogo de variáveis por categoria + interpolação {{var}}"
    - path: "src/lib/relatorios/templates-galeria.ts"
      provides: "Galeria: Foco em Conversas, Resumo Executivo, Foco em Performance"
    - path: "src/app/api/cron/relatorios/route.ts"
      provides: "Cron diário que substitui relatorios-semanais"
    - path: "src/app/(app)/relatorios/novo-relatorio-dialog.tsx"
      provides: "Dialog central Novo Relatório com blocos, templates, variáveis e preview"
  key_links:
    - from: "vercel.json"
      to: "/api/cron/relatorios"
      via: "schedule diário"
      pattern: "api/cron/relatorios"
    - from: "src/app/api/cron/relatorios/route.ts"
      to: "src/lib/relatorios/engine.ts"
      via: "devidoHoje + geração por config"
      pattern: "devidoHoje"
    - from: "src/app/(app)/relatorios/novo-relatorio-dialog.tsx"
      to: "src/actions/relatorio-configs.ts"
      via: "Server Actions de CRUD + preview"
      pattern: "relatorio-configs"
---

<objective>
Reformular o módulo /relatorios: de relatório semanal fixo por cliente para relatórios CONFIGURÁVEIS (semanal ou mensal) compostos por blocos de métricas por conta de anúncio, com seleção de campanhas, métricas personalizáveis, templates de mensagem com variáveis, bloco final de Resumo Compilado e cron diário único que gera os relatórios devidos no dia.

Purpose: eliminar a montagem manual de relatórios e cobrir o caso real do cliente com 2 contas (leads numa, vendas noutra) que precisa de bloco por conta + compilado.
Output: novas tabelas relatorio_configs/relatorio_blocos (migration aplicada), engine pura testada (Vitest), cron diário substituindo o semanal (mantém 2 slots Vercel), tela /relatorios reformulada com dialog "Novo Relatório".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260715-la8-reformular-modulo-de-relatorios-configur/260715-la8-CONTEXT.md
@src/lib/relatorios/gerar-relatorio.ts
@src/lib/relatorios/templates-whatsapp.ts
@src/lib/relatorios/parse-actions-extended.ts
@src/actions/relatorios.ts
@src/app/(app)/relatorios/relatorios-content.tsx
@src/app/api/cron/relatorios-semanais/route.ts
@src/lib/db/schema.ts
@vercel.json

Referências visuais (ler as necessárias antes da Task 3): imagens_referencia_relatorios/*.png

<interfaces>
Contratos existentes que o executor DEVE reutilizar (não recriar):

From src/lib/relatorios/gerar-relatorio.ts:
```typescript
export type MetricasConta = {
  contaId: string; contaNome: string;
  spend: number; reach: number; impressions: number; clicks: number;
  landingPageView: number; addToCart: number; checkout: number;
  compras: number; leads: number; conversas: number; engajamento: number;
  linkClicks: number; receita: number;
  roas: number | null; cpv: number | null; cpl: number | null; cpConv: number | null;
  ticketMedio: number | null; cpm: number | null; ctr: number | null; taxaCheckoutCompra: number | null;
}
```
A agregação em `agregarPorConta` lê `campaignInsights` (spend/impressions/clicks/reach + jsonb actions/actionValues via `parseActionsRelatorio`/`parseReceitaRelatorio`). Para nível "campanhas selecionadas", adicionar filtro `inArray(campaignInsights.campaignId, ids)`.

From src/lib/db/schema.ts:
- `relatorios` (pgTable): id, clienteId, clienteNome, tipo (text), periodoInicio/Fim (date), conteudo (text), geradoEm — MANTER, só adicionar `configId` nullable.
- `adAccounts`: id (uuid), clienteId, plataforma ('meta'), nome, ativo.
- `campaignInsights`: adAccountId, campaignId, campaignName, date, spend, impressions, clicks, reach, actions (jsonb), actionValues (jsonb).

From src/lib/date-br.ts: `hojeBrasilia()`, `dataMenosDias(n)` — usar para "hoje" no fuso BR.

Cron atual: vercel.json tem 2 slots (sync-meta diário 0 9 * * *, relatorios-semanais 0 10 * * 1). Proteção por CRON_SECRET (Bearer), padrão a copiar de src/app/api/cron/relatorios-semanais/route.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema (migration aplicada) + engine pura de período/agendamento/variáveis/compilado sob TDD</name>
  <files>src/lib/db/schema.ts, drizzle/0023_relatorios_configuraveis.sql, src/lib/relatorios/engine.ts, src/lib/relatorios/engine.test.ts, src/lib/relatorios/variaveis.ts, src/lib/relatorios/variaveis.test.ts, src/lib/relatorios/templates-galeria.ts</files>
  <behavior>
    Vitest (módulos PUROS — zero import de db/auth/react, padrão decisão 260714-ita):
    - interpolarVariaveis: substitui {{investimento}}, {{leads}}, {{date_range}} etc. a partir de um MetricasConta + período; variável desconhecida vira "—" (nunca lança); formatação pt-BR (R$ 1.234,56 / 1,5% / 2,00x)
    - calcularPeriodo(config, hoje): semanal com periodoDias=7 e envio na segunda → retorna segunda a domingo ANTERIORES (sem contar o dia do envio); mensal → mês anterior completo quando periodoDias=null, senão últimos N dias completos; configurável em dias
    - devidoHoje(config, hojeYYYYMMDD): semanal só no diaSemana configurado; mensal só no diaMes (diaMes 31 em fevereiro grampeia para o último dia do mês — testar); config inativa nunca é devida
    - compilarBlocos(metricasPorBloco[]): soma spend/receita/leads/compras/conversas etc. e recalcula derivadas (roas/cpl/cpv/cpConv/cpm/ctr) — mesma matemática de consolidarContas
    - montarTextoRelatorio(config, blocosComMetricas, periodo): cabeçalho com variáveis + N blocos (mensagem de cada bloco interpolada) + compilado opcional; snapshot de um caso com 2 blocos + compilado
  </behavior>
  <action>
    1. Schema Drizzle (src/lib/db/schema.ts), seguindo o padrão das tabelas existentes:
       - `relatorioConfigs` ('relatorio_configs'): id uuid pk, clienteId fk clientes cascade, nome text notNull, frequencia text notNull ('semanal'|'mensal'), diaSemana integer (0-6, para semanal), diaMes integer (1-31, para mensal), periodoDias integer (null = padrão da frequência), horarioEnvio text (SALVO PARA O FUTURO, não usado — limitação Hobby aceita), destinoTipo text ('privado'|'grupo', futuro), destinoValor text (número ou nome do grupo, futuro), cabecalho text notNull (template com variáveis, ex.: contém {{date_range}}), incluirCompilado boolean default true, mensagemCompilado text, ativo boolean notNull default true, createdAt/updatedAt.
       - `relatorioBlocos` ('relatorio_blocos'): id uuid pk, configId fk relatorio_configs cascade, ordem integer notNull, adAccountId fk ad_accounts (Meta hoje; UI mostra Google Ads desabilitado "em breve" — NÃO modelar plataforma extra), nivel text notNull ('conta'|'campanhas'), campanhasSelecionadas jsonb (array de campaignId, null quando nivel='conta'), metricas jsonb notNull (array de chaves do catálogo), mensagem text notNull (template com variáveis).
       - `relatorios`: adicionar coluna `configId` uuid nullable fk relatorio_configs set null; tipo passa a aceitar 'automatico' além de 'semanal'|'manual' (coluna já é text — sem migration disso).
       - Relations correspondentes.
    2. Gerar migration: `npx drizzle-kit generate --name relatorios_configuraveis`. APLICAR via script Node pontual (padrão OBRIGATÓRIO da decisão 260715-1rq: NUNCA `drizzle-kit migrate`; ler DIRECT_URL de .env.local, conferir information_schema antes, separar por '--> statement-breakpoint', rodar dentro de sql.begin()). Se a aplicação falhar por ambiente, registrar como migration GERADA e NÃO aplicada no SUMMARY e seguir (todo o código degrada graciosamente? Não — a tela nova depende das tabelas; nesse caso registrar deviation).
    3. src/lib/relatorios/variaveis.ts (PURO): catálogo `CATALOGO_VARIAVEIS` agrupado por categoria — gerais (cliente, conta, date_range, periodo_inicio, periodo_fim), investimento (investimento, cpm), cliques (cliques, ctr, cpc, impressoes, alcance), leads (leads, cpl), conversas (conversas, custo_por_conversa), vendas (compras, receita, roas, cpa/cpv, ticket_medio, add_to_cart, checkout), pagina (visitas_pagina, custo_por_visita). Cada item: chave, label pt-BR, categoria, formato ('moeda'|'numero'|'percent'|'roas'|'texto'). Exportar `interpolarVariaveis(template, contexto)` aceitando sintaxe {{chave}} e também <CHAVE> maiúscula (ex.: <DATA> ≡ {{date_range}}).
    4. src/lib/relatorios/engine.ts (PURO): calcularPeriodo, devidoHoje, compilarBlocos, montarTextoRelatorio conforme <behavior>. Tipos: `RelatorioConfigInput`, `BlocoComMetricas = { bloco, metricas: MetricasConta }`.
    5. src/lib/relatorios/templates-galeria.ts: galeria com no mínimo 3 templates pt-BR — "Foco em Conversas", "Resumo Executivo", "Foco em Performance" — cada um: id, nome, descricao, cabecalho, mensagemBloco (com variáveis do catálogo), metricasSugeridas. Migrar o espírito dos textos/emojis de templates-whatsapp.ts (que permanece intocado para o fluxo manual legado).
    6. TDD: escrever os testes ANTES da implementação; RED → GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/relatorios/engine.test.ts src/lib/relatorios/variaveis.test.ts</automated>
  </verify>
  <done>Testes novos passando (suite total continua verde); migration 0023 gerada e aplicada (ou deviation registrada); catálogo e galeria exportados; nenhum módulo puro importa db/auth/react.</done>
</task>

<task type="auto">
  <name>Task 2: Geração por config + Server Actions CRUD/preview + cron diário único (substitui o semanal)</name>
  <files>src/lib/relatorios/gerar-relatorio-config.ts, src/actions/relatorio-configs.ts, src/actions/relatorios.ts, src/app/api/cron/relatorios/route.ts, src/app/api/cron/relatorios-semanais/route.ts (DELETAR), vercel.json</files>
  <action>
    1. src/lib/relatorios/gerar-relatorio-config.ts (server, com db): `gerarRelatorioDeConfig(configId, hoje?)` — carrega config + blocos ordenados; calcula período via engine.calcularPeriodo; para cada bloco agrega campaign_insights da conta (reaproveitar a lógica de agregarPorConta de gerar-relatorio.ts — extrair/exportar helper `agregarContaPeriodo(adAccountId, inicio, fim, campanhaIds?)` com filtro opcional `inArray(campaignId, ids)` quando nivel='campanhas'); queries SEQUENCIAIS (pool max=3, padrão 260714-j92 — nada de Promise.all interno); monta texto via engine.montarTextoRelatorio; retorna { texto, periodo, blocos }. Sem dados em todos os blocos → null.
    2. src/actions/relatorio-configs.ts ('use server', getCurrentUser em todas): criarRelatorioConfig (valida com Zod: nome 3-80, frequencia enum, diaSemana 0-6 obrigatório se semanal, diaMes 1-31 se mensal, ≥1 bloco, cada bloco com adAccountId + ≥1 métrica + mensagem não vazia; insert config + blocos em db.transaction), atualizarRelatorioConfig (replace de blocos na mesma transação), alternarAtivo, excluirRelatorioConfig, listarRelatorioConfigs (join cliente, retorna nome/cliente/frequencia/ativo + proximoEnvio calculado com engine.devidoHoje projetando os próximos 60 dias — helper puro no engine se simples), previewRelatorio(configDraft) — gera texto com dados REAIS dos últimos período (ou aviso "sem dados no período" pt-BR) SEM persistir, para o preview do dialog. Listar também `listarContasComCampanhas(clienteId)`: contas Meta ativas do cliente + campanhas distintas dos últimos 90 dias (campaignId/campaignName de campaign_insights) para a UI de seleção.
    3. Cron diário: criar src/app/api/cron/relatorios/route.ts (runtime nodejs, maxDuration 60, proteção CRON_SECRET idêntica à rota antiga): busca configs ativas, filtra com engine.devidoHoje(hojeBrasilia()), para cada devida gera via gerarRelatorioDeConfig e insere em `relatorios` com tipo 'automatico' + configId (try/catch por config — erro em uma não derruba as demais); responde { ok, devidas, gerados, semDados, erros }. DELETAR src/app/api/cron/relatorios-semanais/route.ts.
    4. vercel.json: substituir a linha do relatorios-semanais por `{ "path": "/api/cron/relatorios", "schedule": "0 10 * * *" }` (diário 07h BR) — TOTAL CONTINUA 2 CRONS (decisão locked: limite Hobby).
    5. src/actions/relatorios.ts: manter gerarRelatorio/gerarRelatoriosEmLote/listarHistoricoRelatorios (fluxo manual legado continua funcionando); listarHistoricoRelatorios passa a devolver também tipo 'automatico' (já devolve, coluna text) — só ajustar o type/label.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Cron antigo deletado, vercel.json com exatamente 2 crons (sync-meta + relatorios diário); actions CRUD/preview compilando com validação Zod; gerarRelatorioDeConfig gera texto com blocos + compilado; build de tipos limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Tela /relatorios reformulada — dialog Novo Relatório com blocos, templates, variáveis clicáveis e preview</name>
  <files>src/app/(app)/relatorios/page.tsx, src/app/(app)/relatorios/relatorios-content.tsx, src/app/(app)/relatorios/novo-relatorio-dialog.tsx</files>
  <action>
    Antes de codar, ler as imagens de imagens_referencia_relatorios/ relevantes (modal novo relatório, galeria de templates, catálogo de campos, blocos) — espelhar a ESTRUTURA sem copiar pixel a pixel; manter o design system do app (shadcn, mesmos primitivos do CRM). Textos 100% pt-BR.

    1. relatorios-content.tsx reformulada com duas áreas:
       a) "Relatórios configurados": listagem (nome, cliente, frequência semanal/mensal + dia, próximo envio, badge Ativo/Pausado) com Switch de ativo (alternarAtivo — pausar NÃO perde config), editar (reabre o dialog preenchido) e excluir. Botão "Novo Relatório" no topo.
       b) "Relatórios gerados": histórico existente (manual + automático) com botão Copiar (navigator.clipboard) — envio WhatsApp é COPIAR E COLAR, nenhuma integração de API (decisão locked). Manter a geração manual legada existente (gerar por cliente/lote) como está, apenas reorganizada visualmente.
    2. novo-relatorio-dialog.tsx (Dialog shadcn central, padrão do CRM 260715-gmf; RHF + zodResolver):
       - Dados gerais: nome, cliente (select), frequência (semanal → select dia da semana; mensal → select dia 1-31), período em dias (default 7/mês anterior, editável), horário de envio (input salvo, com hint "envio no horário exato disponível em breve"), destino (toggle Privado/Grupo + campo número ou nome do grupo — apenas salvos, hint "envio automático em breve").
       - Blocos de métricas (lista dinâmica, adicionar/remover/reordenar simples por setas): cada bloco = plataforma (Meta selecionável; "Google Ads — em breve" disabled), conta de anúncio do cliente (via listarContasComCampanhas), nível (Conta inteira | Campanhas selecionadas → checklist de campanhas), métricas (checkboxes a partir de CATALOGO_VARIAVEIS agrupado por categoria), mensagem do bloco (Textarea) com galeria de templates (aplicar preenche a mensagem) e chips de variáveis clicáveis por categoria que inserem {{chave}} na posição do cursor.
       - Cabeçalho do relatório: Textarea com variáveis gerais (ex.: {{cliente}}, {{date_range}}).
       - Toggle "Incluir resumo compilado" + mensagem do compilado (mesmos chips).
       - Botão "Ver preview": chama previewRelatorio e mostra o texto num bloco monoespaçado com botão Copiar; "Salvar" só persiste via criarRelatorioConfig/atualizarRelatorioConfig, com toast de sucesso/erro pt-BR.
    3. page.tsx: atualizar o subtítulo para refletir relatórios configuráveis (semanal/mensal) prontos para copiar no WhatsApp.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>/relatorios compila e builda; dialog cria/edita config com N blocos, templates, variáveis clicáveis e preview; toggle ativo persiste sem apagar config; histórico com copiar; nenhuma menção a envio automático como se já funcionasse (hints "em breve" honestos, padrão 260715-1rq).</done>
</task>

</tasks>

<verification>
- `npx vitest run` — suite completa verde (237 testes existentes + novos da engine/variáveis).
- `npm run build` — build Next limpo.
- vercel.json: exatamente 2 entradas de cron; nenhuma referência restante a relatorios-semanais no código (`grep -r relatorios-semanais src/`vazio).
- Fluxo manual: criar config semanal (segunda, 7 dias) com 2 blocos + compilado → preview mostra bloco por conta + resumo somado → salvar → aparece na listagem com próximo envio.
</verification>

<success_criteria>
- Relatórios configuráveis semanal/mensal com blocos por conta, seleção de campanhas, métricas por bloco, templates com variáveis e compilado opcional — tudo pt-BR.
- Cron diário único gera os devidos do dia (semanais no dia da semana, mensais no dia do mês com grampeamento de fim de mês), mantendo 2 slots Vercel.
- WhatsApp = copiar e colar; destino/horário salvos para o futuro sem fingir funcionalidade.
- Lógica pura (período, devidoHoje, variáveis, compilado, montagem de texto) coberta por Vitest.
</success_criteria>

<output>
After completion, create `.planning/quick/260715-la8-reformular-modulo-de-relatorios-configur/260715-la8-SUMMARY.md`
</output>
