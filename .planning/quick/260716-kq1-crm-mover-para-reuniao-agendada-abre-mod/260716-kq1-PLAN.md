---
phase: quick-260716-kq1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/crm/reuniao.ts
  - src/lib/crm/reuniao.test.ts
  - src/actions/crm-atividades.ts
  - src/components/crm/reuniao-dialog.tsx
  - src/components/crm/kanban-crm.tsx
autonomous: true
requirements: [KQ1-01]
must_haves:
  truths:
    - "Arrastar um card para a coluna 'Reunião agendada' abre um modal pedindo data/hora (título e observação opcionais) — o card NÃO se move antes da confirmação"
    - "Cancelar/fechar o modal deixa o card exatamente onde estava (nenhuma action disparada)"
    - "Confirmar cria a atividade tipo 'reuniao' no card do lead (crm_tarefas) E o evento no Google Calendar, e só então move o card"
    - "Sem Google conectado, a atividade é criada e o card move mesmo assim, com aviso em português de que o evento não foi criado"
  artifacts:
    - path: "src/components/crm/reuniao-dialog.tsx"
      provides: "Dialog controlado de agendamento de reunião (padrão MotivoPerdaDialog)"
    - path: "src/actions/crm-atividades.ts"
      provides: "Server action criarReuniaoCrm (atividade + evento Google Calendar)"
    - path: "src/lib/crm/reuniao.ts"
      provides: "Helper puro ehEtapaReuniaoAgendada (detecção da coluna por nome, sem acento/caixa)"
  key_links:
    - from: "src/components/crm/kanban-crm.tsx"
      to: "src/components/crm/reuniao-dialog.tsx"
      via: "estado pendenteReuniao interceptando onDragEnd (mesmo contrato do MotivoPerdaDialog)"
      pattern: "pendenteReuniao"
    - from: "src/actions/crm-atividades.ts"
      to: "src/lib/google/calendar.ts"
      via: "criarEvento com try/catch (NAO_CONECTADO = degradação graciosa)"
      pattern: "criarEvento"
---

<objective>
Ao mover um card para a coluna "Reunião agendada" no Kanban da /crm, abrir um modal pedindo data/hora (título/observação opcionais) que, ao confirmar, cria a atividade de reunião no card do lead (crm_tarefas, tipo 'reuniao') E o evento correspondente no Google Calendar numa ação só — e só então move o card. Cancelar não move o card.

Purpose: hoje mover para "Reunião agendada" só muda a etapa — a reunião em si (atividade + agenda) exige passos manuais que a equipe esquece.
Output: dialog novo + server action combinada + interceptação no kanban, tudo em pt-BR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/crm/kanban-crm.tsx
@src/components/crm/motivo-perda-dialog.tsx
@src/components/crm/criar-atividade-dialog.tsx
@src/actions/crm-atividades.ts
@src/lib/google/calendar.ts
@src/lib/validations/crm.ts
@src/lib/crm/dados.ts

<interfaces>
Já verificado — NÃO propor migration nenhuma:
- `crm_tarefas` (schema.ts:791) já tem `tipo` (inclui 'reuniao'), `dataInicio`, `dataFim`, `dataVencimento` (obrigatória — receber dataFim, como faz criarAtividadeCrm), `notas`, `donoId`, `contatoId`, `oportunidadeId`.
- Google Calendar: `criarEvento({ titulo, descricao?, local?, inicio, fim })` em src/lib/google/calendar.ts (server-only), datas RFC3339 com offset `-03:00` (constante OFFSET_BRASILIA interna ao módulo; usar o sufixo literal '-03:00' na action). Lança `Error(NAO_CONECTADO)` quando não há conta conectada — exportado como `NAO_CONECTADO`.
- `criarAtividadeCrm(input: AtividadeInput)` em src/actions/crm-atividades.ts: padrão `{ data } | { error }`, getCurrentUser + getWorkspaceAtual no topo, `registrarAtividadeCrm` de src/lib/crm/atividades (helper em src/lib, nunca exportado de arquivo 'use server'), revalidatePath('/crm').
- Kanban: `onDragEnd` em kanban-crm.tsx já intercepta destino PERDIDO com `pendentePerda` + `MotivoPerdaDialog` + `confirmarPerda` (update otimista → action → rollback). Seguir EXATAMENTE esse padrão. `ColunaKanban` traz `etapa: { id, nome, ... }`; `OportunidadeCard` tem `id, titulo, contatoId?, etapaId, status` (conferir o shape real em src/lib/crm/dados.ts — se o card não carregar contatoId, buscar na action pela oportunidade).
- `moverOportunidade(id, etapaId)` e `reabrirOportunidade(id)` em src/actions/crm.ts (mover de Ganho/Perdido para etapa exige reabrir antes, sequencial).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro de detecção da etapa + server action criarReuniaoCrm</name>
  <files>src/lib/crm/reuniao.ts, src/lib/crm/reuniao.test.ts, src/actions/crm-atividades.ts</files>
  <behavior>
    Testes (Vitest, padrão dos módulos puros do repo) para `ehEtapaReuniaoAgendada(nome: string): boolean`:
    - 'Reunião agendada' → true
    - 'reuniao agendada' (sem acento, minúscula) → true
    - '  Reunião Agendada  ' (espaços/caixa) → true
    - 'Proposta enviada' → false
    - 'Reunião realizada' → false (só a etapa exata, não qualquer nome com "reunião")
    Implementação: normalizar com .normalize('NFD') removendo diacríticos + trim + lowercase e comparar com 'reuniao agendada'. Módulo PURO: zero imports de db/react/auth.
  </behavior>
  <action>
    1. Criar `src/lib/crm/reuniao.ts` com `ehEtapaReuniaoAgendada` (RED→GREEN com o teste acima).
    2. Em `src/actions/crm-atividades.ts`, adicionar `criarReuniaoCrm(input)` seguindo o padrão de `criarAtividadeCrm` no MESMO arquivo:
       - Input: `{ oportunidadeId: string, titulo?: string, data: string /* YYYY-MM-DD */, horaInicio: string /* HH:mm */, horaFim: string /* HH:mm */, observacao?: string }`. Validar com Zod inline no arquivo (ou schema em src/lib/validations/crm.ts, seguindo onde vive atividadeSchema): data/horas obrigatórias, fim > início, título com default `'Reunião'` quando vazio.
       - getCurrentUser() + getWorkspaceAtual() no topo (mensagens de erro em pt-BR idênticas ao padrão existente).
       - Buscar a oportunidade (id + workspaceId) para obter `contatoId` e `titulo` do negócio — retorna `{ error: 'Negócio não encontrado.' }` se não existir. Queries SEQUENCIAIS (pool max=3), nunca Promise.all.
       - Inserir em `crmTarefas`: tipo 'reuniao', titulo (default 'Reunião — {título do negócio}' se o usuário não informou), notas = observacao ?? null, dataInicio/dataFim como `new Date(\`${data}T${hora}\`)`, dataVencimento = dataFim (OBRIGATÓRIO — mantém a heurística "sem contato +7d"), donoId = currentUser.id, contatoId e oportunidadeId.
       - `registrarAtividadeCrm(workspace.id, currentUser, { tipo: 'tarefa_criada', contatoId, oportunidadeId, detalhe: titulo })`.
       - Criar o evento no Google Calendar com `criarEvento` de `@/lib/google/calendar`: inicio = `\`${data}T${horaInicio}:00-03:00\``, fim idem com horaFim, descricao = observacao. Envolver em try/catch PRÓPRIO: se lançar (inclusive NAO_CONECTADO), NÃO falhar a action — retornar `{ data: { id, eventoCriado: false, avisoCalendar: 'Reunião criada, mas o evento não foi criado no Google Calendar (conta não conectada ou erro na API).' } }`. Sucesso → `{ data: { id, eventoCriado: true } }`. A atividade no CRM nunca é desfeita por falha do Calendar.
       - `revalidatePath('/crm')` antes do return.
       - NÃO mover a oportunidade dentro desta action — quem move é o kanban (reusa moverOportunidade/reabrirOportunidade existentes), mesmo desenho do fluxo de perda.
  </action>
  <verify>
    <automated>npx vitest run src/lib/crm/reuniao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Helper puro testado (5+ casos passando); criarReuniaoCrm exportada seguindo o padrão { data } | { error }, com degradação graciosa do Calendar e sem migration.</done>
</task>

<task type="auto">
  <name>Task 2: ReuniaoDialog + interceptação no kanban</name>
  <files>src/components/crm/reuniao-dialog.tsx, src/components/crm/kanban-crm.tsx</files>
  <action>
    1. Criar `src/components/crm/reuniao-dialog.tsx` no MESMO padrão do `motivo-perda-dialog.tsx` (dialog controlado, reset ao abrir via ajuste-durante-o-render, cancelar = onCancel):
       - Props: `{ open, nomeNegocio?, onCancel, onConfirm }` onde `onConfirm({ titulo, data, horaInicio, horaFim, observacao })`.
       - Campos: Input de título (placeholder 'Reunião — {nome do negócio}', opcional), Input type=date (default hoje, helper hojeLocal igual ao criar-atividade-dialog), dois Input type=time (defaults ex.: 13:00/13:30) com a duração calculada ao lado (copiar helpers hojeLocal/montarIso/duracao do criar-atividade-dialog — são locais àquele arquivo, duplicar é ok no repo), Textarea de observação opcional.
       - Botão confirmar 'Agendar reunião' desabilitado quando data/horas inválidas ou fim <= início. Header com ícone CalendarClock (lucide) e DialogDescription explicando que a atividade E o evento no Google Calendar serão criados. Textos 100% pt-BR. Sem cor hex fixa — tokens do tema (regra do dark mode).
    2. Em `kanban-crm.tsx`:
       - Importar `ehEtapaReuniaoAgendada` e montar um lookup das etapas de reunião a partir de `colunas` (ex.: `const etapasReuniao = new Set(colunas.filter(c => ehEtapaReuniaoAgendada(c.etapa.nome)).map(c => c.etapa.id))`).
       - Estado `pendenteReuniao: { id: string; nome: string; destino: string; origem: string } | null` (mesmo desenho de pendentePerda).
       - Em `onDragEnd`, ANTES do bloco otimista (logo após o guard de PERDIDO): se `etapasReuniao.has(destino)` e origem !== destino → `setPendenteReuniao(...)` e `return`. Nada acontece antes da confirmação.
       - `confirmarReuniao(valores)`: re-localiza o card (acharCard — o quadro pode ter mudado), guarda `anterior`, move otimista para a etapa destino (status 'aberta', etapaId = destino, motivoPerda null), então SEQUENCIALMENTE: (a) se origem era GANHO/PERDIDO, `reabrirOportunidade(id)` primeiro (rollback + toast.error se falhar); (b) `moverOportunidade(id, destino)` (rollback se falhar); (c) `criarReuniaoCrm({ oportunidadeId: id, ...valores })` — se `{ error }`, NÃO reverter o movimento (a etapa mudou de verdade): toast.error('Card movido, mas a reunião não foi criada: {error}'). Se `data.avisoCalendar`, `toast.warning(avisoCalendar)`; senão `toast.success('Reunião agendada e evento criado no Google Calendar.')`. Finalizar com `router.refresh()`.
       - Renderizar `<ReuniaoDialog open={!!pendenteReuniao} nomeNegocio={pendenteReuniao?.nome} onCancel={() => setPendenteReuniao(null)} onConfirm={(v) => { void confirmarReuniao(v); setPendenteReuniao(null) }} />` ao lado do MotivoPerdaDialog.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Arrastar para "Reunião agendada" abre o modal sem mover o card; cancelar não move; confirmar move + cria atividade + evento (ou aviso gracioso sem Google conectado); build e typecheck limpos.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — suíte inteira verde (nenhum teste existente quebrado).
- `npm run build` limpo.
- Fluxo manual (dev): arrastar card → modal → cancelar (card fica) → arrastar de novo → confirmar → card move, atividade aparece na ficha do lead (aba Atividades), evento aparece na /agenda (Google conectado em produção).
</verification>

<success_criteria>
- Modal em pt-BR intercepta o drag para "Reunião agendada" (detecção por nome da etapa, tolerante a acento/caixa).
- Cancelar nunca move o card nem dispara action.
- Confirmar cria atividade tipo 'reuniao' em crm_tarefas (dataVencimento = dataFim) + evento no Google Calendar via infra existente (criarEvento) + registra em crm_atividades + move o card.
- Falha do Calendar não perde a atividade nem o movimento — aviso claro ao usuário.
- Zero migration; zero OAuth novo.
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260716-kq1-crm-mover-para-reuniao-agendada-abre-mod/260716-kq1-SUMMARY.md`
</output>
