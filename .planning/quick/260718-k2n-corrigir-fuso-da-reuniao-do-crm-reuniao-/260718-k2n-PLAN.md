---
phase: quick-260718-k2n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/crm/reuniao.ts
  - src/lib/crm/reuniao.test.ts
  - src/actions/crm-atividades.ts
  - scripts/corrigir-fuso-reunioes-crm.ts
autonomous: true
requirements: [FUSO-REUNIAO, TAREFA-ESPELHO, CORRECAO-DADO]
must_haves:
  truths:
    - "Reunião do CRM agendada 20/07 15:00 BRT grava data_inicio = 2026-07-20T18:00:00Z (não 3h a mais)"
    - "Ao agendar reunião no CRM, uma tarefa espelho aparece no quadro /tarefas no dia da reunião"
    - "Existe script de dry-run que lista as reuniões antigas afetadas antes de corrigir em produção"
  artifacts:
    - path: "src/lib/crm/reuniao.ts"
      provides: "montarInstanteBrasilia(data, hora) — helper puro do instante BRT"
      contains: "export function montarInstanteBrasilia"
    - path: "src/lib/crm/reuniao.test.ts"
      provides: "teste do helper (15:00 BRT → 18:00Z)"
      contains: "montarInstanteBrasilia"
    - path: "src/actions/crm-atividades.ts"
      provides: "fix do fuso + insert da tarefa espelho em try/catch próprio"
    - path: "scripts/corrigir-fuso-reunioes-crm.ts"
      provides: "script pontual dry-run + --executar, backup JSON, +3h nas reuniões antigas"
  key_links:
    - from: "src/actions/crm-atividades.ts"
      to: "src/lib/crm/reuniao.ts"
      via: "import montarInstanteBrasilia"
      pattern: "montarInstanteBrasilia"
    - from: "src/actions/crm-atividades.ts"
      to: "tabela tarefas"
      via: "db.insert(tarefas)"
      pattern: "insert\\(tarefas\\)"
---

<objective>
Corrigir dois bugs da reunião do CRM:

1. **Fuso errado**: `criarReuniaoCrm` grava `dataInicio`/`dataFim` com `new Date(\`${v.data}T${v.horaInicio}\`)` SEM offset → na Vercel (UTC) fica 3h a mais.
2. **Reunião não aparece no quadro /tarefas**: só existe em `crm_tarefas`, nunca vira tarefa da fonte de `/tarefas` (tabela `tarefas`).

Além disso, corrigir o dado JÁ gravado errado em produção (reunião da Daíla Aires) via script pontual dry-run.

Purpose: reunião do CRM mostra o horário certo e o time enxerga a reunião no quadro do dia.
Output: helper puro testado, fix na action, tarefa espelho, script de correção de dado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contratos já existentes no código, extraídos do codebase. Use direto. -->

Tabela `tarefas` (src/lib/db/schema.ts:313) — campos relevantes p/ a tarefa espelho:
```
titulo: text NOT NULL
notas: text | null
descricao: text | null
status: enum ('a_fazer' default)
prioridade: enum ('baixa'|'media'|'alta'|'urgente', default 'media')
data: date NOT NULL         // 'YYYY-MM-DD' — o quadro /tarefas filtra por AQUI
clienteId: uuid | null      // é LEAD, não cliente → null
responsavelId: uuid | null  // → currentUser.id
recorrencia: enum ('nenhuma' default)
ehMolde: boolean (default false)
etiquetas: jsonb            // string[]
```
Fonte do quadro (src/lib/tarefas/dados.ts): `getTarefasDoPeriodo` lê `tarefas` WHERE `ehMolde=false` e `data` no intervalo. Só precisamos inserir uma linha avulsa (ehMolde=false, recorrencia 'nenhuma', tarefaMaeId null).

crm-atividades.ts já importa: `db`, `crmContatos/crmOportunidades/crmTarefas`, `getCurrentUser`, `criarEvento`. Precisará adicionar `tarefas` ao import de schema e `montarInstanteBrasilia` de `@/lib/crm/reuniao`.

`criarReuniaoCrm` (src/actions/crm-atividades.ts ~177-234): monta `titulo`, `dataInicio`/`dataFim`, insere em `crmTarefas`, e ABAIXO cria evento no Google Calendar dentro de try/catch próprio usando offset `-03:00` (`${v.data}T${v.horaInicio}:00-03:00`). `v.data` já é a data local escolhida ('YYYY-MM-DD'); `v.horaInicio`/`v.horaFim` são 'HH:MM'.

Padrão de script pontual (scripts/backfill-primeiro-contato.ts): `import postgres`, `postgres(process.env.DIRECT_URL!, { max: 1 })`, flag de argv p/ efetivar, `sql.begin(tx => ...)`, rodar com `npx tsx --env-file=.env.local scripts/...`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro montarInstanteBrasilia + teste + aplicar fix do fuso</name>
  <files>src/lib/crm/reuniao.ts, src/lib/crm/reuniao.test.ts, src/actions/crm-atividades.ts</files>
  <behavior>
    - montarInstanteBrasilia('2026-07-20', '15:00') → Date com getTime() === Date.parse('2026-07-20T18:00:00Z') (15:00 BRT = 18:00 UTC)
    - montarInstanteBrasilia('2026-07-20', '15:40') → getTime() === Date.parse('2026-07-20T18:40:00Z')
    - Independe do fuso do processo (mesmo resultado em UTC e em BRT) — offset explícito -03:00
  </behavior>
  <action>
    Em src/lib/crm/reuniao.ts adicionar helper PURO (zero imports de db/react/auth):
    `export function montarInstanteBrasilia(data: string, hora: string): Date { return new Date(\`${'${data}'}T${'${hora}'}:00-03:00\`) }`
    com comentário em português explicando que o offset -03:00 fixo evita o bug de gravar em UTC na Vercel (mesmo offset que o evento do Google já usa). NÃO usar `new Date` sem offset.

    Em src/lib/crm/reuniao.test.ts adicionar um `describe('montarInstanteBrasilia')` cobrindo o behavior acima (comparar por `.getTime()` contra `Date.parse('...Z')`). Manter os testes existentes de ehEtapaReuniaoAgendada.

    Em src/actions/crm-atividades.ts (`criarReuniaoCrm`, ~linhas 178-179): importar `montarInstanteBrasilia` de `@/lib/crm/reuniao` e trocar:
    `const dataInicio = new Date(\`${'${v.data}'}T${'${v.horaInicio}'}\`)` → `const dataInicio = montarInstanteBrasilia(v.data, v.horaInicio)`
    `const dataFim = new Date(\`${'${v.data}'}T${'${v.horaFim}'}\`)` → `const dataFim = montarInstanteBrasilia(v.data, v.horaFim)`
    NÃO mexer no formatarHora do ficha-lead — ele já renderiza certo no fuso do navegador quando o instante gravado estiver correto (per D-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/crm/reuniao.test.ts</automated>
  </verify>
  <done>Teste do helper passa; criarReuniaoCrm usa montarInstanteBrasilia nas duas linhas; formatarHora intocado.</done>
</task>

<task type="auto">
  <name>Task 2: Tarefa espelho no quadro /tarefas (try/catch próprio)</name>
  <files>src/actions/crm-atividades.ts</files>
  <action>
    Em `criarReuniaoCrm`, DEPOIS do bloco try/catch do Google Calendar (que já preenche `meetLink`), adicionar um insert ESPELHO na tabela `tarefas` em try/catch PRÓPRIO — falha aqui NUNCA quebra o agendamento (mesmo padrão do try/catch do Google logo acima). Adicionar `tarefas` ao import de `@/lib/db/schema`.

    Valores do insert (per D-03):
    - `titulo`: `Reunião: ${'${nomeLead ?? oportunidade.titulo}'}` (incluir horário se fizer sentido, ex.: ` (${'${v.horaInicio}'})`)
    - `data`: `v.data` direto (já é a data local escolhida 'YYYY-MM-DD' no fuso BR — NÃO derivar de dataInicio, que é UTC)
    - `prioridade`: 'alta'
    - `etiquetas`: `['Reunião']`
    - `responsavelId`: `currentUser.id`
    - `clienteId`: `null` (é lead, não cliente)
    - `descricao` (ou `notas`): incluir o link do Meet quando houver (`meetLink`), senão a observação
    - `ehMolde`: false, `recorrencia`: 'nenhuma', `tarefaMaeId`: null (tarefa avulsa, NÃO molde)

    Independente: concluir/cancelar a tarefa espelho não sincroniza de volta com o CRM (documentar no SUMMARY). Colocar `console.error('[criarReuniaoCrm] tarefa espelho', e)` no catch. Manter `revalidatePath('/crm')` existente; adicionar `revalidatePath('/tarefas')` dentro do try do espelho após o insert.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Insert em `tarefas` com data=v.data, prioridade 'alta', etiquetas ['Reunião'], em try/catch próprio; build verde.</done>
</task>

<task type="auto">
  <name>Task 3: Script pontual de correção do dado (dry-run + --executar)</name>
  <files>scripts/corrigir-fuso-reunioes-crm.ts</files>
  <action>
    Criar script pontual seguindo o padrão do repo (ver scripts/backfill-primeiro-contato.ts): `import postgres`, `postgres(process.env.DIRECT_URL!, { max: 1 })`, rodar com `npx tsx --env-file=.env.local scripts/corrigir-fuso-reunioes-crm.ts` (dry-run por padrão) e `--executar` p/ efetivar. Comentário-cabeçalho em português explicando o contexto (reuniões do CRM gravadas 3h atrasadas antes do deploy do fix de fuso).

    Lógica (per D-02):
    1. SELECT das reuniões afetadas: `crm_tarefas WHERE tipo='reuniao' AND created_at < <corte>` (usar `now()` como corte default; documentar no comentário que o corte deve ser ANTES do deploy do fix). Selecionar id, titulo, data_inicio, data_fim, data_vencimento.
    2. DRY-RUN (default): imprimir EXATAMENTE cada linha afetada (id, titulo, horários atuais e como ficariam +3h) p/ conferência humana. Não alterar nada. Mensagem clara: "Prévia — rode com --executar para gravar".
    3. Se `--executar`: ANTES de alterar, gravar backup JSON das linhas em `scripts/backup-fuso-reunioes-<timestamp>.json` (padrão dos backups já existentes em scripts/). Depois, dentro de `sql.begin`, `UPDATE crm_tarefas SET data_inicio = data_inicio + interval '3 hours', data_fim = data_fim + interval '3 hours', data_vencimento = data_vencimento + interval '3 hours' WHERE <mesma condição>` e imprimir quantas linhas foram corrigidas.

    NUNCA usar drizzle-kit migrate. O passo `--executar` em produção é MANUAL do usuário após o deploy — o executor só cria o script e valida que o dry-run lista as linhas corretamente (não precisa efetivar).
  </action>
  <verify>
    <automated>npx tsx --env-file=.env.local scripts/corrigir-fuso-reunioes-crm.ts</automated>
  </verify>
  <done>Dry-run roda sem erro e LISTA as reuniões tipo='reuniao' afetadas (incluindo a da Daíla Aires) sem alterar nada; --executar faz backup JSON + soma 3h em transação.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — verde (helper coberto)
- `npm run lint` — zerado
- `npm run build` — verde
- Dry-run do script lista a reunião da Daíla Aires (20/07 15:00-15:40) como afetada
</verification>

<success_criteria>
- Reunião nova do CRM grava instante correto (15:00 BRT → 18:00Z)
- Tarefa espelho aparece no quadro /tarefas no dia da reunião, prioridade alta, etiqueta 'Reunião'
- Script de correção existe, dry-run confere e --executar (manual do usuário) corrige o dado antigo
- lint + vitest + build verdes; tudo em português
</success_criteria>

<output>
Após completar, criar `.planning/quick/260718-k2n-corrigir-fuso-da-reuniao-do-crm-reuniao-/260718-k2n-SUMMARY.md`.
No SUMMARY documentar: (a) tarefa espelho é INDEPENDENTE — concluir/cancelar não sincroniza de volta ao CRM; (b) o passo `--executar` do script é MANUAL do usuário após o deploy.
</output>
