# Quick Task 260717-00n — SUMMARY

**Concluída:** 2026-07-17 — commits `feature 1`, `feature 2`, `feature 3` (ver PLAN para o conteúdo de cada uma).

## Verificação
- 2146+ testes vitest passando (31 novos: semaforo 13, regras-campanha 9, acoes-dia 9).
- Typecheck e eslint limpos; `next build` OK (o erro local de @react-pdf/renderer era node_modules desatualizado — `npm install` resolveu).
- Migrations aditivas `campaign_insights.effective_status` e `alertas.silenciado_ate` APLICADAS direto no banco em transação (verificadas no information_schema).

## Pendências de validação manual (UAT)
- [ ] Editar meta de CPL no Organizar → chip do card muda ao salvar
- [ ] Cliente sem metas usa defaults do objetivo
- [ ] Rodar um sync → coluna Status do detalhamento preenche (era "—")
- [ ] Sync gera alertas novos; segundo sync não duplica; auto-resolve ao normalizar
- [ ] "Ações do dia" não recomenda escalar campanha com alerta ativo
- [ ] Score de Saúde clicável mostra breakdown

## Decisões de implementação
- Metas do semáforo vivem no jsonb `kpis` (junto com visibilidade) — zero migration, spec pedia "salva junto com a visibilidade".
- "Marcar como feito" das Ações do dia é localStorage por dia/cliente (não tabela) — reaparece no dia seguinte se a condição persistir.
- Frequência de campanha aproximada por impressões/alcance dos campaign_insights (adset/frequency diários não são sincronizados).
