# Quick Task 260717-00n: Spec "Semáforo de Metas, Alertas Proativos e Painel de Ações"

Spec completa fornecida pelo usuário (3 features + 3 correções), implementada em 3 commits sequenciais nesta tarefa.

## Feature 1 — Semáforo de metas por métrica
- Módulo puro `src/lib/trafego/semaforo.ts` (status bom/atenção/ruim/sem_dados, defaults por objetivo, resolverMetas, piorStatus, scoreSemaforo, breakdown) sob TDD.
- Metas salvas no MESMO jsonb `kpis` de preferencias_campanhas (item ganha `meta {bom, ruim, ativa}`) — zero migration; UI no Organizar.
- Chips + borda nos KPI cards; células CPM/Custo por resultado coloridas na tabela (gasto ≥ R$20); cards da home com resultado, custo/resultado e anel de status; health score recalculado pelo semáforo (ponderado por gasto) com popover de breakdown.
- Fix: Regiões "Unknown" → "Outros/não identificado" (some com 0 resultados).

## Feature 2 — Alertas proativos diários
- `src/lib/alertas/regras-campanha.ts` (puro + I/O agregado) sob TDD: gasto_sem_resultado (R$50/dia crítico), custo_acima_meta (3d seguidos), ctr_baixo (anúncio, <1% c/ 1500 imp.), gasto_disparado (>2x média 7d), entrega_parada, conta_com_problema. Integrado ao motor persistido (dedup/auto-resolução de graça).
- Colunas aditivas APLICADAS no banco: campaign_insights.effective_status e alertas.silenciado_ate.
- Sync grava effective_status por campanha (fetchCampaignStatuses); tabela mostra Status com dot (Ativa/Pausada/Reprovada em vermelho) — fix 1 da spec.
- /alertas: agrupado por cliente + ações Ver campanha (deep-link), Silenciar 7d, Resolver; sininho ignora silenciados; bloco do painel com críticos + contagem de avisos.

## Feature 3 — Painel "Ações do dia"
- `src/lib/trafego/acoes-dia.ts` (puro + I/O) sob TDD: Cortar (anúncio), Escalar (+20% com custo estável ≤ meta, freq<3, sem alerta), Renovar criativo (freq>3 ou CTR −30% com resultado na meta; prioridade sobre Escalar).
- Seção acima de Criativos campeões; "Marcar como feito" via localStorage por dia; estado vazio "conta saudável ✓".

## Fora de escopo respeitado
Nenhuma chamada de escrita à Meta; sem e-mail/WhatsApp.
