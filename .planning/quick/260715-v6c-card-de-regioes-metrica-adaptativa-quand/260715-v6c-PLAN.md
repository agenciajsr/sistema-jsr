---
phase: quick-260715-v6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/trafego/demografia.ts
  - src/lib/trafego/demografia.test.ts
  - src/lib/trafego/painel.ts
  - src/components/trafego/regioes-section.tsx
autonomous: false
requirements: [REG-01, REG-02, REG-03]
must_haves:
  truths:
    - "Quando o Meta entrega resultados da chave-herói por região (soma > 0), o card segue igual a hoje: título 'Regiões que mais vendem/geram leads/geram conversas', ranking pela chave-herói e custo por resultado"
    - "Quando a soma dos resultados da chave-herói entre regiões é 0 mas há investimento/cliques, o card ranqueia por cliques no link, com título honesto ('Regiões com mais tráfego') e custo por clique no link"
    - "No modo fallback o card exibe nota explicando que o Meta não entrega compras/leads de pixel por região (privacidade), por isso o ranking usa cliques no link"
    - "A decisão da métrica é dirigida pelo dado — nenhum hardcode por tipo/nicho de cliente"
    - "A seção de demografia (idade × gênero) continua exatamente como está"
  artifacts:
    - path: "src/lib/trafego/demografia.ts"
      provides: "rankingDeRegioes — mecânica pura que agrega regiões e escolhe a métrica do ranking"
      contains: "export function rankingDeRegioes"
    - path: "src/lib/trafego/demografia.test.ts"
      provides: "Testes do rankingDeRegioes (modo herói, modo fallback, tudo zerado)"
      contains: "rankingDeRegioes"
    - path: "src/lib/trafego/painel.ts"
      provides: "getPainelCampanhas devolvendo regioes como RankingRegioes"
      contains: "rankingDeRegioes"
    - path: "src/components/trafego/regioes-section.tsx"
      provides: "Card adaptativo (título, métrica, custo e nota conforme o modo)"
      contains: "metrica"
  key_links:
    - from: "src/lib/trafego/painel.ts"
      to: "src/lib/trafego/demografia.ts"
      via: "import { rankingDeRegioes }"
      pattern: "rankingDeRegioes\\(dedup"
    - from: "src/app/(app)/campanhas/page.tsx"
      to: "src/components/trafego/regioes-section.tsx"
      via: "prop regioes={painel.regioes} (agora RankingRegioes)"
      pattern: "RegioesSection"
---

<objective>
Tornar o card "Regiões" adaptativo à disponibilidade real do dado: quando o Meta não
entrega conversões de pixel quebradas por região (soma da chave-herói = 0), o ranking
cai para cliques no link, com título honesto, custo por clique no link e uma nota
explicando a limitação.

Purpose: hoje o card mostra investimento por região com resultados zerados — parece bug,
mas é restrição de privacidade do Meta (diagnóstico fechado, ver Contexto). Mostrar 0
sem explicação destrói a confiança no painel.
Output: mecânica pura sob TDD em demografia.ts + painel + UI do card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ETAPA-2-CAMPANHAS.md
@src/lib/trafego/demografia.ts
@src/lib/trafego/demografia.test.ts
@src/lib/trafego/metricas.ts
@src/lib/trafego/painel.ts
@src/components/trafego/regioes-section.tsx
@src/app/(app)/campanhas/page.tsx

<diagnostico_fechado>
NÃO reinvestigar, NÃO tentar outras chamadas da Graph API. Testado ao vivo na conta real
act_1763213724519896 (30d, 407 compras reais):
- sem breakdown / age,gender / country → omni_purchase = 407 (chega)
- breakdowns=region (em qualquer nível: account, campaign, ad) → omni_purchase = 0
- region + use_unified_attribution_setting / action_attribution_windows → ainda 0
- breakdowns=dma → erro #100 (não suportado)
O Meta NÃO entrega conversões de PIXEL (offsite) por região — privacidade pós-iOS 14.

O que o Meta ENTREGA por região (mesma conta, 30d): link_click (34225), page_engagement,
video_view, onsite_conversion.total_messaging_connection (59 — conversas chegam!),
onsite_conversion.lead (1 — só formulário instantâneo). NADA de omni_purchase / purchase /
offsite_conversion.fb_pixel_*.

Consequência: a regra abaixo (dirigida pelo dado) resolve sozinha o caso lead-onsite
(chega → modo herói) vs lead-de-pixel (não chega → modo fallback), sem hardcode por cliente.
</diagnostico_fechado>

<escopo>
Sem sync, sem migration: `regiao_insights` já grava `actions` completo e
`parseActionsExtendido` já extrai `linkClicks`. Mudança PURA de painel + UI.
NÃO tocar na demografia (idade × gênero) — está correta e entrega compras normalmente.
</escopo>

<interfaces>
De src/lib/trafego/metricas.ts (já existe, usar direto):
```ts
export type ResultadoActionsExtendido = {
  leads: number; vendas: number; conversas: number; linkClicks: number
  adicoesCarrinho: number; visualizacoesLp: number; engajamento: number; videoViews: number
}
export function parseActionsExtendido(actions: unknown): ResultadoActionsExtendido
```

De src/lib/trafego/aggregate.ts:
```ts
export type ChaveHeroi = 'vendas' | 'leads' | 'conversas'
```

Hoje em src/lib/trafego/demografia.ts (a MUDAR):
```ts
export type LinhaRegiao = {
  region: string; spend: number; impressions: number; clicks: number
  resultados: number; custoPorResultado: number | null
}
export function agregarRegioes(rows: LinhaRegiaoBruta[], chave: ChaveHeroi): LinhaRegiao[]
```

Consumidores atuais:
- painel.ts: `regioes = agregarRegioes(dedup, heroi.chave)`; reexporta `LinhaRegiao`;
  `painelVazio()` devolve `regioes: []`; campo `regioes: LinhaRegiao[]` em PainelCampanhas.
- regioes-section.tsx: props `{ regioes, heroiChave, labelHeroi }`, TITULO_POR_HEROI,
  barra proporcional com `var(--chart-1)`.
- page.tsx: `<RegioesSection regioes={painel.regioes} heroiChave={painel.heroi.chave} labelHeroi={painel.heroi.label} />`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rankingDeRegioes — mecânica pura da métrica adaptativa (TDD)</name>
  <files>src/lib/trafego/demografia.ts, src/lib/trafego/demografia.test.ts</files>
  <behavior>
    Novos tipos e função pura em demografia.ts (zero import de db/auth/react — só
    parseActionsExtendido e ChaveHeroi, como já é hoje):

    ```ts
    export type LinhaRegiao = {
      region: string
      spend: number
      impressions: number
      clicks: number
      linkClicks: number            // NOVO — sempre preenchido
      resultados: number            // resultado da chave-herói (pode ser 0)
      custoPorResultado: number | null
    }

    export type MetricaRegiao = 'heroi' | 'linkClicks'

    export type RankingRegioes = {
      metrica: MetricaRegiao        // 'heroi' = dado disponível; 'linkClicks' = fallback
      linhas: LinhaRegiao[]         // ordenadas pela métrica escolhida
    }

    export function rankingDeRegioes(rows: LinhaRegiaoBruta[], chave: ChaveHeroi): RankingRegioes
    ```

    Regra DIRIGIDA PELO DADO (sem hardcode de cliente/nicho):
    - agrega por região somando campanhas (mesma lógica do agregarRegioes de hoje),
      extraindo também `linkClicks` via parseActionsExtendido
    - se `soma(resultados) > 0` entre TODAS as regiões → metrica = 'heroi',
      ordena por resultados desc (desempate spend desc),
      custoPorResultado = spend / resultados (null quando resultados = 0)
    - senão → metrica = 'linkClicks', ordena por linkClicks desc (desempate spend desc),
      custoPorResultado = spend / linkClicks (null quando linkClicks = 0)
    - lista vazia → { metrica: 'heroi', linhas: [] }

    Testes a escrever ANTES (no describe novo `rankingDeRegioes`, junto dos existentes):
    - Modo herói: há resultados da chave → metrica === 'heroi', ordem por resultados desc,
      custoPorResultado = spend/resultados (reaproveitar o cenário do teste atual de
      agregarRegioes: Minas Gerais 10 leads/R$5 antes de Sao Paulo 6 leads/R$30)
    - Modo fallback: chave 'vendas', actions só com `link_click` e `page_engagement`,
      spend > 0 → metrica === 'linkClicks', ordem por linkClicks desc,
      custoPorResultado = spend/linkClicks
    - Fallback com conversas onsite presentes: chave 'conversas' e actions com
      `onsite_conversion.total_messaging_connection` > 0 → metrica === 'heroi'
      (prova que a regra é dirigida pelo dado, não pelo tipo de cliente)
    - Tudo zerado (actions null, spend '0.00') → metrica === 'linkClicks',
      linkClicks 0, custoPorResultado null, não lança
    - Lista vazia → linhas [] e não lança
  </behavior>
  <action>
    RED: escrever o describe `rankingDeRegioes` em src/lib/trafego/demografia.test.ts com
    os casos acima, usando o helper `linhaRegiao()` já existente no arquivo. Rodar e ver falhar.

    GREEN: implementar em src/lib/trafego/demografia.ts. SUBSTITUIR `agregarRegioes` por
    `rankingDeRegioes` (não manter as duas — agregarRegioes só tem um consumidor, painel.ts,
    atualizado na Task 2) e migrar/adaptar o describe `agregarRegioes` existente para a nova
    função, preservando as asserções de agregação. Adicionar `linkClicks` a `LinhaRegiao`.
    Atualizar o comentário de topo do arquivo mencionando a regra adaptativa e a razão
    (Meta não entrega conversão de pixel por região).

    Manter `resultadoDaChave` como está. Não tocar em agregarDemografia,
    deduplicarJanelaMaisRecente nem objetivoDaCampanha.
  </action>
  <verify>
    <automated>npx vitest run src/lib/trafego/demografia.test.ts</automated>
  </verify>
  <done>Testes do rankingDeRegioes passam (herói, fallback, conversas onsite, zerado, vazio); demografia.ts sem import de db/auth/react.</done>
</task>

<task type="auto">
  <name>Task 2: Ligar o painel ao rankingDeRegioes</name>
  <files>src/lib/trafego/painel.ts</files>
  <action>
    Em src/lib/trafego/painel.ts (Query D — regiao_insights, ~linha 497-525):
    - trocar o import `agregarRegioes` por `rankingDeRegioes` e adicionar os tipos
      `MetricaRegiao` e `RankingRegioes` ao import de './demografia'
    - reexportar: `export type { LinhaDemografia, LinhaRegiao, ObjetivoChip, MetricaRegiao, RankingRegioes }`
    - mudar o campo do PainelCampanhas de `regioes: LinhaRegiao[]` para `regioes: RankingRegioes`
    - `painelVazio()`: `regioes: { metrica: 'heroi', linhas: [] }`
    - no try da Query D: `let regioes: RankingRegioes = { metrica: 'heroi', linhas: [] }` e
      `regioes = rankingDeRegioes(dedup, heroi.chave)` (manter o dedupe por
      `${r.campaignId}|${r.region}` e o catch silencioso que já existem)

    Não mexer na Query C (demografia) nem na ordem/sequencialidade das queries — seguem
    sequenciais (pool max=5, nunca Promise.all).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`npx tsc --noEmit` limpo no painel; getPainelCampanhas devolve `regioes` como RankingRegioes.</done>
</task>

<task type="auto">
  <name>Task 3: Card de regiões adaptativo (título, métrica, custo e nota)</name>
  <files>src/components/trafego/regioes-section.tsx, src/app/(app)/campanhas/page.tsx</files>
  <action>
    src/components/trafego/regioes-section.tsx:
    - props passam a ser `{ ranking: RankingRegioes, heroiChave: ChaveHeroi, labelHeroi: string }`
      (importar RankingRegioes de '@/lib/trafego/painel'); `const top = ranking.linhas.slice(0, 10)`
    - valor exibido/ordenado por linha e a barra proporcional passam a usar
      `ranking.metrica === 'heroi' ? r.resultados : r.linkClicks` (max continua com fallback 1)
    - título: modo 'heroi' → TITULO_POR_HEROI[heroiChave] (como hoje);
      modo 'linkClicks' → "Regiões com mais tráfego"
    - rótulo da unidade: modo 'heroi' → labelHeroi.toLowerCase() (como hoje);
      modo 'linkClicks' → "cliques no link"
    - custo: manter o sufixo atual `/result.` no modo 'heroi'; no modo 'linkClicks' usar
      `/clique no link` (mesmo campo custoPorResultado, que a mecânica já calculou pela
      métrica escolhida; segue escondido quando null)
    - NOTA (só no modo 'linkClicks'), junto/acima da nota de ~30d que já existe, no mesmo
      estilo discreto (rounded-md bg-muted/50 text-xs text-muted-foreground), texto:
      "O Meta não entrega compras e leads de pixel separados por região (limitação de
      privacidade da plataforma). Por isso este ranking usa cliques no link."
    - manter `var(--chart-1)` na barra (cores só via --chart-N do globals.css); manter o
      estado vazio e a nota de ~30d atuais; atualizar o comentário de topo do arquivo
    - texto 100% em português

    src/app/(app)/campanhas/page.tsx:
    - `<RegioesSection ranking={painel.regioes} heroiChave={painel.heroi.chave} labelHeroi={painel.heroi.label} />`
    - não mexer em DemografiaSection nem na ordem das seções
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Card renderiza título/métrica/custo conforme ranking.metrica; nota da limitação aparece só no fallback; tsc e lint limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Card "Regiões" agora escolhe a métrica do ranking pelo dado disponível: mantém a
    chave-herói quando o Meta entrega resultados por região; cai para cliques no link
    (título "Regiões com mais tráfego" + nota explicando a limitação de privacidade do
    Meta) quando a soma da chave-herói é 0.
  </what-built>
  <how-to-verify>
    1. `npm run dev` e abrir http://localhost:3000/campanhas
    2. Selecionar o cliente "BM 02 - CA1 - Melzinho" (herói = vendas, o caso do
       diagnóstico): o card deve mostrar "Regiões com mais tráfego", ranking por cliques
       no link (São Paulo etc. com números > 0, NÃO mais zeros), custo por clique no link
       e a nota sobre a limitação do Meta.
    3. Selecionar um cliente cujo herói seja "conversas" (mensagens): se houver conversas
       por região, o card deve manter o título "Regiões que mais geram conversas" e o
       ranking pela chave-herói, SEM a nota.
    4. Conferir que a seção de Demografia (idade × gênero) segue igual, com compras.
    5. Conferir que a nota de "~30 dias da sincronização" continua no card.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que ficou errado</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/trafego/demografia.test.ts` — verde
- `npx tsc --noEmit` — limpo
- `npm run lint` — limpo
- Nenhuma alteração em sync, migrations ou na demografia (idade × gênero)
</verification>

<success_criteria>
- Soma da chave-herói > 0 → card idêntico ao comportamento de hoje (título, ranking, custo por resultado)
- Soma da chave-herói = 0 com spend/cliques → ranking por cliques no link, título "Regiões com mais tráfego", custo por clique no link e nota da limitação do Meta
- Decisão 100% dirigida pelo dado — nenhuma referência a tipo/nicho de cliente na regra
- Mecânica nova vive em src/lib/trafego/demografia.ts sob TDD, sem import de db/auth/react
</success_criteria>

<output>
Ao concluir, criar `.planning/quick/260715-v6c-card-de-regioes-metrica-adaptativa-quand/260715-v6c-SUMMARY.md`
</output>
</content>
