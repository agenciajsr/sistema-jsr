# Quick Task 260712-h4y: Melhorar Dashboard (5 pontos)

**Created:** 2026-07-12
**Mode:** quick

## Task 1: Gráfico de Evolução Financeira (receita/despesa últimos 6 meses)

**files:** `src/lib/dashboard/data.ts`, `src/components/dashboard/evolucao-financeira.tsx` (novo), `src/app/(app)/painel/page.tsx`
**action:**
1. Em `data.ts`: adicionar query que busca receitas e despesas agrupadas por mês (últimos 6 meses) da tabela `transacoes`
2. Criar componente `EvolucaoFinanceira` com Recharts (LineChart, 2 linhas: receita verde, despesa vermelha)
3. Inserir no layout do painel entre a seção de KPIs e a linha do meio

**verify:** Componente renderiza com dados do banco, mostra 2 linhas com labels corretos
**done:** Gráfico de tendência financeira visível no dashboard

## Task 2: Atividade Recente Expandida (transações + novos clientes + contratos)

**files:** `src/lib/dashboard/data.ts`, `src/components/dashboard/atividade-recente.tsx`
**action:**
1. Em `data.ts`: expandir query de atividade para incluir transações recentes (tipo='receita', status='pago') e novos clientes (createdAt recente)
2. Adicionar tipos 'transacao' e 'novo_cliente' ao AtividadeItem
3. Unificar, ordenar por data desc e limitar a 6 itens
4. Atualizar componente para exibir ícones diferenciados por tipo

**verify:** Feed mostra mix de acompanhamentos, pagamentos recebidos e novos clientes
**done:** Atividade recente diversificada

## Task 3: Filtro de Período no Header

**files:** `src/components/dashboard/filtro-periodo.tsx` (novo), `src/app/(app)/painel/page.tsx`, `src/lib/dashboard/data.ts`
**action:**
1. Criar componente client `FiltroPeriodo` com select de mês/ano (últimos 12 meses)
2. Usar searchParams na page para receber mês/ano selecionado
3. Passar mês/ano para `getDashboardData(mes, ano)` ajustando as queries financeiras

**verify:** Trocar mês no seletor recarrega dados do período selecionado
**done:** Seletor de período funcional no header do dashboard

## Task 4: Campanhas Saúde — melhorar score com metas do cliente

**files:** `src/components/dashboard/campanhas-saude.tsx`, `src/lib/dashboard/data.ts`
**action:**
1. Incluir metaCpa/metaRoas do cliente nos dados de ClientePerformance
2. Na função calcularSaude, usar metas personalizadas quando disponíveis (comparar ROAS vs metaRoas, CPA vs metaCpa)
3. Adicionar indicador visual de tendência (↑/↓) comparando com período anterior se disponível

**verify:** Clientes com metas personalizadas exibem score baseado nas suas metas
**done:** Health score usa metas reais do cliente

## Task 5: Responsividade Mobile

**files:** `src/app/(app)/painel/page.tsx`, componentes dashboard diversos
**action:**
1. Ajustar grid do painel: KPIs 2 colunas em mobile, 1 coluna para seções de cards
2. Gráfico de evolução: altura reduzida em mobile
3. Tabela de performance: scroll horizontal em mobile
4. Cards inferiores: stack vertical em telas < 768px
5. Testar que nada quebra nos breakpoints sm/md/lg

**verify:** Layout não quebra em viewport 375px
**done:** Dashboard responsivo para mobile
