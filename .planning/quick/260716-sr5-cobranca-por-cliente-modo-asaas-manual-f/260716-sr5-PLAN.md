---
phase: quick-260716-sr5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0033_modo_cobranca.sql
  - scripts/aplicar-migration-0033.ts
  - src/lib/db/schema.ts
  - src/lib/cobrancas/regras.ts
  - src/lib/cobrancas/regras.test.ts
  - src/lib/cobrancas/gerar.ts
  - src/lib/cobrancas/dados.ts
  - src/actions/cobrancas.ts
  - src/components/crm/converter-cliente-dialog.tsx
  - src/components/ficha/faturas-cliente.tsx
  - src/components/ficha/cobranca-cliente.tsx
  - src/app/(app)/contratos/tabela-contratos.tsx
  - src/app/(app)/financeiro/page.tsx
  - src/app/(app)/financeiro/cobrancas-tab.tsx
autonomous: true
requirements: [SR5-MODO, SR5-CONVERSAO, SR5-ABA-COBRANCAS, SR5-FIX-BOTAO]

must_haves:
  truths:
    - "Cliente em modo manual_pix NUNCA gera chamada ao Asaas (nem customer, nem payment), mas a fatura local mensal nasce pendente sem invoiceUrl"
    - "O modo de cobrança é escolhido no dialog de conversão de lead ganho e editável na aba Faturas da ficha"
    - "A aba Cobranças em /financeiro lista todos os clientes ativos com modo, status da fatura do mês corrente e ações por linha"
    - "O botão Gerar cobrança em /contratos nunca falha silenciosamente: todo desfecho vira toast pt-BR de sucesso/aviso/erro"
  artifacts:
    - path: "drizzle/0033_modo_cobranca.sql"
      provides: "Coluna clientes.modo_cobranca text NOT NULL com backfill a partir de usa_asaas"
    - path: "scripts/aplicar-migration-0033.ts"
      provides: "Script de aplicação manual (padrão 0032), NÃO executado neste plano"
    - path: "src/app/(app)/financeiro/cobrancas-tab.tsx"
      provides: "Aba Cobranças consolidada (client component com actions e toasts)"
    - path: "src/lib/cobrancas/dados.ts"
      provides: "getVisaoCobrancas() — queries agregadas sequenciais"
  key_links:
    - from: "src/lib/cobrancas/gerar.ts"
      to: "clientes.modoCobranca"
      via: "gerarCobrancaDoMes lê o modo e pula o bloco Asaas quando manual_pix"
      pattern: "modoCobranca"
    - from: "src/app/(app)/financeiro/page.tsx"
      to: "getVisaoCobrancas"
      via: "fetch sequencial após o lote 2 + TabsTrigger 'cobrancas'"
      pattern: "getVisaoCobrancas"
    - from: "src/components/crm/converter-cliente-dialog.tsx"
      to: "modo_cobranca"
      via: "campo novo no dialog persistido pela action de conversão"
      pattern: "modo_cobranca|modoCobranca"
---

<objective>
Fase 5 Parte 2 — Controle de cobrança POR CLIENTE: modo `automatico_asaas` vs `manual_pix` (cliente manual nunca toca o Asaas, mas a fatura local mensal continua nascendo para controle), escolha do modo na conversão do lead ganho + edição na aba Faturas da ficha, aba "Cobranças" consolidada em /financeiro com ações por linha, e correção do botão "Gerar cobrança no Asaas" em /contratos que falha silenciosamente.

Purpose: sem o modo por cliente, o Asaas cobraria taxa em clientes que sempre pagam PIX direto na chave do dono; e sem a visão consolidada não há como saber rapidamente quem já pagou o mês.
Output: migration 0033 (GERADA, NÃO aplicada), fluxo de geração respeitando o modo, 3 pontos de UI (dialog de conversão, ficha, /financeiro) e botão de /contratos confiável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260716-qzu-fase-5-parte-1-cobranca-asaas-cliente-co/260716-qzu-SUMMARY.md
@src/lib/cobrancas/gerar.ts
@src/lib/cobrancas/regras.ts
@src/lib/cobrancas/dados.ts
@src/actions/cobrancas.ts
@src/lib/asaas/client.ts
@src/lib/db/schema.ts
@src/components/ficha/faturas-cliente.tsx
@src/components/ficha/cobranca-cliente.tsx
@src/components/crm/converter-cliente-dialog.tsx
@src/app/(app)/contratos/tabela-contratos.tsx
@src/app/(app)/financeiro/page.tsx

<interfaces>
Contratos existentes (Parte 1) que este plano estende — NÃO redesenhar:

De src/actions/cobrancas.ts:
```typescript
export type ResultadoAcaoCobranca =
  | { ok: true; invoiceUrl?: string | null; aviso?: string }
  | { ok: false; erro: string }
export async function gerarCobrancaManual(contratoId: string): Promise<ResultadoAcaoCobranca>
export async function confirmarRecebimentoManual(cobrancaId: string): Promise<ResultadoAcaoCobranca>
```

De src/lib/cobrancas/gerar.ts:
```typescript
export async function garantirClienteAsaas(clienteId: string): Promise<string>
export async function gerarCobrancaDoMes(contrato, competencia, { criadoVia }): Promise<ResultadoCobranca>
export async function gerarCobrancasMensais(): Promise<ResumoCobrancasMensais>
```

De src/lib/asaas/client.ts: `asaasDisponivel(): boolean` (degradação sem env).

Schema atual (src/lib/db/schema.ts): `clientes.usaAsaas: boolean('usa_asaas').notNull().default(false)` — coluna ANTIGA, será depreciada; `clientes.asaasCustomerId`; tabela `cobrancas` com índice único parcial (contrato_id, competencia) WHERE criado_via='automatico'.

Ponto de bug conhecido em gerarCobrancaManual: o insert usa `criadoVia: 'manual'`, que NÃO bate no índice único parcial (só cobre 'automatico') — clicar o botão quando a fatura do mês já existe cria linha DUPLICADA local em vez de reaproveitar/retentar o Asaas na fatura existente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fundação — migration 0033 (modo_cobranca), geração respeitando o modo e fix do fluxo manual</name>
  <files>drizzle/0033_modo_cobranca.sql, scripts/aplicar-migration-0033.ts, src/lib/db/schema.ts, src/lib/cobrancas/gerar.ts, src/lib/cobrancas/regras.ts, src/lib/cobrancas/regras.test.ts, src/actions/cobrancas.ts</files>
  <behavior>
    Novos testes em regras.test.ts (módulo puro, zero db) para o helper de decisão:
    - Test 1: `deveUsarAsaas({ modoCobranca: 'automatico_asaas' })` → true
    - Test 2: `deveUsarAsaas({ modoCobranca: 'manual_pix' })` → false
    - Test 3: modo desconhecido/nulo (dado legado) → false (seguro: nunca cobrar taxa por engano)
  </behavior>
  <action>
    **Migration 0033** (`drizzle/0033_modo_cobranca.sql`, ADITIVA, NÃO aplicar — orquestrador aplica):
    - `ALTER TABLE clientes ADD COLUMN modo_cobranca text NOT NULL DEFAULT 'manual_pix';` (default seguro: manual nunca gera custo no Asaas)
    - Backfill: `UPDATE clientes SET modo_cobranca = 'automatico_asaas' WHERE usa_asaas = true;` e também `WHERE asaas_customer_id IS NOT NULL` (quem já foi cadastrado no Asaas é automático)
    - `COMMENT ON COLUMN clientes.usa_asaas IS 'DEPRECIADA — substituída por modo_cobranca (0033). Não usar em código novo.';`
    - Separar statements com `--> statement-breakpoint` como nas migrations anteriores.

    **Script** `scripts/aplicar-migration-0033.ts`: copiar o padrão EXATO de `scripts/aplicar-migration-0032.ts` (DIRECT_URL, sql.begin transação, pré-checagem de que a 0032 foi aplicada via information_schema, confirmação pós-aplicação). NÃO executar (memória do projeto: migrations na mão, orquestrador aplica).

    **Schema** (`src/lib/db/schema.ts`): adicionar `modoCobranca: text('modo_cobranca').notNull().default('manual_pix')` em clientes; comentar `usaAsaas` como depreciada (manter a coluna no schema para não quebrar código antigo).

    **Regras** (`src/lib/cobrancas/regras.ts`): exportar `type ModoCobranca = 'automatico_asaas' | 'manual_pix'` e helper puro `deveUsarAsaas(cliente: { modoCobranca: string | null }): boolean` — true SOMENTE se modoCobranca === 'automatico_asaas' (RED→GREEN conforme behavior).

    **gerar.ts**:
    - `gerarCobrancaDoMes`: incluir `modoCobranca` no select do cliente; a fatura local nasce SEMPRE (inalterado); o bloco Asaas (garantirClienteAsaas + criarCobranca) só roda se `asaasDisponivel() && deveUsarAsaas(cliente)`. Cliente manual → retorna `{ criada: true, invoiceUrl: null }` sem tocar o Asaas.
    - NOVO `retentarAsaasNaFatura(cobrancaId)` (ou lógica equivalente dentro da action): dado uma fatura pendente sem asaasPaymentId de cliente automático, chama garantirClienteAsaas + criarCobranca e grava asaasPaymentId/invoiceUrl na MESMA linha — é o backfill que o botão manual precisa.

    **Fix do bug de gerarCobrancaManual** (`src/actions/cobrancas.ts`) — causa do "atualizou mas não fez nada":
    - ANTES de inserir, buscar fatura existente do (contratoId, competência atual) com status pendente/vencida. Se existir: NÃO duplicar. Se ela já tem invoiceUrl → `{ ok: true, invoiceUrl }` com aviso "A fatura deste mês já existia — link reaproveitado."; se não tem e o cliente é automático → retentar o Asaas nela (backfill acima) e devolver o link ou erro legível do Asaas; se cliente é manual_pix → `{ ok: false, erro: 'Este cliente está em cobrança manual (PIX direto) — não geramos cobrança no Asaas. Use "Confirmar recebimento" na ficha.' }` quando a fatura já existe, ou apenas criar a fatura local com aviso claro quando não existe.
    - Se não existir fatura: fluxo atual (gerarCobrancaDoMes) — que agora respeita o modo.
    - Garantir que TODO caminho retorna `ResultadoAcaoCobranca` com mensagem pt-BR (nenhum throw escapa sem virar `{ ok: false, erro }` — o try/catch externo já existe, manter).

    **Novas actions** em `src/actions/cobrancas.ts` (mesmo padrão: getCurrentUser, revalidatePath, retorno ResultadoAcaoCobranca):
    - `setModoCobranca(clienteId, modo: 'automatico_asaas' | 'manual_pix')` — valida com Zod enum, atualiza clientes.modoCobranca (e usaAsaas em espelho para não dessincronizar telas antigas: true se automático).
    - `cadastrarClienteNoAsaas(clienteId)` — chama garantirClienteAsaas em try/catch; erro do Asaas (ex.: sem CPF/CNPJ) volta como `{ ok: false, erro }` legível.
    - `gerarCobrancaDoMesPorCliente(clienteId)` — resolve o contrato assinado vigente do cliente (reaproveitar `contratoElegivel`) e delega para a mesma lógica de gerarCobrancaManual; sem contrato vigente → erro pt-BR claro. Usada pela aba Cobranças.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cobrancas/regras.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Migration 0033 gerada (NÃO aplicada) + script no padrão 0032; deveUsarAsaas testado (3+ casos); gerarCobrancaDoMes pula o Asaas para manual_pix; gerarCobrancaManual não duplica fatura e retenta o Asaas na linha existente; actions novas retornam sempre erro/sucesso legível pt-BR.</done>
</task>

<task type="auto">
  <name>Task 2: Modo na conversão do lead ganho e edição na ficha do cliente</name>
  <files>src/components/crm/converter-cliente-dialog.tsx, src/components/ficha/faturas-cliente.tsx, src/components/ficha/cobranca-cliente.tsx, src/lib/cobrancas/dados.ts, src/app/(app)/clientes/[id]/page.tsx</files>
  <action>
    **Dialog de conversão** (`converter-cliente-dialog.tsx`): adicionar campo "Modo de cobrança" (Select/RadioGroup pt-BR: "Automático via Asaas (boleto/link)" | "Manual via PIX (direto na chave)"), default `manual_pix`. Propagar o valor até a action de conversão (`converterOportunidadeEmCliente` — localizar em src/actions/* ou src/lib/crm/*, estender o schema Zod de entrada com `modoCobranca` enum) e gravar em `clientes.modoCobranca` no insert do cliente (espelhar usaAsaas = modo automático). Seguir o padrão visual das seções já existentes do dialog (duração/serviço/mensalidade).

    **Aba Faturas da ficha** (`faturas-cliente.tsx`): no topo da aba, mostrar o modo atual como badge ("Asaas" azul / "Manual PIX" âmbar, com variantes dark:) + controle de troca (Select ou botão "Alterar modo") chamando `setModoCobranca` com toast de sucesso/erro (sonner). Para isso a página `clientes/[id]/page.tsx` (ou `getFaturasDoCliente` em dados.ts) precisa passar `modoCobranca` do cliente como prop — query sequencial, sem Promise.all novo.

    **Componente antigo** `cobranca-cliente.tsx` (switch usaAsaas): trocar o switch booleano pelo mesmo seletor de modo (chamando setModoCobranca) OU, se a aba Faturas já cobre o caso, reduzir o componente a exibir o modo como badge apontando para a aba Faturas — NÃO deixar dois controles divergentes (usa_asaas vs modo_cobranca) editáveis ao mesmo tempo. `setUsaAsaas` em src/actions/financeiro.ts pode permanecer, mas nenhuma UI deve mais chamá-la.

    Tudo 100% pt-BR; dark mode nas badges (padrão `dark:` já usado nas badges de status da Parte 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Conversão de lead ganho pede o modo de cobrança e persiste; aba Faturas mostra e edita o modo com toast; nenhum controle remanescente edita usa_asaas diretamente pela UI.</done>
</task>

<task type="auto">
  <name>Task 3: Aba "Cobranças" em /financeiro (visão consolidada) + toasts confiáveis em /contratos</name>
  <files>src/lib/cobrancas/dados.ts, src/app/(app)/financeiro/cobrancas-tab.tsx, src/app/(app)/financeiro/page.tsx, src/app/(app)/contratos/tabela-contratos.tsx</files>
  <action>
    **Dados** (`src/lib/cobrancas/dados.ts`): nova `getVisaoCobrancas()` com poucas queries AGREGADAS SEQUENCIAIS (pool max=3/5 — nunca Promise.all interno, padrão do STATE.md): (1) clientes ativos/aguardando_inicio/em_aviso com id, nome, modoCobranca, asaasCustomerId; (2) contratos assinados desses clientes (para saber quem tem contrato vigente via contratoElegivel); (3) cobranças da competência corrente (competenciaDe(hojeBrasilia())) desses clientes. Merge em memória → linhas `{ clienteId, nome, modo, temContratoVigente, contratoId, fatura: { id, status, valor, vencimento, invoiceUrl } | null }`.

    **Componente** (`cobrancas-tab.tsx`, client): tabela pt-BR com colunas Cliente | Modo (badge "Asaas"/"Manual PIX") | Fatura do mês (badge paga/pendente/vencida/"Sem fatura", variantes dark:) | Valor | Vencimento | Ações. Ações por linha via useTransition + toast sonner:
    - "Cadastrar no Asaas" (só se modo automático e asaasCustomerId nulo) → `cadastrarClienteNoAsaas`
    - "Gerar cobrança do mês" (se tem contrato vigente e sem fatura do mês, ou fatura sem link em modo automático) → `gerarCobrancaDoMesPorCliente`
    - "Quitar (PIX manual)" (se fatura pendente/vencida) → `confirmarRecebimentoManual`
    - Fatura com invoiceUrl → link "Ver fatura" (target _blank)
    Estados: linha sem contrato vigente mostra "—" com title explicativo; banner discreto quando `asaasDisponivel()` é false (passar como prop do server). TODO resultado de action vira toast success/error/warning — nada silencioso.

    **Wiring** (`financeiro/page.tsx`): dentro de `carregarDados`, buscar `getVisaoCobrancas()` como fetch SEQUENCIAL depois do lote 2 (não engordar os Promise.all existentes); adicionar `<TabsTrigger value="cobrancas">Cobranças ({n})</TabsTrigger>` após "A Receber" (n = clientes com fatura pendente/vencida ou sem fatura) e o `<TabsContent value="cobrancas">` com Card no padrão das outras abas.

    **/contratos** (`tabela-contratos.tsx`): revisar o handler `gerarCobranca` para cobrir os retornos novos da action (aviso de fatura reaproveitada → toast.info/warning com o texto; erro → toast.error; sucesso com invoiceUrl → toast.success incluindo ação/menção ao link). Ajustar tooltip do botão: para cliente manual_pix o clique deve resultar em toast claro (o erro pt-BR da Task 1), nunca silêncio. Se a linha da tabela já souber o modo do cliente (getContratosConsolidados), pode desabilitar com tooltip "Cliente em cobrança manual (PIX)" — opcional, só se o dado já estiver disponível barato.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Aba Cobranças lista todos os clientes ativos com modo + status da fatura do mês e as 3 ações funcionam com toast; /financeiro mantém o padrão de abas e a carga sequencial; botão de /contratos sempre dá feedback visível.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — suíte inteira verde (415+ testes, novos casos de deveUsarAsaas incluídos).
- `npx tsc --noEmit` e `npm run build` — sem erros (ECONNREFUSED na geração estática sem banco é esperado).
- Migration 0033 ADITIVA, NÃO aplicada durante a execução; script segue o padrão 0032.
- Grep de sanidade: nenhum caminho de cliente manual_pix chama criarCliente/criarCobranca do Asaas; nenhuma action de cobrança retorna undefined/void para a UI.
</verification>

<success_criteria>
- Cliente manual_pix: fatura local mensal nasce pendente sem invoiceUrl (cron e gatilho de assinatura), quitável pelo botão manual; zero chamadas ao Asaas.
- Cliente automatico_asaas: fluxo Asaas completo da Parte 1 intacto.
- Modo escolhido na conversão do lead ganho e editável na aba Faturas.
- Aba Cobranças em /financeiro consolidada com ações por linha e toasts.
- Botão de /contratos corrigido: não duplica fatura, retenta o Asaas na fatura existente e todo desfecho tem toast pt-BR.
</success_criteria>

<output>
After completion, create `.planning/quick/260716-sr5-cobranca-por-cliente-modo-asaas-manual-f/260716-sr5-SUMMARY.md`
</output>
