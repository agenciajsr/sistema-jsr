---
phase: quick-260717-qq6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/alertas/types.ts
  - src/lib/alertas/avaliar.ts
  - src/lib/alertas/avaliar-operacional.ts
  - src/lib/alertas/avaliar-operacional.test.ts
  - src/lib/alertas/calcular.ts
  - src/lib/crm/sla-contato.ts
  - src/lib/crm/sla-contato.test.ts
  - src/lib/db/schema.ts
  - drizzle/0034_primeiro_contato.sql
  - scripts/aplicar-migration-0034.ts
  - src/actions/crm-atividades.ts
  - src/lib/crm/dados.ts
  - src/components/crm/card-oportunidade.tsx
autonomous: true
requirements: [QUICK-QQ6]
must_haves:
  truths:
    - "Fatura pendente vencendo em ≤3 dias ou vencida gera alerta persistido interno (dedup + resolução automática ao pagar/cancelar)"
    - "Contrato em aguardando_assinatura há >3 dias gera alerta persistido"
    - "Concluir a primeira atividade/tarefa comercial de um lead carimba primeiro_contato_em na oportunidade"
    - "Card do kanban de lead aberto sem 1º contato mostra 'aguardando 1º contato há Xh' e fica destacado em vermelho ao estourar 24h"
    - "Lead aberto com SLA de 24h estourado gera alerta persistido"
  artifacts:
    - path: "src/lib/alertas/avaliar-operacional.ts"
      provides: "avaliadores puros: cobranças (vencendo/vencida), assinatura pendente, SLA 1º contato"
    - path: "src/lib/crm/sla-contato.ts"
      provides: "módulo puro do SLA: SLA_PRIMEIRO_CONTATO_HORAS=24, horasAguardando, estourouSla, textoAguardando"
    - path: "drizzle/0034_primeiro_contato.sql"
      provides: "migration aditiva primeiro_contato_em (NÃO aplicada automaticamente)"
    - path: "scripts/aplicar-migration-0034.ts"
      provides: "script pontual de aplicação manual (padrão dos 0029-0033)"
  key_links:
    - from: "src/lib/alertas/calcular.ts"
      to: "avaliar-operacional.ts"
      via: "queries sequenciais + avaliadores, resultados entram no array de ordenarPorSeveridade"
      pattern: "avaliarCobrancas|avaliarAssinaturaPendente|avaliarSlaPrimeiroContato"
    - from: "src/actions/crm-atividades.ts (concluirAtividadeCrm e criação de atividade de contato)"
      to: "crm_oportunidades.primeiro_contato_em"
      via: "UPDATE condicional (só se null) dentro de try/catch tolerante a coluna inexistente"
      pattern: "primeiroContatoEm"
    - from: "src/components/crm/card-oportunidade.tsx"
      to: "src/lib/crm/sla-contato.ts"
      via: "campo novo no OportunidadeCard vindo de dados.ts"
      pattern: "aguardando 1º contato"
---

<objective>
Três alertas operacionais internos integrados à infra de alertas persistidos existente: (1) régua de inadimplência interna sobre `cobrancas`, (2) lembrete de contrato aguardando assinatura >3 dias, (3) SLA de 1º contato de 24h no CRM (carimbo `primeiro_contato_em`, indicador no card do kanban, alerta ao estourar).

Purpose: o Asaas notifica o cliente, mas o TIME não tem visão de inadimplência/assinaturas paradas/leads sem contato — tudo entra no ciclo de vida de alertas já existente (dedup, reabertura, resolução automática, silenciar).
Output: avaliadores puros testados, 1 migration aditiva GERADA (script pontual, NÃO executada), carimbo do 1º contato nas actions, indicador no card do kanban.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/alertas/types.ts
@src/lib/alertas/avaliar.ts
@src/lib/alertas/calcular.ts
@src/lib/alertas/persistir.ts
@src/lib/db/schema.ts (cobrancas ~l.109, contratos ~l.72, crmOportunidades ~l.800)
@src/actions/crm-atividades.ts
@src/lib/crm/dados.ts (heurística semContato ~l.475-485)
@src/components/crm/card-oportunidade.tsx

<interfaces>
Já existentes — usar, não recriar:

```typescript
// src/lib/alertas/types.ts
export type TipoAlerta = 'contrato_vencendo' | 'pagamento_vencido' | ... // união a ESTENDER
export interface Alerta { id; tipo; severidade: 'critico'|'atencao'|'info'; titulo; detalhe; clienteNome; clienteId; dataRelevante }

// src/lib/alertas/calcular.ts — calcularAlertasAtuais(): queries SEQUENCIAIS + avaliadores puros,
// tudo concatenado em ordenarPorSeveridade([...]). Alerta.id = chaveDedup estável (ex.: `contrato-${id}`).
// persistir.ts cuida de dedup/reabertura/resolução automática — NADA a mudar lá.

// cobrancas: status 'pendente'|'paga'|'vencida'|'cancelada', vencimento (date), competencia 'YYYY-MM'
// contratos: statusFluxo text — 'aguardando_dados'|'dados_recebidos'|'aguardando_assinatura'|'assinado';
//   enviadoParaAssinaturaEm timestamp (base do "há mais de 3 dias")
// crmOportunidades: status 'aberta'|'ganha'|'perdida', createdAt
// concluirAtividadeCrm (src/actions/crm-atividades.ts l.202) — ponto do carimbo na conclusão
// criarAtividade... (mesmo arquivo, registrarAtividadeCrm l.64/168) — atividade de contato registrada
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Avaliadores puros — inadimplência interna + assinatura pendente + SLA (módulos) — e integração no calcular.ts</name>
  <files>src/lib/alertas/types.ts, src/lib/alertas/avaliar-operacional.ts, src/lib/alertas/avaliar-operacional.test.ts, src/lib/crm/sla-contato.ts, src/lib/crm/sla-contato.test.ts, src/lib/alertas/calcular.ts</files>
  <behavior>
    avaliar-operacional.test.ts (vitest, padrão de regras-campanha.test.ts):
    - avaliarCobrancas: pendente vencendo em 3/1/0 dias → tipo 'fatura_vencendo', severidade 'atencao' (0-1 dia = 'critico'); pendente vencendo em 5 dias → nada; status 'vencida' → tipo 'fatura_vencida', 'critico', detalhe cita dias de atraso ("em atraso há N dias") e valor formatado pt-BR; 'paga'/'cancelada' → nada; id estável `fatura-vencendo-${id}` / `fatura-vencida-${id}` (chaves DISTINTAS: vencendo→vencida resolve um e abre outro)
    - avaliarAssinaturaPendente: statusFluxo 'aguardando_assinatura' com enviadoParaAssinaturaEm há >3 dias → tipo 'assinatura_pendente', 'atencao', id `assinatura-${contratoId}`, detalhe com dias parado; ≤3 dias ou outro statusFluxo → nada; enviadoParaAssinaturaEm null → usa createdAt como fallback
    sla-contato.test.ts:
    - SLA_PRIMEIRO_CONTATO_HORAS === 24 (constante exportada)
    - horasAguardando(criadaEm, agora) → horas decimais; estourouSla(criadaEm, agora) → boolean no limite exato (24h em ponto = estourado)
    - textoAguardando(horas) → "aguardando 1º contato há 3h", "há 26h", ≥48h vira "há 2d" (pt-BR)
    - avaliarSlaPrimeiroContato (em avaliar-operacional.ts): oportunidade aberta, primeiroContatoEm null, criada há >24h → tipo 'sla_primeiro_contato', 'atencao', id `sla-contato-${oportunidadeId}`, clienteNome = título/contato do lead; com primeiroContatoEm preenchido ou <24h ou status != aberta → nada
  </behavior>
  <action>
    1. Estender TipoAlerta em types.ts com 'fatura_vencendo' | 'fatura_vencida' | 'assinatura_pendente' | 'sla_primeiro_contato' (aditivo).
    2. Criar src/lib/crm/sla-contato.ts PURO (zero import de db/react): constante SLA_PRIMEIRO_CONTATO_HORAS = 24 bem comentada (configurável = editar aqui; sem UI por ora), horasAguardando, estourouSla, textoAguardando.
    3. Criar src/lib/alertas/avaliar-operacional.ts PURO com os 3 avaliadores acima recebendo rows tipadas (mesmo estilo de avaliar.ts: input types locais, formatarMoeda local ou extraído).
    4. Integrar em calcular.ts: 3 queries novas SEQUENCIAIS (nunca Promise.all, pool max=3) — (a) cobrancas com status 'pendente' e vencimento ≤ hoje+3d OU status 'vencida', join clientes p/ nome; (b) contratos com statusFluxo='aguardando_assinatura' join clientes; (c) crm_oportunidades abertas com primeiro_contato_em null, join crm_contatos p/ nome (leftJoin). A query (c) DENTRO de try/catch retornando [] — a coluna primeiro_contato_em pode ainda não existir no banco (migration manual pendente); mesma degradação graciosa do padrão getWorkspaceAtual. Concatenar os resultados dos avaliadores no array de ordenarPorSeveridade.
    Mensagens todas em pt-BR. Nenhuma mudança em persistir.ts (dedup/ciclo de vida já cobrem os tipos novos).
  </action>
  <verify>
    <automated>npx vitest run src/lib/alertas/avaliar-operacional.test.ts src/lib/crm/sla-contato.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Avaliadores puros testados; calcularAlertasAtuais inclui os 3 novos tipos com queries sequenciais e degradação graciosa; suíte inteira verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0034 primeiro_contato_em + script pontual + carimbo do 1º contato nas actions do CRM</name>
  <files>src/lib/db/schema.ts, drizzle/0034_primeiro_contato.sql, scripts/aplicar-migration-0034.ts, src/actions/crm-atividades.ts</files>
  <action>
    1. Adicionar em crmOportunidades no schema.ts: primeiroContatoEm: timestamp('primeiro_contato_em', { withTimezone: true }) — nullable, aditivo, com comentário explicando a semântica (quando a 1ª tarefa/atividade de contato do lead é concluída/registrada).
    2. Gerar a migration com npx drizzle-kit generate (conferir que sai SÓ o ALTER TABLE ADD COLUMN; renomear/ajustar para drizzle/0034_primeiro_contato.sql se o kit nomear diferente, mantendo journal coerente). NUNCA rodar drizzle-kit migrate.
    3. Criar scripts/aplicar-migration-0034.ts copiando o padrão de scripts/aplicar-migration-0033.ts (lê DIRECT_URL, separa por '--> statement-breakpoint', sql.begin() transacional, checagem prévia em information_schema se a coluna já existe). NÃO EXECUTAR o script — aplicação é manual pelo usuário (decisão 260715-1rq).
    4. Carimbar o 1º contato em src/actions/crm-atividades.ts:
       - Em concluirAtividadeCrm (l.202): após marcar concluida, UPDATE crm_oportunidades SET primeiro_contato_em = now() WHERE id = oportunidadeId AND primeiro_contato_em IS NULL (idempotente — só a primeira conclui o carimbo).
       - No fluxo de criação/registro de atividade de contato (o criar-atividade que chama registrarAtividadeCrm l.64 — ler o arquivo e identificar quais tipos representam contato real: ligação/whatsapp/e-mail/reunião; conclusão de tarefa e registro de atividade de contato carimbam, criação de tarefa FUTURA não).
       - Ambos os UPDATEs dentro de try/catch que só loga (console.error) — coluna pode não existir até a migration ser aplicada; o fluxo principal NUNCA quebra por causa do carimbo.
    Não tocar em drizzle-kit migrate nem aplicar nada no banco.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f drizzle/0034_primeiro_contato.sql && test -f scripts/aplicar-migration-0034.ts && grep -q "primeiroContatoEm" src/lib/db/schema.ts && grep -q "primeiro_contato_em" src/actions/crm-atividades.ts</automated>
  </verify>
  <done>Coluna no schema Drizzle, SQL aditivo gerado, script pontual pronto (não executado), carimbo idempotente e tolerante a coluna ausente nos dois pontos de contato.</done>
</task>

<task type="auto">
  <name>Task 3: Indicador de SLA no card do kanban (via dados.ts) sem quebrar a heurística semContato</name>
  <files>src/lib/crm/dados.ts, src/components/crm/card-oportunidade.tsx</files>
  <action>
    1. dados.ts: na query dos cards (getCrmVisaoGeral), selecionar também primeiro_contato_em — mas de forma tolerante: como a migration pode não estar aplicada, usar sql`` com to_jsonb/exceção NÃO — mais simples e no padrão do projeto: fazer uma query agregada SEPARADA e sequencial (SELECT id, primeiro_contato_em FROM crm_oportunidades WHERE ...) dentro de try/catch retornando Map vazio quando a coluna não existe; merge em memória no montarCard. Adicionar ao OportunidadeCard os campos: aguardando1oContato: boolean (status aberta E primeiroContatoEm null) e horasAguardando1oContato: number | null (via horasAguardando de sla-contato.ts). A heurística semContato (+7d, l.475-485) fica INTOCADA — o SLA de 24h é um sinal mais fino e independente.
    2. card-oportunidade.tsx: quando aguardando1oContato, exibir linha discreta "aguardando 1º contato há {textoAguardando}" (text-xs text-muted-foreground); quando estourouSla (≥24h), destacar em vermelho: texto text-red-600 dark:text-red-400 + borda/anel sutil no card (ex.: ring-1 ring-red-500/40) — sempre com variante dark:. Não conflitar com o aviso "Não contatado" existente (se os dois ativos, o de SLA prevalece visualmente). pt-BR em tudo.
    3. Rodar a suíte completa + build para garantir zero regressão no kanban (drag, polling 30s, dialogs intocados).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Card de lead aberto sem 1º contato mostra o tempo aguardando; ao estourar 24h fica vermelho (light e dark); heurística +7d e KPIs existentes inalterados; suíte e build verdes.</done>
</task>

</tasks>

<verification>
- vitest completo verde (novos testes de avaliar-operacional e sla-contato inclusos)
- tsc --noEmit e npm run build verdes
- drizzle/0034_primeiro_contato.sql contém APENAS ALTER TABLE ADD COLUMN (aditivo) e NÃO foi aplicado
- calcularAlertasAtuais não usa Promise.all (queries sequenciais)
</verification>

<success_criteria>
- 3 novos grupos de alerta integrados ao ciclo de vida persistido existente sem mudanças em persistir.ts
- Carimbo primeiro_contato_em idempotente e tolerante a migration pendente
- Indicador de SLA no card com dark mode, sem quebrar semContato +7d
- Migration aditiva gerada + script pontual seguindo o padrão do projeto, aplicação manual a cargo do usuário
</success_criteria>

<output>
After completion, create `.planning/quick/260717-qq6-alertas-regua-inadimplencia-interna-lemb/260717-qq6-SUMMARY.md`
</output>
