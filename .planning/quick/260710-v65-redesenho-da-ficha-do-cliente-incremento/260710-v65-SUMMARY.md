---
phase: quick-260710-v65
plan: 01
subsystem: clientes
tags: [ui, ficha-cliente, redesign, mock, abas]
requires:
  - "src/lib/mock/dashboard.ts (clientesTrafegoMock, financeiroMock)"
  - "src/lib/mock/extra.ts (checklistMock, acompanhamentoMock)"
  - "src/components/stat-card.tsx"
  - "src/components/mock-notice.tsx"
  - "src/actions/contratos.ts (getContratosDoCliente, deleteContrato)"
  - "src/actions/clientes.ts (deleteCliente)"
  - "src/components/contrato-form.tsx"
provides:
  - "getMockDaFicha(nome) — compila dados de exemplo da ficha por nome do cliente"
  - "ChecklistCliente — client component de checklist interativo (estado local)"
  - "Ficha do cliente redesenhada como hub com cabeçalho, 4 KPIs e 4 abas"
affects:
  - "src/app/(app)/clientes/[id]/page.tsx"
tech-stack:
  added: []
  patterns:
    - "Server Component (page) renderizando Tabs (client island) com children server-side"
    - "Mock casado por nome com fallback neutro (nunca quebra fora do mock)"
    - "Cobrança derivada do financeiro mock (LOCKED: controle manual, sem banco)"
key-files:
  created:
    - "src/lib/mock/ficha-cliente.ts"
    - "src/components/ficha/checklist-cliente.tsx"
  modified:
    - "src/app/(app)/clientes/[id]/page.tsx"
decisions:
  - "Cobrança é mock/visual derivada do financeiroMock (status 'próximo' → 'pendente', senão 'pago'); persistência real via Asaas fica para incremento funcional futuro"
  - "Status 'vencido' de cobrança existe no tipo mas não é atingível pelo mock atual (reservado para quando houver dados reais)"
  - "getMockDaFicha vive em src/lib/mock (server-safe, sem hooks); checklist interativo isolado em client component para manter a page como Server Component async"
metrics:
  duration: 4min
  tasks: 2
  files: 3
  completed: 2026-07-11
---

# Quick 260710-v65: Redesenho da Ficha do Cliente (incremento) Summary

Ficha do cliente (`/clientes/[id]`) transformada em hub central com cabeçalho estilo Painel, faixa de 4 KPIs (StatCard com cor semântica) e corpo organizado em 4 abas (Contrato & Cobrança, Contas de anúncio, Checklist, Acompanhamento), preservando 100% do comportamento real existente (dados do banco, ContratoForm, exclusões admin-only com AlertDialog e copy idêntica, notFound).

## O que foi construído

**Task 1 — Helper de mock + checklist interativo** (commit `6a5fe3f`)
- `src/lib/mock/ficha-cliente.ts`: `getMockDaFicha(nomeCliente)` casa `clientesTrafegoMock`, `financeiroMock`, `checklistMock` e `acompanhamentoMock` pelo NOME do cliente e deriva um `CobrancaMock`. Retorna sempre objeto válido (arrays vazios / undefined tratados) para nomes desconhecidos.
- `src/components/ficha/checklist-cliente.tsx`: client component (`'use client'`) com `useState` local; `Checkbox` controlado alterna `feito` por id, texto marcado com `line-through text-muted-foreground`, `frequencia` como Badge, estado vazio tratado. Não toca banco nem server actions.

**Task 2 — Ficha redesenhada como hub** (commit `7cb6edb`)
- Cabeçalho estilo Painel: nome grande, Badge de status (STATUS_COLOR preservado), nicho, contato, botões Editar/Excluir (admin-only).
- Faixa de 4 KPIs (StatCard): MRR do cliente (success/Wallet), Próxima cobrança (primary/CalendarClock), Verba rodando (warning/TrendingUp), Status da conta (cor conforme status/Activity). Todos com fallback `'—'`.
- 4 abas (`Tabs defaultValue="contrato"`) com emojis:
  - 📄 Contrato & Cobrança: Contrato Atual + Histórico + `ContratoForm` REAIS + bloco Cobrança MOCK (Usa Asaas, Dia de cobrança, Badge de status com cor semântica) sob MockNotice.
  - 📊 Contas de anúncio: MockNotice + verba com Progress + lista de campanhas (mock); estado vazio fora do mock.
  - ✅ Checklist: MockNotice + `<ChecklistCliente>`.
  - 📝 Acompanhamento: MockNotice + timeline (Avatar/autor/data/nota) + Textarea desabilitado; estado vazio tratado.

## Comportamento real preservado (verificado)

- `notFound()` para cliente inexistente — inalterado.
- `getContratosDoCliente(id)` + `getCurrentUser()` no mesmo `Promise.all` — inalterado.
- `isAdmin = usuario?.role === 'admin'` — inalterado.
- Server actions inline `excluirClienteAction` / `excluirContratoAction` com `'use server'` e `redirect('/clientes')` — idênticas.
- AlertDialogs de excluir cliente e excluir contrato com a copy de confirmação EXATAMENTE igual à atual (strings inalteradas).
- Botão Editar (`/clientes/${id}/editar`), mapas STATUS_LABEL/STATUS_COLOR/NICHO_LABEL, formatadorMoeda, formatarData, registrosAnteriores — reaproveitados.

## Deviations from Plan

None - plano executado exatamente como escrito.

## Known Stubs

Todos os stubs abaixo são INTENCIONAIS, marcados visualmente com `MockNotice`, e serão resolvidos em incrementos funcionais futuros (integração Meta/Google Ads — Fase 2/3; persistência de cobrança/checklist/acompanhamento). Fazem parte do escopo declarado do plano (incremento VISUAL/estrutural).

- `src/app/(app)/clientes/[id]/page.tsx` (aba Cobrança): dados de cobrança derivados de mock (`getMockDaFicha`), sob MockNotice — persistência real via Asaas é incremento futuro.
- `src/app/(app)/clientes/[id]/page.tsx` (aba Contas de anúncio): verba/campanhas do `clientesTrafegoMock`, sob MockNotice — dados reais chegam na integração Meta (Fase 2/3).
- `src/app/(app)/clientes/[id]/page.tsx` (aba Checklist): marcações não persistem (estado local), sob MockNotice.
- `src/app/(app)/clientes/[id]/page.tsx` (aba Acompanhamento): Textarea de nova nota `disabled`, sob MockNotice — persistência futura.

## Verificação

- `npx tsc --noEmit`: sem erros.
- `npm run lint`: apenas os 2 erros pré-existentes documentados (`src/components/ui/sidebar.tsx`, `src/hooks/use-mobile.ts`) — nenhum erro novo introduzido.

## Commits

- `6a5fe3f` feat(quick-260710-v65): helper de mock por nome + checklist interativo
- `7cb6edb` feat(quick-260710-v65): redesenhar ficha do cliente como hub com abas

## Self-Check: PASSED

- Arquivos criados/modificados: todos FOUND (ficha-cliente.ts, checklist-cliente.tsx, page.tsx).
- Commits: `6a5fe3f` e `7cb6edb` FOUND no histórico.
