---
phase: quick-260710-v65
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mock/ficha-cliente.ts
  - src/components/ficha/checklist-cliente.tsx
  - src/app/(app)/clientes/[id]/page.tsx
autonomous: true
requirements: [REDESIGN-FICHA-CLIENTE]
must_haves:
  truths:
    - "Ao abrir /clientes/[id] de um cliente existente, o usuário vê um cabeçalho estilo Painel (nome grande, badge de status, nicho, contato) com botões Editar/Excluir à direita"
    - "O usuário vê uma faixa de 4 KPIs (StatCard) derivados do mock casando pelo NOME do cliente; se o nome não existir no mock, os cards mostram estado neutro sem quebrar a página"
    - "O corpo está organizado em abas (Contrato & Cobrança, Contas de anúncio, Checklist, Acompanhamento) com emojis nos títulos"
    - "A aba Contrato & Cobrança mostra contrato atual + histórico REAIS (do banco) + ContratoForm funcionando, mais um bloco de cobrança MOCK (usa Asaas, dia de cobrança, status com cor semântica) sob MockNotice"
    - "As ações de excluir cliente/contrato continuam admin-only, com AlertDialog e a copy de confirmação idêntica à atual"
    - "O checklist é interativo em estado local (marca/desmarca) sem persistir, e a página não quebra para clientes fora do mock"
  artifacts:
    - path: "src/lib/mock/ficha-cliente.ts"
      provides: "Helper que compila o mock (tráfego, financeiro, checklist, acompanhamento, cobrança derivada) por nome do cliente"
      contains: "export function getMockDaFicha"
    - path: "src/components/ficha/checklist-cliente.tsx"
      provides: "Client component do checklist interativo (estado local)"
      contains: "'use client'"
    - path: "src/app/(app)/clientes/[id]/page.tsx"
      provides: "Ficha do cliente redesenhada como hub com abas"
      contains: "Tabs"
  key_links:
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "src/lib/mock/ficha-cliente.ts"
      via: "getMockDaFicha(cliente.nome)"
      pattern: "getMockDaFicha"
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "src/components/ficha/checklist-cliente.tsx"
      via: "render dentro da aba Checklist"
      pattern: "ChecklistCliente"
    - from: "src/app/(app)/clientes/[id]/page.tsx"
      to: "getContratosDoCliente / deleteCliente / deleteContrato"
      via: "dados reais + server actions preservados"
      pattern: "getContratosDoCliente"
---

<objective>
Transformar a Ficha do Cliente (`src/app/(app)/clientes/[id]/page.tsx`) no hub central do sistema, aplicando a direção visual já aprovada no Painel (incremento 1): cabeçalho rico, faixa de KPIs (StatCard, cor semântica), e corpo organizado em ABAS. Checklist e Acompanhamento passam a viver DENTRO do cliente. Todo o comportamento REAL existente (dados do banco, contratos, ContratoForm, exclusões admin-only com AlertDialog, notFound) é preservado sem alteração de copy ou de regras.

Purpose: É na ficha do cliente que o Jacson olha quando pensa "como está o cliente X" — hoje os dados vivem espalhados. Este incremento é VISUAL/estrutural: cobrança/checklist/acompanhamento usam dados de exemplo (mock) sob MockNotice; a persistência real é um incremento funcional FUTURO.
Output: 1 helper de mock, 1 client component (checklist), e a page redesenhada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

Arquivo a redesenhar (ficha ATUAL — preserva dados reais + server actions inline):
@src/app/(app)/clientes/[id]/page.tsx

Referência visual APROVADA (replicar linguagem: emojis nos títulos, cor semântica, cards, MockNotice):
@src/app/(app)/painel/page.tsx

Componentes e mock a reaproveitar:
@src/components/stat-card.tsx
@src/components/mock-notice.tsx
@src/lib/mock/dashboard.ts
@src/lib/mock/extra.ts
@src/actions/contratos.ts
@src/components/contrato-form.tsx

<interfaces>
<!-- Contratos que o executor DEVE usar diretamente. Sem exploração adicional necessária. -->

De src/lib/mock/dashboard.ts:
```ts
type ClienteTrafego = {
  id: string; nome: string; nicho: 'E-commerce'|'Negócio Local'|'Infoproduto'
  contaStatus: 'ativa'|'atencao'|'problema'; contas: number
  verbaGasta: number; verbaTotal: number; ultimaSync: string
  campanhas: { nome: string; plataforma: 'Meta Ads'|'Google Ads'; status: 'ativa'|'pausada'; gasto: number; resultado: number }[]
}
type FinanceiroClienteMock = { cliente: string; mrr: number; diaCobranca: number; status: 'em dia'|'próximo' }
export const clientesTrafegoMock: ClienteTrafego[]
export const financeiroMock: FinanceiroClienteMock[]
```

De src/lib/mock/extra.ts:
```ts
type ChecklistItemMock = { id: string; cliente: string; tarefa: string; frequencia: 'diária'|'semanal'|'mensal'; feito: boolean }
type AcompanhamentoMock = { id: string; cliente: string; data: string; autor: string; nota: string }
export const checklistMock: ChecklistItemMock[]
export const acompanhamentoMock: AcompanhamentoMock[]
```

De src/components/stat-card.tsx:
```ts
type StatCardColor = 'primary'|'success'|'warning'|'danger'
function StatCard(props: {
  label: string; value: string; icon: React.ComponentType<{className?:string}>
  color?: StatCardColor; trend?: {value:string;direction:'up'|'down';positive?:boolean}; helper?: string
}): JSX.Element
```

De src/actions/contratos.ts (JÁ usados na page atual — manter):
```ts
getContratosDoCliente(clienteId): Promise<{ contratoAtual, historico }>
deleteContrato(id)
```

Componentes UI disponíveis (shadcn, já instalados — NÃO adicionar dependências):
Tabs/TabsList/TabsTrigger/TabsContent (@/components/ui/tabs),
Avatar/AvatarFallback (@/components/ui/avatar),
Progress (@/components/ui/progress), Checkbox (@/components/ui/checkbox),
Textarea (@/components/ui/textarea), Card/CardHeader/CardTitle/CardContent (@/components/ui/card),
Badge (@/components/ui/badge).

Tokens de cor semântica em globals.css: bg-chart-success / text-chart-success (verde=ok/em dia),
bg-chart-warning / text-chart-warning (âmbar=atenção/pendente/próximo),
bg-chart-danger / text-destructive (vermelho=crítico/vencido),
bg-alert-warning-soft / bg-alert-danger-soft (fundos suaves de severidade).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper de mock por nome + componente de checklist interativo</name>
  <files>src/lib/mock/ficha-cliente.ts, src/components/ficha/checklist-cliente.tsx</files>
  <action>
Criar DOIS arquivos que a page consumirá.

1) `src/lib/mock/ficha-cliente.ts` — helper server-safe (sem 'use client', sem hooks) que casa o mock pelo NOME do cliente e NUNCA quebra quando não encontra:
   - Importar `clientesTrafegoMock`, `financeiroMock` de `@/lib/mock/dashboard` e `checklistMock`, `acompanhamentoMock` de `@/lib/mock/extra`.
   - `export type CobrancaMock = { usaAsaas: boolean; diaCobranca: number | null; status: 'pago' | 'pendente' | 'vencido' }`.
   - `export function getMockDaFicha(nomeCliente: string)` retornando um objeto:
     - `trafego: ClienteTrafego | undefined` = `clientesTrafegoMock.find(c => c.nome === nomeCliente)`
     - `financeiro: FinanceiroClienteMock | undefined` = `financeiroMock.find(f => f.cliente === nomeCliente)`
     - `checklist: ChecklistItemMock[]` = `checklistMock.filter(i => i.cliente === nomeCliente)`
     - `acompanhamento: AcompanhamentoMock[]` = `acompanhamentoMock.filter(a => a.cliente === nomeCliente)`
     - `cobranca: CobrancaMock` DERIVADA (mock/visual — LOCKED: controle manual, sem banco neste incremento): se `financeiro` existe → `{ usaAsaas: true, diaCobranca: financeiro.diaCobranca, status: financeiro.status === 'próximo' ? 'pendente' : 'pago' }`; se não existe → `{ usaAsaas: false, diaCobranca: null, status: 'pendente' }`.
   - Reexportar os tipos usados pela page se conveniente (ou a page importa direto dos módulos de mock). Garantir que `getMockDaFicha` sempre retorna um objeto válido mesmo para nome desconhecido (arrays vazios, undefineds tratados).

2) `src/components/ficha/checklist-cliente.tsx` — client component do checklist interativo (mesma mecânica da página /checklist atual: estado LOCAL, não persiste):
   - Primeira linha: `'use client'`.
   - Props: `{ itens: { id: string; tarefa: string; frequencia: 'diária' | 'semanal' | 'mensal'; feito: boolean }[] }`.
   - `useState` inicializado com os itens; renderizar cada item com `Checkbox` (de `@/components/ui/checkbox`) controlado (`checked`), `onCheckedChange` alterna o `feito` daquele id no estado local.
   - Item marcado: texto com `line-through text-muted-foreground`. Mostrar a `frequencia` como Badge pequena (âmbar para pendentes é opcional; manter simples).
   - Se `itens` estiver vazio: mostrar `<p className="text-sm text-muted-foreground">Nenhum item de checklist para este cliente.</p>`.
   - NÃO chamar server actions; NÃO importar do banco.

Português em toda a UI. Não adicionar dependências.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit</automated>
  </verify>
  <done>Os dois arquivos existem, tipam sem erro em tsc, e getMockDaFicha retorna objeto válido para nome inexistente (arrays vazios / undefined tratados).</done>
</task>

<task type="auto">
  <name>Task 2: Redesenhar a page como hub com cabeçalho, KPIs e abas</name>
  <files>src/app/(app)/clientes/[id]/page.tsx</files>
  <action>
Reescrever `src/app/(app)/clientes/[id]/page.tsx` mantendo-a como SERVER COMPONENT async. PRESERVAR sem alteração toda a lógica real atual:
- `notFound()` quando o cliente não existe.
- Query real do cliente + `getContratosDoCliente(id)` + `getCurrentUser()` (mesmo `Promise.all`).
- `isAdmin = usuario?.role === 'admin'`.
- Server actions inline `excluirClienteAction` / `excluirContratoAction` ('use server') com `redirect('/clientes')` — IDÊNTICAS.
- AlertDialog de excluir cliente e excluir contrato com a COPY DE CONFIRMAÇÃO EXATAMENTE igual à atual (não reescrever os textos). Botão Editar (`Link href={/clientes/${id}/editar}`).
- Mapas STATUS_LABEL/STATUS_COLOR/NICHO_LABEL, `formatadorMoeda`, `formatarData` — reaproveitar.
- `registrosAnteriores` (histórico sem o contrato atual) — reaproveitar.
- `<ContratoForm clienteId={id} />` — manter.

ADICIONAR (novo layout):
1. Chamar `getMockDaFicha(cliente.nome)` → `{ trafego, financeiro, checklist, acompanhamento, cobranca }`.

2. CABEÇALHO estilo Painel: nome grande (`text-2xl font-semibold tracking-tight` ou similar ao atual `text-[28px]`), Badge de status usando STATUS_COLOR atual, nicho, contato (bloco de contato existente). Botões Editar/Excluir à direita (bloco atual, admin-only preservado).

3. FAIXA DE KPIs — `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4`, 4 `StatCard` (importar ícones de `lucide-react`: Wallet, CalendarClock, TrendingUp, Activity/AlertTriangle):
   - "MRR do cliente" → `financeiro ? formatadorMoeda.format(financeiro.mrr) : '—'`, color success, Wallet.
   - "Próxima cobrança" → `cobranca.diaCobranca ? 'Dia ' + cobranca.diaCobranca : '—'`, color primary, CalendarClock, helper conforme status.
   - "Verba rodando" → `trafego ? formatadorMoeda.format(trafego.verbaTotal) : '—'`, color warning, TrendingUp, helper "somando as contas de ads".
   - "Status da conta" → texto legível do `trafego?.contaStatus` ('ativa'→'Ativa'/success, 'atencao'→'Atenção'/warning, 'problema'→'Problema'/danger); se sem trafego → '—' neutro. Escolher color do StatCard conforme o status.
   Nunca quebrar: todos os campos tratam undefined com fallback '—'.

4. ABAS com `Tabs` (defaultValue="contrato"). `TabsList` com 4 `TabsTrigger` (emojis nos rótulos). Tabs é client island mas renderiza children server-side normalmente (a page continua server component):
   - "📄 Contrato & Cobrança" (value="contrato"): bloco Contrato Atual (contratoAtual real) + Histórico de Contratos (registrosAnteriores real, com o AlertDialog de excluir contrato admin-only) + `<ContratoForm clienteId={id} />` — REAIS. Abaixo, um bloco "Cobrança" com `MockNotice` (texto deixando claro: controle manual/exemplo, persistência real vem em incremento futuro) exibindo: "Usa Asaas: Sim/Não" (cobranca.usaAsaas), "Dia de cobrança: X" (cobranca.diaCobranca ?? '—'), e Badge de status pago(verde)/pendente(âmbar)/vencido(vermelho) com cor semântica (chart-success/chart-warning/destructive).
   - "📊 Contas de anúncio" (value="contas"): `MockNotice` (dados reais chegam na integração Meta, Fase 2/3). Se `trafego`: mostrar Nº de contas (`trafego.contas`), verba `formatadorMoeda.format(trafego.verbaGasta)` de `...verbaTotal` com `<Progress value={(trafego.verbaGasta/trafego.verbaTotal)*100} />`, e lista de `trafego.campanhas` (nome, plataforma, status ativa/pausada com cor semântica, gasto formatado, resultado). Se sem trafego: estado vazio "Este cliente ainda não tem contas de anúncio no sistema de exemplo."
   - "✅ Checklist" (value="checklist"): `MockNotice` + `<ChecklistCliente itens={checklist.map(i => ({ id: i.id, tarefa: i.tarefa, frequencia: i.frequencia, feito: i.feito }))} />` (import de `@/components/ficha/checklist-cliente`).
   - "📝 Acompanhamento" (value="acompanhamento"): `MockNotice` + timeline de `acompanhamento` (Avatar com AvatarFallback = inicial do autor, autor, data, nota). Campo de nova nota = `<Textarea disabled placeholder="Registro de notas será salvo quando a persistência for implementada." />`. Se vazio: "Nenhum registro de acompanhamento ainda."

Linguagem visual do Painel: emojis nos títulos de aba/seção, cards com `border-none shadow-sm` onde fizer sentido, cor com significado. Português em toda a UI. NÃO adicionar dependências, NÃO alterar schema/migrations.
  </action>
  <verify>
    <automated>cd "C:/Users/jacso/OneDrive/Documentos/projeto_agencia_jsr" && npx tsc --noEmit && npm run lint 2>&1 | tail -n 20</automated>
  </verify>
  <done>`npx tsc --noEmit` sem erros; `npm run lint` sem erros NOVOS (apenas os 2 pré-existentes em ui/sidebar.tsx e hooks/use-mobile.ts). A ficha renderiza cabeçalho + 4 KPIs + 4 abas; contrato/histórico/ContratoForm/exclusões admin-only preservados com copy idêntica; clientes fora do mock não quebram (fallbacks '—' e estados vazios).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` sem erros novos.
- `npm run lint` — apenas os 2 erros pré-existentes documentados (ui/sidebar.tsx, hooks/use-mobile.ts).
- Comportamento real preservado: notFound, contrato atual/histórico via getContratosDoCliente, ContratoForm, exclusões admin-only com AlertDialog e copy idêntica, botão Editar.
- Cobrança/Checklist/Acompanhamento/Contas exibem dados de exemplo sob MockNotice, sem tocar no banco.
</verification>

<success_criteria>
- Ficha do cliente redesenhada como hub: cabeçalho estilo Painel + faixa de 4 KPIs (StatCard) + 4 abas com emojis.
- Mock casado por nome com fallback neutro para clientes desconhecidos (nunca quebra).
- Checklist interativo em estado local isolado num client component.
- Zero dependências novas, zero migrations, zero mudança de schema.
</success_criteria>

<output>
After completion, create `.planning/quick/260710-v65-redesenho-da-ficha-do-cliente-incremento/260710-v65-SUMMARY.md`
</output>
