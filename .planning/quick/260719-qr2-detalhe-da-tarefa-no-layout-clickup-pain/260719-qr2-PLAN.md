---
phase: quick-260719-qr2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tarefas/quadro.ts
  - src/lib/tarefas/quadro.test.ts
  - src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx
  - src/app/(app)/tarefas/[id]/tarefa-lateral.tsx
  - src/app/(app)/tarefas/[id]/painel-atividade.tsx
autonomous: true
requirements: [QUICK-QR2]
must_haves:
  truths:
    - "Painel 'Atividade' fixo à direita mostra comentários e histórico intercalados por data, do mais antigo para o mais recente"
    - "Caixa 'Escreva um comentário...' fica sempre visível no rodapé do painel Atividade e envia comentário"
    - "Comentário próprio pode ser excluído direto do feed"
    - "Metadados (Status, Datas, Estimativa, Responsável, Prioridade, Etiquetas, Cliente) aparecem em grade 2 colunas estilo ClickUp e continuam editáveis inline"
    - "Notas, Anexos, Checklists, recorrência e excluir tarefa continuam acessíveis e funcionais"
    - "Em telas < lg o painel Atividade cai para baixo do conteúdo principal"
  artifacts:
    - path: "src/app/(app)/tarefas/[id]/painel-atividade.tsx"
      provides: "Painel Atividade estilo ClickUp (feed unificado + composer fixo)"
      min_lines: 80
    - path: "src/lib/tarefas/quadro.ts"
      provides: "montarFeedAtividade — merge puro de comentários + atividades"
      contains: "montarFeedAtividade"
  key_links:
    - from: "src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx"
      to: "painel-atividade.tsx"
      via: "render do painel na coluna direita com handlers enviarComentario/removerComentario"
      pattern: "PainelAtividade"
    - from: "src/app/(app)/tarefas/[id]/painel-atividade.tsx"
      to: "src/lib/tarefas/quadro.ts"
      via: "montarFeedAtividade(comentarios, atividades)"
      pattern: "montarFeedAtividade"
---

<objective>
Reformar a página de detalhe da tarefa (/tarefas/[id]) para o layout do ClickUp dos prints de referência (prints_aleatorios/clickup_01.png e clickup_02.png): conteúdo principal à esquerda (~2/3) com grade de metadados em 2 colunas, e painel "Atividade" fixo à direita (~1/3, scroll próprio) unindo comentários + histórico em feed cronológico único, com caixa de comentário sempre visível no rodapé do painel.

Purpose: hoje comentários e histórico vivem escondidos em abas; o usuário quer o fluxo de trabalho do ClickUp, onde a conversa da tarefa está sempre à vista.
Output: painel-atividade.tsx novo, tarefa-detalhe.tsx reorganizado, helper puro montarFeedAtividade testado. Sem migration, sem mudança de schema, sem action nova.
</objective>

<context>
@./CLAUDE.md
@prints_aleatorios/clickup_01.png
@prints_aleatorios/clickup_02.png
@src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx
@src/app/(app)/tarefas/[id]/tarefa-lateral.tsx
@src/app/(app)/tarefas/[id]/page.tsx
@src/lib/tarefas/dados.ts
@src/lib/tarefas/quadro.ts
@src/actions/tarefas.ts

<interfaces>
Já existe TUDO de dados e actions — NÃO redescobrir, NÃO criar action nova:

De src/lib/tarefas/dados.ts:
```typescript
export type ComentarioTarefa // { id, autorId, autorNome, texto, createdAt }
export type AtividadeTarefa  // { id, autorId, autorNome, tipo, campo, de, para, createdAt ... }
export type AnexoTarefa
export type TarefaDetalhe    // já traz comentarios, atividades, anexos, checklist, notas
```

De src/lib/tarefas/quadro.ts (puros, testados):
```typescript
export function textoAtividade(atv): { frase: string; valor: string | null }
export function tempoRelativo(iso, agoraIso): string
export const STATUS_CLASSE, PRIORIDADE_CLASSE, STATUS_ORDEM
export function iniciais(nome), corDoAvatar(id), corDaEtiqueta(nome)
```

De src/actions/tarefas.ts: criarComentario(tarefaId, { texto }), deletarComentario(id), atualizarTarefa(id, campos) — retornam `{ error }` ou `{ data }`.

Em tarefa-detalhe.tsx já existem os handlers enviarComentario/removerComentario e o padrão salvarCampo; em tarefa-lateral.tsx existem AnexoLinha, AtividadeLinha e o card de Notas (EditorNotas).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro montarFeedAtividade (merge comentários + histórico) sob TDD</name>
  <files>src/lib/tarefas/quadro.ts, src/lib/tarefas/quadro.test.ts (ou arquivo de teste existente do módulo em src/lib/tarefas)</files>
  <behavior>
    - Teste 1: intercala comentários e atividades por createdAt ASCENDENTE (mais antigo primeiro, como no ClickUp — o composer fica embaixo e o scroll começa no fim).
    - Teste 2: atividades com tipo 'comentou' são EXCLUÍDAS do feed (o comentário real já entra como item próprio — evitar duplicata).
    - Teste 3: cada item sai discriminado: `{ kind: 'comentario', comentario }` ou `{ kind: 'evento', atividade }`, com key estável (`c-{id}` / `a-{id}`).
    - Teste 4: listas vazias → feed vazio; empate de createdAt não quebra (ordem estável).
  </behavior>
  <action>
    Adicionar em src/lib/tarefas/quadro.ts (módulo puro, zero import de react/db):

    ```typescript
    export type ItemFeed =
      | { kind: 'comentario'; key: string; createdAt: string; comentario: ComentarioLike }
      | { kind: 'evento'; key: string; createdAt: string; atividade: AtividadeEntrada & { id: string; createdAt: string } }
    export function montarFeedAtividade(comentarios, atividades): ItemFeed[]
    ```

    Usar tipos estruturais mínimos (como os demais helpers de quadro.ts fazem) para não acoplar em dados.ts. Filtrar `tipo === 'comentou'`, concatenar, ordenar por createdAt asc (sort estável do JS). Escrever os testes ANTES no arquivo de testes já existente do módulo tarefas (seguir o padrão dos testes de tempoRelativo/textoAtividade).
  </action>
  <verify>
    <automated>npx vitest run src/lib/tarefas</automated>
  </verify>
  <done>montarFeedAtividade exportada, 4+ testes novos verdes, suíte src/lib/tarefas inteira verde.</done>
</task>

<task type="auto">
  <name>Task 2: Painel "Atividade" fixo à direita (feed + composer no rodapé)</name>
  <files>src/app/(app)/tarefas/[id]/painel-atividade.tsx (novo), src/app/(app)/tarefas/[id]/tarefa-lateral.tsx</files>
  <action>
    Criar componente client `PainelAtividade` fiel ao print clickup_01/02, painel direito:

    - Estrutura: card de altura cheia com 3 regiões — header ("Atividade" + ícone History), corpo com scroll próprio (`flex-1 overflow-y-auto`), e composer FIXO no rodapé (fora da área de scroll).
    - Corpo: renderiza `montarFeedAtividade(tarefa.comentarios, tarefa.atividades)`:
      - `kind === 'comentario'` → card destacado (rounded-lg border bg-muted/40, dark ok via tokens): avatar (iniciais/corDoAvatar), nome em negrito, `tempoRelativo(createdAt, agora)` à direita, corpo `whitespace-pre-wrap text-sm`. Se `autorId === usuarioId`, DropdownMenu "..." com "Excluir" (chama onRemoverComentario) — reaproveitar o markup da atual aba Comentários.
      - `kind === 'evento'` → linha discreta (text-xs text-muted-foreground): "**{autorNome}** {frase} {valor}" usando textoAtividade; manter o Badge colorido para status/prioridade como o AtividadeLinha atual faz (pode reusar/adaptar AtividadeLinha passando uma prop `compacta`, ou duplicar o trecho pequeno — preferir reuso).
    - Feed em ordem ascendente + `useEffect` que rola o container para o fim ao montar e quando o nº de itens cresce (ref no div de scroll, `scrollTop = scrollHeight`).
    - Composer: Textarea "Escreva um comentário..." (rows=2, auto-resize opcional) + botão de enviar (ícone Send/SendHorizonal) desabilitado quando vazio/salvando; Enter sem Shift envia. Props: `onEnviarComentario(texto)`, `enviando`.
    - Estado vazio: "Nenhuma atividade ainda." no corpo.
    - Props do componente: `{ tarefa, agora, usuarioId, enviando, onEnviarComentario, onRemoverComentario }` — o componente NÃO chama actions direto; recebe handlers.
    - Em tarefa-lateral.tsx: NÃO deletar o arquivo — manter AnexoLinha e o card de Notas exportados (serão usados pela Task 3). O card "Atividade Recente" e AtividadeLinha podem migrar/ajustar conforme o reuso decidido.
    - Tudo em português; usar só tokens do tema (bg-card, text-muted-foreground etc.) — dark mode automático; classes `dark:` só se precisar de cor fora dos tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>painel-atividade.tsx compila, feed unificado renderiza comentários (cards com excluir) + eventos (linhas discretas) intercalados, composer fixo no rodapé enviando via handler.</done>
</task>

<task type="auto">
  <name>Task 3: Reorganizar tarefa-detalhe.tsx no layout ClickUp (grade 2 colunas + painel à direita, nada se perde)</name>
  <files>src/app/(app)/tarefas/[id]/tarefa-detalhe.tsx</files>
  <action>
    Reorganizar o corpo do TarefaDetalhe mantendo TODOS os handlers/estados existentes (salvarCampo, checklist, anexos, notas, recorrência, excluir — não reescrever lógica, só layout):

    1. **Grid raiz**: `grid grid-cols-1 gap-6 lg:grid-cols-3`. Esquerda `lg:col-span-2`; direita `lg:col-span-1` com `<PainelAtividade ...>` passando `enviarComentario`/`removerComentario` já existentes. No painel, dar altura de tela no desktop: wrapper `lg:sticky lg:top-6 lg:h-[calc(100vh-6rem)]` (ajustar ao offset real do layout) para ficar "fixo à direita com scroll próprio"; no mobile ele cai naturalmente para baixo do conteúdo (grid 1 coluna).
    2. **Grade de metadados estilo ClickUp** (print: 2 colunas, ícone + rótulo à esquerda, valor à direita, SEM cara de formulário): substituir a grade atual de 8 células com borda por `grid grid-cols-1 gap-x-10 md:grid-cols-2` com linhas `[ícone lucide 4 + rótulo text-muted-foreground w-28~32] [valor]`. Reaproveitar EXATAMENTE os controles inline atuais (Select com SELECT_CELULA, Inputs date com DATA_CELULA, etiquetas com corDaEtiqueta):
       - Coluna 1: Status (badge STATUS_CLASSE no trigger), Datas (Início → Prazo na MESMA linha, dois inputs date lado a lado + badge de recorrência com ícone Repeat quando `recorrencia !== 'nenhuma'`, clicável abrindo o sheet de recorrência já existente), Tempo estimado (ícone Clock).
       - Coluna 2: Responsável (avatar + nome), Prioridade (bandeira/ArrowUp), Etiquetas (chips + input "adicionar"), Cliente (Building2 + select).
    3. **Descrição** logo abaixo da grade (mesmo click-to-edit atual). Se o texto tiver > ~8 linhas, mostrar recolhido com `line-clamp` + botão "Expandir"/"Recolher" (estado local) — como no print.
    4. **Notas e Anexos e Checklists continuam no corpo esquerdo**: manter as Tabs atuais REDUZIDAS a Detalhes / Checklists / Anexos (remover as abas Comentários e Atividade — agora vivem no painel direito; remover também estados `aba`/onIrParaAba que apontavam para elas). Incluir o card de Notas (de tarefa-lateral.tsx) dentro da aba Detalhes, abaixo da Descrição — NADA de notas pode ser perdido.
    5. Barra superior, sheet de recorrência, AlertDialog de excluir, botão "Marcar como concluída": intactos.
    6. Limpar imports mortos; textos em português; dark mode via tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/tarefas</automated>
  </verify>
  <done>Página compila; grade 2 colunas ClickUp editável; painel Atividade à direita (sticky no desktop, embaixo no mobile); Notas/Anexos/Checklists/recorrência/excluir todos acessíveis; abas Comentários/Atividade removidas sem perda de função.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` limpo.
- `npx vitest run src/lib/tarefas` verde (inclui os testes novos de montarFeedAtividade).
- Conferência manual rápida: /tarefas/[id] com comentários + histórico mostra feed intercalado, composer fixo envia, excluir comentário próprio funciona, metadados editam inline, notas/anexos/checklist intactos, mobile empilha.
</verification>

<success_criteria>
Layout do detalhe da tarefa fiel aos prints do ClickUp: conteúdo 2/3 + painel "Atividade" 1/3 fixo à direita com feed cronológico único (comentários destacados + eventos discretos) e caixa de comentário sempre visível; grade de metadados 2 colunas; zero funcionalidade perdida; sem migration; testes e tsc verdes.
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260719-qr2-detalhe-da-tarefa-no-layout-clickup-pain/260719-qr2-SUMMARY.md`
</output>
