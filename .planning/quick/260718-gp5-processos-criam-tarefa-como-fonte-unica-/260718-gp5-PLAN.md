---
phase: quick-260718-gp5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tarefas/quadro.ts
  - src/lib/tarefas/quadro.test.ts
  - src/app/(app)/tarefas/tarefas-quadro.tsx
  - src/lib/processos/gerar.ts
  - src/lib/processos/gerar.test.ts
  - src/actions/processos.ts
  - src/components/ficha/processos-cliente.tsx
  - src/app/(app)/clientes/[id]/page.tsx
  - src/lib/alertas/calcular.ts
autonomous: true
requirements: [QUICK-GP5]
must_haves:
  truths:
    - "Ativar onboarding/retenção/saída de um cliente cria UMA tarefa vinculada (título 'Onboarding — {nome}' etc., prioridade alta, data do dia em fuso BR) com os itens do modelo como checklist da tarefa"
    - "Ativação dupla (webhook Asaas + Autentique + baixa manual) NUNCA duplica a tarefa do processo"
    - "A aba Processos da ficha do cliente mostra e opera o checklist da TAREFA do processo (processo_itens não é mais lido)"
    - "O alerta onboarding_parado dispara a partir do checklist da tarefa do processo"
    - "O quadro /tarefas tem coluna 'Atrasadas' (vermelha) com a_fazer/em_andamento de data < dia visualizado, que somem de Pendentes/Em Andamento e continuam concluíveis; a barra de stats inclui a coluna"
  artifacts:
    - path: "src/lib/tarefas/quadro.ts"
      provides: "Coluna derivada 'atrasada' (lógica pura): colunaDaTarefa, agrupamento e estatísticas com 5 colunas"
    - path: "src/lib/processos/gerar.ts"
      provides: "gerarProcessoParaCliente criando tarefa + tarefa_checklist_items (fonte única), idempotente"
    - path: "src/components/ficha/processos-cliente.tsx"
      provides: "Aba Processos operando o checklist da tarefa do processo"
  key_links:
    - from: "src/lib/processos/gerar.ts"
      to: "tarefas / tarefa_checklist_items"
      via: "insert com etiqueta identificadora processo:{tipo}"
      pattern: "processo:"
    - from: "src/lib/alertas/calcular.ts"
      to: "tarefa_checklist_items"
      via: "join tarefas com etiqueta processo:onboarding"
      pattern: "processo:onboarding"
    - from: "src/app/(app)/tarefas/tarefas-quadro.tsx"
      to: "src/lib/tarefas/quadro.ts"
      via: "COLUNAS com 'atrasada' + agrupamento por coluna derivada"
      pattern: "atrasada"
---

<objective>
Processos (onboarding/retenção/saída) passam a criar uma TAREFA como fonte única do checklist — some a leitura de `processo_itens` — e o quadro /tarefas ganha a coluna derivada "Atrasadas".

Purpose: unificar a operação diária em /tarefas (um só lugar para tudo que precisa ser feito) e dar visibilidade imediata do que está atrasado.
Output: gerar.ts reescrito criando tarefa+checklist, aba Processos lendo a tarefa, alerta onboarding_parado lendo o checklist da tarefa, quadro com 5 colunas. SEM migration (identificação via `tarefas.etiquetas` jsonb existente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/processos/gerar.ts
@src/lib/tarefas/quadro.ts
@src/app/(app)/tarefas/tarefas-quadro.tsx
@src/components/ficha/processos-cliente.tsx
@src/actions/processos.ts
@src/lib/alertas/calcular.ts
@src/lib/alertas/avaliar-operacional.ts
@src/app/(app)/clientes/[id]/page.tsx
@src/lib/db/schema.ts

<interfaces>
Contratos existentes que o executor deve usar direto (extraídos do código):

De src/lib/db/schema.ts:
- `tarefas`: titulo, subtitulo, status ('a_fazer'|...), prioridade ('baixa'|'media'|'alta'|'urgente'), data (date 'YYYY-MM-DD'), clienteId, etiquetas (jsonb string[]), ehMolde (default false), fixada, codigoNum/codigo gerados pelo banco.
- `tarefaChecklistItems`: tarefaId, texto, concluido (boolean), ordem, grupo (text, default 'Checklist').
- `processoModeloItens`: tipo, titulo, ordem, opcional, ativo — CONTINUA sendo a fonte dos itens ao gerar.

De src/lib/processos/gerar.ts (assinatura que NÃO muda — chamadores em actions/cobrancas.ts:280, actions/clientes.ts:81, actions/processos.ts, api/webhooks/asaas/route.ts:114, lib/contratos/assinatura.ts:27 ficam intocados):
```typescript
export type TipoProcesso = 'onboarding' | 'retencao' | 'saida'
export async function gerarProcessoParaCliente(clienteId: string, tipo: TipoProcesso): Promise<number>
```

De src/lib/alertas/avaliar-operacional.ts (avaliador PURO — não muda):
```typescript
export type OnboardingInput = { clienteId: string; clienteNome: string; pendentes: number; iniciadoEm: Date }
export function avaliarOnboardingParado(processos: OnboardingInput[], agora?: Date): Alerta[]
```

De src/lib/tarefas/quadro.ts (hoje): COLUNAS_ORDEM/COLUNA_LABEL/COLUNA_PONTO/COLUNA_BARRA/COLUNA_HELPER são Record<TarefaStatus, ...>; agruparPorStatus, estatisticasDoQuadro, tarefasDaVisaoDiaria(tarefas, dia).
Fuso: já existe `dataBrasiliaDeIso(iso)` em quadro.ts ('YYYY-MM-DD' em America/Sao_Paulo via en-CA) — reusar o padrão para a data da tarefa do processo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coluna derivada "Atrasadas" no quadro /tarefas (lógica pura + UI)</name>
  <files>src/lib/tarefas/quadro.ts, src/lib/tarefas/quadro.test.ts, src/app/(app)/tarefas/tarefas-quadro.tsx</files>
  <behavior>
    Testes (vitest, no arquivo de teste existente do módulo — se não houver, criar src/lib/tarefas/quadro.test.ts):
    - colunaDaTarefa: a_fazer com data < dia → 'atrasada'; em_andamento com data < dia → 'atrasada'; a_fazer/em_andamento com data === dia → coluna própria; concluida/nao_realizada NUNCA viram 'atrasada' (mesmo com data passada).
    - agruparPorColuna(tarefas, dia): as 5 chaves existem SEMPRE (mesmo vazias); tarefa atrasada aparece SÓ em 'atrasada' (sai de a_fazer/em_andamento).
    - estatisticasDoQuadro(tarefas, dia): porColuna inclui 'atrasada'; total soma as 5; percentualConclusao inalterado (concluídas/total).
    - Regressão: dia sem atrasadas → comportamento idêntico ao atual (contagens das 4 colunas batem).
  </behavior>
  <action>
    Em src/lib/tarefas/quadro.ts (módulo PURO — zero import novo de db/react):
    - Novo tipo `ColunaQuadro = TarefaStatus | 'atrasada'` ("atrasada" é DERIVADA, decisão 3 — NÃO é status novo no banco/enum).
    - `colunaDaTarefa(t: { status; data }, dia: string): ColunaQuadro` — a_fazer/em_andamento com `data < dia` → 'atrasada' (comparação de strings 'YYYY-MM-DD', nunca new Date, regra de fuso do projeto).
    - `COLUNAS_ORDEM` vira `ColunaQuadro[]` com 'atrasada' PRIMEIRO: ['atrasada','a_fazer','em_andamento','concluida','nao_realizada'] (discrição: atraso é o que mais importa ver).
    - Estender COLUNA_LABEL ('Atrasadas'), COLUNA_HELPER ('Vencidas e abertas'), COLUNA_PONTO/COLUNA_BARRA ('bg-destructive') e, se usado no cabeçalho, STATUS_CLASSE — SÓ tokens destructive existentes, zero cor nova.
    - `agruparPorColuna<T>(tarefas: T[], dia: string): Record<ColunaQuadro, T[]>` substituindo o uso de agruparPorStatus no quadro (manter agruparPorStatus se outros consumidores existirem — conferir com grep; se só o quadro usa, pode refatorar in-place mudando a assinatura).
    - `estatisticasDoQuadro(tarefas, dia)` passa a receber o dia e contar por ColunaQuadro (5 chaves). Guardas de divisão por zero preservadas.
    Em tarefas-quadro.tsx:
    - Trocar agruparPorStatus/estatisticasDoQuadro pelas versões com `dados.dia`; renderizar a 5ª coluna igual às demais (o "Adicionar tarefa" da coluna 'atrasada' pode linkar para `/tarefas/nova?data=${dados.dia}` sem param status).
    - Barra de stats: o grid `lg:grid-cols-6` vira `lg:grid-cols-7` (Total + 5 colunas + rosca). Tarefa atrasada continua concluível normalmente (o card/sheet já permite mudar status — nada a fazer além de exibir).
    - `tarefasDaVisaoDiaria` NÃO muda (atrasadas já entram na visão do dia por `data <= dia`).
    Tudo em português.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tarefas/quadro.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Quadro com 5 colunas; a_fazer/em_andamento com data anterior ao dia visualizado aparecem só em "Atrasadas" (vermelha); stats do rodapé com as 5 colunas; testes puros verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gerarProcessoParaCliente cria TAREFA como fonte única (idempotente)</name>
  <files>src/lib/processos/gerar.ts, src/lib/processos/gerar.test.ts, src/actions/processos.ts</files>
  <behavior>
    Testes (vitest) da lógica PURA extraída (não mockar o db — extrair helpers puros e testá-los):
    - `tituloDoProcesso(tipo, nome)`: 'onboarding' → 'Onboarding — {nome}', 'retencao' → 'Retenção — {nome}', 'saida' → 'Encerramento — {nome}'.
    - `etiquetaDoProcesso(tipo)`: 'processo:onboarding' etc. (chave de idempotência estável).
    - `grupoDoProcesso(tipo)`: 'Onboarding' | 'Retenção' | 'Encerramento'.
    - `itensParaChecklist(modelo)`: preserva ordem; item `opcional: true` ganha sufixo ' (opcional)' no texto; lista vazia → [].
    - `hojeBrasilia(agora: Date)`: 'YYYY-MM-DD' em America/Sao_Paulo (en-CA), determinístico com o agora injetado.
  </behavior>
  <action>
    Reescrever src/lib/processos/gerar.ts (decisão 1 — TAREFA é a fonte única):
    - MANTER a assinatura `gerarProcessoParaCliente(clienteId, tipo): Promise<number>` e o try/catch que só loga (gerar NUNCA quebra a ativação — chamadores webhooks/actions ficam intocados).
    - Idempotência: SELECT count de `tarefas` com `clienteId` = cliente, `ehMolde = false` e etiqueta presente — `sql\`${tarefas.etiquetas} @> ${JSON.stringify([etiquetaDoProcesso(tipo)])}::jsonb\`` (jsonb containment; sem migration — decisão 4 permite usar campos existentes). Se > 0 → return 0. Mesmo desenho check-then-insert do código atual (ativação dupla webhook+manual não duplica na prática; janela de corrida é a mesma de antes).
    - Buscar o modelo em `processoModeloItens` (tipo, ativo=true, ordem asc) — INALTERADO (decisão 5). Modelo vazio → return 0.
    - Buscar `clientes.nome` (select só do nome). Cliente inexistente → return 0.
    - Inserir a tarefa: titulo = tituloDoProcesso, prioridade 'alta', status 'a_fazer', data = hojeBrasilia(new Date()), clienteId, etiquetas = [grupoDoProcesso(tipo), etiquetaDoProcesso(tipo)] (a 1ª é o chip legível no card; a 2ª é a chave técnica). `.returning({ id })`.
    - Inserir os `tarefaChecklistItems`: tarefaId, texto = itensParaChecklist(modelo), ordem do modelo, grupo = grupoDoProcesso(tipo) (decisão 1: grupo = nome do processo).
    - Return = quantidade de itens do checklist criados.
    - `processo_itens` NÃO é mais escrito nem lido aqui (0 linhas em produção — seguro abandonar na leitura, decisão 5; a tabela e o schema ficam como estão, sem drop, sem migration).
    Em src/actions/processos.ts:
    - `gerarChecklistProcesso` continua igual (chama gerarProcessoParaCliente); ajustar a mensagem de erro se necessário ('Tarefa do processo ja existe ou o modelo esta vazio.').
    - `atualizarStatusProcessoItem` e imports de `processoItens` podem ser REMOVIDOS depois que a Task 3 parar de usá-los (fazer a remoção aqui só se a Task 3 desta mesma execução já tiver trocado a UI; senão, remover na Task 3).
    Comentários e commits em português.
  </action>
  <verify>
    <automated>npx vitest run src/lib/processos/gerar.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Ativar processo cria 1 tarefa 'alta' com checklist agrupado pelo nome do processo; chamada dupla retorna 0 na segunda (nenhuma duplicata); helpers puros testados; nenhuma escrita em processo_itens.</done>
</task>

<task type="auto">
  <name>Task 3: Aba Processos lê a tarefa do processo + alerta onboarding_parado pelo checklist da tarefa</name>
  <files>src/app/(app)/clientes/[id]/page.tsx, src/components/ficha/processos-cliente.tsx, src/lib/alertas/calcular.ts, src/actions/processos.ts</files>
  <action>
    Ficha do cliente (page.tsx) — decisão 2:
    - Substituir a query de `processoItens` por UMA query sequencial (padrão do repo, nunca inflar o Promise.all): buscar as tarefas de processo do cliente (`clienteId`, `ehMolde=false`, etiqueta `@>` 'processo:onboarding'/'processo:retencao'/'processo:saida' — pode ser 1 query com OR dos 3 containments) + os `tarefaChecklistItems` dessas tarefas (ordem asc). Montar por tipo: `{ tarefaId, data, status, itens: [{ id, texto, concluido, ordem }] }` e passar aos componentes. Manter try/catch com degradação graciosa.
    Componente processos-cliente.tsx:
    - `ChecklistProcesso` passa a operar itens de `tarefa_checklist_items`: checkbox alterna `concluido` chamando a action EXISTENTE de checklist de tarefa (procurar em src/actions/tarefas.ts — ex. alternar/atualizar item de checklist; reusar, com revalidatePath da ficha se a action não revalidar `/clientes/[id]` — nesse caso, criar wrapper fino em actions/processos.ts que chama a lógica e revalida a ficha). Remover os estados 'nao_se_aplica'/'Reativar'/'Não se aplica' (checklist de tarefa é binário; itens opcionais já vêm com sufixo ' (opcional)' no texto). Progresso = feitos/total.
    - Adicionar link/botão "Abrir tarefa" apontando para o detalhe da tarefa (conferir a rota existente do detalhe — ex. /tarefas/[id]; se o detalhe for sheet dentro de /tarefas, linkar `/tarefas?dia={data}` — usar o que existir, sem inventar rota).
    - Estado vazio + botão "Gerar checklist do modelo" (chama gerarChecklistProcesso) e ModeloDialog (edição do modelo) ficam como estão.
    - Atualizar o tipo `ItemProcesso` exportado (consumido pela page) para o novo shape.
    Alerta (calcular.ts) — decisão 2:
    - Trocar a query de onboarding: em vez de `processo_itens`, agregar por cliente a partir de `tarefas` (etiqueta @> 'processo:onboarding', ehMolde=false) JOIN `tarefa_checklist_items` JOIN `clientes`: pendentes = count FILTER (WHERE NOT concluido), iniciadoEm = min(tarefas.createdAt). `avaliarOnboardingParado` puro fica INTOCADO.
    Limpeza:
    - Remover `atualizarStatusProcessoItem` e o import de `processoItens` de actions/processos.ts (nenhum consumidor restante — confirmar com grep). Schema `processoItens` permanece (sem migration, sem drop).
    Verificação final: rodar a suíte inteira + build.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Aba Processos exibe e opera o checklist da tarefa (concluir/reabrir persiste), com link para a tarefa; nenhuma leitura de processo_itens sobrou no app (grep limpo fora do schema); alerta onboarding_parado alimentado pelo checklist da tarefa; suíte completa e build verdes.</done>
</task>

</tasks>

<verification>
- `grep -rn "processoItens" src --include=*.ts --include=*.tsx` → só o schema (e nada de leitura/escrita em runtime).
- Suíte vitest completa verde (inclui regressões do quadro e dos avaliadores de alerta).
- `npm run build` verde.
- Manual (pós-deploy): ativar um cliente de teste → tarefa "Onboarding — {nome}" aparece em /tarefas e na aba Processos da ficha; repetir a ativação não duplica; tarefa de ontem em a_fazer aparece na coluna Atrasadas.
</verification>

<success_criteria>
- Decisão 1: tarefa única por processo/cliente (título, data BR, prioridade alta, checklist com grupo do processo), idempotente.
- Decisão 2: ficha e alerta onboarding_parado leem o checklist da tarefa.
- Decisão 3: coluna "Atrasadas" derivada, vermelha (destructive), com stats — sem status novo no banco.
- Decisão 4: zero migration; tudo em português; lógica pura sob vitest.
- Decisão 5: modelo continua a fonte dos itens; processo_itens abandonado na leitura (0 linhas em produção).
</success_criteria>

<output>
Após concluir, criar `.planning/quick/260718-gp5-processos-criam-tarefa-como-fonte-unica-/260718-gp5-SUMMARY.md`
</output>
